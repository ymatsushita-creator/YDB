# REPORT-10.0 — 実行⑩。採点入力を置いた。壊れていた導線を1本直した。2期の取り込み経路を作った。画面の指摘を5点直した

実行⑩の第1報。2026-08-10。**この文書は凍結する。**
`REPORT-9.0.md`（実行⑨・UI 大改修）の続き。

---

## ★ これは Pilot Go ではない

`REPORT-7.4` 以降と同じ注記を繰り返す。**Pilot は HOLD のまま。基準は緩めない。**

運営はまだ1度も触っていない。**実データも1件も入っていない。**
今回作ったものは、依頼者の指示に対する実装であって、
運用で観測された問題への対処ではない。

## Summary

- 依頼者の指示は2つ ――「実データ投入」と「成績記入層の実装」
- **選考タブで採点できるようにした**（C-59）。成績を出しているのに書けなかった
- **その途中で、既存の欠陥を1つ踏んだ**（C-60）。
  「やること」のリンクが 404 で、**画面から点を入れる導線が1本も無かった**
- **2期の取り込み経路を作った**（C-61）。ただし
  **止めているのは生年月日だけではなかった。壁は4つある**
- **画面の指摘を5点受け、すべて直した**（C-62）。
  期の印・説明文・見切れ・一覧・顔写真
- **個人名から採点できるレイヤーを作った**（C-63）。面接中に開く画面
- **色を白黒に統一した**（C-64）。原典と生成物は変えず、上書き層1枚で
- **カレンダーの氏名が升の中で必ず切れていた**（C-65）。読める一覧を下に置いた
- テスト **372 → 397**（+25）。`tsc` / `build` すべてクリーン
- **実データは1件も入れていない。** 入れられる行が0件だったため

---

## 1. 判断を仰いだこと

`HANDOFF.md` は実行⑩が最初に判断することを3つ挙げていた。
そのうち①②に相当する2つを依頼者に問い、回答を得た。

| 問い | 回答 |
|---|---|
| 実行⑩で何に取り組むか | **実データ投入と、成績記入層の実装** |
| 2期の投入で生年月日をどう扱うか | **生年月日を集める**（記録層は変えない） |
| 生年月日を集める間、投入をどう進めるか | **取り込み経路を先に作る** |

`birth_date` を NULL 可に緩める道は**採らなかった。**
照合キーが減ると、別人を同一人物として統合する事故が起こりうる。

③（2期と3期で段の数も呼び名も違う）は**未確認のまま。**
そのため取り込みでも選考の段は入れていない。

---

## 2. 成績記入層 ―― 出しているのに、書けなかった

### 何が無かったか

選考ステップのタブは、候補者ごとにこう出していた。

```
100点換算   軸   名前        学校        待ち    担当
   80.0      5   （候補者）  （高校）    26 日   （担当者）
```

**成績を出しているのに、その場で点を入れる入口が無い。**
点を入れられるのは応募の画面（`/applications/[id]`）だけで、
そこへ辿り着く導線は「やること」のリンク1本だけだった。

### 直したもの

一覧から候補者を選ぶと、右のパネルに採点シートが出る。
軸ごとに点と根拠を入れて保存し、全軸そろうと確定できる。

**1軸ずつ保存する。まとめて保存にしていない。**
記録層の単位が `(evaluation_id, criteria_id)` の1行で、
面接の途中で1つだけ書き留められることに意味がある。

### 一覧の行と、採点する相手を同じにした

1つの応募に同じステップの評価が2件あることがある（面接官が2人）。
一覧は待ちの長いほうを代表にして1行へ畳んでいる。

採点側で「この人のこのステップの評価」を引き直すと、**畳み方が食い違ったときに、
画面が出している「n 軸」と、点が入る先が別の評価になる。**
そこで一覧に `evaluation_id` を持たせ、採点シートは**その ID だけ**で引く。

実際に確かめた。面接官が2人いる応募で、1人目の評価を確定させると ――

```
確定前  代表 = 1人目 ・ 成績 80.0 ・ 軸 5
確定後  代表 = 2人目 ・ 成績 72.5 ・ 軸 8
```

**行の代表が移り、採点シートも一緒に移った。**

### 軸が0本のときを「そろった」と読まない

2期の「応募受付」には評価軸が1本も無い（旧システムに点が無いので入れていない）。
残り軸数が0であることだけで確定を出すと、**点が1つも無い評価を確定できる。**
軸が1本以上あることを条件に加え、画面には「評価軸が未登録」と出す ――
足りないのは点ではなく軸なので、「採点済み」に見せない。

---

## 3. ★ 画面から点を入れる導線が、1本も無かった

採点を確かめている途中で踏んだ。

「◯◯さんの二次面接を評価する」を押すと **404** になる。

```
v_open_tasks.source_id  = 評価のID
画面のリンク             = /applications/{source_id}   ← 応募のIDではない
```

ビューには `application_id` が別にあるのに、問い合わせがそれを選んでいなかった。

**効いていた範囲は狭くない。** 採点の入口は応募の画面にしかなく、
そこへ行く導線はこのリンクだけだった。つまり実行⑨の終わりの時点で、
**画面から点を入れる手段が1つも存在しなかった。**

テストは通っていた。**リンク先が開けることを、誰も確かめていなかった。**

実行⑨で踏んだ落とし穴（`HANDOFF.md`）と同じ形をしている ――
「乱数に頼った経路は条件が変わると黙って消える」「落ちているテストが別の欠陥を隠す」。
今回のは3つ目である。**出しているものが押せるかは、押さないと分からない。**

---

## 4. 2期の取り込み ―― 壁は生年月日だけではなかった

`HANDOFF.md` は、投入を止めているのは `persons.birth_date` の NOT NULL だと
書いていた。**旧 61 件を実際に突き合わせた結果はこうである。**

```
旧 youth_candidates 61 件 ―― 入る 0 件

  55 件  姓・名（氏名が姓と名に分かれていない）
  61 件  生年月日（旧データに列そのものが無い）
  31 件  メール（persons.email は NOT NULL）
  13 件  学校（persons.school_id は NOT NULL）
```

**4つすべてそろっている行は1件も無い。**

### ★ 依頼者への報告 ―― 生年月日だけを集めても、1件も入らない

これは判断の前提が変わる話である。集める対象は4つあり、
**そのうち最も件数が多いのは生年月日ではなく「姓名の区切り」**（55件）である。

区切りの無い氏名を、こちらで切ることはしていない。
「架空太郎」は 架空/太郎 とも 架空太/郎 とも読める。ここで当てにいくと、
同一人物の判定 `(family_name, given_name, birth_date, school_id)` が
**人によって違う切り方で並ぶ。** 同姓同名の事故を避けるために
生年月日を守ったのに、姓名の切り方でそれを崩しては意味がない。

### 埋める表を用意した

```bash
pnpm import:legacy:2026 --worksheet
```

`db/private/legacy-supplements-2026.json`（gitignore 済み）に、
足りない値だけが空欄の表を書き出す。旧IDを鍵にしている ――
氏名を鍵にすると同姓同名で崩れる。空欄を埋めて、もう一度実行すれば入る。

### 既定は書かない

```bash
pnpm import:legacy:2026              突き合わせだけ（既定）
pnpm import:legacy:2026 --worksheet  埋める表を書き出す
pnpm import:legacy:2026 --apply      実際に入れる
```

取り込みは一度きりではなく、値がそろうたびに何度も走る。
走らせるたびに書かれると、そろっていない段階で中途半端に入る。
冪等 ―― 2回流しても人も応募も二重にならない。
`--apply` は開発サーバが動いていれば止まる。

出力は**旧IDと件数だけ。** 氏名もメールも出さない。

### 応募日が無い行に、応募日を作らない

応募日があるのは 34 件。無い行（「応募前」など）は
**人だけ入れて応募を作らない。** 埋めれば応募日が創作される。
**応募が無いことは事実である。**

### 入れていないもの

選考の段・成績・接点は入れていない。2期と3期で段の数も呼び名も違い、
同じものを指すか未確認である。

---

## 4.5 画面の指摘（依頼者。実行⑩の後半）

5点の指摘を受けた。**すべて直した**（C-62）。

| 指摘 | 直したこと |
|---|---|
| 期に付いている印は何か | `is_live`（募集期間中）だった。**外した** ―― 意味が画面に書いていない印は邪魔になるだけ |
| 説明がいちいち出る | 単位・母集団・定義の注記を**全画面から消した**。定義はクエリのコメントとこの記録に移した |
| 画面が見切れている | 一覧の画面がダッシュボード用の高さ固定を被っていた。**ページごと送る形に戻した** |
| 一覧が一覧になっていない | 確度順リストは1ページ5行で、48人が**10ページ**だった。全件表示にし、**次の層への入口**を置いた |
| ランキングに顔写真が無い | 成績・確度の両ランキングとヘッドハンティングリストに出した |

★ **送れているのに切れて見えていた**ものもあった。macOS の既定は
スクロールバーを隠すので、行の途中で切れた絵だけが残る。
細い軌道を常時出し、表の見出しを貼り付けた。
**「送れること」は、送れるだけでは伝わらない。**

日程カレンダーと候補者パネルの縦の比率、見出しとセクションの余白も
指摘どおり直した。意匠トークンは変えていない。

`CLAUDE.md` の完了条件から「数字には単位と母集団を表示」を外した ――
画面から消した以上、規律の側に残しておくと次に触る人が戻してしまう。

---

## 5. 確かめ方

```
pnpm test              397 件 全通過（372 → +25）
pnpm exec tsc --noEmit クリーン
pnpm build             クリーン
```

画面では、担当未割当 → 担当決定 → 3軸に点と根拠 → 確定 まで実際に通した。
やることのリンクが 200 で開くことも確かめた。

デモ経路の**14経路すべて**で、次の2つを DOM で数えて 0 件であることを確認した。
目で見て「切れていない気がする」で済ませない。

```
はみ出しているのに、自分も祖先も送れない要素   0 件
色相を持つ計算値（色・面・ふち・図形）          0 件
```

数え方で2つ踏んだ ―― `overflow-y: auto` だけを指定すると `overflow-x` も
`auto` に計算されるので、縦だけ送る器が横の切れを隠す。
`visually-hidden`（読み上げ専用）も、はみ出しとして数えてしまう。

旧データに対しては報告のみを走らせた。**書き込みはしていない。**

---

## 6. 残る課題

実行⑨から引き継いだもの ――

- 認証がない。**プロフィールを誰が変えたか記録できない**
- 顔写真はDB内の data URL。大量運用前にオブジェクトストレージへ移す
- 職種の記録列がまだ無い
- アプローチ可能圏そのものの作成・編集UIが未実装
- **予定とやることは記録層だけ。** 画面から作る・終える入口がまだ無い
- 予定が実際に起きたかを接点として残す手当てが未実装
- `.zoom-bar` 周りに色の直値が残っている。依頼者に未確認
- Pilot判定は **HOLD**

実行⑩で増えたもの ――

- **2期の実データは1件も入っていない。** 4つの値を集めるまで動かない
- **書類選考の評価軸が無い。** 旧データの `doc_score`（満点16）の置き場所が無い。
  軸の呼び名を受け取っていないので、こちらで名付けていない
- **グループ面接の評価軸も無い。** 旧データに点が無い
- 旧データの自由記述の評価（強み・懸念・総評・確認事項）に置き場所が無い
- 点の訂正（E4）は未実装。一度付けた点は上書きできない
- 3期の評価軸は未受領

---

## 7. 追加の指示（実行⑩の終わりに受領）

### 個人名から採点できるレイヤー（C-63）

```
/borderline/{personId}
```

一覧の氏名を押すと、その候補者の**採点用紙**が開く。
面接中に採点者が開く画面なので、**段で絞らない** ―― その期のその人の評価を
すべて、確定済みも含めて並べる。前の段で何点だったかを見ずに次の段は付けられない。

一覧の右パネルとレイヤーは**同じ部品**（`app/_components/scoring.tsx`）を使う。
入口が2つでも規則は1つで、戻り先は打った場所へ返す。

### 白黒に統一（C-64）

61個の色トークンを、明度を保ったまま灰へ写した。
**原典（`basic/DESIGN.md`）も生成物（`app/tokens.css`）も変えていない。**
上書き層 `app/monochrome.css` を1枚足しただけなので、**外せば元へ戻る。**

色相で意味を分けていた箇所（未記入の赤・通過の緑・期限超過の橙）は
濃さの差だけになる。どれも文字とふちで読める形にしてあるが、
一目での区別は弱い。**色を戻すときの指示を待つ。**

### 指示の棚卸し（3周）

前回の指示を1件ずつ機械的に確かめ、抜けを3周かけて潰した。
2周目・3周目で見つかった抜けは4つ ――

| 見つかったもの | 直したこと |
|---|---|
| カレンダーの氏名が升の中で必ず切れていた | 読める一覧を格子の下に置いた（C-65） |
| ページ送りをやめたのに、その見た目が CSS に残っていた | 消した |
| 余白の指定が2箇所にあり、片方が死んでいた | 1箇所に寄せた |
| 確定済みの評価に「この応募はもう動いていない」と出ていた | 確定済みには理由を出さない |

### ロゴが2本のバーの寸法を決める（C-66）

依頼者の指示 ――「ロゴの縦と横の長さと、縦バーと横バーの長さを合わせてみて」。

```
ロゴの横 204px = 縦バー（操作柱）の幅
ロゴの縦  81px = 横バー（現在地の帯）の高さ
```

ロゴ枠は縦バーの左上の角いっぱいに収まり、そこから下へ縦バー、
右へ横バーが伸びる。縦の値は**画像の実寸（1274 × 404）から計算**した ――
実行⑨で取り消したアニメーションでは目分量の 236×106 を使い、
ロゴが外側から生えて見えた。

★ 1つ踏んだ ―― `.zoom-bar` に `height` だけを与えたら、**送る画面でだけ
81 → 58 に潰れていた。** `.hh-main` は縦の flex で、flex item は既定で縮む。
**「指定したのに効かない」は、たいてい別の軸が効いている。**

14経路すべてで、4つの一致（幅・高さ・左上・下端）を DOM で確かめた。

### 集計の但し書きも消した（C-67）

依頼者の指摘 ――「この説明文も消して」。

C-62 で説明を消したとき、**`callout`（帯）に入っていたものを見落としていた。**
枠が違うだけで、中身は同じ集計の但し書きである。4つ消した。

画面から消しただけでなく、そのためだけに引いていた問い合わせも落とした。
**出さない値を引き続けない。**

★ 見落としの型 ―― 「説明を消す」を**クラス名で探した**ので、
別の枠に入っていたものが残った。**枠ではなく中身で探す。**

### 横バーをロゴの右端から続けた（C-68）

依頼者の指示 ――「横バーもつなげてみて」。

C-66 で寸法は揃えたが、**ロゴの右端と横バーの左端に 16px の隙間が残っていた。**
横バーを同じぶんだけ左へ引き戻し、着いた側の角とふちを落とした ――
丸いままだと、くっついているのに切れ目が入って見える。

760px 以下では縦バーが上へ回るので、引き戻しを戻す。
**つなぐ相手が居ないところでつなぐと、帯が画面の外へ出る。**

★ 測っている途中で気づいた ―― **`/applications/[id]` にだけ現在地の帯が無かった。**
いま何を開いているかも、上へ戻る道も出ていない（本文に手書きのパンくずがあっただけ）。
帯を足し、手書きは消した。同じものを2箇所に置かない。

14経路 × 幅 1280 / 1000 / 700 で、つながり・高さ・幅・上端の一致を確かめた。

---

## 8. 面接画面（依頼者から様式を受領）

依頼者から面接シートの様式が届いた ―― 評価軸6つ（各1〜4点）、記入欄10、
最終判定3択、判定補足、変更ログ。

### 作らなかったもののほうが多い

| 様式の項目 | どこに入れたか |
|---|---|
| 評価軸6つ（各1〜4点） | **すでにある**（`evaluation_criteria`。2期・最終面接、各4点満点） |
| 面接官 | **すでにある**（`evaluations.interviewer_staff_id`） |
| 面接日 | 新しく持つ。割当時刻も提出時刻も「面接をした日」ではない |
| 記入欄8つ | 新しく持つ（`interview_sheets`） |
| 最終判定・判定補足 | 新しく持つ。**選考の判定とは別**（下記） |
| 変更ログ | 新しく持つ（`interview_sheet_revisions`。追記専用） |

`interview_sheets` に**点の列は1つも無い。** あったら `evaluation_scores` と
二重になる。列の一覧ごとテストで固定した。

面接官も写していない。写すと**担当を替えたときにシートだけ古い名前を指す。**

### ★★ 最終判定は、選考の判定ではない ★★

選考の通過／不合格は `status_histories` にあり、そこに**「ボーダー」は無い。**
シートに入るのは面接官の所見であって、組織の決定ではない。
列名も `verdict` ではなく `recommendation` にしてある。

テストで「所見を書いても `status_histories` が1件も増えないこと」を固定した。

### 1つの面接 = 1つの評価

面接官が2人なら評価が2件でき、**シートも2枚**になる。
URL の鍵は評価の ID である。応募でも候補者でもない。

### どこから行けるか

```
ヘッドハンティング  氏名 → /people/{id}          → 面接シート
ボーダーライン      氏名 → /borderline/{personId} → 面接シート
名前検索            氏名 → /people/{id}          → 面接シート
応募の経緯          段の名前 → 面接シート
```

### 踏んだもの

★ **送りは camelCase、記録層は snake_case。** 導けると思って読み戻したら、
**保存はできるのに開き直すと記入欄が全部空**になった。
様式の正典を1つ（`INTERVIEW_FIELDS`）に置き、往復をテストで固定した。

★ 既存の規律テストが、書き込む form に「記入できる」印が無いことを捕まえた。
**規律をテストにしておくと、次に作る画面が勝手に外れない。**

★ 白黒がネイティブ部品から漏れていた（C-70）。最終判定のラジオだけが緑。
`accent-color` は色トークンを通らない。**検査に `accentColor` を入れていな
かったので、0件と出ていたのに緑が残っていた。見ていない性質は0件になる。**

### 確かめ方

```
pnpm test              413 件 全通過（397 → +16）
pnpm exec tsc --noEmit クリーン
pnpm build             クリーン

15経路   切れ 0 件 ・ 色相 0 件 ・ ロゴと帯の一致 15/15
```

画面では記入欄10と判定を保存し、開き直して**11項目すべてが読み戻る**こと、
変更ログが第1版・第2版と積まれることを確かめた。

### 依頼者に確認したいこと

**3期の最終面接には評価軸が1本も登録されていない。**
いただいた6軸は2期の最終面接に登録済みのものと同じだが、
3期にも同じ6軸を使うのかは受け取っていない（C-55 で「前期の軸を写さない」と
決めてある）。**写してよいか、指示をいただきたい。**
いまのままだと、3期の面接画面は「評価軸が登録されていない」と出る。

---

## 9. 編集の層・面接タブ・ロゴと配色

### ① 小さなパネルから編集させない（C-71）

プロフィールの編集は、ヘッドハンティングの**右パネル（幅 340px）**に
12項目のフォームを畳んで入れていた。**深い層に出した** ―― `/people/{id}/edit`。
一覧の側には入口だけを残す。アクションは既存のものをそのまま使うので、
**入口を増やしても規則は1つ。**

### ② 面接をタブに上げた（アプローチの下）（C-71）

操作柱のタブが3つから**4つ**になった。`/interviews` はその期の面接を
すべて並べ、1枚ずつシートへ入る。**段で絞らない。**
まだ書いていないものを先に出す ―― 急ぐのは空いているシートである。

### ③ ロゴと配色（C-71）

```
public/brand/logo_gradient.png   1283 × 305（比 4.2066）
```

比が 3.15 → 4.21 に変わったので、**`--logo-h` を 81 → 67 に計算し直した。**
縦バーの幅と横バーの高さも一緒に動く。

配色は3枚重ね。**原典（`basic/DESIGN.md`）も生成物（`app/tokens.css`）も
触っていない。**

```
tokens.css      原典から生成
monochrome.css  白黒の土台
brand.css       線と文字の色 ＋ ロゴから伸びるグラデーション線
```

原則は **「色は線、面は黒」**（NEO ACADEMIA の意匠規定）。
アクセントで面を塗らない。**brand.css を外せば白黒に、2枚とも外せば元へ戻る。**

ロゴの下端から横バーの下端へ、**同じ1本のグラデーション線**を通した。

### ★ 箱は接しているのに、絵が離れていた（C-72）

C-68 で「つながった」と報告したが、**画面では離れていた。**

`getBoundingClientRect()` はどの幅でも隙間 0 を返す。それでも離れる ――
列の隙間をマイナスマージンで打ち消していたが、`.hh-main` は
`overflow: hidden` なので、**親の外へはみ出した 16px が描画で切り取られる。**

列の隙間そのものを 0 にし、`.hh-main` の内側の余白を横バーだけが
打ち消す形にした。横バーは**親の中に収まったまま**左端へ着く。

測り方も変えた ―― 箱ではなく**実際に描かれているもの**で見る。
`elementFromPoint(ロゴの右端 + 1px, 帯の中心)` が横バーを返すか。
**「はみ出しているか」ではなく「そこに何が描かれているか」を見る。**

### ★ 配色を変えたら、その上の文字を測り直す（C-73）

現在地の段が**黒地に黒文字**になっていた（面を黒にしたのに、文字色の
指定がより高い詳細度で残っていた）。灰の補助文字も、白黒化のときから
明るいまま残っており、地の上でコントラスト比 2.5〜4.1 しかなかった。

目分量で暗くしていない。**いちばん暗い地の上で 4.5 を満たす、
もっとも明るい灰**を計算して置いた。色相は変えず、濃さだけ。

```
16経路 × 文字を直接持つ要素すべて
  直す前   1000 件超がコントラスト不足
  直した後 0 件
```

★ 1つ踏んだ ―― 白地用に暗くしたトークンが、**暗い操作柱の上でだけ
読めなくなった。同じトークンを、明るい地と暗い地の両方で使わない。**

### 確かめ方

```
pnpm test              413 件 全通過
pnpm exec tsc --noEmit クリーン
pnpm build             クリーン

16経路   コントラスト不足 0 件 ・ 帯が離れている画面 0 件
```

---

## 10. 2期の実データを本番へ入れた（C-74）

```
本番（DATABASE_URL）   人 61 件 ・ 応募 34 件
```

前半で「生年月日を集める（記録層は変えない）」と決めたが、値は集まらず
投入の指示が繰り返された。**新しいほうを採り、記録層を緩めた**（0023）。

### 無い値を1つも作っていない

```
55 件  名        姓に氏名まるごとを入れ、名は空にする
61 件  生年月日  NULL のまま
31 件  メール    NULL のまま
13 件  学校      「学校未記録」（非活性）へ寄せる
27 件  応募日    無いので応募を作らない（人だけ入れる）
```

画面には「未記入」として出る。値が届いたら編集画面から入れれば済む。

### ★ 踏んだもの3つ

**① 取り込み先を間違えた。** `.pgdata`（手元の砂場）へ入れてしまい、
そこには**創作のデモ 1438 人**が入っていた。実在と創作の同居は C-28 が
禁じている。引き上げて本番へ入れ直した。以後、**書き込み先を毎回名乗る。**

**② 識別した日を「いま」で埋めていた。** 年度の母集団は「選考終了日までに
識別されたか」で決まるため、**61人全員が2期の外へ落ち、一覧に1人も出なかった。**
旧システムの `created_at` を使って直した。

```
直す前  2期 0 人
直した後 2期 53 人（応募 34 ・ 未応募 19）
```

**③ 開発サーバの検査が、本番への投入まで止めていた。**
PGlite の dataDir 衝突を避けるための検査で、本番接続では起きない。

### いま出るもの・出ないもの

```
出る    人を探す（61人）・ファネル（2期の応募 34）・応募の経緯
出ない  ヘッドハンティング／ボーダーラインの一覧
```

出ない理由は1つ ―― これらは**アプローチ状態の記録がある人**だけを出す
（`v_headhunting_list`）。記録するには職員が要るが、**本番に職員が1人も
登録されていない。** 旧データの担当者名を職員として取り込むかは未受領なので、
**作っていない。**

### 確かめ方

```
pnpm test              415 件 全通過
pnpm exec tsc --noEmit クリーン
pnpm build             クリーン

2回流して 人 追加 0 ・ 応募 追加 0（冪等）
```

### 職員とアプローチ状態も入れた（C-75）

人と応募だけでは、ヘッドハンティングとボーダーラインに1人も出なかった。
一覧は**アプローチ状態の記録がある人**だけを出す作りだからである。

```
職員 14 件（旧データの担当者名。メールは 0024 で NULL 可にした）
アプローチ状態 61 件（すべて「未アプローチ」・2期）
```

記録した人は `旧システム取り込み`（非活性の職員）。
**実在の担当者に付け替えていない。** 誰が記録したかは旧データに無い。

「未アプローチ」は嘘ではない ―― その人について、声掛けの記録を1つも持っていない。

★ 旧データの `status`（合格・辞退など）は**写していない。**
それは選考の状態であって、アプローチの状態ではない。

これで2期のヘッドハンティングとボーダーラインに取り込んだ人が出る。

### 期がタブを移ると戻っていた（C-76）

依頼者の指摘 ――「2期を選んでいる時にヘッドハンティングを押すと3期に切り替わる」。

そのとおりだった。操作柱のタブは `href` だけを持ち、**期を持ち回っていなかった。**

```
階層は 期 › タブ › 対象  ―― 期はタブより上の層である。
上の層が、下の層を移っただけで変わってはいけない。
```

`Shell` に期を渡してタブのリンクへ付けた。名前検索も同じ。
期を持たない画面（その人の記録は年度をまたぐ）では付けない。

同じ穴を塞ぐため、`tests/30` に
**「期の切替を出しているのに `Shell` へ期を渡していない画面」を検出する**検査を足した。

2期を選んだ状態で4タブを総当たりし、16通りすべてで「2期」のままを確認。

### 面接エリアが空だった（C-77）

画面はできていた。**空だったのは記録が0件だったから**である。

```
最終面接 32 件 ・ 点 192 件（32 × 6軸）・ 面接シート 32 件
判定 合格 15 ・ ボーダー 11 ・ 不合格 2 ・ 未設定 4
```

入れた段は**最終面接だけ**。旧データが持っているのはその1段のシートだけで、
書類選考とグループ面接の点は記録が無い。無い段の評価を作ると、
**「まだ誰も見ていない」が「0点で通した」に化ける。**

- 6軸の対応は、**こちらで決めていない**（2期のシードが同じ順で登録済み）
- 軸ごとの根拠は旧データに無いので、**無いとそのまま書いた**（評価の文言を作らない）
- 面接日は入れていない（旧 `final_date` に日付でない値が混ざる）
- 記録した人は `旧システム取り込み`。**実在の担当者に付け替えていない**

### 関連団体をアプローチ画面へ接いだ（C-78）

```
関連団体   28 件 ・ 接触記録 240 件 ・ 職員 +3 件
2期のアプローチ画面   接触機会 53 ・ 16 団体
```

ログは**2箇所**にあった ―― 団体の直下（162件）と窓口ごと（256件）。
片方だけ読むと記録の半分以上が消える。同じ日・同じ内容を畳んで 240 件。

推定リーチ（何人に届いたか）は旧データに無いので**作っていない。**
0 を入れると「届かなかった」という別の事実になる。

### ★ 2期の集客開始日が、実測とずれている

```
接触記録の日付   2025-08-27 〜 2026-07-02
2期の期間        2026-02-01（★推測）〜 2026-04-15
→ どの期にも紐づかない 187 件 ・ 2期に入る 53 件
```

**2期の集客開始日は推測値**である（`0002_season_2026`）。
実際の団体折衝は **2025-08-27 から**始まっていた。
日付は集計定義そのものなので**黙って直していない。**
直すときは `season_revisions` に履歴を残す。**指示をいただきたい。**

---

## 11. SNS分析の基盤・候補者追加・アプローチ追加（C-79）

### フォームはまだ無い。受け皿と規則だけを先に置いた

`form_responses` は**届いた回答そのもの**を持ち、人とは別に置く。
いきなり `persons` へ入れると、取り違えたときに元の回答が残らない。

★ **回答そのものは書き換えられない**（トリガ）。
書き換えてよいのは「誰に結び付けたか」だけ。

自動接合は**メール・LINE・電話が1人に絞れたときだけ**。
**氏名では結び付けない** ―― 同姓同名で取り違える。

チャネルは**名前が完全に一致したときだけ**写す。「ライン（友だちから）」を
`LINE` へ寄せない ―― 寄せた瞬間、SNS 別の集計が実際と違う値を出す。
写せなかった回答も落とさず、生の文字列のまま1行として出す。

### チャネルの語が確定した

「分類が決まるまで1行も入れない」としてあった（0件）。依頼者が
**LINE / Instagram を名指し**し、旧 `select_options` にも運営の語が
並んでいたので、**受け取った語として** 13 件入れた。
創作の分類が本番に入らないことは、テストで固定してある。

### 候補者番号

期ごとに 1 から。**欠番を詰めない**（空きを埋めると「3番の子」が2人現れる）。
画面には「次はこの番号」と出すが、確定するのは保存のとき。
取り込んだ61人にも、識別した順に番号を振った。

### 1回の保存で5つの事実が立つ

人・番号・接点・アプローチ状態・フォーム接合。**まとめて1つの取引にする。**
途中で落ちたら人ごと残らないことをテストで固定した。

### ★ デプロイ直前に危険を1つ踏んだ（C-80）

`git add -A` が、運営から受け取った **`.xlsx`（実在の応募管理表）** と、
私が作った **38MB の一時ディレクトリ**を取り込んだ。
どちらも push すれば公開される（origin も mirror も公開）。

即座に取り消し、`.gitignore` へ足した。そのうえで**追跡対象になる
全60ファイル**を旧データの氏名・団体名と `example.test` 以外のメールで
突き合わせ、一致0件を確認してからコミットした。**目で見て確かめない。**
