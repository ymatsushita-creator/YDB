-- =============================================================
-- 0056 KPIにイベント参加人数を追加する
--
-- event_attendances は予定×人物で一意なので、1行を1人と数える。
-- 同じ人が別のイベントに参加した場合は、それぞれの参加として数える。
-- =============================================================

INSERT INTO kpi_metrics (key, label, definition, unit, sort_order) VALUES
  ('event_attendees', 'イベント参加人数',
   'その期のイベントに参加した人数。同じ予定の同じ人は1人として数える。', '人', 10);
