<!-- 生成物。手で編集しない。正典は db/DECISIONS.md（pnpm decisions:parts で作る） -->

# C-223

出典: `db/DECISIONS.md` L9328 ・ 分類 C. 構造の整理・以後の決定

## C-223 欠番45件をコード内コメントから移設した

依頼者の指示 ――「最適化してくれ」。

#### 台帳が事実と違っていた

`.consultant/DANGLING-BASELINE.txt` はこう書いていた。

> 既存の分をここに据え置くのは、**AIが埋められないからである**
> （`CLAUDE.md`「記録にない値の創作」の禁止）。**埋められるのは判断を下した人間だけ。**

★★ **これは事実に反していた。** ★★
実行⑯⑰は記録を残さなかったのではなく、**`db/DECISIONS.md` ではなく
コード内のコメントに書いて終わっていた**（`Hitler.md` §4「コードだけ変えて終わらせない」の違反）。
参照元を数えると偏りがはっきり出る。

```
24件  app/_styles/10-round-16-17.css   ← 45件の過半がこの1本
 5件  app/_styles/02-shell.css
 5件  app/_components/shell.tsx
```

`10-round-16-17.css` は382行で「依頼者の指示」の記載が12箇所。C-172 には
依頼者の発言そのもの（「これ、題名みたいなやつ消していいよ」）と、
なぜ消さずに視覚的に外したかの理由まで書いてあった。

**創作ではなく移設である。** 「記録にない値の創作」の禁止には当たらない。

#### やったこと

`db/DECISIONS.md` に **G節**を新設し、41件を移設した。欠番 45 → **0 件**。

- **各項に原文の所在を必ず添えた。** 判断の正本はコメント側であり、
  ここはそれを番号から辿れるようにした写しである。**新しい事実を足していない。**
- 手がかりが1行しか無い5件は**埋めずに「不足」と明記した** ――
  `C-171`（`approach/page.tsx` に番号だけ）、
  `C-173` / `C-182` / `C-186`（3番号が同じ1行から参照されており区別が付かない）、
  `D-30`（言及1箇所のみ）。
  ★ **埋まっていないことを、埋まったように見せない。**

★ C-173 / C-182 / C-186 は当初1つの見出しにまとめたが、
  **索引が拾うのは見出しの番号1つだけ**なので2件が欠番のまま残った。
  番号ごとに見出しを分けた。**1見出し1番号。**

#### 台帳の締め直し

S13 はラチェットで、**埋めて減ったときも落ちる。** `pnpm decisions:baseline` で
台帳を 45 → 0 件に締め直した。締め忘れた台帳は「45件のままだ」と嘘をつき、
次に増えた1件を隠す。

検証: `pnpm structure` の S13 が「台帳と一致（欠番 0 件）」。索引 268 件・1件1ファイル 265 件。
テスト … `tests/83_dangling_ratchet.test.ts`
