<!-- 生成物。手で編集しない。正典は db/DECISIONS.md（pnpm decisions:parts で作る） -->

# C-159

出典: `db/DECISIONS.md` L7842 ・ 分類 C. 構造の整理・以後の決定

### C-159. 訂正しても一覧から消えていなかった（256人）

★★ **打ち消し行が、元の状態をそのままコピーしていた。** ★★

期の判定を直す訂正は「間違った期の取り込み記録を打ち消す」形だったが、
**打ち消し行それ自体が有効な出来事**なので、`v_person_approach_state` は
同じ期に同じ状態を返し続けていた。実測で、表が2期と言っている
**256人が3期の一覧に残ったまま**だった。

```
打ち消しは「その行を無かったことにする」
だがこの製品の一覧は「最後の有効な出来事」で母集団を決める
→ 打ち消しの行にも、意味のある状態を載せなければならない
```

0041 で `not_in_season`（この期の対象ではない・終端）を足した。
`is_terminal` なので `v_headhunting_list` から自動的に外れる。

★ 既存の `declined`（見送り）は使わない。「見送った」と「その期の対象では
  なかった」は違う事実で、混ぜると運営が見送りを数えたときに狂う。

#### あわせて見つかったこと ―― 70人が「一旦解除」で見送りになっていた

表が3期生候補と書いている77人のうち **70人が `見送り`**、記録は
「依頼により3期候補から一旦解除」（8/13）。**こちらの変更ではない。**
依頼者の最新の指示は「エクセルの内容にすべて従い」なので、
表の側へ揃えて67人を未アプローチへ戻した（3人は別の理由で終端のまま）。

★ 戻したのは**その言い回しの見送りだけ。** 運営が別の理由で見送った人には触らない。
★ 打ち消しではなく**追記**である。違っていれば、もう1行で戻せる。

#### 直した結果

```
特別選考リスト  2期 425 人 / 3期 72 人
表と食い違って一覧に出ている人  9 人
  → 全員「旧システムから移した」2期の実記録を持つ人。3期候補でもある。
    **両方に居るのが正しい**（打診は期ごとの事実である）
```

検証: `tests/63_cohort_split.test.ts`（語で決める／位置で決めない）、
本番の帳簿で母集団を数えた。
テスト … `tests/63_cohort_split.test.ts`
