<!-- 生成物。手で編集しない。正典は db/DECISIONS.md（pnpm decisions:parts で作る） -->

# C-221

出典: `db/DECISIONS.md` L8636 ・ 分類 C. 構造の整理・以後の決定

## C-221 意匠の正典を DESIGN.md に一本化し、生成器の語彙をそちらへ合わせた

依頼者の指示 ――「design.md はそのまま入れるだけ」「デザイン系の他の条件は削除して」
「Hitler に従って改修をやってくれ」。

`basic/DESIGN.md` を Iridescent-Black-design-system（927行）へ差し替えた。
旧版1030行は `bcf18f3` に在る。**新旧を現行文書へ併記しない。**

#### 生成器が原典に追いつけていなかった

差し替え直後、`pnpm tokens` は1件目で落ちた ―― `components.nav-link-active: 未知のプロパティ activeIndicator`。
潰しながら進めれば18往復する形だったので、**先に全件を数えた。**

```
CSS_PROP に無いプロパティ  18種（borderColor 16 / description 10 / activeIndicator 3 …）
解決できない参照           12箇所（{gradients.brand-spectrum} 10 / -soft 2）
```

`resolve()` は参照先がオブジェクトだと落ちる。`gradients.brand-spectrum` は `stops` を
持つので、プロパティ名だけ直しても次はここで止まった。

★ **直すのは生成器の側である**（`CLAUDE.md`「生成物を手で編集せず、作り直す道具の側を直す」）。
DESIGN.md が原典である以上、語彙に合わせるのは `scripts/build-tokens.ts` のほう。

#### 決めたこと

★★ **描き方が一意に決まらない指定は、CSS プロパティへ翻訳せずカスタムプロパティで渡す。** ★★

`accent` / `activeIndicator` / `decoration` / `accentBorder` / `focusRing` などを
疑似要素で出すのか `border-image` で出すのかは**画面側の判断**であり、
DESIGN.md は「どの値を使うか」しか決めていない。生成器が描き方まで決めると、
DESIGN.md に書いていない仕様が生成物に生まれる。`--active-indicator: var(--gradient-brand-spectrum)`
の形で値だけ渡し、描画は CSS に委ねた。

- `gradients` は `:root` の `--gradient-*` に**1度だけ**展開する。
  各コンポーネントへ焼き込むと、同じ長い CSS が51箇所へ複製される。
- `mesh` は重ねた `radial-gradient` で作る。単一の `linear-gradient` では
  DESIGN.md が禁じている「等幅の帯」になる。光源の座標は DESIGN.md 末尾の CSS 例から採った。
- `stops` を持たない mesh（`brand-spectrum-soft` / `-vivid`）は
  DESIGN.md の記述どおり `brand-spectrum` の派生なので、その `stops` を借りる。
- `blur` は CSS 値ではなく指示なので `--gradient-*-blur` として別に出す。
- `borderColor` / `rowBorder` / `itemDivider` は `1px solid` を補って出す。
  ★ `border-color` だけを出すと既定の `border-width` が 0 なので**画面には何も出ない。**
- `description` / `usage` は説明文なので出力しない。
- `headerTypography` などは接頭辞付きで渡す。1クラスに複数の書体指定が同居するため、
  素の `font-size` では衝突する。

#### 併せて消した条件

意匠の条件が複数文書に散っていた（`CLAUDE.md`「同じ要件を複数文書へ複製しない」に反する）。

- `CLAUDE.md` —— 「UI の指示書は持たない／画面の形は依頼者が都度指示／先回りして仕様を書かない」
  ★ DESIGN.md が意匠体系そのものになった以上、この条文は原典と正面から矛盾する
- `SUPERVISOR.md` —— 「色・余白・角丸などの見た目だけの変更をUX改善として承認しない」
- `.claude/agents/reviewer.md` —— レビューで色・余白を指摘しない禁止（DESIGN.md との一致を見る、へ）

`app/tokens.css` の生成規約（`README.md` / `process.md`）は残した。
**あれが DESIGN.md を単一の原典にしている条項**であり、消すと逆に原典が割れる。

#### 未了

`director.md`「一覧の数字には単位と母集団を添える」と `CLAUDE.md`「単位と母集団は画面に
書かない（C-62）」が矛盾している。**どちらを残すかは依頼者の判断待ち**（`HANDOFF.md`）。

検証: `pnpm tokens` → colors=67 typography=14 rounded=9 spacing=12 components=51。
型検査通過。テスト 899件 / 207 suite 全pass（`tests/46_glass.test.ts` の
「生成物に手を入れていない」「ガラスのトークンを焼き込んでいない」を含む）。
テスト … `tests/46_glass.test.ts`
