<!-- 生成物。手で編集しない。正典は db/DECISIONS.md（pnpm decisions:parts で作る） -->

# C-207

出典: `db/DECISIONS.md` L9239 ・ 分類 C. 構造の整理・以後の決定

## C-207 送る器にした瞬間、親の角丸が中身を切り抜く

依頼者の指摘。

★★ **送る器（`overflow-y: auto`）にした瞬間、親の角丸が中身を切り抜く。** ★★
`.sidebar-region` は四隅とも `--rounded-xl` なので、
天端のロゴ（白）の**右角まで丸く削られていた。**

ロゴは横バーへ続く辺なので、**親の右側の丸みを落とす。**
★ ここを落とさないまま丸みだけ `.hh-brand` で消しても、
  **切り抜いているのは親なので効かない。**

原文 … `app/_styles/02-shell.css:65`
テスト … `tests/74_bar_seam.test.ts:39`
