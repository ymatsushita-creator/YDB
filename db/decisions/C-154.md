<!-- 生成物。手で編集しない。正典は db/DECISIONS.md（pnpm decisions:parts で作る） -->

# C-154

出典: `db/DECISIONS.md` L7742 ・ 分類 C. 構造の整理・以後の決定

### C-154. 確度の記入が、書き直した順に出ていなかった（自分で踏んだ）

`tests/64` が単体では通り、**全部まとめて走らせると落ちた** ――
Bへ書き直したのにSが現在値のまま、という並びの入れ替わり。

原因は C-132 と同じ。時計が刻めないほど速く2件入ると `occurred_at` も
`created_at` も同着し、**現在の確度が id（乱数）で決まる。**
0035 は `greatest(now(), max(occurred_at) + 1 microsecond)` で塞いであり、
**その作法を写していなかった。**

★ あわせて訂正の形も 0035 に合わせた ―― **訂正行が新しい段階を載せて元に
取って代わる**（打ち消し1行＋新規1行の2行構成をやめた）。
2行に分けると有効な行が2つ並び、取り消した見立てといまの見立てが
同じ重さで残る。

★ **同じ性質のものに別の形を与えない。** 既にある作法を探さずに書いたのが誤り。

検証: `tests/64_confidence_grade.test.ts`（全体走行で2回連続緑を確認）。
テスト … `tests/64_confidence_grade.test.ts`
