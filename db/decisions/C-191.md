<!-- 生成物。手で編集しない。正典は db/DECISIONS.md（pnpm decisions:parts で作る） -->

# C-191

出典: `db/DECISIONS.md` L9080 ・ 分類 C. 構造の整理・以後の決定

## C-191 縮んだときのロゴの器

実行⑰。依頼者の指摘 ――「幅あってねぇだろうが」。

`.hh-brand` は天端を横バーに合わせるため、**負のマージン＋固定高さ**で置いてある。
縮めるときにそれを解かないと、白い器だけが 56px の列からはみ出し、**絵と器の幅が食い違う。**

★★ **高さは横バーと同じ（`--logo-h`）のまま。** ★★
`height: auto` にしたら縦が合わなくなった（依頼者の指摘）――
ロゴの器は横バーと天端も**厚みも**そろえる約束で置いてある（C-66 / C-128）。
**縮めるのは中のロゴだけ。** 器は高さを保ったまま、幅だけ列に合わせる。

原文 … `app/_styles/10-round-16-17.css:154`
