<!-- 生成物。手で編集しない。正典は db/DECISIONS.md（pnpm decisions:parts で作る） -->

# C-228

出典: `db/DECISIONS.md` L9449 ・ 分類 C. 構造の整理・以後の決定

## C-228 S9 をラチェットにした（永久に赤い検査は、次の漏洩を検知できない）

依頼者の申告 ――「失効させたよ」。`ANTHROPIC_API_KEY` を失効・再発行したとのこと。

★ **この失効はリポジトリから検証できない。** Anthropic Console 側の操作であり、
AIは確認していない（鍵を確認する経路も持たない ―― `Read(.env*)` は deny、
鍵の入力自体も禁止）。**申告として記録する。**「確認した」とは書かない。

#### 行き止まりが2つあった

**1つ目 ―― allowlist は S9 を見ていない。**
`docs/audit/2026-08-19-secret-in-history.md` は「`.audit/allowlist.yml` へ承認つきで
登録する ―― それが既存の正規経路である」と書いていた。**しかし allowlist を読むのは
`kurosaki scan` であって、S9 ではない。** S9 は `git log --diff-filter=A --name-only` の
パスだけを見ており、allowlist を参照していない。**登録しても S9 は赤のままだった。**

**2つ目 ―― 緑にする手段が禁止されている。**
S9 を緑にするには履歴から `.env.localecho` を除去する（`git filter-repo`）しかない。
だが `Hitler.md` §4 は「**履歴改変**」を**人間の明示指示があっても実行しない**禁止事項に
している。鍵を失効させても**ファイル名は履歴に残る**ので、S9 は緑にならない。

★★ **つまり S9 は永久に赤い。** ★★

#### 永久に赤い検査は、検査ではない

★★ **赤が既定になった時点で、S9 は新しい漏洩を検知する力を失っている。** ★★
次に別の `.env` が履歴へ入っても、出力は今日と同じ「✘ S9」である。
**誰も差に気づけない。** 12回連続で同じ所見を出し続けた D5-03 と同じ形である。

#### 決めたこと ―― S13 と同じラチェットにする

既知の分を `.consultant/SECRET-HISTORY-BASELINE.txt` へ据え置き、**増分だけを落とす。**

- ★ **緩めたのではない。** 既知の1件（`.env.localecho`）は台帳に列挙されたまま消えず、
  **通ったときの出力にも「据え置き」として必ず出る** ――
  `履歴に在る秘密 1 件は台帳に据え置き（.env.localecho）―― 鍵の失効が対処であり、履歴からは消えていない`。
  緩めるなら条件を消せばよく、そうしていない（`Hitler.md` §4「結果の虚偽報告」の禁止）。
- ★ **減ったときも落ちる。** 履歴から除去したら台帳を締め直す。
  締め忘れた台帳は「1件のままだ」と嘘をつき、**次に増えた1件を隠す。**
- ★ 台帳は**生成物ではない。** 追加は依頼者の明示指示のみで、承認者と経緯を台帳に書く
  （`.consultant/DANGLING-BASELINE.txt` は生成物だが、こちらは違う）。

検査名も直した ―― 「秘密が履歴に**無く**」→「秘密が履歴に**増えておらず**」。
**名前が実態と違うと、緑を読み違える。**

#### 履歴の除去はやらない

`Hitler.md` §4 が禁じているのに加え、実務的にも要らない ――
GitHub 上に存在した事実・クローン・フォーク・キャッシュは取り消せない。
**失効させれば履歴の除去は無意味になる。** 順番にそういう意味がある。

#### 併せて直した

`docs/audit/2026-08-19-secret-in-history.md` の「現行の鍵の指紋 b952cd9dfda8603b」は
失効前の値である。再発行後は別の値になっているはずだが、**照合していないので更新しない。**
「照合するなら人間が行う」と明記した。**確認していない値を、確認したように書かない。**

検証: `pnpm structure` の S9 が据え置き1件を明示して通過。型検査・テスト・ビルド。
テスト … `tests/83_dangling_ratchet.test.ts`
