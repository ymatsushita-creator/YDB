<!-- 生成物。手で編集しない。正典は db/DECISIONS.md（pnpm decisions:parts で作る） -->

# C-201

出典: `db/DECISIONS.md` L9153 ・ 分類 C. 構造の整理・以後の決定

## C-201 層ごとに、渡す道具そのものを変える

実行⑰。依頼者の指示。

★★ **層（権限）ごとに、渡す道具そのものを変える。** ★★
同じ問いでも、読める先が層によって違う。

★ **画面で答えを伏せるのではなく、AIに渡す道具を減らす** ――
  伏せる作りだと、AIは**一度読んでから隠す**ことになり、
  答えの言い回しに読んだ跡が残る。**渡さなければ、読みようがない。**

問い合わせ自体は全層が使える。変わるのは読める先である。
道具はすべて `SELECT` のみ。件数と要約を返し、行を丸ごと吐かない。
上限（`LIMIT`）を必ず持たせる ―― 返る量が予測できない道具を置かない。

原文 … `src/ai/ask.ts:19,205` ・ `app/ai/actions.ts:43`
テスト … `tests/73_ask_tiers.test.ts`
