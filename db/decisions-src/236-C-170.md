## C-170 しまう／出す（縦バー・横バーの収納）

実行⑰。依頼者の指示。

> 「ロゴの下を押したら縦バーを収納、右端を押したら横バーを収納、
>   もう一度押したら復活。ここのモーションは滑らかに」

★ **`'use client'` を増やさない**（`CLAUDE.md`）。隠したチェックボックスと `:has()`
  だけで組む。JS が無くても動く。
★ **`display: none` で消さない** ―― 消すと動きが付かず、パッと切り替わる。
  **幅と高さを送る。**
★ 動きを減らす設定（`prefers-reduced-motion`）を尊重する。
  切り替わりは残し、滑走だけ止める。

原文 … `app/_styles/10-round-16-17.css:97` ・ `app/_components/shell.tsx:133`
