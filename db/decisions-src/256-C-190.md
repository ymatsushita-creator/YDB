## C-190 欲しい人ランキングを広げる

実行⑰。依頼者の指示。

> 「欲しい人ランキングの横幅を80％にして、右の要素をその分で隠して」

★ 左を画面の80%まで広げ、**右列はその下に潜る。**
  右を消さない ―― 隠れているだけで、タブを畳めば戻る。
★ 重ねるのではなく、左が右の上に乗る形にする（字が重ならない）。

★ この規則は `06-layout.css` の `.hh-col-side` の `grid-template-rows` を
  **同じ詳細度で無条件に上書きしていた**（後段にあるため）。

原文 … `app/_styles/10-round-16-17.css:288` ・ `app/_styles/06-layout.css:71`
