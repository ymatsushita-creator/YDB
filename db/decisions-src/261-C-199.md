## C-199 KPIの変数は、記録から数えられる語だけ（0049）

実行⑰。依頼者の指摘。

★ マスタに無い語は**コマンド側で弾く。**
★ 選べるのは**数えられる語だけ** ―― 画面の選択肢をマスタから作る。
★ 変数が空なら実績を数えない（**目標だけのKPI**）。これは欠落ではなく状態である。

原文 … `db/migrations/0049_kpi_metric.sql:36,51` ・ `src/queries/kpi_metrics.ts:4` ・
`app/kpis/actions.ts:28` ・ `src/commands/kpi.ts:22`
テスト … `tests/72_kpi_metric.test.ts`
