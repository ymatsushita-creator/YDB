## C-206 実画面を触って見つけた崩れ（2回目）

**実画面で確認した崩れの束。** 机上では通っていた。

- 順位の算出をやめたので**全行が「前回なし」**になっていた。
  並んでも何も伝えないので、**何も出さない。**
  ★ 「変動なし」との取り違えは起きない ―― そちらは出し続ける。
- 入りきらないときは**柱ごと送る**（中で送るのをやめた）。
- タブも入口も**切らない**（C-205 と同じ症状が2度出た）。
- 新規行が**薄すぎた**（C-185）。ひと目で分かる濃さにし、左に帯を立てる。
  ★ セルを塗っても**入力欄の白**で埋まって見えない ―― 入力欄そのものに色を敷く。
- ホームのKPIが `auto-fit` で1列に落ち、縦に伸びて下が余った。
- KPIの入力が、素の inline 並びだと**選択の幅が隣の見出しへ食い込む。**
- 確度は `0039` で**人が記入する**ものになった。
  タイルに「算出なし」と出し続けるのは**古い言い方**である。

原文 … `app/_components/headhunting.tsx:45` ・ `app/_styles/02-shell.css:63,89` ・
`app/_styles/10-round-16-17.css:261,326,331,342` ・ `app/page.tsx:455` ・ `src/seed/demo.ts:1281`
テスト … `tests/45_home.test.ts:521` ・ `tests/57_confidence_axis.test.ts:50`
