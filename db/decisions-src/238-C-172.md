## C-172 題名は消さずに画面から外す

実行⑰。依頼者の指示。

> 「これ、題名みたいなやつ消していいよ」

★ **消さずに、画面から外す。** `<h1>` を取ると見出しの段が無くなり、
  読み上げで「いまどの画面か」が分からなくなる。文字は残したまま、
  目に見えないところへ置く（`clip-path: inset(50%)`）。
★ 下の一行（件数・次の番号など）は**残す** ―― あれは題名ではなく、その画面の実数である。

原文 … `app/_styles/10-round-16-17.css:197`
