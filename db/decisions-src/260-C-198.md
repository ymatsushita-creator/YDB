## C-198 角丸は一括指定の後ろで明示的に落とす

一括の `0` に頼ると、**後から別の節が `border-radius` を丸ごと書き換えたときに
右側だけ丸みが戻る** ―― 実際それが起きた。
一括指定の後ろで `border-top-right-radius` / `border-bottom-right-radius` /
`border-bottom-left-radius` を個別に落とす。

原文 … `app/_styles/02-shell.css:117`
テスト … `tests/74_bar_seam.test.ts:7,39`
