## C-168 空行は先頭に置き、呼び名を「追加」から「編集」へ

実行⑰。依頼者の指示。

★ **空行は先頭に置く。** 末尾だと、行を足すたびに画面外へ出て見えない。
★ 呼び名を「追加」から「編集」へ。画面がやっているのは追加だけではない。

前回の保存結果と、いまの行の並びのずれを持つ必要がある（`sheet.tsx:106`）。

原文 … `app/_components/sheet.tsx:92,106` ・ `app/_components/shell.tsx:80`
テスト … `tests/66_events_and_forms.test.ts:109`
