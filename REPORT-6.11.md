# REPORT-6.11 — 試運転の足場（Pilot Validation 1件目）

2026-08-07。**10分での判断。** `REPORT-6.0` 〜 `6.10` は凍結済み。
**この報告書も書いた時点で凍結する。**

---

## 何を選んだか、なぜか

**`/pilot` ―― 試運転の手順を1枚。機能ではない。**

Pilot の目的は学習であり、いまの制約は**観測がゼロ**であって機能の不足ではない。
`REPORT-6.10` の優先順位表は全候補の Confidence が 4 以下だった。
**分母が観測なので、機能を足しても表は書き換わらない。**

だから**実装量あたりの学習量が最大のもの**を選んだ ――
「使ってみてください」を「ここを押してください」に変える1枚である。

辞退（積 80）は選ばなかった。**あれは学習を生まない**（既知の穴を埋めるだけ）。
試運転が回れば、辞退より先に直すものが出てくる可能性がある。

## 作ったもの

- `/pilot` ―― 4手順（担当を決める → 保留を解く → 評価する → 判定して訂正する）を、
  **いま実在する1件へ直接リンク**して並べる
- 各手順に「教えてほしいこと」を1問ずつ添える
- 最後に4問（手が止まった場所 / クリックが多い場所 / 実感と合わない数字 /
  **辞退をいまどこに記録しているか**）

**書き込みは無い。記録層も集計定義も1つも増やしていない。**
既存の `getOpenTasks` を読むだけ。手順が終わったら消してよい。

## 確かめたこと

DOM で確認。2027年度の実データから4手順すべてに実在の1件が入った ――
木村悠人（担当を決める）／吉田颯太（保留を解く）／中村颯太（評価する・判定）。
該当が無い種別は「飛ばす」と出る。

- `pnpm test` **272 件** 全通過 ／ `tsc` クリーン ／ `tokens` 差分なし ／ `'use client'` 0件
- **新しいテストは追加していない。** 判断は既存クエリの読み出しと表示のみで、
  種別が4つそろうことは `tests/13` が既に固定している

## 分類（Pilot Validation の語で）

| 分類 | 件数 | 変化 |
|---|---|---|
| **Validated Operator Problem** | **0** | 変わらず。運営が触るまで増えない |
| **Developer Hypothesis** | 7 | `REPORT-6.10` の B-1〜B-7 を改称。**すべて仮説** |
| Technical Debt | 12 | 変わらず |
| Product Backlog | 4 | 変わらず |

## 次

**実装しない。** `/pilot` を運営担当者に開いてもらい、4問の答えを待つ。

そこで出たものが最初の **Validated Operator Problem** になり、
7つの仮説のどれが本物かが決まる。**辞退が1位のままとは限らない** ――
4問目（辞退をいまどこに記録しているか）の答えが、それを直接教えてくれる。
