node_modules/
.DS_Store
.env
.env.local
.next/
out/
dist/
*.tsbuildinfo
.pgdata/
.tmp/

# 実データの受け入れ口。候補者・職員の氏名とメールが入りうる。
# db/seeds/ は追跡下で GitHub の2リモートへ push されるため、
# 実データは必ずこちらへ置く（docs/pilot/DATA-INTAKE.md）。
db/private/

# 旧NEO-YouthからCodexが抽出した個人情報を含む移行データ
Codexから移行した旧YouthDBデータ_*/
scripts/sim-out/

# 実在データを含みうるもの。**追跡しない。**
# `.xlsx` / `.pptx` は運営から受け取った資料で、氏名を含むことがある。
# `:memory:` は PGlite を ':memory:' で開いたときにできる一時ディレクトリ
# （実行⑩で誤って作った。**リポジトリの外に出せない場所なので無視する**）。
/:memory:/
*.xlsx
*.pptx
.vercel

# 顔写真の原本。**実在の人物が写りうる。**
# 両リモートは公開なので、追跡下に入った瞬間に公開される。
お顔データ/
