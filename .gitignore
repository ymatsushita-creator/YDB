node_modules/
.DS_Store
# 秘密の実体。**完全一致で書かない。**
# `.env.local` と完全一致で書いていたため、`.env.localecho`（リダイレクト先を
# 間違えて作られたファイル）が除外されず、APIキーがコミットされた（0c9cd6e）。
# 1文字ずれても拾えるようにパターンで書く。
.env
.env.*
!.env.example
!.env.sample
.next/
out/
dist/
*.tsbuildinfo
.pgdata/
.tmp/

# ★ ここは**受け入れ口ではない**（監査 2026-08-19 D2-01）。
# 実データの置き場はリポジトリの外 ―― 既定 `../YouthDB-private/`。
# 指定は `scripts/intake-dir.ts`（YOUTHDB_INTAKE_DIR。中を指すと止まる）。
# 以下の行は「間違って中に作られた残骸を、せめて追跡させない」ための保険であって、
# 防壁ではない。ignore の1行に守りを預ける形はやめた
# （`.env.localecho` は完全一致の ignore を素通りしてコミットされた）。
db/private/

# 旧NEO-YouthからCodexが抽出した個人情報を含む移行データ
Codexから移行した旧YouthDBデータ_*/
scripts/sim-out/

# 実在データを含みうるもの。**追跡しない。**
# `.xlsx` / `.pptx` は運営から受け取った資料で、氏名を含むことがある。
# 原本はリポジトリの外（`../YouthDB-private/`）にある。ここも残骸への保険。
# `:memory:` は PGlite を ':memory:' で開いたときにできる一時ディレクトリ
# （実行⑩で誤って作った。**リポジトリの外に出せない場所なので無視する**）。
/:memory:/
*.xlsx
*.pptx
.vercel

# 顔写真の原本。**実在の人物が写りうる。**
# 両リモートは公開なので、追跡下に入った瞬間に公開される。
お顔データ/

# パイロット試験の捨てるDB（C-215）。作り直せるので追跡しない。
.pgdata-pilot/

# セッションの一時退避。追跡もデプロイもしない。
.claude-tmp/
