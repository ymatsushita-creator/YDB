# 引き継ぎプロンプト

新しいセッションの最初に、この節（`---` から `---` まで）をそのまま貼る。

この文書は報告書と違って**常に最新に保つ**。版は切らない。
引き継ぎの内容が変わったら、ここを書き換える。

---

YouthDB の開発を引き継いでほしい。
起業家アカデミーの集客〜選考を一元管理するデータベースと、その上のダッシュボード。

**あなたの作業は実行⑨になる。テーマは UI の大改修である**（依頼者の指示）。

## 0. 最初に読む — Pilot は **HOLD** のまま。ただし状態が1つ動いた

**実行⑧でデプロイ先が決まった**（マネージド Postgres / Supabase）。
止まっていた3件が**2件に減った**（6節）。

しかし **Pilot の判定は HOLD のままである。** ブロッカーはソフトウェアの
品質ではなく、**運用上の証拠が無いこと**だからである。

- **Validated Operator Problem は 0 件**（実行⑥終了時から1件も増えていない）
- **実データでは1周していない。誰も運営として使っていない**
- **Pilot Go を、このブロッカーが残っている間に主張しない。基準も緩めない**
- 判定の全文と Go の条件は `docs/pilot/DECISION-LOG.md`（追記専用）

**★ 実行⑦が出した「シミュレーション Go」を Pilot Go と混同しない。**
架空データで6週回して安定を確認しただけで、別の指標である。

### ★ 実行⑧は報告書を書いていない

**`REPORT-8.0.md` が存在しない。** 完了の定義（15節）は PM Report と
Validation Report を求めているが、実行⑧は C-38 の記録とコミットで止まっている。
**実行⑨の最初に書くか、書かないと決めて理由を残すか、どちらかにすること。**
黙って飛ばさない。

## 1. UI 大改修について — 何が正当な根拠で、何がそうでないか

**ここを取り違えると、実行⑦が6週かけて確かめたことを台無しにする。**

### 正当な根拠がある（これは運営の観測ではなく、憲法適合のギャップ）

`director.md` の「実装前の自己チェック」4項目は**着手条件**である。

```
1. 世界観は壊れていないか？
2. 森が主役になっているか？
3. ユーザーは次に何をすべきか一目で分かるか？
4. このUIは「管理画面」ではなく「コックピット」になっているか？
```

**現在の UI が 2 と 4 を満たしているかは、かなり怪しい。**
`design.md`（`CLAUDE.md` より上位）が要求する中核部品のうち、
**Forest Map / Health Ring / Relationship Map / Community Card /
Person Panel / Activity Timeline は1つも実装されていない。**

さらに `design.md` は **`Never: List → Detail`** と明記しているが、
`/people` は List → Detail そのものである。

**これは上位文書と実装の食い違いであって、開発者の思いつきではない。**
だから UI 大改修に着手すること自体は正当化できる。

### ただし、これは Validated Operator Problem ではない

**直しても Validated は0件のままである。** 運営が触っていないのだから、
「この画面で迷った」という事実はどこにも無い。

- **開発者が気づいたことは仮説。運営の行動だけが検証済みの知識になる**
- **「新しい UI のほうが良い」は観測ではない**
- **架空データで「この画面は分かりやすいですか」と訊かない。**
  実行⑦がシミュレーションで6週試して、この規則は1ミリも動かせないと実証した

**UI 大改修の完了報告に「Pilot Go」「Validated」と書かない。**
書いてよいのは「憲法適合のギャップを N 件塞いだ」までである。

## 2. UI 大改修で必ず踏む4つの衝突 ★最重要

**どれも「勝手にどちらかへ寄せない」対象である。**
上位文書どうしが食い違っているか、記録層に事実が無いかのどちらかで、
**下位の判断（画面）では解決できない。**

### 衝突1. 意匠の生成元が2つあり、内容が正面から食い違う

| 文書 | 序列 | 何を言っているか |
|---|---|---|
| `design.md` | **上位**（`CLAUDE.md` の上） | Soft green / Calm / Organic / Spacious。Linear・Attio・Arc・Notion に着想 |
| `basic/DESIGN.md` | トークンの**実際の生成元** | NEO ACADEMIA ブランド。**「色は線、面は黒」** |

`app/tokens.css` は `pnpm tokens` で **`basic/DESIGN.md` から**生成されている。
`design.md` には frontmatter が無く、色の実数値が1つも無いため、
**上位文書のほうがトークンを生成していない。**

しかも `basic/DESIGN.md` は面に使うトークン（`primary` / `brand-navy` /
`card-tint-*`）を**全部白か黒に倒してある。** アクセント色の面塗りが
構造的に作れない。**「Soft green」「Spacious」とは正面から食い違う。**

**`design.md` 付記が「生成元をどうするかは未決」と明記している。**
**決めるのは依頼者である。** 画像から色を起こして推測で埋めない。

### 衝突2. Health —— 記録層に事実が無い

`design.md` は Forest が Health を持つとし、**Health Ring を中核部品に挙げている。**
しかし **C-18 で Health は作らないと決め、事実の旗3つ**
（滞留・休眠・接点なし）**で代えた。**

**記録層に Health の事実が無い。**
`CLAUDE.md` の「無い記録から数字を作らない」は**上位文書でも変えられない**
（憲法が事実を作り替えることはできない）。

**Health Ring を描きたいなら、まず「何を事実として残すか」から始まる。**
画面の都合で 0..1 の数字をその場で合成しない。それは
「表示のために数え直す」であり、記録 > 集計 > 画面の順序を壊す。

### 衝突3. `'use client'` が 0 件であること

実測して確認した ―― `app/` と `src/` に `'use client'` ディレクティブは
**1つも無い**（grep で4件当たるが、すべてコメント内の言及）。

**Forest Map・Relationship Map・ズーム基調のナビゲーションは、
素のサーバコンポーネントでは作れない可能性が高い。**

`director.md` は「ズームであって List → Detail ではない」と要求し、
`design.md` も同じことを言う。**要求を満たすとクライアント境界が要る。**

> クライアント側のライブラリを採用するのは「クライアント境界を新設する」
> 判断と同義なので、**その判断は単独で示し、他の判断に埋め込まない**

**UI 大改修という大きな変更の中に、こっそり混ぜない。**
先に単独で提示して合意を取る。**「地図を作るには必要でした」は事後報告である。**

なお**書き込みはこれに当たらない** ―― 素の `<form action={...}>` は
サーバコンポーネントに置け、JS 無効でも動く（C-20）。ここは崩さない。

### 衝突4. 段の語が未決のまま、画面に出ている

- **D-11: 森・林・木・幹の正典が未決。** 旧語と新語が同居している
- 実装は D-2（「林は人、木と幹は応募」）のまま。`domain.md` の定義と食い違う
- **画面では「連携先」という第3の語で回避している**（C-32）

**UI 大改修は語を大量に扱う。** 新しい画面を作るたびに語を選ぶことになるが、
**未決を「決まった」ことにしない。** 迷ったら既存の回避を踏襲する。

`CLAUDE.md` 冒頭が言うとおり ―― **食い違いを勝手にどちらかへ寄せない。**
食い違いそのものを記録して残す。

## 3. まず状態を確認する

リポジトリは GitHub にある。

- `https://github.com/NEO-AX/YDB.git`（origin）
- `https://github.com/ymatsushita-creator/YDB.git`（mirror。同じ内容を push している）
- **どちらも public。** 実データは `db/private/` にあり追跡下に無いが、
  漏れれば取り消せない。コミットのたびに確認する

```bash
git clone https://github.com/NEO-AX/YDB.git   # 手元に無い場合だけ
pnpm install && pnpm db:reset && pnpm test
```

**テスト310件**が通れば正常。通らなければ、そこが最初の作業。
`pnpm exec tsc --noEmit` と `pnpm build` も通ることを確かめる。
`pnpm tokens` を流して `app/tokens.css` に差分が出ないことも確かめる。

画面は `pnpm dev`（ポート3111）。**公開先が2つある**（7節）。

### この環境で実際に壊した3つ

1. **開発サーバを起動したまま `pnpm db:reset` を実行しない。**
   PGlite の dataDir にプロセス間ロックが無く、**エラーも警告もなく壊れる**
   ―― **実行⑧でガードを入れた。** ポート3111が使われていたら拒否する。
   ただし**ポートを変えて起動していれば素通りする。** 過信しない
2. **`.pgdata` を触っているプロセスを `kill` / `pkill` しない。** 同上
3. **計測は使い捨てのインメモリ DB で行う**（`freshDb()` + `seedDemo()`）。
   `.pgdata` で測ると上の2つを踏む。**実際に2回壊した**

外付けドライブに置いてあるため、**作業中に抜けると作業ディレクトリごと消える。**
実際に起きたが、push 済みだったので何も失われなかった。
復帰したら `git log -1` / `git status` / `pnpm test` で健全性を確かめ、
`.pgdata` は作り直す。**区切りごとにコミットして push する。**

## 4. 読む順番

**文書には序列がある。上が下に優先する。**

```
vision.md     理念（Why）      ← 内容は未受領。枠のみ
     ↓
director.md   憲法（Principles）
     ↓
domain.md     世界設定（Model）
     ↓
design.md     UI（UX/UI）      ← ★実行⑨の主戦場。2節の衝突1・2を必ず読む
     ↓
CLAUDE.md     実装の規律（Engineering）
     ↓
process.md    開発の回し方（Loop）
```

**`vision.md` は最上位だが内容が未受領で、空の文書は何も上書きしない。**
実際に効く最上位は `director.md` である。

1. **`director.md`** — 憲法。**「実装前の自己チェック」4項目は着手条件。**
   **実行⑨は UI 改修なので、この4項目が全面的に効く。提出時に4つへの答えを添える**
2. **`design.md`** — **UI。実行⑨で最も重く効く文書。** UX 原則の DO / DON'T、
   中核部品8つ、ナビゲーションの規則（`Never: List → Detail`）。
   **末尾の「付記」に、トークン生成元が未決であることが書いてある**
3. **`domain.md`** — 世界設定。トポロジー / ワークフロー / リレーションシップの
   3軸を混ぜない。**10節に未解決がある**
4. **`CLAUDE.md`** — 実装の規律と4つの問い。**3節に可視化の規律がある**
   （単位を割らない・絵は数字と併置・コンポーネントに SQL を書かせない・
   個体を描くときは個人を漏らさない）
5. **`process.md`** — MVP モードの回し方と **Pilot Rule**
6. `db/DECISIONS.md` — **記録の本体。** 特に **C-16 〜 C-38**、
   **A-16**（絞ると9000倍遅い）/ A-17 / A-18 / A-19、
   **D-8**（2026年度）/ **D-14** / D-10 / D-11 / D-13（未決）。
   UI 改修に効くのは **C-18**（Health を作らなかった理由）・
   **C-29**（ブランド）・**C-32 / C-37**（画面の言葉）・**C-33**（軸0本の表示）
7. **`REPORT-7.5.md`（最新の報告書。ここから読む）** → `7.4` → `7.3` → `7.2`
   → `7.1` → `7.0` → `REPORT-6.12.md` → … → `1.0`。
   **`REPORT-8.0.md` は存在しない**（0節）
8. `docs/pilot/` — Pilot の道具10点
9. `basic/*.sql` — 原典（SQL）。設計原則7つが冒頭にある。**変更しない**

## 5. いま何ができているか（実行⑧の到達点）

**採用の1周が画面の中で閉じている。実行⑦から機能は増えていない。
実行⑧で変わったのは、置き場所（デプロイ先）である。**

```
今日やることを見る → 候補者を開く → 担当を決める / 替える / 保留にする・解く →
評価する（軸ごとに点と根拠）→ 確定する → 判定する → 次のステップへ
```

通過すると**次のステップの評価行ができ、それが運転席の「担当を決める」として
並ぶ。** 最終ステップの通過で合格になる。
**判定を押し間違えても、打ち消し行の追記で訂正できる**（通過↔不合格）。

- 画面 **9枚** —— `/cockpit`（運転席）、`/forests/[id]`（森）、`/pilot`（試運転）、
  `/`（ファネル）、`/people`、`/people/[id]`、`/applications/[id]`、
  `/operations`、`/sources`
- マイグレーション **15**、テスト **310**（28ファイル）、
  **`'use client'` 0件**、`TODO(MVP)` **16箇所**
- 書き込みの型（C-20。`hold` を含め5回使って問題なし）

```
判定と更新  src/commands/     ← テストから直接呼べる。ここに事実の確認を置く
受け渡し    app/*/actions.ts  'use server'。結果はコードで返す（氏名を URL に入れない）
表示        app/*/page.tsx    素の <form action={...}>。'use client' は増やさない
```

### UI の現状（実行⑨が触る対象）

```
app/_components/ui.tsx     117行  Card / Kpi / SeasonTabs / Empty / LevelBadge / 整形関数
app/_components/charts.tsx 165行  可視化
app/base.css               306行  手書きのスタイル
app/tokens.css             593行  ★生成物。手で編集しない（pnpm tokens）
app/layout.tsx              59行
```

**部品は Card / Kpi / SeasonTabs / Empty / LevelBadge の5つしかない。**
`design.md` が挙げる中核部品8つ（Forest Map・Forest Card・Community Card・
Person Panel・Task Queue・Activity Timeline・Health Ring・Relationship Map）
とは、名前がほとんど重ならない。

**画面にまだ無いもの** —— 点の訂正（`already_scored` で弾かれる）、
辞退（`withdraw`。理由が必須）、申し送り、新規応募の登録、手で作るタスク。

## 6. 止まっている2件 — 誰の判断待ちか

**実行⑧でデプロイ先が決まり、3件から2件に減った。**
**残る2件はあなたには決められない。勝手に決めて進めない。**

| # | 待っているもの | 誰が | これが無いと |
|---|---|---|---|
| ~~1~~ | ~~デプロイ先~~ | — | **解決（実行⑧。Supabase / Option B）** |
| 2 | **候補者の生年月日**（他4件も） | 依頼する側／社長 | 実データで1周できない。**1人も入らない** |
| 3 | **運営4名の日程** | 運営 | 観測が0件のまま |

### 補足

- **2 について。実データ（旧 NEO-Youth の Supabase 全10表）は受領済み**
  （`db/private/legacy-youthdb-2026-08-07/`。`.gitignore` 済み、SHA-256 検証済み）。
  年度・選考ステップ・評価の満点も回答を得て本番シードに入れた（D-8）。
  **それでも実データで1人も応募者を作れない。**
  欲しいデータは `DATA-QUESTIONS.md` 8節に12件あり、**A段（1人も入らない）が最重要。**
  特に**候補者の生年月日は旧 DB に列そのものが無い。**
  `persons.birth_date` は NOT NULL なので、①改めて集める ②記録層を変えて
  NULL 可にする、のどちらかを判断する必要がある（②は記録層の変更なので勝手に決めない）
- **チャネルが0件だと接点が1行も入らない**（`channel_id` は NOT NULL）。
  選考の1周は回るが、**森と流入元は実データを入れても全部0である**（C-31）。
  **これは UI 改修に直接効く** ―― 森を主役にした画面を作っても、
  実データでは全部0で表示される（9節の Developer Hypothesis 5）
- **`step_history` が 0 行だった。** 段階が移った履歴が無いので、
  **滞留と SLA は実データから再現できない。** 3 の実施でしか観測できない
- **3 について。** 4名は `EXIT_CRITERIA.md` の2名以上を上回るので、基準を緩めずに
  満たせる。**うち1名は別日に2回目**（滞留・SLA は日をまたがないと現れない）

## 7. 公開先について — 混同しない

**3つあり、役割がまったく違う。**

| 場所 | 何か | 使ってよい範囲 |
|---|---|---|
| `https://ydb-ten.vercel.app` | **デモモード**（C-30）。起動のたびに使い捨ての DB を組み立てる | 見せるためだけ。**認証なし、保存されない、架空データのみ** |
| **Supabase**（実行⑧で接続） | **Pilot 本番の DB。** マイグレーション15件と2026年度シードが入っている | **実データはまだ入っていない。** 認証もバックアップも未整備 |
| （未着手） | **Pilot 本番の URL** | `DEPLOY-READINESS.md` の残り6項目が必要 |

デモモードは `VERCEL` を検出すると自動で入る（`YOUTHDB_DEMO=1` で明示も可）。
画面右上に赤字で「デモ ・ 架空データ ・ 保存されません」と出る。
**この経路に実データを載せてはならない。**

**`.env.local` に `DATABASE_URL` がある**（`.gitignore` 済み）。
設定すると PGlite ではなく Supabase を見る。**接続文字列を絶対にコミットしない。**

## 8. 実行⑧で足したもの（C-38）

**デプロイ先の決定を受けて、PostgreSQL アダプタを実装した。**
`DEPLOY-READINESS.md` 4節は Option A（1台＋永続ディスク）を推奨していたが、
**依頼側は Option B（マネージド Postgres）を選んだ。**
費用と運用体制の判断であり、推奨は見立てに過ぎないのでそのまま従った。

- **`src/db/postgres.ts`** —— `Db` は `query`/`exec`/`close` の3つだけなので、
  差し替えは `postgres.ts` と `server.ts` の2ファイルに閉じた
- **直接接続は IPv6 専用で届かなかった。** `db.<ref>.supabase.co` は AAAA しか
  持たず、開発機に IPv6 経路が無く `ENOTFOUND`。**Connection Pooler（IPv4）へ**。
  Session pooler（5432）を選んだ ―― セッション単位の `SET` を保持する必要があるため
- **タイムゾーンを `'connect'` イベントで `SET` すると競合した。**
  プールの使い回しと衝突する。起動パラメータ（`-c TimeZone=Asia/Tokyo`）に変えた
- **`tests/00_environment.test.ts` が `DATABASE_URL` 設定時は実接続に対しても
  同じ検査を走らせる形にした。** 元のコメントが要求していた手順を実際に満たす。
  実測: **PostgreSQL 17.6**、要求機能をすべて確認
- **本番マイグレーション導線**（`pnpm db:migrate:production`）。**何も消さない。**
  2回実行して冪等性を確認した
- **`db:reset` の事故防止**（`DATABASE_URL` 設定時と、ポート3111が使用中なら拒否）

**残っているもの** —— `DEPLOY-READINESS.md` 5節の**6項目が未着手**
（バックアップ・認証・DB分離・エラー確認方法・E2E手順）。
**規約上はバックアップと認証を他より先にやる**（消えたデータは戻らないため）。
`supabase init` / `link` も未実行。

## 9. 未決の一覧（Developer Hypothesis / 構造の穴）

**まだ Validated ではない。開発者の仮説として持ち越す。**
**UI 大改修は、これらを「直した」と主張できる立場にない**（運営が触っていない）。

| # | 何が | 出所 |
|---|---|---|
| 1 | 「評価の観点が未登録」を見ても、運営は誰に言えばいいか分からない | `REPORT-7.1` |
| 2 | 運転席のコンテキスト欄は**先頭のやることしか出さない**。保留ボタンに届かない場合があった | `REPORT-7.1` |
| 3 | 「連携先」という語が運営に通じない可能性がある（こちらが作った第3の語） | `REPORT-7.0` |
| 4 | 担当を替えたとき、前の担当が上書きで消えることに気づけない（C-22） | `REPORT-7.0` |
| 5 | **森と流入元が全部0だと、壊れていると誤解される**（チャネル0件のため）。**UI 改修で森を主役にするほど効く** | C-31 |
| 6 | デモの初回コールドスタート（約12秒）を「固まった」と読む | `REPORT-7.0` |
| 7 | 実行⑥から持ち越しの7件 | `REPORT-6.12` |

**Technical Debt**（実行⑥から12件＋実行⑦で3件＋実行⑧で2件）―― 色に対する
自動テストが無い／`getCommunityMap` にテストが無い／デモのコールドスタート／
`tests/24` のしきい値は手元の1台で決めた値／**Session pooler を選んだので
コネクション数がさらに要るなら Transaction pooler を再検討**／
**`supabase init` / `link` 未実行**。

**Product Backlog**（実行⑥から4件、増減なし）。

## 10. 破ってはいけない規律

原典 `basic/` の設計者は、カラムと制約の1つ1つに「なぜそうしたか」を
日本語で書いている。**コメントは装飾ではなく設計の本体。** それに合わせる。

### UI 改修で特に効くもの ★

- **`app/tokens.css` は生成物。手で編集しない**（直すのは `basic/DESIGN.md`）
- **色・字送り・角丸を直値で書かない。** トークンに寄せる
- **可視化コンポーネントに SQL を書かせない。** データ取得は `src/queries/` に集約。
  UI が独自に数え直すと、同じ画面の別の数字と食い違う
- **集計定義をコンポーネント側に書かない**
- **単位の違うものを同じ軸に置かない。並べても割らない。**
  森は推定値、林は人、木と幹は応募。実行①で日次の 林 → 木 転換率を消し、
  実行②で「推定リーチに対する識別率」を作りかけて消したのは、どちらもこの規律
- **絵は必ず数字と併置する。** 森ビューは表とカードの代替ではなく補助
- **個体を描くときは個人を漏らさない。** 表示用 seed は鍵付きハッシュから作り、
  氏名・メール・`person_id` をそのまま使わない
- **画面に内部の識別子（関数名・ビュー名・原則番号）を出さない**（C-32・C-37）。
  `grep -rn "<code>v_\|<code>f_\|（原則" app/*/page.tsx` で機械的に検査できる
- **画面に出した数字の単位と母集団を、注記として画面上に置く**
- **表示の丸めで、注記が画面上の嘘にならないようにする**（実行⑥で発生）
- **`'use client'` を増やす判断は単独で示す**（2節の衝突3）

### 全体に効くもの

- **`basic/*.sql` は変更しない。** 正典として残す。実装は `db/migrations/`。
  **`basic/DESIGN.md` は例外**（トークンの生成元）
- **適用済みマイグレーションは編集しない。** チェックサムが変わってランナーが止まる
- **理由なき差分を作らない。** 原典から変えたら `db/DECISIONS.md` に
  「発見の経緯・なぜそう直したか・どのテストで固定したか」を書く。
  テスト参照が実在するかは `tests/10_decisions_references.test.ts` が機械的に検査する
- **書き込みの判定を `app/` に書かない。** 事実の確認は `src/commands/` に置く（C-20）
- **操作できる母集団を、画面に出す述語と揃える。** フォームの hidden は
  誰でも書き換えられるので、画面に出ていないものを触れないことは UI では守れない
- **氏名や id を URL に入れない。** 履歴にもログにも残る。結果はコードで返す
- **種別が排他なら、枝を並べずに優先順位で決める**（A-18 / C-23）
- **種別ごとに条件を足したら、全体の性質でテストする**
- **止まる前に、その判断が本当に必要かを確かめる**（C-21）
- **日付は必ず `jst_date()` を通す。** 今日は `jst_today()`
- **`status_histories` は追記のみ。** 訂正は打ち消し行の追記で表現する
- **集計マスタは追加と非活性化で運用する**
- **実データ・実在の個人情報をデモ・スクリーンショット・報告書・
  コミットメッセージに使わない。接続文字列もコミットしない**
- **報告書は凍結、記録は更新。** 版は `REPORT-<実行番号>.<連番>.md`
- **シミュレーションで得た「Go」を Pilot Go と呼ばない**（C-36 / `REPORT-7.4`）

優先順位は常にこう。**意匠の改修であっても変わらない。**

```
記録の構造（何を事実として残すか）
  > 集計の定義（その事実をどう数えるか）
    > 画面（数えた結果をどう見せるか）
```

## 11. 壊しやすいもの（UI 改修で特に踏む）

**踏んではいけない地雷が1つある。** `v_forest_season_activity` と
`v_open_tasks` は、**絞り込みを高い供給元まで降ろすと計算量が爆発する。**
`MATERIALIZED` と `ss.season_id` はそのための指定で、外すと **9000倍**
遅くなる（A-16）。`tests/15` が定義そのものを見張り、`tests/24` が
絞り込みの有無を両方測る。

**森を主役にする UI は、この2つのビューを最も強く叩く。**
新しい画面から呼ぶときは、必ず絞り込みあり・なしの両方で測る。

- **`v_person_season_state` は `persons × seasons` の直積。** 全件 0.2 秒程度で
  当面は問題にならないが、人が増えれば効く
- **集合を返す関数を結合の内側に直接置かない**（実行③。一覧が444秒かかった）
- **性能の比較で `LIMIT` だけを付けない**（実行④。438.9秒が3ミリ秒に見えた）

## 12. 進め方と、これまでに踏んだ失敗

```
1. 動く状態にする
2. 実データで動かして計測する
3. 見つけた欠陥を先にテストへ書き、落ちることを確認する
4. 直す
5. 理由を DECISIONS.md に記録する
6. コミットする
```

3 を先にするのは、直したつもりで直っていないのを防ぐため。

**サイクルの回し方**（`process.md`）—— 1機能を終えたら次をすぐ実装しない。
PM Report → Validation Report → 残課題を4分類（**Validated Operator Problem** /
**Developer Hypothesis** / Technical Debt / Product Backlog）→ 次を推奨する。
**優先順位の最大値ではなく、実装量あたりの学習量が最大のものを選ぶ。**

**サイクル開始時に必ず答える** ――
「この改善は実際の運営で観測された問題か、開発中に想像した問題か」。
**実行⑨の UI 大改修は、正直に「後者」と答えるところから始まる**（1節）。

**繰り返さないこと。**

- **修正が別の場所を壊す。** 変更のたびに「この前提が崩れる箇所」を数え直す。
  **UI 大改修は影響範囲が広い。特に効く**
- **記録が実装より厳密に見える。** 実行⑥で2回、実行⑦でも1回起きた ――
  D-14 に「`channel_id` は NULL 可」と書いたが実際は NOT NULL だった（C-31）。
  **記録を信じた人は確認をやめる。記録が無いより悪い**
- **デモデータが検証したい経路を踏んでいない。同じ見落としを5回繰り返した。**
  **どこに置くかも経路の一部である**
- **目視は検証ではない。** DOM か SQL の値で確かめる。
  **UI 改修では特に誘惑が強い。「見た目が良くなった」は検証ではない**
- **作り物のデータから出た数字をドキュメントに残さない。** 追試できない
- **単位の違うものを割らない**（推定リーチ ÷ 実人数）
- **表示の丸めで、注記が画面上の嘘になる**
- **丸め込みが、欠陥そのものを隠す。辻褄が合っている＝正しい、ではない**
- **バグが偶然に作っていた経路は、バグを直すと一緒に消える**（`tests/13`）
- **`LEFT JOIN` の `ON` に絞り込みを書いても、行は落ちない**（A-17）
- **同じ言葉を2つの定義で使わない**（実行③。「林」が2つの意味を持った）
- **「数えるか」と「動いているか」を同じ述語で扱わない。**
  `v_countable_applications` と `v_active_applications` は別物
- **文書だけ直して実装が追随しないと、語が2重定義になる**（実行⑤）
- **内部の識別子を画面に出さない**（C-32 → C-37。読む範囲を画面全体に
  広げて初めて8件見つかった）
- **架空データのシミュレーションは、実運用の観測の代わりにならない**

## 13. 技術構成

- PostgreSQL 15+ 想定。開発とテストは **PGlite**（WASM 版 PostgreSQL 18）。
  **本番は Supabase**（PostgreSQL 17.6、Session pooler 経由。実行⑧で実測）
- `src/db/client.ts` の `Db` は `query`/`exec`/`close` の3つだけ。
  **`DATABASE_URL` があれば `src/db/postgres.ts`**、無ければ PGlite（`.pgdata`）
- **`isDemoMode()`**（`src/db/server.ts`）。`VERCEL` を検出すると
  インメモリの使い捨て DB を起動時に組み立てる（C-30）
- Next.js App Router（**サーバコンポーネントのみ、`'use client'` が0件**）。
  **クライアント境界の新設は単独の判断として示す**（2節の衝突3）
- テストは `node:test`。フレームワーク無し。**28ファイル・310件**
- Tailwind も shadcn も認証も lint 設定も持たない。**不足ではなく設計判断**
  （ただし**認証は、公開する時点で前提が変わる**）。
  **UI 大改修で CSS フレームワークを入れるなら、それも単独の判断である**
- デザイントークンは `basic/DESIGN.md`（NEO ACADEMIA ブランド）の frontmatter から
  `app/tokens.css` を生成（`pnpm tokens`）。**生成物は手で編集しない**
- ロゴは `public/brand/logo_black.png` / `logo_white.png`

## 14. 未決の一覧（構造・仕様）

### 実行⑤で決まったこと（実装はまだ追随していない）

- **トポロジーとワークフローを混ぜない。** 森・林・木は Forest / Community /
  Person という**エンティティの階層**。Interested / Applicant / Accepted は
  Person に付く**状態**。層が違う
- **Application は Entity。属性へ降格させない**
- **Member は Person の属性ではない。** 合格 ≠ 在籍
- **Unknown は Person の状態として持てない**

**実装は D-2 のままで、`domain.md` の定義とコードは食い違っている（D-11）。**
中途半端に直すほうが危険なので、このまま置く。
**画面では「連携先」という第3の語で回避している**（C-32）。

### `domain.md` 10節

| # | 内容 | 扱い |
|---|---|---|
| 10-1 | `partner_relations` と Relationship の**語が衝突する** | **未決。** 森と人は「接触があった」で代用 |
| 10-2 | **Task の定義が無い** | 記録層を作らず**導出**にした（C-17） |
| 10-3 | **Interested（芽）の判定事実** | **触っていない** |
| 10-4 | **Forest / Community の実体** | `partners` に親を1本足した（C-16） |
| 10-5 | **Membership の粒度** | **触っていない** |
| 10-6 | 実装段階を憲法で上書きするか | Health は作らず事実の旗で代えた（C-18）★2節の衝突2 |
| 10-7 | `director.md` の IA に重複 | **触っていない**（憲法なので下位から直せない） |

### 以前からの未確定

- **D-8 `seasons` の実数値。** ★解けた（2026年度・4ステップ・6軸・書類16点満点）。
  **応募締切のみ推測値**
- **D-10 個人情報削除を受けた人の応募を、木に数え続けるか。** **未決のまま**
- **D-11 段の語（森・林・木・幹）が旧定義のまま。** **未決のまま。★2節の衝突4**
- **D-14 書類選考の軸は凍結。流入チャネルは廃止ではなく未実施。**
  **チャネルが0件だと森・流入元が全部0になる**（C-31）
- **トークンの生成元（`design.md` か `basic/DESIGN.md` か）が未決。★2節の衝突1**

仮に置いたものは `DECISIONS.md` **D-12** に一覧がある。
コード中の `TODO(MVP)` は **16 箇所**。**推測で埋めた事実は1つも無い。**

## 15. リポジトリの外にあるもの

- `<repo>/../academy-ui-assets/` … **UI 素材ライブラリ**（実行②で作成、約10MB）。
  **実行⑨で使う可能性がある。** リポジトリに入れるかは未決（実行②から持ち越し）
- `<repo>/.pgdata/` … PGlite のデータ。`pnpm db:reset` で作り直せる
- `<repo>/.env.local` … **`DATABASE_URL`（Supabase）。`.gitignore` 済み。
  絶対にコミットしない**
- `<repo>/db/private/legacy-youthdb-2026-08-07/` … **実データの受け入れ口。**
  `.gitignore` 済み。**まだ `.pgdata/` へは取り込んでいない**（6節）
- `<repo>/scripts/sim-out/` … シミュレーションの生の出力（JSON）。`.gitignore` 済み
- ルート直下の **`.pptx` 2件**（DB構造説明・社長向け説明、2026-08-08）は
  **追跡下に無い。** 扱いは未決

## 16. 版の規約と完了の定義

- 報告書は `REPORT-<実行番号>.<連番>.md`。連番は0から。
  **実行⑨の第1報は `REPORT-9.0.md`**
- **`REPORT-8.0.md` は存在しない**（0節。実行⑧は報告書を書いていない）
- **報告書は書いたら凍結する。** 追記が要るときは新しい連番を作る
- **記録（`db/DECISIONS.md`）は更新する。** ここだけは最新を保つ
- **この `HANDOFF.md` も更新する。** 版は切らない
- **コミットは origin と mirror の両方へ push する**

完了の定義。

- [ ] `pnpm test` が全通過する（新しい判断にはテストが1つ以上ある）
- [ ] `pnpm exec tsc --noEmit` がクリーン
- [ ] `pnpm build` が通り、`pnpm tokens` で `app/tokens.css` に差分が出ない
- [ ] 実データで動かし、**DOM か SQL の値で**結果を確認した（目視だけで判断しない）
- [ ] 画面に出した数字の**単位と母集団**が、注記として画面上にある
- [ ] **画面に内部の識別子が出ていない**（`grep` で検査した）
- [ ] `db/DECISIONS.md` に理由とテスト参照を書いた
- [ ] `TODO(MVP)` の一覧を出した
- [ ] **PM Report と Validation Report を出した**
- [ ] **`director.md` の自己チェック4項目への答えを添えた**（UI 改修では必須）
- [ ] **2節の4つの衝突について、勝手に寄せていない**（判断が要るものは判断を仰いだ）
- [ ] **Pilot Go を主張していない**（HOLD が続いている間）
- [ ] **シミュレーションの「Go」と Pilot Go を混同していない**
- [ ] **UI 改修の成果を「Validated」と書いていない**（運営が触っていない）

---
