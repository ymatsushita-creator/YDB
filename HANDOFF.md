# HANDOFF.md — 実行⑩への引き継ぎ

## 現在の状態

- 主画面はヘッドハンティング / ボーダーライン / アプローチの3つ
- 現行語彙は「アプローチ可能圏 / 構成コミュニティ」
- 旧生態系比喩は現行UI・現行仕様で使用しない
- 適用済みSQLの `forest` / `community` は互換性のため内部に残る
- 候補者プロフィール、担当者メモ、顔写真、アプローチ状態を画面から編集できる
- 成績・根拠・判定は既存の選考記録画面から編集する
- 確度・順位・集計値は導出値なので直接編集しない

## 実行⑨で追加した記録層

- `0016_approach.sql` — アプローチ状態の追記履歴
- `0017_candidate_confidence.sql` — 確度スコア
- `0018_person_profile_editing.sql` — 顔写真とプロフィール変更スナップショット

## 文書整理

現行要件を複数文書へ複製する運用を廃止した。
各文書の責任は `CLAUDE.md` 冒頭にある。旧条文はGit履歴を参照する。
凍結済み `REPORT-*.md` と過去の `db/DECISIONS.md` は変更しない。

## 残る課題

- 認証がないため、プロフィール変更者はまだ記録できない
- 顔写真はDB内の data URL。大量運用前にオブジェクトストレージへ移す
- 学年・卒業年度・職種の記録列はまだない
- アプローチ可能圏そのものの作成・編集UIは未実装
- Pilot判定はHOLD。実運用の観測はまだない

## 完了確認

```bash
pnpm test
pnpm exec tsc --noEmit
pnpm build
pnpm tokens
```

変更理由は `db/DECISIONS.md` に追記し、次の報告書は `REPORT-10.0.md` とする。
