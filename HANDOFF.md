# HANDOFF.md — 実行⑯への引き継ぎ

実行⑮は**帯の見た目**（C-128）と、**訂正の道が無い記録を潰すこと**だった。
経緯は `REPORT-15.0.md`、決定は `db/DECISIONS.md` の **C-128 〜 C-133**。

---

## 実行⑯が最初に判断すること

### ★★ 直した画面が、依頼者の手元へ届いていない（C-134）

実行⑮の途中で依頼者から送られた画面は、**C-128 の1つ前（`b643378^`）の
描画**だった ―― 通し番号・折り返し・天端の切れ・11px が**すべて一致**した。

```
コードは直っている（テストも通っている）。**出していないだけ**である
開発サーバなら再起動 ／ 本番なら pnpm deploy:production
```

★ **「直した」と「見えている」は別。** 見た目の指示に答えるときは、
**どこに出せば見えるのか**まで一緒に言う。

### ★★ 0036 / 0037 が本番へ入っていない ―― 出すのは依頼者の判断

```
0036  打ち消しの範囲を縛るトリガ（別の人・別の団体・別の期を消せた穴を塞ぐ）
0037  点と根拠の変更ログ（E4 の記録層）
```

★ **こちらでは流していない。** 出すときは `pnpm db:migrate:production`
（控えは道具が書き込む前に自分で取る）。**実行できる塊で渡したら、渡した
あとの状態を本番の帳簿に聞く**（C-121。渡した時点で流したのと同じである）。

★ 0036 はトリガの追加だけ、0037 は新しい表だけで、**既存の行には触らない。**

### ★ 依頼者の指示待ち ―― 受領していない値

```
3期の日程4つ        すべて推測値（2期の周期を1年ずらした。C-55）
3期の目標応募数      未受領（2期の 100 を写していない）
書類選考の軸        両期とも空。応募管理表に**呼び名が無い**（満点16だけ確定）
グループ面接の軸     両期とも空。旧データに点が無い
確度の帯            運営の基準（020/050/080/100％＋状態語）が未取り込み（C-127）
シート52の39人      二次選考の満点が表から読めない
```

★ **呼び名を受け取ってから足す。** こちらで名付けるとマスタとして固定化する。

### ★★ 本番の場所を間違えない（C-123 / C-124）

```
本番        https://youthdb.vercel.app   yuzy742's projects / youthdb
見ない場所   yujis / ydb  ―― mirror（ymatsushita-creator/YDB）。依頼者の判断で
            push しないので古いまま
```

**出すときは `pnpm deploy:production`**。`.vercel/project.json` を読んで
約束した先かを指紋で照合してから渡す。`vercel --prod` を直に打つと照合を飛ばせる。

★ 実行⑮の環境では `vercel ls` が **Not authorized** だった。
**この場所からは本番へ出せない**（依頼者の手元で打つ形になる）。

### ★ 本番はデモモードのまま（C-94）

実データを出す前に必要なもの ―― **①認証 → ②戻し方の確認 → ③DBの分離。
①を飛ばして出さない**（C-84）。

---

## 実行⑮で分かったこと（同じ穴に落ちないために）

### ★★ 記録層に形があることと、道が通っていることは別

打ち消し列を持つ表は4つ。**画面から打ち消せたのは1つだけ**だった。
`person_notes` に至っては、コマンドが打ち消しを受け取れるのに
**渡していたのはテストだけ**である。

```
規律に「打ち消し行で行う」と書いてあっても、その手段が SQL しかなければ、
運用者にとっては**その規律は存在しない**
```

見張りを置いた（`tests/58`）―― 打ち消し列を持つ表が増減したら落ちる。

### ★★ 後から足した表は、**先にある表の「縛り」まで写していない**

0016 / 0027 / 0035 は訂正チェーンの再帰判定まで写したのに、
**打ち消せる範囲の縛り（0007）は写していなかった。**
写経は「動く部分」だけが写り、**防いでいる部分は落ちる。**

### ★★ 同じ瞬間に2件入ると、`now()` は手掛かりにならない

`occurred_at` も `created_at` も `now()` なので、続けて書くと同着し、
**現在の状態が id（乱数）で決まっていた**（C-132）。
PGlite の時計はミリ秒刻みで、**テストで普通に同着する。**

```
テストが「ときどき落ちる」は、テストの都合ではなく製品の性質を指していた
```

### ★ 狭いほうから通す

E4（点の訂正）は**未確定の評価だけ**にした。**広いほうは要ると分かってから
足せるが、逆はできない** ―― 通っているあいだに書かれた行は残る。

### ★ 合言葉の内側は見ていない（変わらず）

画面の確認は、**同じ CSS で同じ形だけを再現した頁**を作って測るところまで。
**見ていないものを見たと書かない。** 実データの画面は依頼者に見てもらう。

---

## 画面の形は依頼者が決める

`design.md` は無い（実行⑨で削除）。**先回りして仕様を書かない。**
決まったことだけを `CLAUDE.md` と `basic/DESIGN.md` に残す。

★ 実行⑮で画面に足したのは**訂正の入口だけ**である（依頼者の指示の範囲）。
畳み（`<details>`）と2つ目のボタン（`formAction`）で、
**`'use client'` は増やしていない**（C-95 / C-104）。

---

## 残る課題

- ★ **0036 / 0037 が本番未適用**。出すのは依頼者の判断
- ★ 次にそろえるものは**依頼者の指示待ち**（日程・目標応募数・軸の呼び名・確度の帯）
- 認証がない。**誰が入ったかを記録できない**（C-84）。
  メモの「書いた人」も点を直した人も**自己申告か、空である**
- 本番はデモモードのまま。実データを出すのは依頼者の判断（C-94）
- 推薦枠は**「置いていない」へ戻せない**（状態は NOT NULL。記録の無さを表す行が無い）
- シート52 の39人の結果を取り込んでいない（二次選考の満点が表から読めない）
- 応募管理表は52シート中6枚しか使っていない（未使用の一覧は `REPORT-13.0.md`）
- 関わり方（0030 / 0031）は自由入力なので**集計できない**。語を受け取ったらマスタへ
- AWARD参加・関連学生を**接点として入力する道が画面に無い**
- ★ **顔写真を全員に入れる前に、写真の配り方を決める**（C-125）
- 予定とやることを画面から作る・終える入口がまだ無い
- 2期の集客開始日が推測値のまま
- 表は全件を出す（ページ送りが無い）。重さは測った（C-125）―― 効くのは写真
- 団体の名前・既存行の接点は表で直せない
- `/pilot` は繋いだが、**Pilot が走れば役目を終える。** 観測が取れたら消す
- Pilot判定は **HOLD**

---

## 守ること

- `basic/` は変更しない。**適用済みマイグレーションを編集しない**（新番号で足す）
- **`schema_migrations` は `migrate()` 自身の帳簿。マイグレーション番号で育てない**（C-121）
- `'use client'` は**表と追従光の2つだけ**（C-95 / C-104）。増やさない
- 層の判定は `src/auth/tiers.ts` の `canOpen` **1箇所**
- **操作可能な母集団と画面に出す母集団を一致させる**
- **帯に重み付けの札（必須／加点）を出さない**（C-134。依頼者の指示）。
  重み付けは記録層に残っている ―― 消したのは出し方であって、事実ではない
- **打ち消しは同じ相手の中だけ**（0036）。打ち消す相手は**コマンドが記録層から引く**
  ―― 画面から出来事の ID を渡させると、見ていない行を打ち消せる
- **状態の出来事は、直前より後ろに置く**（C-132）。`now()` は同着する
- **点は確定前だけ直せる**（C-133）。保存と訂正の入口を1つにまとめない
- 実在個人情報をデモ・テスト・スクリーンショット・報告書・コミット文へ使わない
- 実データは `db/private/`（gitignore 済み）。`*.xlsx` も追跡外。
  **両リモートは公開である**
- `.env.local` の接続文字列を出力しない・コミットしない
- ★ **`git add -A` を確かめずに打たない**（C-80）
- 開発サーバ起動中に `pnpm db:reset` を打たない
- 記録にない値を創作しない。**読めない値を、意味のある列にしない**
- 単位と母集団は**画面に書かない**（C-62）
- **記録に無い格付け・閾値を画面に名乗らせない**（C-127）
- 落とす空白は `src/commands/text.ts` の `BLANK_CHARS` **1箇所**（C-126）
- **本番へ書くコマンドを実行できる塊で渡したら、渡したあとの状態を読み直す**（C-121）

---

## 完了確認

```bash
pnpm test
pnpm exec tsc --noEmit
pnpm build
```

現在 **638件 全通過**。`tsc` / `build` クリーン。

見張りとして置いてあるもの（実行⑮で足した2つ）――

```
tests/58_corrections.test.ts   打ち消しの道・打ち消しの範囲・置いた順
tests/20_score.test.ts         点の訂正（E4。確定後は直せない・版は追記専用）
```

変更理由は `db/DECISIONS.md` に **C-135 から**追記し、
次の報告書は `REPORT-16.0.md` とする（実行⑮の追補は `REPORT-15.1.md`）。
