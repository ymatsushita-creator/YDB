# HANDOFF.md — 実行⑮への引き継ぎ

実行⑭は**説明のつかない本番書き込みの特定**と、**3期を2期にそろえること**だった。
経緯は `REPORT-14.0.md`、決定は `db/DECISIONS.md` の **C-121・C-122**。

★ 実行⑬から最上段で引き継がれた2件は、**どちらも片付いている。**

---

## 実行⑮が最初に判断すること

### ★ 依頼者の指示待ち ―― 次に何をそろえるか

「まず2期で完成させて、それを3期に適用していく流れ」は**果たした**（C-122）。
2期と3期は**段・軸・重み付けが完全に同じ形**になっている。

次の指示が無い状態で、こちらから足せる形は無い。**残っているのは受領待ちである。**

```
3期の日程4つ        すべて推測値（2期の周期を1年ずらした。C-55）。★由来の印は消していない
3期の目標応募数      未受領（2期の 100 を写していない）
書類選考の軸        両期とも空。応募管理表に**呼び名が無い**（満点16だけ確定）
グループ面接の軸     両期とも空。旧データに点が無い（A〜Eは組分けで評価ではない）
```

★ **呼び名を受け取ってから足す。** こちらで名付けるとマスタとして固定化する。

### ★ 本番はデモモードのまま（C-94）

```
https://youthdb.vercel.app   VERCEL=1 → デモ（PGlite インメモリ）。実データは1件も出ていない
```

実データを出すのは依頼者の判断である。出す前に必要なもの ――
**①認証 → ②戻し方の確認 → ③DBの分離。①を飛ばして出さない**（C-84）。

★ `applied_by`（C-121）が答えるのは「**どの道具が書いたか**」であって、
**人ではない。** 認証はまだ無い。

### ★ 本番DBの状態（実行⑭の適用後）

```
帳簿       0001 … 0035 適用済み（最後は 2026-08-13 16:20:11 JST・★名乗り無し）
           ―― 名乗り無し ＝ applied_by を足す前に入った行。以後の適用には名乗りが残る
シード     0001〜0006（0006 が3期を2期にそろえる）
2期・3期   特別選考(軸9 必須3/加点6) 応募受付(0) 書類選考(0) グループ面接(0) 最終面接(軸6)
デモ期     期9999（幻のデモ期。依頼者の指示で入れたもの。応募9件は創作）
実データ   人512 ・ 応募43（うち9件はデモ期） ・ 評価41 ・ 接点39 ・ 団体28
```

控え ―― `db/private/backups/`（gitignore 済み）。**`db:migrate:production` が
書き込む前に自分で取る**ようになった。手動の `pnpm db:backup` も従来どおり。
★ **取れたことは、戻せることの証明ではない。** 戻し方は `docs/pilot/BACKUP.md`。

---

## 実行⑭で分かったこと（同じ穴に落ちないために）

### ★★ 実行できる塊で渡した本番書き込みは、自分が流したのと同じ

実行⑬の「説明のつかない書き込み」の答えは、**実行⑬自身が最終報告の末尾に出した
```bash の塊**だった。この環境ではそれは押せるボタンになる。41秒後に本番へ
0033〜0035 と seed 0005 が入り、14秒後に依頼者から「やれ」が届いていた。

```
渡す前の状態を前提に話を続けない。渡したあとの状態は、本番の帳簿に聞く
```

### ★★ 状態を伝える道具が嘘をつくのは、状態を読まないより悪い

読み違えを塞ぐために足した「適用前 ―― …」の行が、**1回目に嘘をついた**
（`applied_by` を読もうとして落ち、catch が「帳簿が無い」に翻訳した）。
本番の帳簿は35行あった。`ledgerTail()` に切り出してテストの下に置いた。

### ★ 行数から段数を推測しない

実行⑬は控えの `schema_migrations 35` を「0032まで」と読んだ。**35行＝0035まで**。
帳簿は**名前で**言わせる。

### ★ `xmin` は「誰が書いたか」の手掛かりになる

`applied_at` が同じでも、`xmin`（書いたトランザクションの番号）の連番を見れば
**1回の実行で続けて書かれたか**が分かる。0035 の直後の連番で seed 0005 が
入っていたことが、走った道具の特定につながった。

★ ただし `track_commit_timestamp` は **off** なので、xmin から時刻は引けない。

### ★ `application_name` は印字可能なASCIIしか通らない

それ以外は PostgreSQL が `\xNN` へ置き換える。**名乗りに日本語を使わない。**

### ★ 期をまたぐ検査は、期で絞る（C-54 から3度目）

`tests/22` の「最終面接に6軸」「満点24」は期で絞っておらず、3期に同じ6軸が
入った途端に12軸・48点になった。**「1件しかない」を前提にしない。**

### ★ 本番の形は、シードが作る形と違うことがある

3期の特別選考9軸は実行⑫の取り込みが**本番へ直接**入れたもので、
**シードには無かった。** 流す前に「流す先の形」を作って当ててみる（`tests/52`）。

---

## 画面の形は依頼者が決める

`design.md` は無い（実行⑨で削除）。**先回りして仕様を書かない。**
決まったことだけを `CLAUDE.md` と `basic/DESIGN.md` に残す。

★ 実行⑭は**画面を1行も触っていない。** 触ったのは記録層・シード・道具・テストだけ。

---

## 残る課題

- ★ **次にそろえるものは依頼者の指示待ち**（3期の日程・目標応募数・軸の呼び名は未受領）
- 認証がない。**誰が入ったかを記録できない**（C-84）。
  表の「入力者」もメモの「書いた人」も**自己申告である**
- 本番はデモモードのまま。実データを出すのは依頼者の判断（C-94）
- **シート52 の39人の結果を取り込んでいない**（二次選考の満点が表から読めない）
- 応募管理表は52シート中6枚しか使っていない（未使用の一覧は `REPORT-13.0.md`）
- 関わり方（0030 / 0031）は自由入力なので**集計できない**。語を受け取ったらマスタへ
- 推薦枠ステイタス（0035）は**画面から訂正できない**（打ち消しは SQL でしか書けない）
- AWARD参加・関連学生を**接点として入力する道が画面に無い**
- 点の訂正（E4）は未実装
- 顔写真はDB内の data URL。大量運用前にオブジェクトストレージへ
- 予定とやることを画面から作る・終える入口がまだ無い
- 2期の集客開始日が推測値のまま
- 表は全件を出す（ページ送りが無い）。**数百行の重さを測っていない**
- 団体の名前・既存行の接点は表で直せない
- `/pilot` は繋いだが、**Pilot が走れば役目を終える。** 観測が取れたら消す
- Pilot判定は **HOLD**

---

## 守ること

- `basic/` は変更しない。**適用済みマイグレーションを編集しない**（新番号で足す）
- **`schema_migrations` は `migrate()` 自身の帳簿。マイグレーション番号で育てない**
  （新しい DB では帳簿を作ってから 0001 を流す。列は起動処理と ALTER で足す。C-121）
- `'use client'` は**表と追従光の2つだけ**（C-95 / C-104）。増やさない
- 層の判定は `src/auth/tiers.ts` の `canOpen` **1箇所**。画面ごとに書かない
- **操作可能な母集団と画面に出す母集団を一致させる**
- 実在個人情報をデモ・テスト・スクリーンショット・報告書・コミット文へ使わない
- 実データは `db/private/`（gitignore 済み）。`*.xlsx` も追跡外。
  **両リモートは公開である**（`origin` = NEO-AX/YDB ・ `mirror` = ymatsushita-creator/YDB）
- `.env.local` の接続文字列を出力しない・コミットしない
- ★ **`git add -A` を確かめずに打たない**（C-80）。運営から受け取った
  `.xlsx` / `.pptx` はリポジトリ直下に置かれる
- 開発サーバ起動中に `pnpm db:reset` を打たない
- 記録にない値を創作しない。**読めない値を、意味のある列にしない**
- 単位と母集団は**画面に書かない**（C-62）。定義はクエリのコメントと `db/DECISIONS.md`
- **本番へ書くコマンドを実行できる塊で渡したら、渡したあとの状態を読み直す**（C-121）

---

## 完了確認

```bash
pnpm test
pnpm exec tsc --noEmit
pnpm build
```

現在 **590件 全通過**。`tsc` / `build` クリーン。

見張りとして置いてあるもの ――

```
tests/45_home.test.ts               ホーム・タブ・帯の幾何・'use client' が2つのままか
tests/49_reachable_layers.test.ts   入る道の無い階層が0件のままか
tests/48_criteria_weighting.test.ts 重み付け（必須/加点）と、軸を勝手に足していないか
tests/50_partner_recommendation.test.ts 推薦枠が期ごとに分かれているか
tests/51_migration_provenance.test.ts  **誰が適用したかが帳簿に残るか**
tests/52_season3_alignment.test.ts     **本番の形に 0006 を当てても収束するか**
tests/10_decisions_references.test.ts  DECISIONS が実在しないテストを指していないか
```

変更理由は `db/DECISIONS.md` に **C-123 から**追記し、
次の報告書は `REPORT-15.0.md` とする。
