# 引き継ぎプロンプト

新しいセッションの最初に、この節（`---` から `---` まで）をそのまま貼る。

この文書は報告書と違って**常に最新に保つ**。版は切らない。
引き継ぎの内容が変わったら、ここを書き換える。

---

YouthDB の開発を引き継いでほしい。
起業家アカデミーの集客〜選考を一元管理するデータベースと、その上のダッシュボード。

**あなたの作業は実行⑦になる。**

## 0. 最初に読む — 現在は **HOLD**（実装停止中）

**開発は止まっている。** ブロッカーはソフトウェアの品質ではなく、
**運用上の証拠が無いこと**である。

MVP は完成し、**採用の1周が画面の中で閉じている。** しかし
**実運用データでは1周していない。誰も運営として使っていない。**

- **既定の方針: 証拠を集めることを、機能を足すことより優先する。**
  「次に何をするか」の答えは**機能ではなく観測**である
- **Pilot Go を、このブロッカーが残っている間に主張しない。基準も緩めない**
- 判定の全文と Go の条件は `docs/pilot/DECISION-LOG.md`（追記専用）
- **総括は `REPORT-6.12.md`。まずこれを読む**

**止まっている理由は3件で、すべてあなたには決められない**（5節）。
勝手に決めて進めず、待つあいだにできることをする（7節）。

## 1. まず状態を確認する

リポジトリは GitHub にある。

- `https://github.com/NEO-AX/YDB.git`（origin）
- `https://github.com/ymatsushita-creator/YDB.git`（mirror。同じ内容を push している）

直近の作業環境では `/Volumes/KIOXIA_2TB/NEO/YouthDB` に置いてあった。
以降このディレクトリを `<repo>` と書く。手元に無ければ clone する。

```bash
git clone https://github.com/NEO-AX/YDB.git   # 手元に無い場合だけ
pnpm install && pnpm db:reset && pnpm test
```

**テスト289件**が通れば正常。通らなければ、そこが最初の作業。
`pnpm exec tsc --noEmit` と `pnpm build` も通ることを確かめる。
`pnpm tokens` を流して `app/tokens.css` に差分が出ないことも確かめる。

画面は `pnpm dev`（ポート3111）。

### この環境で実際に壊した3つ

1. **開発サーバを起動したまま `pnpm db:reset` を実行しない。**
   PGlite の dataDir にプロセス間ロックが無く、**エラーも警告もなく壊れる**
2. **`.pgdata` を触っているプロセスを `kill` / `pkill` しない。** 同上
3. **計測は使い捨てのインメモリ DB で行う**（`freshDb()` + `seedDemo()`）。
   `.pgdata` で測ると上の2つを踏む。**実際に2回壊した**

外付けドライブに置いてあるため、**作業中に抜けると作業ディレクトリごと消える。**
実際に起きたが、push 済みだったので何も失われなかった。
復帰したら `git log -1` / `git status` / `pnpm test` で健全性を確かめ、
`.pgdata` は作り直す（突然の切断を受けているため）。
**区切りごとにコミットして push する。**

## 2. 読む順番

**文書には序列がある。上が下に優先する。**

```
vision.md     理念（Why）      ← 内容は未受領。枠のみ
     ↓
director.md   憲法（Principles）
     ↓
domain.md     世界設定（Model）
     ↓
design.md     UI（UX/UI）
     ↓
CLAUDE.md     実装の規律（Engineering）
     ↓
process.md    開発の回し方（Loop）
```

**`vision.md` は最上位だが内容が未受領で、空の文書は何も上書きしない。**
実際に効く最上位は `director.md` である。
各文書の責務と食い違いの扱いは `CLAUDE.md` 冒頭の Document Hierarchy にある。
**食い違いを勝手にどちらかへ寄せない。**

1. **`director.md`** — 憲法。「実装前の自己チェック」4項目は**着手条件**
2. **`domain.md`** — 世界設定。トポロジー（構造）/ ワークフロー（イベント）/
   リレーションシップ（関係）の3軸を混ぜない。**10節に未解決がある**
3. **`design.md`** — UI。**この文書はトークンを生成しない**（frontmatter が無い）
4. **`CLAUDE.md`** — 実装の規律と4つの問い
5. **`process.md`** — MVP モードの回し方と **Pilot Rule**
6. `db/DECISIONS.md` — **記録の本体。** 原典から変えた点と理由。
   特に **A-16**（絞ると9000倍遅い）/ A-17 / A-18 / A-19 / F-1、
   **C-16 〜 C-28**（森の実体・やること・書き込み・確定・判定・訂正・実年度の隔離）、
   **D-12**（MVP で置いた仮定と TODO(MVP) 全部）、**D-8**（2026年度。★2列が推測値）、
   D-10 / D-11 / D-13 / D-14（未決）
7. **`REPORT-6.12.md`（総括。ここから読む）** → `6.11` → `6.10` → … → `6.0`
   → `REPORT-5.0.md` → 遡って `4.0` `3.0` `2.2` `2.1` `2.0` `1.1` `1.0`
8. `docs/pilot/` — Pilot の道具10点（4節）
9. `basic/*.sql` — 原典。設計原則7つが冒頭にある。**変更しない**

## 3. いま何ができているか（実行⑥の到達点）

**採用の1周が画面の中で閉じている。**

```
今日やることを見る → 候補者を開く → 担当を決める / 替える / 保留を解く →
評価する（軸ごとに点と根拠）→ 確定する → 判定する → 次のステップへ
```

通過すると**次のステップの評価行ができ、それが運転席の「担当を決める」として
並ぶ。** 最終ステップの通過で合格になり、その応募のやることは空になる。
**判定を押し間違えても、打ち消し行の追記で訂正できる**（通過↔不合格）。

- 画面 **9枚** —— `/cockpit`（運転席）、`/forests/[id]`（森）、`/pilot`（試運転）、
  `/`（ファネル）、`/people`、`/people/[id]`、`/applications/[id]`、
  `/operations`、`/sources`
- マイグレーション **15**、テスト **289**、`'use client'` **0件**
- 書き込みの型（C-20 で決まり、4回使って問題なし）

```
判定と更新  src/commands/     ← テストから直接呼べる。ここに事実の確認を置く
受け渡し    app/*/actions.ts  'use server'。結果はコードで返す（氏名を URL に入れない）
表示        app/*/page.tsx    素の <form action={...}>。'use client' は増やさない
```

**画面にまだ無いもの** —— 点の訂正（`already_scored` で弾かれる）、
辞退（`withdraw`。理由が必須）、申し送り、新規応募の登録、手で作るタスク。

**踏んではいけない地雷が1つある。** `v_forest_season_activity` と
`v_open_tasks` は、**絞り込みを高い供給元まで降ろすと計算量が爆発する。**
`MATERIALIZED` と `ss.season_id` はそのための指定で、外すと9000倍遅くなる
（A-16）。`tests/15` が定義そのものを見張っている。

## 4. 現在のフェーズ — Pilot Validation

目的は機能の完成ではなく、**運営担当者から最大限の学習を得ること。**

### 優先順位

```
1. 実運用で業務が止まる問題
2. データの整合性を壊す問題
3. 運営担当者が迷うUI
4. UX改善
5. 新機能追加
```

新機能は上位4項目を妨げない場合のみ。

### サイクルの回し方

**1機能を終えたら、次をすぐ実装しない。** 次の順で立ち止まる。

1. **PM Report**（Goal / What Changed / Operator Value / Remaining Friction /
   Technical Debt / Risks / Recommendation（Option A・B）/ Confidence）
2. **Validation Report**（誰が検証したか / 実データで確認した内容 /
   未検証のケース / 今後確認すべきケース）
3. **残課題を4分類** ―― **Validated Operator Problem** /
   **Developer Hypothesis** / Technical Debt / Product Backlog
4. 次を推奨する。**優先順位の最大値ではなく、
   実装量あたりの学習量（learning per implementation）が最大のものを選ぶ**

**サイクル開始時に必ず答える** ――
「この改善は実際の運営で観測された問題か、開発中に想像した問題か」。

### Pilot Rule（`process.md` 末尾）

> Pilot の目的は**不具合ではなく誤解**を見つけること。
> 運営が迷ったら、**運営ではなく画面が間違っている**と考える。

- **開発者が気づいたことは仮説。運営の行動だけが検証済みの知識になる**
- **要望・感想は観測ではない。** 「あったら良い」で Validated にしない
- **「操作できたが迷った」は「手が止まった」と同じ重み。**
  押せた場所はバグとして報告されないので、訊かなければ永久に見えない
- **運営の言葉をこちらの語に翻訳して記録しない。**
  `domain.md` に無い語が出たら、世界設定の側が足りていない可能性を疑う

### Pilot の道具（`docs/pilot/`）

| 文書 | 使うとき |
|---|---|
| `CHECKLIST.md` | 実施前。社長15分 / 運営40分 / 初めて触る人20分 |
| `OBSERVATION-SHEET.md` | 実施中。**対象者ごとに1枚コピー** |
| `REVIEW.md` | 実施後。4分類と**昇格の条件** |
| `EXIT_CRITERIA.md` | Go / Hold / No-Go の判定と判定シート |
| `REPORT-TEMPLATE.md` | 報告書。リポジトリ直下へコピーして連番を付ける |
| `CEO-REQUEST.md` | 年度の実数値の依頼（5問・回答5分） |
| `DATA-INTAKE.md` | **実データの受け取り方（git に入れない）** |
| `DECISION-LOG.md` | 判定の記録（**追記専用**） |
| `DATA-QUESTIONS.md` | **受領した実データの中身と、確定／推測の別** |
| `DEPLOY-READINESS.md` | URL 試運転の実測（満たしている / 不足 / ブロッカー） |

**昇格の規則（`REVIEW.md` 3）が最も重要。** 迷ったら昇格させない ――
急いで Validated にすることの利益は無い。

**2026-08-07 時点で Validated Operator Problem は0件**（誰も運営として
使っていない）。Developer Hypothesis 7 / Technical Debt 12 / Product Backlog 4。

## 5. 止まっている3件 — 誰の判断待ちか

**1 と 3 はあなたには決められない。勝手に決めて進めない。**
**2 は 2026-08-07 に解けた。** 実データも回答も揃い、**残るのは作業だけ**である。

| # | 待っているもの | 誰が | これが無いと |
|---|---|---|---|
| 1 | **デプロイ先**（A: 1台＋永続ディスク / B: マネージド Postgres） | 依頼する側 | URL 試運転が始められない |
| 2 | **`.pgdata/` への取り込み**（DB・年度・ステップ・満点はすべて受領済み） | ―（作業） | 実データで1周できない |
| 3 | **運営4名の日程** | 運営 | 観測が0件のまま |

### 補足

- **1 について。** `DEPLOY-READINESS.md` 4節に2案の比較がある。
  **Pilot には A で足りる**と見立てた（利用者は数人、重要なのは消えないことと
  戻せることでスケールではない）。ただし費用と運用体制の話なので判断を仰ぐ
- **2 について。これはもう社長待ちではない。** 実データ（旧 NEO-Youth の
  Supabase 全10表）を 2026-08-07 に受領し、年度・選考ステップ・評価の満点も
  回答を得た。本番のシードに入れ済み（D-8 追記 / `tests/22`）。
  **残るのは取り込みの作業だけ**である（`DATA-INTAKE.md` 3節の 4）。
  **未確定だった2つも指示が出た**（D-14）―― 書類選考の軸の呼び名は**凍結**
  （後で決める）、流入チャネルは**廃止ではなく未実施**。
  **どちらも空のまま取り込む。** 入れたら消せないマスタなので、
  「まだ出来ていない」を「確定した」として扱わない。
  **ただしチャネルが0件だと接点が1行も入らない**（`channel_id` は NOT NULL）。
  選考の1周は回るが、**森と流入元は実データを入れても全部0**である（C-31）
  **★ 実データを `db/seeds/` に置かない**（git 追跡下で2リモートへ公開される）。
  受け入れ口は `db/private/legacy-youthdb-2026-08-07/`（`.gitignore` 済み、
  SHA-256 検証済み）。手順は `DATA-INTAKE.md`
- **実年度と創作のデモは同居できない**（C-28）。`pnpm db:reset` は
  サンプル＋デモで、**実年度は入らない。** 実年度は `*.production.sql` にあり、
  本番の環境にだけ入る。混ぜると創作の応募が実在の年度にぶら下がる
- **`step_history` が 0 行だった。** 段階が移った履歴が無いので、
  **滞留と SLA は実データから再現できない。** 3 の実施でしか観測できないことが確定した
- **3 について。** 4名は `EXIT_CRITERIA.md` の2名以上を上回るので、
  基準を緩めずに満たせる。**うち1名は別日に2回目**（滞留・SLA は日をまたがないと
  現れない。これまでの検証はすべて同日）

## 6. 判断が来たら着手する順（Deployment Readiness）

デプロイ先が決まってから、**1つずつ。新機能は足さない。**

| # | やること | なぜ Pilot に必須か |
|---|---|---|
| 1 | **`db:reset` の事故防止** | 確認も環境判定もなく `rm -rf` する。**先に入れる** |
| 2 | **バックアップと復元の手順＋コマンド** | 成功条件の「復旧できる」そのもの |
| 3 | **認証（最小）** | URL を知れば実在候補者の氏名を読め、判定まで書ける |
| 4 | **Pilot DB の分離**（環境変数でデータ位置を分ける） | 開発の操作が Pilot に届かないように |
| 5 | **本番のマイグレーション手順**（全消しでない導線） | 2 の復元とセット |
| 6 | **エラーの確認方法**（`error.tsx` とログの出力先） | 何が起きたか分からないと戻せない |
| 7 | **公開環境での E2E 手順** | 実データで1周できることを URL 上で1回通す |

**1と2を認証より先にする。** 消えたデータは戻らないが、バックアップがあれば
認証の穴は「読まれた」で済む ―― **可逆性が違う。**

7 が終わった時点で `docs/pilot/CHECKLIST.md` の実施に入る。

## 7. 判断を待つ間にやってよいこと

**数字の妥当性に依存しない改善だけ**（`CHECKLIST.md` 0-1-1 の表）。

| 進めてよい | 待つ |
|---|---|
| 操作の流れ・手が止まる場所・迷う場所 | 数字が実感と合うか |
| 押し間違い・想定外の操作 | 定員に対する充足率の読み方 |
| 言葉の食い違い | 森の並び順が納得できるか |
| クリック数・入力の速さ | 滞留日数が現実の感覚と合うか |

**架空データで「この数字は合っていますか」と訊かない。** 必ず「合っていません」と
返る（架空だから）。観測として無価値なうえ、運営の信頼を削る。

**パレットの保留は 2026-08-07 に解けた**（C-29）。依頼者の指示で
`basic/DESIGN.md` を **NEO ACADEMIA ブランド**に差し替え、ロゴも入れた。
色は規定に書かれた値をそのまま置いている（画像から吸っていない）。
**★印の6色だけは、白背景で読めないネオン色から色相を保って派生させた値**で、
`DESIGN.md` の中で区別できるようにしてある。
最重要原則は**「色は線、面は黒」** ―― 面に使うトークンは全部白か黒に倒してあり、
アクセント色の面塗りが構造的に作れない。
**`app/tokens.css` は生成物。手で編集しない**（直すのは `DESIGN.md`）。
色・字送り・角丸を直値で書かない規律は変わらない。

## 8. 破ってはいけない規律

原典 `basic/` の設計者は、カラムと制約の1つ1つに「なぜそうしたか」を
日本語で書いている。**コメントは装飾ではなく設計の本体。** それに合わせる。

- **`basic/` は変更しない。** 正典として残す。実装は `db/migrations/`
- **適用済みマイグレーションは編集しない。** チェックサムが変わって
  ランナーが止まる。新しい番号で足す
- **理由なき差分を作らない。** 原典から変えたら `db/DECISIONS.md` に
  「発見の経緯・なぜそう直したか・どのテストで固定したか」を書く。
  テスト参照が実在するかは `tests/10_decisions_references.test.ts` が機械的に検査する
- **集計定義をコンポーネント側に書かない。** データ取得は `src/queries/` に集約する
- **書き込みの判定を `app/` に書かない。** サーバアクションは Next の実行時が
  要るためテストから呼べない。事実の確認は `src/commands/` に置く（C-20）
- **操作できる母集団を、画面に出す述語と揃える。** フォームの hidden は
  誰でも書き換えられるので、画面に出ていないものを触れないことは UI では守れない
- **氏名や id を URL に入れない。** 履歴にもログにも残る。結果はコードで返す
- **種別が排他なら、枝を並べずに優先順位で決める**（A-18 / C-23）
- **種別ごとに条件を足したら、全体の性質でテストする**
- **止まる前に、その判断が本当に必要かを確かめる。** 制約を1行読み直せば
  消える問いで、一度サイクルを止めた（C-21）
- **日付は必ず `jst_date()` を通す。** `::date` と `CURRENT_DATE` は
  接続のタイムゾーン依存。今日は `jst_today()`
- **`status_histories` は追記のみ。** UPDATE も DELETE もトリガが拒否する。
  訂正は打ち消し行の追記で表現する
- **集計マスタは追加と非活性化で運用する**
- **`app/tokens.css` は生成物。手で編集しない**
- **実データ・実在の個人情報をデモ・スクリーンショット・報告書・
  コミットメッセージに使わない**
- **報告書は凍結、記録は更新。** 版は `REPORT-<実行番号>.<連番>.md`

優先順位は常にこう。**意匠の改修であっても変わらない。**

```
記録の構造（何を事実として残すか）
  > 集計の定義（その事実をどう数えるか）
    > 画面（数えた結果をどう見せるか）
```

## 9. 進め方と、これまでに踏んだ失敗

```
1. 動く状態にする
2. 実データで動かして計測する
3. 見つけた欠陥を先にテストへ書き、落ちることを確認する
4. 直す
5. 理由を DECISIONS.md に記録する
6. コミットする
```

3 を先にするのは、直したつもりで直っていないのを防ぐため。

**繰り返さないこと。**

- **修正が別の場所を壊す。** 変更のたびに「この前提が崩れる箇所」を数え直す
- **記録が実装より厳密に見える。** 「テストを示した」と書いて実際には無かった。
  実行⑥でも2回起きた ―― 「根拠は空にできない」と書いてあるのに空にできた
  （A-19）、「JS を配信していない」と書いてあるのに配信していた（F-1）。
  **記録を信じた人は確認をやめる。記録が無いより悪い**
- **デモデータが検証したい経路を踏んでいない。同じ見落としを5回繰り返した。**
  **どこに置くかも経路の一部である**（実行④は登場人物を終わった年度に置いて
  ③の形を締め出し、実行⑥は「保留かつ利益相反」の組み合わせが無くて
  二重計上が表に出なかった）
- **目視は検証ではない。** DOM か SQL の値で確かめる
- **作り物のデータから出た数字をドキュメントに残さない。** 追試できない。
  残すのはデータ量に依存しない計算量のほう
- **単位の違うものを割らない**（推定リーチ ÷ 実人数。実行②で作りかけて消した）
- **表示の丸めで、注記が画面上の嘘になる。** 実行⑥でも起きた ――
  「要注意の順に並ぶ」と書きながら平常の森が先頭だった
- **丸め込みが、欠陥そのものを隠す。辻褄が合っている＝正しい、ではない**
- **バグが偶然に作っていた経路は、バグを直すと一緒に消える**（`tests/13`）
- **集合を返す関数を結合の内側に直接置かない。**（実行③）
  外側の行ごとに評価され、一覧が 444 秒かかった
- **性能は「絞り込みあり」と「なし」の両方で測る。**（実行⑥、A-16）
  A-11 は絞ると速くなって見逃した。A-16 は**絞ると 9000 倍遅くなった。**
  片側だけを測る限り、どちらかの向きを必ず取り逃す
- **性能の比較で `LIMIT` だけを付けない。**（実行④）`ORDER BY` を外すと
  入れ子ループが打ち切られ、438.9 秒が 3 ミリ秒に見えた
- **`LEFT JOIN` の `ON` に絞り込みを書いても、行は落ちない。**（実行⑥、A-17）
- **同じ言葉を2つの定義で使わない。**（実行③）
  「林」が2つの意味を持ち、同じ年度で 2,576 対 661 になっていた
- **「数えるか」と「動いているか」を同じ述語で扱わない。**（実行④）
  `v_countable_applications` と `v_active_applications` は別物
- **文書だけ直して実装が追随しないと、語が2重定義になる。**（実行⑤）
  移行するときは機械的な置換ではなく、旧ビューを非活性化して新しい語で作り直す

## 10. 技術構成

- PostgreSQL 15+ 想定。開発とテストは **PGlite**（WASM 版 PostgreSQL 18、
  デーモン不要）。`src/db/client.ts` の `Db` は `query`/`exec`/`close` の
  3つだけなので、本番ドライバへの差し替えは `src/db/server.ts` に閉じる。
  **`DATABASE_URL` を設定すると意図的に throw する**（アダプタ未実装）
- Next.js App Router（**サーバコンポーネントのみ、`'use client'` が0件**）。
  **訂正（F-1）**: かつて「クライアント JavaScript を一切配信していない」と
  書いてあったが誤り。App Router は RSC のため React の実行時を必ず配る。
  正しくは**クライアント部品を1つも書いていない**。
  クライアント側のライブラリを採用するのは「クライアント境界を新設する」判断と
  同義なので、**その判断は単独で示し、他の判断に埋め込まない**。ただし
  **書き込みはこれに当たらない**（素の `<form action={...}>` はサーバ
  コンポーネントに置け、JS 無効でも動く。C-20）
- テストは `node:test`。フレームワーク無し
- Tailwind も shadcn も認証も lint 設定も持たない。**不足ではなく設計判断**
  （ただし**認証は、公開する時点で前提が変わる**。`DEPLOY-READINESS.md` B-2）
- デザイントークンは `basic/DESIGN.md` の frontmatter から
  `app/tokens.css` を生成（`pnpm tokens`）。生成物は手で編集しない

## 11. 未決の一覧

### 実行⑤で決まったこと（実装はまだ追随していない）

`REPORT-5.0.md` 4節に全文。要点だけ。

- **トポロジーとワークフローを混ぜない。** 森・林・木は Forest / Community /
  Person という**エンティティの階層**。Interested / Applicant / Accepted は
  Person に付く**状態**。層が違う
- **Application は Entity。属性へ降格させない。** Person の状態は
  Applications[] から**導出する**（物理カラムを持たない）
- **Member は Person の属性ではない。** 合格 ≠ 在籍
- **Unknown は Person の状態として持てない。** 行が無いものに状態は付かない。
  Unknown の規模を表すのが森の属性 Reach。**この境界をまたいで割り算をしない**

**実装は D-2（「林は人、木と幹は応募」）のままである。**
`domain.md` の定義とコードは食い違っている（D-11）。
中途半端に直すほうが危険なので、このまま置く。

### `domain.md` 10節（実行⑥で一部を仮に置いた）

| # | 内容 | 実行⑥での扱い |
|---|---|---|
| 10-1 | `partner_relations` と Relationship の**語が衝突する** | **未決。** 森と人は「接触があった」で代用 |
| 10-2 | **Task の定義が無い** | 記録層を作らず**導出**にした（C-17） |
| 10-3 | **Interested（芽）の判定事実** | **触っていない** |
| 10-4 | **Forest / Community の実体** | `partners` に親を1本足した（C-16） |
| 10-5 | **Membership の粒度** | **触っていない** |
| 10-6 | 実装段階を憲法で上書きするか | Health は作らず事実の旗で代えた（C-18） |
| 10-7 | `director.md` の IA に重複 | **触っていない**（憲法なので下位から直せない） |

### 以前からの未確定

- **D-8 `seasons` の実数値。** 本番シードに年度が1行も無い。
  定員36・目標100は受領済みで、足りないのは年度・4つの日付・ステップ・評価の観点
- **D-10 個人情報削除を受けた人の応募を、木に数え続けるか。**
  森・林・生涯サマリは削除済みを外すが、木と幹は数え続けている。
  過去年度の数字が動く変更なので触っていない
- **D-11 段の語（森・林・木・幹）が旧定義のまま。**
  旧語と新語が同居している。字が違うので二重定義にはなっていないが、過渡的

仮に置いたものは全部 `DECISIONS.md` **D-12** に一覧がある。
コード中の `TODO(MVP)` は **31 箇所**。**推測で埋めた事実は1つも無い。**

## 12. リポジトリの外にあるもの

git 管理下に入っていないため、環境が変わると付いてこない。

- `<repo>/../academy-ui-assets/` … UI 素材ライブラリ（実行②で作成、約10MB）。
  検証済みの素材は `next-shadcn-dashboard-starter`（MIT）1件だけ。
  `reports/` に8つのレポートがある。詳細は `REPORT-2.1.md`
- `<repo>/.pgdata/` … PGlite のデータ（61MB）。`pnpm db:reset` で作り直せる
- `<repo>/db/private/` … **実データの受け入れ口。**`.gitignore` 済み

**未決が1つ。** `../academy-ui-assets/reports/` の8ファイル（80KB）は git 管理外で、
リポジトリに入れる（`docs/ui-assets/` など）か添付で渡し続けるかを決めていない。
ZIP（10MB）と参考画像は入れない方針。

## 13. 版の規約と完了の定義

- 報告書は `REPORT-<実行番号>.<連番>.md`。連番は0から。
  **実行⑦の第1報は `REPORT-7.0.md`**
- **報告書は書いたら凍結する。** 追記が要るときは新しい連番を作る
- **記録（`db/DECISIONS.md`）は更新する。** ここだけは最新を保つ
- **この `HANDOFF.md` も更新する。** 版は切らない
- 過去の報告書は書き換えない。訂正は新しい版に書く
- **コミットは origin と mirror の両方へ push する**

完了の定義。

- [ ] `pnpm test` が全通過する（新しい判断にはテストが1つ以上ある）
- [ ] `pnpm exec tsc --noEmit` がクリーン
- [ ] 実データで動かし、**DOM か SQL の値で**結果を確認した（目視だけで判断しない）
- [ ] 画面に出した数字の**単位と母集団**が、注記として画面上にある
- [ ] `db/DECISIONS.md` に理由とテスト参照を書いた
- [ ] `TODO(MVP)` の一覧を出した
- [ ] **PM Report と Validation Report を出した**（4節）
- [ ] **Pilot Go を主張していない**（HOLD が続いている間）

---
