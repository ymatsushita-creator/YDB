# HANDOFF.md — 実行⑪への引き継ぎ

実行⑩は**採点入力**と**取り込み経路**、そして**画面の作り直し**だった。
依頼者の指示は「実データ投入」「成績記入層の実装」、
後半に画面の指摘が5点。**画面の形を決めるのは依頼者である。**

---

## 実行⑪が最初に判断すること

### ★ フォームはまだ無い。受け皿だけがある（C-79）

`form_responses` は届いた回答をそのまま持つ。**回答は書き換えられない** ――
変えてよいのは「誰に結び付けたか」だけ（トリガが拒否する）。

自動接合は**メール・LINE・電話が1人に絞れたときだけ**。
**氏名では結び付けない**（同姓同名で取り違える。旧データの55人は
姓名の区切りすら無い）。

チャネルは**名前が完全に一致したときだけ**写す。似た語へ寄せない。
写せなかった回答も落とさず、生の文字列のまま集計に1行として出す。

Google フォーム側ができたら、`ingestFormResponse`（`src/commands/intake.ts`）へ
渡すだけでよい。**画面も集計もそのまま動く。**

### ★ 2期の実データは**入っている**（C-74）

```
本番（DATABASE_URL）  人 61 ・ 応募 34
```

生年月日・メール・姓名の区切り・学校は**受け取れていないまま**入れてある
（0023 で「無いこともある」に変えた）。**1つも埋めていない。**
画面には「未記入」として出る。値が届いたら、編集画面から入れれば済む。

```
職員 14 件（旧データの担当者名。メールは無い ―― 0024 で NULL 可にした）
アプローチ状態 61 件（すべて「未アプローチ」）
最終面接 32 件 ・ 点 192 件 ・ 面接シート 32 件（C-77）
関連団体 28 件 ・ 接触記録 240 件（C-78）
```

★ **2期の集客開始日（2026-02-01）は推測値で、実測とずれている。**
団体の折衝は **2025-08-27 から**始まっていた。そのため接触記録 240 件のうち
**187 件がどの期にも紐づかない**（2期に入るのは 53 件）。
日付は集計定義そのものなので**黙って直していない。**
直すときは `season_revisions` に履歴を残す（原則4）。**依頼者の指示待ち。**

ヘッドハンティングとボーダーラインにも出る（C-75）。
一覧は `v_headhunting_list`＝**アプローチ状態の記録がある人**だけを出す作りなので、
取り込みが初期値を1件ずつ置いてある。
記録した人は `旧システム取り込み`（非活性の職員）で、
**実在の担当者に付け替えていない。**

★ 旧データの `status`（合格・辞退など）は**写していない。**
それは選考の状態であって、アプローチの状態ではない。
段の呼び名の対応も未確認。**運営の言葉を翻訳して記録しない。**

★ 書き込み先を間違えないこと。**アプリが読むのは `DATABASE_URL`。**
`.pgdata` は手元の砂場で、**創作のデモ 1438 人が入っている。**
実在と創作を同居させない（C-28）。取り込みは毎回、書き込み先を名乗る。

---

### 旧データで、まだ受け取れていないもの

前の引き継ぎは「壁は `birth_date` の NOT NULL」と書いていた。
**旧 61 件を実際に突き合わせたら、壁は4つあった。**

```
旧 youth_candidates 61 件 ―― 入る 0 件

  55 件  姓・名（氏名が姓と名に分かれていない）  ← いちばん多い
  61 件  生年月日（旧データに列そのものが無い）
  31 件  メール（persons.email は NOT NULL）
  13 件  学校（persons.school_id は NOT NULL）

  4つそろっている行は 1 件も無い
```

**入れるために記録層を緩めた**（0023 / C-74）。埋める表はそのまま使える ――

```bash
pnpm import:legacy:2026 --worksheet   # db/private/ に空欄の表が出る
pnpm import:legacy:2026               # 突き合わせだけ（既定。書かない）
pnpm import:legacy:2026 --apply       # 埋まってから
```

**区切りの無い氏名を勝手に切らない。**「架空太郎」は 架空/太郎 とも
架空太/郎 とも読める。切り方が人ごとに違うと、同一人物の判定
`(family_name, given_name, birth_date, school_id)` がその場で崩れる。
生年月日を守った意味が消える。

`birth_date` を NULL 可に緩める道は**採らなかった**（依頼者の判断）。

### 成績の置き場所が、まだ足りない

旧データが持っている成績のうち、**受け皿があるのは最終面接の6軸だけ**である。

```
旧 candidates 32件
  sec2_score_* 6軸          → 受け皿あり（2期・最終面接の6軸）
  doc_score（満点16）        → ★書類選考に評価軸が無い。呼び名が未受領
  sec2_eval / overall /
  strengths / concerns /
  overall_comment /
  check_points               → ★置き場所が無い（点ではない評価文）
旧 youth_candidates 61件
  status / ob_* 11個         → ★受け皿が無い
```

**こちらで名付けない。** 運営が使っていない語がマスタとして固定化する。
呼び名を受け取ってから足す。

### 段の呼び名は、まだ未確認

```
2期  応募受付 → 書類選考 → グループ面接 → 最終面接（4本）
3期            書類審査 → グループ面接 → 最終面接（3本）
```

**同じものを指しているか未確認。受け取った語のまま置いてある。**
取り込みでも選考の段は入れていない。**段数を決め打ちするコードを書かない**
―― テストも段数を数えずに書いてある（`tests/32_selection_loop.test.ts`）。

### 3期に、まだ受け取っていない値がある

日付4つは**すべて推測値**（2期の周期を1年ずらしただけ）。
評価軸と目標応募数は未受領。実際の日程が来たら黙って上書きせず、
`season_revisions` に履歴を残して直す（C-54 / C-55）。

---

## いま画面はこうなっている

```
期（1期・2期・3期）        ← 一番上の層。タブを移っても保たれる
 └ タブ                    ヘッドハンティング / ボーダーライン / アプローチ
    └ 対象                 候補者・応募・段
```

- 上の帯は**現在地の表示**（`2期 › ボーダーライン › 氏名`）。
  エクスプローラのパスと同じで、**期の切替はここに置かない**
- ★ **期はタブより上の層。タブを移っても保たれる**（C-76）。
  `Shell` へ `seasonId` を渡すこと。渡し忘れると押した先で既定の期へ戻る。
  期を持たない画面（その人の記録）では**渡さない** ―― 空の `season=` は害になる。
  `tests/30` が「期の切替を出しているのに渡していない画面」を検出する
- **期の切替は操作柱（左）の一番下。** 見やすい場所に置かない ―― 依頼者の指示。
  **募集中の印（緑の点）は出さない**（C-62）
- 共通シェルは**各ページが被る**（レイアウトではない）。`'use client'` は **0件**
- 未記入は**ふち付きの札**。札が付くのは「書けるのに空」だけで、
  **導出できないものは付かない**（C-56）。いまは白黒なので濃さの差だけになっている
- 現行語彙は「アプローチ可能圏 / 構成コミュニティ」。旧生態系比喩は使わない
  （適用済みSQLの `forest` / `community` は互換性のため内部に残る）

編集できるもの ―― 候補者プロフィール、担当者メモ、顔写真、アプローチ状態、
成績・根拠・判定。**確度・順位・集計値は導出値なので直接編集しない。**

### ★ 画面に説明を書かない（C-62）

単位・母集団・集計の定義・タイムゾーンの注記を、画面ごとに置いていた。
**依頼者の指示で全画面から消した。** `CLAUDE.md` の完了条件も直してある。

定義を捨てたのではない ―― **置き場所をクエリのコメントと `db/DECISIONS.md`
に移した。** 画面に無いことと、根拠が無いことは違う。
**次に触る人が親切のつもりで戻さないこと。**

残っているのは記録だけ（担当者メモ・申し送り・根拠エピソード・単位1語）と、
**いま起きていることの報せ**（`3 件が SLA を超えて止まっている` など）。

★ 探すときは**枠ではなく中身で探す。** 最初 `unit-note` / `footnote` /
`note` のクラス名で探したため、`callout`（帯）に入っていた但し書きが
残っていた（C-67）。

### ★ ロゴが2本のバーの寸法を決める（C-66）

```
ロゴの横 204px = 縦バー（操作柱）の幅
ロゴの縦  81px = 横バー（現在地の帯）の高さ
```

`--logo-w` / `--logo-h` / `--logo-pad` の3つが出どころで、
縦バーも横バーもこれを参照する。**1箇所直せば両方動く。**

横バーはロゴの右端から**隙間なく続く**（C-68 / C-72）。着いた側の角は落としてある。

★★ **箱で測ると通るのに、画面では離れていることがある**（C-72）。
列の隙間をマイナスマージンで打ち消すと、`overflow: hidden` の親が
**はみ出した分を描画で切り取る。** `getBoundingClientRect()` は接していると答える。
いまは列の隙間を 0 にし、`.hh-main` の内側の余白を横バーだけが打ち消す形にしてある。

**確かめるときは箱ではなく描画で見る** ――
`elementFromPoint(ロゴの右端 + 1px, 帯の中心)` が横バーを返すか。
760px 以下では縦バーが上へ回るので、引き戻しも角も元に戻す ――
**つなぐ相手が居ないところでつなぐと、帯が画面の外へ出る。**

現在地の帯は**全画面にある**。`/applications/[id]` にだけ無く、
本文に手書きのパンくずが置かれていたのを実行⑩で揃えた。

★ **ロゴ画像を差し替えたら、縦の値を計算し直す。**
いまの画像は `logo_gradient.png` = 1283 × 305（比 4.2066）。
枠の横から余白を引いた幅を比で割り、余白を足したものが枠の縦になる。
**目分量で置かない。** 実行⑩で差し替えたとき、比が 3.15 → 4.21 に変わり
`--logo-h` を 81 → 67 にしている。

### ★ 配色は3枚重ね。**原典は触っていない**（C-64 / C-71）

```
app/tokens.css      basic/DESIGN.md から生成。触らない
app/monochrome.css  白黒の土台（依頼者の指示で一度モノクロにした）
app/brand.css       ロゴのグラデーションから採った線と文字の色
```

**brand.css を外せば白黒に、2枚とも外せば元の色に戻る。**

★★ 原則は **「色は線、面は黒」**（NEO ACADEMIA の意匠規定）。
アクセントで面を塗らない。塗ってよい面は白と黒だけ。
**色で意味を伝える作りを増やさない** ―― 形と文字で読めることが前提。

★ **ネイティブ部品は色トークンを通らない。** ラジオ・チェックボックス・
選択範囲・フォーカス枠は `accent-color` などの専用の口を持つ。
実際、最終判定のラジオだけが緑で残っていた（C-70）。
色を数えるときは `accentColor` も見る ―― **見ていない性質は0件になる。**

★ **面の色を変えたら、その上の文字を必ず測り直す**（C-73）。
現在地の段が黒地に黒文字になっていた。灰の補助文字も 2.5〜4.1 しかなかった。
いまは16経路で**コントラスト不足 0 件**。基準は本文 4.5・大きな文字 3.0。

★ **同じトークンを、明るい地と暗い地の両方で使わない。**
白地用に暗くしたら、暗い操作柱の上の文字だけが読めなくなった。

### ★ 面接シートは「点では表せないもの」だけを持つ（C-69）

```
点と根拠        evaluation_scores        （軸は evaluation_criteria）
面接官          evaluations              （シートに写さない。替えたら一緒に動く）
記入欄10・判定  interview_sheets         ＋ interview_sheet_revisions（変更ログ）
選考の通過/不合格 status_histories        ★ シートの「最終判定」とは別物
```

★★ シートの最終判定（合格・ボーダー・不合格）は**選考の判定ではない。**
「ボーダー」は選考の側に存在しない。列名も `recommendation` にしてある。
**混ぜない。** 混ぜると、誰がいつ決めたのかが辿れなくなる。

★ 記入欄の様式は `INTERVIEW_FIELDS`（`src/commands/interview.ts`）が正典。
**送りの名前（camelCase）と列名（snake_case）を対で持つ。**
片方から導けると思って書いたら、保存はできるのに開き直すと全部空になった。

### ★ 採点は2箇所から打てる（C-59 / C-63）

```
一覧の右パネル              選考タブで選んだ相手の、その段だけ
/borderline/{personId}      氏名を押して開く。その期の全段。確定済みも並ぶ
/interviews/{evaluationId}  面接シート。点では表せない所見はこちら
```

**書き込む画面は、狭いパネルに畳まない**（C-71）。
プロフィールの編集は `/people/{id}/edit`、面接は `/interviews/{id}`。
一覧の側に置くのは**入口だけ**にする。

どちらも `app/_components/scoring.tsx` の同じ部品を使う。
**入口が2つでも規則は1つ。** 判定を画面側に書き足さない。

### ★ 高さの扱いは、画面の型で違う（C-57 / C-58 / C-62）

```
枠が2つ並ぶ画面（.hh-grid あり）  ヘッドハンティング・ボーダーライン
  ページは 100vh 固定。列を比率で割り付け、カードは自分の中で送る
  → 中身が増えてもセクションの位置が動かない

一覧の画面（.hh-grid なし）        選考オペレーション・人を探す・ファネルほか
  外枠ごと送る。カードは中身なりの高さ
  → 潰さない。並べる
```

前者を後者にも効かせていたのが、実行⑩で直した見切れの原因である。
カード1枚が数行ぶんまで潰れ、**表が見出しだけになっていた。**

★ **送れているのに切れて見える**ことがある。macOS の既定はスクロールバーを
隠すので、行の途中で切れた絵だけが残る。細い軌道を常時出してある。
**確かめるときは「はみ出しているのに、自分も祖先も送れない要素」を
DOM で数える。** 目で見て済ませない。

その数え方で2つ踏んだ ――
`overflow-y: auto` だけを指定すると **`overflow-x` も `auto` に計算される**
ので、縦だけ送る器が横の切れを隠す。
`visually-hidden`（読み上げ専用。`clip-path` で 1px に畳んである）も
はみ出しとして数えてしまう。**見えていないものは切れていない。**

### 一覧は全件、その先に次の層がある（C-62）

ページ送りは無い。カードの中で送る。
一覧の行から、確度順は「その人の記録」へ、選考タブは「その応募の経緯」へ。
**一覧で終わらせない。**

---

## 実行⑩で足したもの

| | |
|---|---|
| 採点シート | 選考タブから点と根拠を入れて確定できる（C-59） |
| `listCandidatesByStep` の `evaluation_id` | 一覧の行が、採点する評価を名指しする |
| `src/import/legacy_2026.ts` | 取り込みの判定。**DB に触らない** |
| `scripts/import-legacy-2026.ts` | 取り込みの実行。既定は書かない（C-61） |
| 一覧の全件表示と、次の層への入口 | 5行のページ送りをやめた（C-62） |
| ランキングの顔写真 | 成績・確度・ヘッドハンティングリスト（C-62） |
| 採点レイヤー `/borderline/{personId}` | 氏名を押すと開く。段で絞らない（C-63） |
| `app/monochrome.css` | 白黒の上書き層。**外せば色が戻る**（C-64） |
| カレンダーの予定一覧 | 升の中の氏名は必ず切れる。下に読める形で置く（C-65） |
| `0022_interview_sheets.sql` | 面接シートの現在値と変更ログ（C-69）**唯一のマイグレーション** |
| 面接画面 `/interviews/{evaluationId}` | 依頼者の様式。記入欄10・最終判定3択・変更ログ |
| 面接タブ `/interviews` | **4つ目のタブ**（アプローチの下）。その期の面接を全件（C-71） |
| 編集画面 `/people/{id}/edit` | プロフィールとアプローチ状態。**小さなパネルから外した**（C-71） |
| `app/brand.css` | ロゴのグラデーションから採った線と文字の色（C-71） |
| `0025_form_responses.sql` | フォーム回答の受け皿と候補者番号（C-79） |
| `db/seeds/0004_channels.sql` | 流入チャネル13件。**確定した語**（C-79） |
| 候補者追加 `/people/new` | 人・番号・接点・アプローチ状態・フォーム接合を1回で |
| アプローチ追加 `/approach/new` | 団体への接触を1件。推定リーチは空でよい |
| `--logo-w` / `--logo-h` | ロゴが縦バーの幅と横バーの高さを決める（C-66） |

マイグレーションは **0022 の1つだけ**（面接シート）。他は実行⑨のままである。

決定は `db/DECISIONS.md` の **C-59 … C-80**。実行⑨ぶんは **C-39 … C-58**。

---

## 踏んだ落とし穴（同じ穴に落ちないために）

### 出しているものが押せるかは、押さないと分からない

「◯◯さんの二次面接を評価する」を押すと **404** になっていた（C-60）。
`v_open_tasks.source_id` は**評価のID**で、応募のIDではない。
採点の入口は応募の画面にしかなく、そこへ行く導線はこのリンクだけだった。
つまり実行⑨の終わりの時点で、**画面から点を入れる手段が1つも無かった。**

テストは通っていた。**リンク先が開けることを誰も確かめていなかった。**

### 乱数に頼っていた経路は、条件が変わると黙って消える

**3回起きた。** 年度を4→2に減らしたら「年度に紐づかない接点」が消え、
3期を足したら「再応募して合格」が消えた（乱数の並びがずれた）。
テストは通っていた ―― 経路が無いことは失敗ではないからである。
**確かめたい経路は種で明示的に置く。**

### 「スクロールさせない」は「縮める」ではない

```
切れる  … 情報が失われる。最悪
重なる  … 読めない。次に悪い
スクロール … 読める
```

縮めるなら必ず内側にスクロール容器を対にする。目で見ずに測って確かめる。

### 逆仕訳は、件数で数えると二重になる

訂正を訂正すると元が有効に戻る。そのとき同じ段に**有効な通過が2件**並ぶ。
**件数で合否を判定してはいけない。`EXISTS` で見る。**
最初これを「二重にならない」とテストに書いて落ちた ――
**誤っていたのは実装ではなくテストだった。**

### 落ちているテストが、別の欠陥を隠す

`setPersonApproachState` が `UUID.test` を非束縛で渡しており、
アプローチ状態の編集が**画面からもテストからも必ず例外で落ちていた**（C-46）。
それを守るはずのテストが**先に別の理由で落ちており**、欠陥を隠していた。
**赤いテストを放置しない。**

### 整理は行番号で切る

ムダなコード削除で、**隣の関数を巻き込んで2回消した**。
`git checkout` で戻して行範囲で切り直した。

---

## 残る課題

- 認証がない。**プロフィールを誰が変えたか記録できない**
- 取り込んだ61人の生年月日・メール・姓名の区切り・学校は**受け取れていない**
- 入れた段は**最終面接だけ**。書類選考とグループ面接は旧データに点が無い
- 面接日を入れていない（旧 `final_date` に日付でない値が混ざる）
- 接点は入れていない
- 職員のメールが無い。同姓同名の職員を見分ける手段が氏名しか無い
- **書類選考とグループ面接の評価軸が無い。** 呼び名が未受領
- 旧データの自由記述の評価（強み・懸念・総評・確認事項）に置き場所が無い
- 点の訂正（E4）は未実装。一度付けた点は上書きできない
- 顔写真はDB内の data URL。大量運用前にオブジェクトストレージへ移す
- 職種の記録列がまだ無い
- アプローチ可能圏そのものの作成・編集UIが未実装
- **予定とやることは記録層だけ。** 画面から作る・終える入口がまだ無い
- 予定が実際に起きたかを接点として残す手当てが未実装
- ボーダーラインのタブは画像どおり4つ固定。載らない選考ステップは件数で明示している
- `.zoom-bar` 周りに色の直値が残っている。**意匠トークンに寄せていない。** 依頼者に未確認
- Pilot判定は **HOLD**。実運用の観測はまだ無い

---

## 守ること

- `basic/` は変更しない。**適用済みマイグレーションを編集しない**（新番号で足す）
- 実在個人情報をデモ・テスト・スクリーンショット・報告書・コミット文へ使わない
- 実データは `db/private/`（gitignore 済み）。**両リモートは公開である。**
  コミット前に毎回確かめる
- `.env.local` の接続文字列を出力しない・コミットしない
- ★ **`git add -A` を確かめずに打たない**（C-80）。運営から受け取った
  `.xlsx` / `.pptx` はリポジトリの直下に置かれることがある。
  コミット前に、**追跡対象になる全ファイル**を旧データの氏名・団体名と
  `example.test` 以外のメールで突き合わせる。目で見て確かめない
- 開発サーバ起動中に `pnpm db:reset` を打たない
- 記録にない値を創作しない。作るなら**由来を消さない**（3期の日付がその例）

## 完了確認

```bash
pnpm test
pnpm exec tsc --noEmit
pnpm build
pnpm tokens
```

現在 **442件 全通過**。変更理由は `db/DECISIONS.md` に **C-81 から**追記し、
次の報告書は `REPORT-11.0.md` とする。
