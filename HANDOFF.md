# 引き継ぎプロンプト

新しいセッションの最初に、この節（`---` から `---` まで）をそのまま貼る。

この文書は報告書と違って**常に最新に保つ**。版は切らない。
引き継ぎの内容が変わったら、ここを書き換える。

---

YouthDB の開発を引き継いでほしい。
起業家アカデミーの集客〜選考を一元管理するデータベースと、その上のダッシュボード。

**あなたの作業は実行⑦になる。**

> ## ★ 最初に読む — 現在の判定は **HOLD** である（2026-08-07）
>
> **開発は止まっている。** ブロッカーはソフトウェアの品質ではなく、
> **運用上の証拠が無いこと**である。デモデータでは採用の1周を完了したが、
> **実運用データでは1周していない。**
>
> **既定の方針: 証拠を集めることを、機能を足すことより優先する。**
> 「次に何をするか」の答えは**機能ではなく観測**である。機能を足すのは、
> それが観測を取るために必要なときだけ（例: `/pilot`）。
>
> Go の条件・現状・判定の全文は **`docs/pilot/DECISION-LOG.md`**。
> **確認は取れた**（2026-08-07）―― 運営担当者は **4名**（`EXIT_CRITERIA.md` の
> 2名以上を上回るので、基準を緩めずに満たせる）。実データは
> **既存の DB をそのまま受け取る**。受け取り方は `docs/pilot/DATA-INTAKE.md`。
> **実データを `db/seeds/` に置かない**（git 追跡下で2リモートへ公開される）。
> 受け入れ口は `db/private/`（`.gitignore` 済み）。
>
> **Pilot Go を、このブロッカーが残っている間に主張しない。**

**MVP モードは継続する。** 設計論で止まらず、運用できるものを増やす。
実行⑥で運転席（`/cockpit`）と森の画面が動くようになった。
**次は、運用して壊れるところを見つける段である。**

## まず状態を確認する

リポジトリは GitHub にある。

- `https://github.com/NEO-AX/YDB.git`（origin）
- `https://github.com/ymatsushita-creator/YDB.git`（mirror。同じ内容を push している）

直近の作業環境では `/Volumes/KIOXIA_2TB/NEO/YouthDB` に置いてあった。
以降このディレクトリを `<repo>` と書く。手元に無ければ clone する。

```bash
git clone https://github.com/NEO-AX/YDB.git   # 手元に無い場合だけ
pnpm install && pnpm db:reset && pnpm test
```

**テスト272件**が通れば正常。通らなければ、そこが最初の作業。
`pnpm exec tsc --noEmit` も通ることを確かめる。
`pnpm tokens` を流して `app/tokens.css` に差分が出ないことも確かめる。

画面は `pnpm dev`（ポート3111）。**開発サーバを起動したまま `db:reset` を
実行しないこと。** PGlite の dataDir にプロセス間ロックが無く、
エラーも警告もなく後勝ちで壊れる。

HEAD は実行⑥の締めコミット。

## 読む順番

**文書には序列がある。上が下に優先する。**

```
vision.md     理念（Why）      ← 内容は未受領。枠のみ
     ↓
director.md   憲法（Principles）
     ↓
domain.md     世界設定（Model）
     ↓
design.md     UI（UX/UI）
     ↓
CLAUDE.md     実装の規律（Engineering）
     ↓
process.md    開発の回し方（Loop。MVP モード）
```

**`process.md` は実行⑥で受領した。** 1機能ずつ選び、実装し、自己レビューし、
人のレビューを受け、コミットし、学ぶ。**設計論で2サイクル止まったら仮定を置いて進む。**
上位文書と衝突する3点（Documentation follows implementation / Forest Health /
Relationship）は、寄せずに `process.md` 末尾の付記に残してある。

**実行⑥で `vision.md` が最上位へ移った。**「なぜ作るか」が「どう作るか」より
上位に立つ、という指示による。ただし内容は未受領のままで、**空の文書は
何も上書きしない。** 理念が届くまで、実際に効く最上位は `director.md` である。
各文書の責務と食い違いの扱いは `CLAUDE.md` 冒頭の Document Hierarchy にある。

1. **`director.md`** — 憲法。実装者ではなくレビュアーとして振る舞う。
   「実装前の自己チェック」4項目は**着手条件**であって、後からのレビューではない
2. **`domain.md`** — 世界設定。**ここが最重要。**
   トポロジー（構造）/ ワークフロー（イベント）/ リレーションシップ（関係）の
   3軸を混ぜない。森・林・木は**エンティティの階層**であって選考ステータスではない。
   **10節に未解決の項目がある**
3. **`design.md`** — UI。**この文書はまだトークンを生成しない**（frontmatter が無い）
4. **`CLAUDE.md`** — 実装の規律と4つの問い
5. `README.md` — 起動手順
6. `db/DECISIONS.md` — **これが記録の本体。** 原典から変えた点と理由。
   特に冒頭の宣言、**A-16（絞ると 9000 倍遅くなった）** / A-17 / A-11 / A-14、
   **C-16 〜 C-19（森の実体・いまやること・要注意の旗・件と人）**、
   **D-12（MVP で置いた仮定と TODO(MVP) 全部）**、
   D-11（段の語が再定義された。実装は未追随）、D-8 と D-10（未確定）、E 節
7. **`docs/pilot/DECISION-LOG.md`（現在の判定 HOLD。ここから読む）**
   → `docs/pilot/EXIT_CRITERIA.md`（Pilot を終える条件と Go / Hold / No-Go）
   → `REPORT-6.11.md`（試運転の足場 `/pilot`）
   → `REPORT-6.10.md`（残課題の分類と優先順位。実装なし）
   → `REPORT-6.8.md`（**採用の1周が閉じた。到達点はここ**）
   → `REPORT-6.5.md`（レトロスペクティブ。5サイクルの知見と設計更新）
   → `REPORT-6.9.md`（判定の訂正。実運用フェーズ1件目）
   → `REPORT-6.8.md`（採用の1周が閉じた）
   → `REPORT-6.7.md`（E2 点と根拠の保存。PM Report 形式の初回）
   → `REPORT-6.6.md`（評価の第1段 E1）
   → `REPORT-6.5.md`（**レトロスペクティブ。5サイクルの知見と設計更新**）
   → `REPORT-6.4.md`（担当を替える。二重計上の発見）
   → `REPORT-6.3.md`（保留を解く。止まらずに進めた記録）
   → `REPORT-6.2.md`（初めての書き込み）
   → `REPORT-6.1.md`（森のマップと `process.md` の受領）
   → `REPORT-6.0.md`（実行⑥。運転席と森を作った本編）
   → `REPORT-5.0.md`（文書の序列と世界設定。コード変更なし）
   → `REPORT-4.0.md` → `REPORT-3.0.md` → `REPORT-2.2.md` → 遡って `2.1` `2.0` `1.1` `1.0`
8. `basic/*.sql` — 原典。設計原則7つが冒頭にある。**変更しない**

## 実行⑥で作ったもの（いま動いている）

**MVP は完成し、採用の1周が画面の中で閉じた。** そのあと Pilot Phase に入り、
**いまは HOLD（実装停止）である**（冒頭の枠を読む）。

報告書は `REPORT-6.0` 〜 `6.11` の12本。全文はそちらにある。要点だけ。

| 版 | 何をしたか |
|---|---|
| 6.0 | 運転席と森の実体化（0012）。**A-16: 絞ると 9000 倍遅くなる集計を直した** |
| 6.1 | 森のマップ。`process.md` 受領 |
| 6.2 | **初めての書き込み**（担当を決める）。F-1: 「クライアント JS 0件」の記録が誤りだった |
| 6.3 | 保留を解く。**止まる前に制約を読み直せば消える問いだった** |
| 6.4 | 担当を替える。A-18: 保留＋利益相反で二重計上 |
| 6.5 | **レトロスペクティブ（5サイクル）。**0014 でやることを優先順位式に組み替えた |
| 6.6 | E1 評価すべき軸を見せる |
| 6.7 | E2 点と根拠を1軸ずつ保存。**A-19: 「根拠は必須」が空白の種類で破れていた**（0015） |
| 6.8 | **E3 確定 と D1 判定。採用の1周が閉じた** |
| 6.9 | D2 判定の訂正。実運用フェーズ1件目 |
| 6.10 | 残課題の分類と優先順位（実装なし） |
| 6.11 | `/pilot` 試運転の足場（機能ではない） |

そのあと `docs/pilot/` に道具5点を作り、**HOLD 判定を受けた**。

- **森が記録層の実体になった。** `partners.parent_partner_id` を足し、
  親を持たない団体が森（Forest）、親を持つ団体が林（Community）。
  2段まで（3段目はトリガが拒否）。集計は必ず `v_partner_forest` を通す
- **「次に何をすべきか」が導出で成立した。** `v_open_tasks` が4種
  （評価する / 担当を決める / 保留を解く / 担当を替える）を、
  既存の事実だけから出す。**Task の記録層は作っていない**（`domain.md` 10-2 は未決）
- **画面が2枚増えた。** `/cockpit`（運転席）と `/forests/[id]`（森を1つ開く）。
  既存の6枚は1行も変えていない。ナビの先頭だけ運転席にした
- **要注意の森は点数ではなく事実の旗**（滞留 / 休眠 / 接点なし）。
  Health は実装段階[3]で、原典が着手判断を1年後と書いている

**踏んではいけない地雷が1つ増えた。** `v_forest_season_activity` と
`v_open_tasks` は、**絞り込みを高い供給元まで降ろすと計算量が爆発する。**
`MATERIALIZED` と `ss.season_id` はそのための指定で、外すと 9000 倍遅くなる
（A-16）。`tests/15` が定義そのものを見張っている。

## 主題 — いまは Pilot Phase（MVP は完成済み）

> **MVP モードは終わった。** 以下の原則は「作るとき」の指針として引き続き
> 有効だが、**いまは作る段ではない。** 現在の既定は
> **証拠を集めることを機能を足すことより優先する**である（冒頭の枠）。
> 新しく作ってよいのは、**観測を取るために必要なもの**だけ（例: `/pilot`）。

**目的は完全なドメインモデルではない。運用できる最小のプロダクトである。**

```
Working software > Perfect architecture
Real feedback    > Assumptions
Iteration        > Perfection
Shipping         > Discussion
```

成功の定義は「ドメインモデルが完成した」ではない。
**「採用担当者が今日これを使える」**である。

### 凍結する概念

**Forest / Community / Person / Task の4つで足りる。**
これ以外の第一級エンティティを、実装上どうしても必要でない限り増やさない。

TODO のまま置くもの ——
Membership / Connector / Relationship / Ambassador / Alumni /
Forest Health / Ecosystem Score / Advanced Analytics。

迷ったら、ドメインを広げずに TODO を書く。

### 実装のルール

設計判断に直面したら:

1. **単純なほうを選ぶ**
2. 不確かなら **TODO(MVP)** を残す
3. **実装を続ける**
4. **未解決の設計論で進行を止めない**

### UI の目標

運転者が**5秒以内**に次へ答えられること。

- いま何をすべきか
- どの Forest が要注意か
- どの Person が待っているか
- 何が止まっているか

それ以外は二の次。

### 許される簡略化

次は**やってよい**。ただし必ず `TODO(MVP)` で印を付ける。過剰設計にしない。

- データの一時的な重複
- モック値のハードコード
- 関係の単純化
- 正規化の先送り
- エッジケースの無視

### アーキテクチャのルール

**MVP が機能しないという理由がない限り、既存の構造を作り直さない。**
書き直しより拡張を選ぶ。大規模なリファクタを避ける。
きれいな設計より、動く実装を優先する。

**具体的には ―― `REPORT-5.0.md` 5節にある比喩語174箇所の付け替えは、実行⑥では行わない。**
既存のダッシュボードとオペレーション画面はそのまま残し、運用者向けの入口を新しく足す。

### 成果物の出し方

実装ごとに:

1. 機能を実装する
2. 一時的な妥協を説明する
3. `TODO(MVP)` の一覧を出す
4. 次の機能へ進む

**実装が答え無しには不可能な場合を除き、設計上の問いで止まらない。**

### 分析ループの禁止

新しく動く機能を1つも生まないまま、設計の議論が**2サイクル続いたら議論を止める**。
不確かさを `TODO(MVP)` として記録し、妥当な仮定を置いて実装を続ける。

MVP 期は、理論的な正しさより前進が価値を持つ。

## 実行⑤で決まったこと（実装はまだ追随していない）

`REPORT-5.0.md` 4節に全文がある。要点だけ。

- **トポロジーとワークフローを混ぜない。**
  森・林・木は Forest / Community / Person という**エンティティの階層**。
  Interested / Applicant / Accepted は Person に付く**状態**。層が違う
- **Application は Entity。属性へ降格させない。**
  一意制約は `(person_id, season_id)` なので、年度が違えば同一人物が何件でも持てる。
  Person の状態は Applications[] から**導出する**（物理カラムを持たない）
- **Member は Person の属性ではない。**
  `Application → Accepted → Enrollment → Membership`。**合格 ≠ 在籍**
- **Connector は Relationship の Role。** Person の状態ではない
- **Unknown は Person の状態として持てない。** 行が無いものに状態は付かない。
  Unknown の規模を表すのが森の属性 Reach（`partner_reaches` の推定値）。
  **この境界をまたいで割り算をしない**

**重要 ―― 実装は D-2（「林は人、木と幹は応募」）のままである。**
`domain.md` の定義とコードは食い違っている。詳細は `DECISIONS.md` D-11。
中途半端に直すほうが危険なので、MVP が動くまでこのまま置く。

## リポジトリの外にあるもの

git 管理下に入っていないため、環境が変わると付いてこない。

- `<repo>/../academy-ui-assets/` … UI 素材ライブラリ（実行②で作成、約10MB）。
  検証済みの素材は `next-shadcn-dashboard-starter`（MIT）1件だけ。
  `reports/` に8つのレポートがある。詳細は `REPORT-2.1.md`
- `<repo>/.pgdata/` … PGlite のデータ。`pnpm db:reset` でいつでも作り直せる

**未決が1つ。** `../academy-ui-assets/reports/` の8ファイル（80KB）は git 管理外で、
リポジトリに入れる（`docs/ui-assets/` など）か添付で渡し続けるかを決めていない。
ZIP（10MB）と参考画像は入れない方針。

## このプロジェクトの規律（MVP モードでも変わらない）

原典 `basic/` の設計者は、カラムと制約の1つ1つに「なぜそうしたか」を
日本語で書いている。コメントは装飾ではなく設計の本体。それに合わせる。

- **`basic/` は変更しない。** 正典として残す。実装は `db/migrations/`
- **理由なき差分を作らない。** 原典から変えたら必ず `db/DECISIONS.md` に
  「発見の経緯・なぜそう直したか・どのテストで固定したか」を書く。
  テスト参照が実在するかは `tests/10_decisions_references.test.ts` が機械的に検査する
- **適用済みマイグレーションは編集しない。** チェックサムが変わって
  ランナーが止まる。新しい番号で足す
- **日付は必ず `jst_date()` を通す。** `::date` と `CURRENT_DATE` は
  接続のタイムゾーン依存。今日は `jst_today()`
- **`status_histories` は追記のみ。** UPDATE も DELETE もトリガが拒否する。
  訂正は打ち消し行の追記で表現する
- **集計定義をコンポーネント側に書かない。** データ取得は `src/queries/` に集約する
- **集計マスタは追加と非活性化で運用する**
- **`app/tokens.css` は生成物。手で編集しない**
- **報告書は更新せず版を上げる。** 記録（`DECISIONS.md`）は更新する

優先順位は常にこう。**意匠の改修であっても変わらない。**

```
記録の構造（何を事実として残すか）
  > 集計の定義（その事実をどう数えるか）
    > 画面（数えた結果をどう見せるか）
```

## 進め方

「動かして計測し、そこで初めて分かる欠陥を潰す」往復で進めてきた。

1. 動く状態にする
2. 実データで動かす
3. 見つけた欠陥を**先にテストへ書き、落ちることを確認する**
4. 直す
5. 理由を `DECISIONS.md` に記録する
6. コミットする

3 を先にするのは、直したつもりで直っていないのを防ぐため。

これまでに踏んだ失敗（繰り返さないこと）:

- **修正が別の場所を壊す。** 変更のたびに「この前提が崩れる箇所」を数え直す
- **記録が実装より厳密に見える。** 「テストを示した」と書いて実際には
  無かった。参照が嘘だと読んだ人は確認をやめる。記録が無いより悪い
- **デモデータが検証したい経路を踏んでいない。** **同じ見落としを4回繰り返した。**
  4回目（実行④）は、登場人物を「終わった年度に置く」という判断そのものが、
  ③に関わる形をデモから締め出していた。**どこに置くかも経路の一部である**
- **目視は検証ではない。** DOM か SQL の値で確かめる
- **作り物のデータから出た数字をドキュメントに残さない。** 追試できない。
  残すのはデータ量に依存しない計算量のほう
- **単位の違うものを割った指標を作りかけた。**（実行②）
  新しい画面を作るときは、既に消した指標と同じ形になっていないか確かめる
- **表示の丸めで、注記が画面上の嘘になった。**（実行②）
  丸めは表示の最後だけで行い、合計は丸める前の値から作る
- **集合を返す関数を結合の内側に直接置かない。**（実行③）
  外側の行ごとに関数全体が評価され、一覧が 444 秒かかった。
  **氏名で絞ると速いので、手元で軽く触るだけでは気づけない。**
  一覧は必ず素の状態（絞り込みなし）で開いて確かめる
- **同じ言葉を2つの定義で使わない。**（実行③）
  「林」が2つの意味を持ち、同じ年度で 2,576 対 661 になっていた
- **「数えるか」と「動いているか」を同じ述語で扱わない。**（実行④）
  集計の母集団（`v_countable_applications`）と運用の母集団（`v_active_applications`）は別物
- **丸め込みが、欠陥そのものを隠す。**（実行④）**辻褄が合っている＝正しい、ではない**
- **バグが偶然に作っていた経路は、バグを直すと一緒に消える。**（実行④）
  直したあとに、それまで踏めていた経路がまだ踏めているかを確かめる（`tests/13`）
- **性能は「絞り込みあり」と「なし」の両方で測る。**（実行⑥、A-16）
  A-11 は絞ると速くなって見逃した。A-16 は**絞ると 9000 倍遅くなった。**
  片側だけを測る限り、どちらかの向きを必ず取り逃す。
  実行③以降の「一覧は素の状態で開いて確かめる」だけでは足りない
- **`LEFT JOIN` の `ON` に絞り込みを書いても、行は落ちない。**（実行⑥、A-17）
  削除済みの除外を外部結合の条件に書いて、1文字も効いていなかった
- **計測は使い捨てのインメモリ DB で行う。**（実行⑥）`.pgdata` を触るプロセスを
  途中で止めると壊れる。実際に2回壊した。`freshDb()` + `seedDemo()` なら捨てるだけで済む
- **外付けドライブは作業中に抜ける。**（実行⑥）USB が抜けて作業ディレクトリごと
  消えた。**リポジトリは無事だった**（HEAD は push 済みと一致、`git fsck` も実害なし）
  が、未コミットの1ファイルが宙に浮いた。**区切りごとにコミットして push する。**
  復帰したら `git log -1` / `git fsck` / `pnpm test` の3つで健全性を確かめ、
  `.pgdata` は作り直す（突然の切断を受けているため）
- **性能の比較で `LIMIT` だけを付けない。**（実行④）`ORDER BY` を外すと
  入れ子ループが打ち切られ、438.9 秒が 3 ミリ秒に見えた
- **文書だけ直して実装が追随しないと、語が2重定義になる。**（実行⑤）
  `domain.md` で森・林・木・幹を定義し直したが、コード174箇所は旧定義のまま。
  **移行するときは機械的な置換ではなく、旧ビューを非活性化して新しい語で作り直す**

## 技術構成

- PostgreSQL 15+ 想定。開発とテストは **PGlite**（WASM 版 PostgreSQL 18、
  デーモン不要）。`src/db/client.ts` の `Db` は `query`/`exec`/`close` の
  3つだけなので、本番ドライバへの差し替えは `src/db/server.ts` に閉じる
- Next.js App Router（**サーバコンポーネントのみ、`'use client'` が0件**）。
  **訂正（実行⑥、F-1）**: かつてここに「クライアント JavaScript を一切
  配信していない」と書いてあったが**誤りである。** App Router は RSC の
  ペイロードを流すため React の実行時を必ず配る。正しくは
  **クライアント部品を1つも書いていない**（状態を持つ対話部品が無い）。
  クライアント側のライブラリを採用するのは「クライアント境界を新設する」判断と
  同義になるので、**その判断は単独で示し、他の判断に埋め込まない**。
  ただし**書き込みはこれに当たらない。** サーバアクションを素の
  `<form action={...}>` に渡す形はサーバコンポーネントに置けて、
  `'use client'` は0件のまま、JS 無効でも動く（C-20）
- テストは `node:test`。フレームワーク無し
- Tailwind も shadcn も認証も lint 設定も持たない。**不足ではなく設計判断である**
- デザイントークンは `basic/DESIGN.md` の frontmatter から
  `app/tokens.css` を生成（`pnpm tokens`）。生成物は手で編集しない

### 意匠の現状（重要）

`basic/DESIGN.md` の中身は **Notion のブランド分析**である（`name: Notion-design-analysis`、
`version: alpha`）。起業家アカデミーの意匠として何が正しいかは、まだ決めていない。
**既存画面の色や字送りを「意図された設計」として扱わない。**

改訂版 `design.md` はリポジトリ直下にあるが、**frontmatter が無いため
トークンを生成できない**（`#hex` 0件、`px/rem` 0件）。
`scripts/build-tokens.ts` は `basic/DESIGN.md` を読み続けている。

貫通率の実測（実行⑤）—— `app/tokens.css` の140変数のうち描画へ到達しているのは47個。
**タイポグラフィは17グループ中1つしか効いていない**（`base.css` が字を直値で書いているため）。
色は健全で、`.tsx` 内のハードコード色は0件。詳細は `REPORT-5.0.md` 2節。

## いま確認が要ること

MVP モードでは、**これらで実装を止めない。** 妥当な仮定を置き `TODO(MVP)` を残して進む。

### `domain.md` 10節の未解決（実行⑥で一部を仮に置いた）

| # | 内容 | 実行⑥での扱い |
|---|---|---|
| 10-1 | `partner_relations` と Relationship の**語が衝突する** | **未決のまま。** 森と人の結び付きは「接触があった」で代用 |
| 10-2 | **Task の定義が無い** | 記録層を作らず**導出**にした（C-17）。手で作るタスクは表せない |
| 10-3 | **Interested（芽）の判定事実** | **触っていない** |
| 10-4 | **Forest / Community の実体** | `partners` に親を1本足した（C-16）。別テーブルにするかは未決 |
| 10-5 | **Membership の粒度** | **触っていない** |
| 10-6 | 実装段階を憲法で上書きするか | Health（[3]）は作らず、事実の旗で代えた（C-18） |
| 10-7 | `director.md` の IA に重複がある | **触っていない**（憲法なので `domain.md` からは直せない） |

仮に置いたものは全部 `DECISIONS.md` D-12 に一覧がある。
**推測で埋めた事実は無い。**

### 実行⑥から指示を仰ぎたいこと（`REPORT-6.0.md` 8節）

- **`/cockpit` が表を3つ持っている。** `director.md` は「テーブルを作らない」と
  書いている。やることの列であって在庫の一覧ではない、という判断で作ったが、
  **憲法の字面には反している。** カード表現へ寄せるか、この形でよいか
- **休眠の日数（60 日）** が仮の値。引数にしてあるので決まれば1箇所で変わる

### 以前からの未確定

**`seasons` の実数値。** 「年度・定員(36)・目標応募数(100)は実数値」との指示を
受けているが、それがどの年度で、4つの日付がいつかを確認できていない。
そのため本番シードに `seasons` を入れていない。`src/seed/demo.ts` の4年度はすべて創作。
詳細は `DECISIONS.md` D-8。

**個人情報削除を受けた人の応募を、木に数え続けるか。**
森・林・生涯サマリは削除済みを除外しているのに、木と幹は数え続けている。
同じ年度で 林 < 木 が起こりうる。過去年度の応募数と合格数が動く変更なので触っていない。
詳細は `DECISIONS.md` D-10。

## 実行⑦の着手点 —— **まず実装しない**

> **最初にやることは、機能の実装ではない。**
>
> 1. 冒頭の HOLD の枠と `docs/pilot/DECISION-LOG.md` を読む
> 2. **確認待ちの1件**（運営担当者は2名以上か1名以上か）の答えを得る
> 3. 社長への依頼（`docs/pilot/CEO-REQUEST.md`）の回答が届いているか確かめる
> 4. 届いていれば → 本番シードへ**最小の変更**（年度・ステップ・観点）を入れ、
>    実データで1周してから判定に進む
> 5. 届いていなければ → **数字の妥当性に依存しない観測だけ**を進める
>    （`docs/pilot/CHECKLIST.md` 0-1-1 の表）
>
> **「無いから作る」を理由にしない。** 観測が入るまで、優先順位表の
> Confidence は上がらない（`REPORT-6.10`）。

以下は、観測が入って実装に戻るときのための地図である。

UI の目標4つは、実行⑥で一通り答えが出た。

| 問い | いまあるもの | まだ無いもの |
|---|---|---|
| いま何をすべきか | `v_open_tasks` の4種、`/cockpit` | **手で作るタスク**（連絡・催促・資料送付） |
| どの森が要注意か | `v_forest_activity` / `v_forest_season_activity`、旗3つ | Health、**Owner（担当）** |
| 誰が待っているか | `getWaitingPersons`（人で畳んである） | — |
| 何が止まっているか | 期限超過、森単位への集約 | — |

**やること4種のうち3種が実行⑥で入った** ―― 担当を決める（C-20）／
保留を解く（C-21）／担当を替える（C-22）。**残るのは「評価する」だけ。**
書き込みの型はここで決まったので、次の書き込みはこれに倣う。

```
判定と更新  src/commands/   ← テストから直接呼べる。ここに事実の確認を置く
受け渡し    app/*/actions.ts  'use server'。結果はコードで返す（氏名を URL に入れない）
表示        app/*/page.tsx    素の <form action={...}>。'use client' は増やさない
```

**「評価する」は1サイクル単位に割った**（`REPORT-6.5.md` 5節）。
`evaluation_scores` の一意制約が `(evaluation_id, criteria_id)` なので、
**1軸ずつ保存する操作をスキーマが最初から許している。**ここが分割線。

| # | 単位 | 書き込み | 状態 |
|---|---|---|---|
| E1 | 評価すべき軸を見せる | なし | **済**（C-24） |
| E2 | 1軸だけ保存する（点＋根拠） | `evaluation_scores` に1行 | **済**（C-25） |
| E3 | 確定する（`submitted` にする） | `evaluations` の2列 | **済**（C-26） |
| D1 | 判定する（通過／不合格）＋次ステップ生成 | `status_histories` に1行 | **済**（C-26） |
| E4 | 点の訂正 | 未定 | 未 |

## 採用の1周は閉じている（実行⑥の到達点）

```
今日やることを見る → 候補者を開く → 担当を決める / 替える / 保留を解く →
評価する（軸ごとに点と根拠）→ 確定する → 判定する → 次のステップへ
```

通過すると次のステップの評価行ができ、**それが運転席の「担当を決める」として
並ぶ。** 最終ステップの通過で合格になり、その応募のやることは空になる。

**判定の訂正は入った**（C-27）。打ち消し行の追記で通過↔不合格を差し替える。
新しい概念は足していない ―― 記録層が最初から持っていた仕組みに入口を付けた。

**画面にまだ無いもの** ―― 点の訂正（`already_scored` で弾かれる）、
辞退（`withdraw`。理由が必須）、申し送り、新規応募の登録。

## 実運用フェーズの優先順位（指示）

```
1. 実運用で業務が止まる問題
2. データの整合性を壊す問題
3. 運営担当者が迷うUI
4. UX改善
5. 新機能追加
```

新機能は上位4項目を妨げない場合のみ。**サイクル開始時に必ず答える** ――
「この改善は実際の運営で観測された問題か、開発中に想像した問題か」。
観測された問題を常に優先する。

報告は **PM Report**（Goal / What Changed / Operator Value /
Remaining Friction / Technical Debt / Risks / Recommendation（Option A/B）/
Confidence）と **Validation Report**（誰が検証したか / 実データで確認した内容 /
未検証のケース / 今後確認すべきケース）。

### Pilot Phase の道具（`docs/pilot/`）

**開発は停止中。** いまのスプリントの目的は運営担当者から学習を得ることである。

| 文書 | 使うとき |
|---|---|
| `docs/pilot/CHECKLIST.md` | 実施前。社長15分／運営担当者40分／初めて触る人20分 |
| `docs/pilot/OBSERVATION-SHEET.md` | 実施中。**対象者ごとに1枚コピー** |
| `docs/pilot/REVIEW.md` | 実施後。4分類と**昇格の条件** |
| `docs/pilot/REPORT-TEMPLATE.md` | 報告書。リポジトリ直下へコピーして連番を付ける |
| `docs/pilot/EXIT_CRITERIA.md` | **Pilot を終える条件と Go / Hold / No-Go の判定** |

**Pilot はデモデータでしか走らない。** 本番シードには `withdraw_reasons` しか
無く、年度・選考ステップ・評価軸が1行も無い（D-8）。したがって
**「数字が実感と合うか」はこの Pilot では学習できない。**
学習できるのは**操作の流れと言葉**である。

**昇格の規則（`REVIEW.md` 3）が最も重要。**
要望・感想・開発者の気づきは Validated にしない。**行動だけが証拠になる。**
迷ったら昇格させない ―― 急いで Validated にすることの利益は無い。

**Pilot は無期限に続けない**（`EXIT_CRITERIA.md`）。終わりの条件は
運営担当者2名以上・1周5回以上（合格と不合格の両方を含む）・観測20件以上・
**飽和**、そして「業務が止まる」「数字が間違う」の Validated が**0件**。
**機能の数では判定しない。**

**Go の必須条件に「実データで1周」がある。** いま満たせない ―― 本番シードに
年度が無いため。ブロッカーはコードではなく**情報**（年度の実数値）である。

**依頼書は `docs/pilot/CEO-REQUEST.md`**（回答5分・5問）。
聞くのは年度・4つの日付・ステップ名・評価の観点・定員36の年度確認だけで、
**設計や製品の相談は含めない。** 定員36・目標100は既に実数値として受領済み
（D-8）なので、確認1問に縮めてある。`sla_days` は NULL 可なので聞かない。

**この値を回避する実装をしない。** 届くまでは
**数字の妥当性に依存しない使い勝手の観測だけ**を進める
（`CHECKLIST.md` 0-1-1 の表）。届いたら本番シードへ最小の変更で追加し、
実データで1周してから判定に進む。**Hold のまま基準を緩めて Go にしない。**

★ **面接官と候補者の情報はシードに入れない。** `db/seeds/` は GitHub の
2つのリモートへ push される。氏名とメールは個人情報なので、
**git に残らない経路で投入する**（依頼書にもそう書いてある）。

### Operator Validation フェーズの進め方（実行⑥の途中で追加）

**1機能を終えたら、次をすぐ実装しない。** 次の順で立ち止まる。

1. PM Report を出す
2. Validation Report を出す
3. **残課題を4つに分類する**（Pilot Validation から改称）
   ―― **Validated Operator Problem** / **Developer Hypothesis** /
   Technical Debt / Product Backlog
4. 次を推奨する。**ただし優先順位の最大値ではなく、
   実装量あたりの学習量（learning per implementation）が最大のものを選ぶ。**
   小さい実装で運営のフィードバックが大きく増えるなら、大きい機能より優先する

**開発者が気づいたことは仮説である。** 運営の行動だけが検証済みの知識になる。
だから `Developer Observation` は `Developer Hypothesis` と呼ぶ。

**Pilot の目的は不具合ではなく、誤解を見つけることである**（`process.md` の
Pilot Rule）。**運営が迷ったら、運営ではなく画面が間違っていると考える。**
「操作できたが迷った」は「手が止まった」と同じ重みで扱う ――
押せた場所はバグとして報告されないので、訊かなければ永久に見えない。

**「無いから作る」を理由にしない。** Pilot の目的は機能の完成ではなく**学習**である。

**2026-08-07 時点で Observed Operator Problem は0件である**（誰も運営として
使っていない）。そのため全候補の Confidence が 4 以下に留まっている。
分類と見積りの全文は `REPORT-6.10.md`。

成功基準は**「運営担当者が、より少ない判断・より少ないクリックで採用を
進められるようになったか」**の1つだけ。

**E2 の注意点。** `rationale` は必須で空文字も通らない（0005）。
上限は `scale_max` 参照のためトリガで担保されている。
再応募者限定の軸は、再応募でない応募に書くとトリガが拒否する ――
**画面が出す軸とトリガの規則が食い違うと「保存できない軸」ができる**（C-24）。

**5サイクルごとにレトロスペクティブを行う**（指示）。1回目は `REPORT-6.5.md`。
**次は E1 から数えて5回目**（E1・E2・E3 …）。

その先の候補 ——

1. **手で作るタスク。** 運用の大半はこちらのはずである。`tasks` テーブルと
   それを書く画面が要る。書き込みの型は C-20 で決まっているので、
   残る判断は**記録層に何を持たせるか**（`domain.md` 10-2）
2. **Owner（担当）。** 森に担当者が付かないと「誰のボールか」が森の段で答えられない
3. `/people` が 1.39 秒。実行③の 444 秒からは直っているが他の2〜3倍ある

画面 8枚 —— `/cockpit`（運転席。森はマップで出す）、`/forests/[id]`（森）、
`/`（ファネル）、`/people`、`/people/[id]`、`/applications/[id]`、
`/operations`、`/sources`。
クエリ —— `src/queries/cockpit.ts`、`dashboard.ts`、`drilldown.ts`。

## 版の規約

- 報告書は `REPORT-<実行番号>.<連番>.md`。連番は0から。**実行⑦の第1報は `REPORT-7.0.md`**
- Pilot の結果を書くときは `docs/pilot/REPORT-TEMPLATE.md` を使う（7項目が決まっている）
- **報告書は書いたら凍結する。** 追記が要るときは新しい連番を作る
- **記録（`db/DECISIONS.md`）は更新する。** ここだけは最新を保つ
- **この `HANDOFF.md` も更新する。** 版は切らない
- 過去の報告書は書き換えない。訂正は新しい版に書く
- **コミットは origin と mirror の両方へ push する**

## 完了の定義

- [ ] `pnpm test` が全通過する（新しい判断にはテストが1つ以上ある）
- [ ] `pnpm exec tsc --noEmit` がクリーン
- [ ] 実データで動かし、**DOM か SQL の値で**結果を確認した（目視だけで判断しない）
- [ ] `db/DECISIONS.md` に理由とテスト参照を書いた
- [ ] `TODO(MVP)` の一覧を出した
- [ ] **運転者が5秒で4つの問いに答えられるか**を言える

---
