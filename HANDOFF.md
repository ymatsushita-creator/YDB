# HANDOFF.md — 実行⑩への引き継ぎ

実行⑨は **UI の大改革**だった。依頼者が画像で構造を指示し、
こちらはそれを実装して記録層を後ろに足した。**画面の形を決めるのは依頼者である。**

---

## いま画面はこうなっている

```
期（1期・2期・3期）        ← 一番上の層。タブを移っても保たれる
 └ タブ                    ヘッドハンティング / ボーダーライン / アプローチ
    └ 対象                 候補者・応募・段
```

- 上の帯は**現在地の表示**（`2期 › ボーダーライン › 最終面接`）。
  エクスプローラのパスと同じで、**期の切替はここに置かない**
- **期の切替は操作柱（左）の一番下。** 見やすい場所に置かない ―― 依頼者の指示
- 「運転席」という名は消した。ファネル・森・流入元のタブも消し、3つに畳んだ
- 共通シェルは**各ページが被る**（レイアウトではない）。`'use client'` は **0件**
- **ページ全体はスクロールしない。** カードが個別に内側でスクロールし、
  中身の量に関わらず**各セクションの位置は動かない**（C-57 / C-58）
- 現行語彙は「アプローチ可能圏 / 構成コミュニティ」。旧生態系比喩は使わない
  （適用済みSQLの `forest` / `community` は互換性のため内部に残る）
- 未記入は**赤**。ただし赤は「書けるのに空」だけで、**導出できないものは灰**（C-56）

編集できるもの ―― 候補者プロフィール、担当者メモ、顔写真、アプローチ状態、
成績・根拠・判定。**確度・順位・集計値は導出値なので直接編集しない。**

---

## 実行⑨で足した記録層

| | |
|---|---|
| `0016_approach.sql` | アプローチ状態の追記履歴 |
| `0017_candidate_confidence.sql` | 確度スコア。**本番の規則は0件**なので画面は「規則未登録」と出る（0%ではない） |
| `0018_person_profile_editing.sql` | 顔写真とプロフィール変更スナップショット |
| `0019_appointments.sql` | 予定（日程）。接点とは別の表。取り消しても消さない |
| `0020_manual_tasks.sql` | 人が作るやること。導出の4種（`v_open_tasks`）とは別物 |
| `0021_season_cohort.sql` | `seasons.cohort_number` ―― 期の番号 |

決定は `db/DECISIONS.md` の **C-39 … C-58**。

---

## 実行⑩が最初に判断すること

### ① 2期の実データ投入が、止まっている

依頼者は **「2期にリアルデータを使うことを許可する」** と明言した。
だが**入れられていない。** 理由は1つ ――

```
旧テーブル（db/private/legacy-youthdb-2026-08-07/）
  youth_candidates.json  61件
  candidates.json        32件
        ↓
  生年月日の列が無い

persons.birth_date は NOT NULL（basic/academy_schema.sql:50）
しかも氏名照合の3項目の1つである
```

**道は2つ。どちらも選ばれていない。**

1. **生年月日を集める** ―― 記録層を変えずに済む。依頼者側の手間になる
2. **`birth_date` を NULL 可にする** ―― 記録層の変更であり、
   **同姓同名の判別方法そのものが変わる。** `basic/` は変更しないので
   新番号のマイグレーションで緩めることになる

★ ②は「列を1つ緩めるだけ」に見えるが、**そうではない。**
照合キーが2項目に減ると、**別人を同一人物として統合する事故**が起こりうる。
安く見えるほうを黙って選ばないこと。**依頼者に選ばせる。**

### ② 3期に、まだ受け取っていない値がある

3期は**本番にある**。受領済みは選考フロー（書類審査 → グループ面接 → 最終面接）と
定員36 だけ。

```
★推測  集客開始 2027-02-01
★推測  応募開始 2027-03-10
★推測  応募締切 2027-03-22
★推測  選考終了 2027-04-15
        ―― 4つとも2期の周期を1年ずらしただけ。周期が同じ保証は無い

未受領  評価軸、目標応募数
```

**日付は集計定義そのものである。** 実際の日程が来たら黙って上書きせず、
`season_revisions` に履歴を残して直す（C-54 / C-55）。
2期の6軸や目標100を写していない ―― 写すと**運営が使っていない軸**が固定化する。

### ③ 2期と3期で、段の数も呼び名も違う

```
2期  応募受付 → 書類選考 → ... （4本）
3期            書類審査 → グループ面接 → 最終面接（3本）
```

**同じものを指しているか未確認。受け取った語のまま置いてある。**
運営の言葉をこちらの語に翻訳して記録しない（Pilot Rule）。
**段数を決め打ちするコードを書かない** ―― テストも段数を数えずに書いてある
（`tests/32_selection_loop.test.ts`）。

---

## 実行⑨で踏んだ落とし穴（同じ穴に落ちないために）

### 乱数に頼っていた経路は、条件が変わると黙って消える

これは**3回起きた。**

- 「年度に紐づかない接点」が、年度を4→2に減らしたら消えた
- 「再応募して合格」が、3期を足したら消えた（乱数の並びがずれた）

どちらも**運よく生成されていただけ**だった。テストは通っていた ―― 経路が
無いことは失敗ではないからである。**確かめたい経路は種で明示的に置く。**

### 「スクロールさせない」は「縮める」ではない

指示どおり縮めた結果、**中身が切れて届かなくなった**（3回）。
次に `overflow: visible` にしたら**文字が重なった**（カード373px に中身677px）。

```
切れる  … 情報が失われる。最悪
重なる  … 読めない。次に悪い
スクロール … 読める
```

決着 ―― ページは 100vh 固定、列は `minmax(0, Nfr)` で比率固定、
**カードは縮まない。縮めるなら必ず内側にスクロール容器を対にする。**
目で見ずに `getBoundingClientRect()` で測って確かめること。

### 逆仕訳は、件数で数えると二重になる

訂正を訂正すると元が有効に戻る。そのとき同じ段に**有効な通過が2件**並ぶ。
**件数で合否を判定してはいけない。`EXISTS` で見る。**
最初これを「二重にならない」とテストに書いて落ちた ――
**誤っていたのは実装ではなくテストだった。**

### 落ちているテストが、別の欠陥を隠す

`setPersonApproachState` が `UUID.test` を非束縛で渡しており、
アプローチ状態の編集が**画面からもテストからも必ず例外で落ちていた**（C-46）。
それを守るはずのテストが**先に別の理由で落ちており**、欠陥を隠していた。
**赤いテストを放置しない。**

### 整理は行番号で切る

ムダなコード削除で、**隣の関数を巻き込んで2回消した**
（`getForest` / `getCommunities` / `ForestPersonRow`）。
`git checkout` で戻して行範囲で切り直した。

---

## 取り消した変更

**シェルの伸びるアニメーション（C-59）は入れて、依頼者の指示で戻した**（`ac0c96a`）。
記録はGit履歴にある。**現行文書に旧条文を併記しない。**

もし再度やるなら要点だけ ――
`transform: scale()` は中身ごと歪むので使わない。幅・高さの animate は
毎フレーム再レイアウトになり表とカレンダーが動く。`clip-path` なら描画だけ変わる。
ロゴの実寸は **204 × 97**（目分量の 236×106 では外側から生えて見えた）。

---

## 残る課題

- 認証がない。**プロフィールを誰が変えたか記録できない**
- 顔写真はDB内の data URL。大量運用前にオブジェクトストレージへ移す
- 職種の記録列がまだ無い
- アプローチ可能圏そのものの作成・編集UIが未実装
- **予定とやることは記録層だけ。** 画面から作る・終える入口がまだ無い
- 予定が実際に起きたかを接点として残す手当てが未実装
- ボーダーラインのタブは画像どおり4つ固定。載らない選考ステップは件数で明示している
- `.zoom-bar` 周りに色の直値が残っている（`app/base.css:295-306`。
  `#2b2b2b` / `#fff` / `#444` / `#111`）。
  **意匠トークンに寄せていない。** 依頼者に未確認
- Pilot判定は **HOLD**。実運用の観測はまだ無い

---

## 守ること

- `basic/` は変更しない。**適用済みマイグレーションを編集しない**（新番号で足す）
- 実在個人情報をデモ・テスト・スクリーンショット・報告書・コミット文へ使わない
- 実データは `db/private/`（gitignore 済み）。**両リモートは公開である。**
  コミット前に毎回確かめる
- `.env.local` の接続文字列を出力しない・コミットしない
- 開発サーバ起動中に `pnpm db:reset` を打たない
- 記録にない値を創作しない。作るなら**由来を消さない**（3期の日付がその例）

## 完了確認

```bash
pnpm test
pnpm exec tsc --noEmit
pnpm build
pnpm tokens
```

現在 **372件 全通過**。変更理由は `db/DECISIONS.md` に **C-59 から**追記し、
次の報告書は `REPORT-10.0.md` とする。
