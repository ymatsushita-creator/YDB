# 引き継ぎプロンプト

新しいセッションの最初に、この節（`---` から `---` まで）をそのまま貼る。

この文書は報告書と違って**常に最新に保つ**。版は切らない。
引き継ぎの内容が変わったら、ここを書き換える。

---

YouthDB（`/Volumes/KIOXIA_2TB/NEO/YouthDB`）の開発を引き継いでほしい。
起業家アカデミーの集客〜選考を一元管理するデータベースと、その上のダッシュボード。

## まず状態を確認する

```bash
cd /Volumes/KIOXIA_2TB/NEO/YouthDB && pnpm install && pnpm db:reset && pnpm test
```

テスト97件が通れば正常。通らなければ、そこが最初の作業。

画面は `pnpm dev`（ポート3111）。**開発サーバを起動したまま `db:reset` を
実行しないこと。** サーバが削除前のデータファイルを掴んだままになる。

## 読む順番

1. `README.md` — 起動手順と、触る前に知るべきこと
2. `db/DECISIONS.md` — **これが記録の本体。** 原典から変えた点と理由。
   特に冒頭の宣言、D 節（確定した仕様と保留）、E 節（記録漏れだった差分）
3. `REPORT-1.0.md` → `REPORT-1.1.md` — これまでの経緯。1.0 は凍結済み
4. `basic/*.sql` — 原典。設計原則7つが冒頭にある。**変更しない**

## この プロジェクトの規律

原典 `basic/` の設計者は、カラムと制約の1つ1つに「なぜそうしたか」を
日本語で書いている。コメントは装飾ではなく設計の本体。それに合わせる。

- **`basic/` は変更しない。** 正典として残す。実装は `db/migrations/`
- **理由なき差分を作らない。** 原典から変えたら必ず `db/DECISIONS.md` に
  「発見の経緯・なぜそう直したか・どのテストで固定したか」を書く。
  テスト参照が実在するかは `tests/10_decisions_references.test.ts` が検査する
- **適用済みマイグレーションは編集しない。** チェックサムが変わると
  ランナーが止まる。新しい番号で足す
- **日付は必ず `jst_date()` を通す。** `::date` と `CURRENT_DATE` は
  接続のタイムゾーン依存。今日は `jst_today()`
- **`status_histories` は追記のみ。** UPDATE も DELETE もトリガが拒否する。
  訂正は打ち消し行の追記で表現する（訂正を訂正すると元が復活する）
- **集計マスタは追加と非活性化で運用する。** 最初に入れた値が事実上の
  初期値として固定化されるため、本番シードは分類が決まるまで空にしてある
- **報告書は更新せず版を上げる。** 記録（`DECISIONS.md`）は更新する。
  役割を混ぜない

## 進め方

「動かして計測し、そこで初めて分かる欠陥を潰す」往復で進めてきた。
各ループの手順:

1. 動く状態にする
2. 実データで動かす
3. 見つけた欠陥を**先にテストへ書き、落ちることを確認する**
4. 直す
5. 理由を `DECISIONS.md` に記録する
6. コミットする

3 を先にするのは、直したつもりで直っていないのを防ぐため。

これまでに踏んだ失敗（繰り返さないこと）:

- **修正が別の場所を壊す。** 集計の定義を変えたら、それを引くビューの
  結合の多重度が変わっていた。変更のたびに「この前提が崩れる箇所」を数え直す
- **記録が実装より厳密に見える。** 「テストを示した」と書いて実際には
  無かった。参照が嘘だと読んだ人は確認をやめる。記録が無いより悪い
- **デモデータが検証したい経路を踏んでいない。** 画面に出しただけでは
  検証にならない。直した分岐が実際に実行されているかまで確かめる。
  同じ見落としを3回繰り返した
- **目視は検証ではない。** スクリーンショットで「描画された」と判断した
  グラフが、実は枠外にはみ出して切れていた。DOM を測って初めて分かった
- **作り物のデータから出た数字をドキュメントに残さない。** 追試できない。
  残すのはデータ量に依存しない計算量のほう

## いま確認が要ること

**`seasons` の実数値。** 「年度・定員(36)・目標応募数(100)は実数値」との
指示を受けているが、それがどの年度で、4つの日付
（`outreach_start_date` / `application_open_date` / `application_close_date` /
`selection_end_date`）がいつかを確認できていない。
そのため本番シード（`db/seeds/0001_reference.sql`）に `seasons` を入れていない。
`src/seed/demo.ts` の4年度はすべて創作。詳細は `DECISIONS.md` D-8。

## 次にやりうること

優先順位は指示を仰ぐこと。候補は `DECISIONS.md` の D-9 にある。

- ダッシュボード(4)（流入元）。森 `partner_reaches` が画面のどこにも
  出ていない。`f_partner_reach_summary` は実装済みだが集計値のテストが無い
- 本番 PostgreSQL 対応。`src/db/server.ts` に node-postgres アダプタを足す。
  そのとき `pg.types.setTypeParser` で `int8`(20) と `date`(1082) を
  同時に直さないと、数値が文字列で返り、表示日付が1日ずれる
- 実装段階[2]（Touchpoint 分析、Partner 系）以降

## 技術構成

- PostgreSQL 15+ 想定。開発とテストは **PGlite**（WASM 版 PostgreSQL 18、
  デーモン不要）。`src/db/client.ts` の `Db` は `query`/`exec`/`close` の
  3つだけなので、本番ドライバへの差し替えは `src/db/server.ts` に閉じる
- Next.js App Router（サーバコンポーネントのみ、クライアント JS なし）
- テストは `node:test`。フレームワーク無し
- デザイントークンは `basic/DESIGN.md` の frontmatter から
  `app/tokens.css` を生成（`pnpm tokens`）。生成物は手で編集しない

---
