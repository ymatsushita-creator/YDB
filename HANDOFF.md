# HANDOFF.md — 実行⑱への引き継ぎ

実行⑰は **AI分析（C-164〜165）／連携団体の引き継ぎ漏れ（C-174）／
書類選考の4軸（C-187）／画面の作り直し**だった。
決定は `db/DECISIONS.md`、直前の経緯は `REPORT-16.0.md` 以降。

---

## ★★ 依頼者から受けた、まだ終わっていない指示（番号表） ★★

**上から順に処理する。** 依頼者の指示：「一個ずつ順番にやれ」。
終わったら状態を「完了」に書き換え、**この表を消さない。**

| # | やること | 状態 |
|---|---|---|
| ① | 縦バーのボタンをスリムに・詰める | 完了（C-189） |
| ①-b | ロゴ全体をボタン化。縮小しても縦横比そのまま | 完了（C-188 / C-191） |
| ② | 欲しい人ランキングを横80%、右の要素はその分に隠す | 完了（C-190） |
| ③ | **KPI画面** ―― ホームの推移を小さくし、KPIを表示・編集・アーカイブ。<br>題名・変数・数・メモを **ALL権限**が入力し、結果をビジュアライズ | 完了（C-192） |
| ④ | 候補者リストの列を統一。確度順リストを「候補者を編集」と同じ要素に。<br>**両方の画面に顔写真** | 完了（C-193） |
| ⑤ | 連携団体を編集する画面をスプシ形式に。既存の連携も閲覧。<br>候補者編集画面を模倣 | 完了（C-194） |
| ⑥ | 今までのコードに無駄が無いか点検 | **未着手（次はここ）** |
| ⑦ | パイロットテストを **経営層／平社員の2セット** | 未着手 |
| ⑧ | リポジトリ構成が健全か調べ、構造を最適化。<br>**AIが読むとき邪魔になる情報を消す・移す・捨てる** | 未着手 |

### 積み残し（番号表の外。依頼者へ確認が要る）

- **論理力の点を `evaluation_scores` へ自動で流す処理**。軸は 0010 で入ったが、
  AIの点をそこへ書く口がまだ無い。
- **人に見せる門の閾値**。「基準をクリアしたものから人に見せろ」と指示されたが、
  何点以上かが未定（3点以上／2点以上）。**決まるまで実装しない。**
- **KPI目標と実績の突き合わせ**。目標（`recruitment_course_targets` 14件）は
  `/funnel` に出したが、実績の区分が記録の側に無い。当てると違う母集団の割り算になる。
- **アーカイブを戻す口**が無い（候補者）。DBを直に叩けば戻る。
- **CLI用のAPI鍵は無効**。`.env.local` の `ANTHROPIC_API_KEY` は例文のまま。
  ただしALL権限の `/ai` へ実物の鍵を貼れば、保存せず1件ずつ実行できる（C-195）。
- **表の氏名を右スクロールでも見せる件**は、貼り付け（sticky）を3回とも
  字が重なって失敗し、**番号の列だけに戻した**（C-186）。
  やるなら列の並びを変える形で。`left` を手で足す作りは幅が変わるたび壊れる。

---

## 実行⑰で入った主なもの

- **0044 / 0045** AI分析。`ai_pre_assessments` / `ai_pre_viewpoints`。
  観点は論理力1本、0〜4点。**所見つきで残る。**
- **0046** 提携団体の追加列（推薦可能人数・提携期日・最適連絡時期・所在地）。
- **seed 0009** 2期→3期の団体状態の引き継ぎ（170件に状態が付いた）。
- **seed 0010** 書類選考の4軸16点（論理力／NEOとの親和性／やり遂げた実績／
  コミットする意志）。★ 論理力はAIが付け、**成績に自動で入る**という
  依頼者の判断（実行⑯の「成績として扱わない」を上書きした）。

## 作法（実行⑰で繰り返し落ちた点）

- **見た目の指示は、デプロイして初めて届く。** `pnpm deploy:production`。
- **貼り付け（sticky）に幅を手で足さない。** 3回とも字が重なった。
- **画面から消すときは `display: none` にしない。** 見出しの段が読み上げから
  消える。場所を取らないところへ畳む。
- **テストは「軸が0本」「件数がN」を前提にしているものが多い。**
  シードを足したら、落ちた見張りを**実態に合わせて**直す。
