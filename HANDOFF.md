# HANDOFF.md — 実行⑭への引き継ぎ

実行⑬は**応募管理表の掘り起こし**と**帯の一体化**、そして
**「詰めていない階層」の点検3周**だった。
経緯は `REPORT-13.0.md`、決定は `db/DECISIONS.md` の **C-116 … C-120**。

---

## 実行⑭が最初に判断すること

### ★★ 最優先 ―― **説明のつかない本番書き込みがある**

実行⑬の最後に `pnpm db:migrate:production` を流したところ、

```
migrations applied: 0
```

**0033〜0035 は、こちらが流す前に既に本番へ適用されていた**
（`schema_migrations.applied_at` = 2026-08-13T07:20:11Z。明示実行より前）。

調べた範囲では ――

```
openPostgres()      マイグレーションを流さない
src/db/backup.ts    dump は読むだけ（INSERT は restore 側）
getDb()             非デモでも Postgres を開くだけで migrate を呼ばない
Vercel のビルド     VERCEL=1 → デモ（PGlite インメモリ）
```

**何が適用したのか特定できていない。** 疑わしいのは、ローカルの `pnpm build` が
Next の仕様で `.env.local` を自動で読み、`DATABASE_URL`（本番）を向いていること
（ビルド出力に `Environments: .env.local` と出る）。ただし `getDb()` は
migrate を呼ばないので、**これだけでは説明がつかない。**

★ 結果として本番の状態は正しく、実データも無傷だった（応募43・評価41）。
**だが「意図しない経路で本番へ書き込みが起きうる」なら、これは C-88 と同じ性質の穴である。**
憶測で塞がず、まず**何が書いたのかを突き止める。**

調べ方の当たり ―― `applied_at` の前後にこちらが打ったコマンドを洗う。
`pnpm build` を `DATABASE_URL` を外した環境で流して差分を見る。
`src/db/` の中で `migrate(` を呼んでいる箇所を全部数える。

### ★ 依頼者の指示で**未着手**のもの ―― 2期を3期へ適用する

依頼者の言葉は「**まず2期で完成させて、それを3期に適用していく流れ**」。
2期は実行⑬で完成させた（下記）。**3期への適用は1行も書いていない。**

```
2期（完成）  特別選考(1) → 応募受付(2) → 書類選考(3) → グループ面接(4) → 最終面接(5)
             特別選考の軸9本（必須A〜C ＝ 3 ・ 加点D〜I ＝ 6）
3期（現状）  書類審査(1) → グループ面接(2) → 最終面接(3)
             特別選考の段と9軸を持つ（C-105 で先に入れてあった）
             ★ 呼び名が2期と違う（「書類選考」ではなく「書類審査」）。
               同じものを指すかは**確認していない**（運営の語を翻訳しない）
             ★ 3期の候補者は空（C-115。315人をいったん外した）
```

**呼び名がそろっていない**ことを黙って揃えない。依頼者に確かめる。

### ★ デプロイ済み。**本番はデモモードのまま**（C-94）

```
https://youthdb.vercel.app        Production Ready（実行⑬の e2edf53）
```

`VERCEL=1` → デモ経路。**実データは1件も出ていない。**
実データを出すのは依頼者の判断であって、こちらが決めることではない。
出す前に必要なもの ―― **①認証 → ②バックアップの手順確認 → ③DBの分離。**
**①を飛ばして出さない**（合言葉は層で共有で、誰が入ったかを記録できない。C-84）。

### ★ 本番DBは実行⑬時点で最新（0035まで）

```
本番       0001 … 0035 適用済み ・ シード 0001〜0005
2期の段    特別選考(1) 応募受付(2) 書類選考(3) グループ面接(4) 最終面接(5)
2期の軸    特別選考 必須3・加点6 ／ 最終面接 6（段分けなし）
推薦枠     未連絡 / メール送信済み / アポ実施済み / 対象外
実データ   人61 ・ 応募43 ・ 評価41 ・ 接触記録240（実行⑬で壊していない）
```

バックアップ ―― `db/private/backups/2026-08-13T07-20-55-135Z/`（gitignore 済み）。
★ **取れたことは、戻せることの証明ではない。** 戻し方は `docs/pilot/BACKUP.md`。

---

## 応募管理表（`2期応募管理.xlsx`）―― まだ掘り切っていない

★ **これは追跡外である**（`.gitignore` の `*.xlsx`）。実在の応募者の氏名・
メール・評価が入っている。**両リモートは公開である。**

52シートある。実行⑬で使ったのは6枚だけで、**残りは読んでいない。**

```
使った   特別選考 / 005_面談シート / 013_選考フロー / 011_提携団体 /
         003_2期生アプローチリスト / シート52（読んだが取り込んでいない）
未使用   012_カレンダー(1017行) / 014_WBS / 006_ユース募集施策 / 007_担当者別KPI /
         008_イベント集客 / 009_説明会応募フォーム / アカデミア説明会アンケート結果 /
         015_ペルソナ / 募集要項 / 拡散依頼リスト / 4月イベント一覧 / SNS施策 /
         2期生合格者オンボーディング / 20251213_AWARD 参加者・申込者 ほか
```

### ★ 取り込まなかったものと、その理由

```
シート52 の39人の選考結果   一次(7〜16) 二次(39〜67) 最終(合格30/補欠5/辞退3/不合格1)
                            → **二次選考結果の満点が表から読めない。**
                              書類選考の軸の呼び名も無い。**分母の無い点を入れない**
書類選考・グループ面接の軸   段の呼び名と工程はあるが、**採点の軸の呼び名が無い**
                            → 0002 の保留と同じ理由。名付けるとマスタとして固定化する
最適連絡時期                138件あるが **126件が「1.0」** ―― 読めない値を列にしない
所在地・SNS・関連URL        **0件**。列はあるが1件も入っていない
AWARDへの応募有無           **接点（touchpoints）で表せる**（0028 の原則）。列を足さない
関連するアカデミア生         `touchpoints` が人と団体を結ぶので**導出できる**。列を足さない
```

★ **列を足すことは、答えを出すことではない。**
同じ事実が2箇所に載れば、必ずどちらかが古くなる。

★ 足さないと決めた2つ（AWARD応募有無・関連するアカデミア生）は、
**記録層にはあるが、接点として入力する道が画面にまだ無い。**

---

## 実行⑬で踏んだもの（同じ穴に落ちないために）

### ★★ 高さを指定した枠には `flex: 0 0 auto` を必ず添える ―― **3度目**

```
C-66   `--logo-h` を指定したのに、送る画面だけ 81 → 58 に潰れた
C-113  操作柱の評価基準が 0 まで潰れ、見出しだけが残った
実行⑬  ロゴ枠が 67 → 24 に潰れ、隣の横バーと厚みが違った（依頼者の「幅が違う」）
```

縦・横の flex の子は既定で `flex-shrink: 1`。**指定した高さは守られない。**

### ★ `sticky` の `top` は枠の余白の内側から測る

縦バーに `top: var(--space-md)` を置いていたため、**縦バーだけ 16px 下がっていた。**
厚みが同じでも天端がずれれば2枚に見える。`top: 0` にした。

### ★★ 「出る」ボタンが押せなくなっていた

縦バーの足元（期の切り替え・デモ札・**出る**）が **60px はみ出して枠の外**にあった。
高さ固定なのに中身が縮まず、あふれたぶんが切り取られていた。
**送るのはタブの並びだけ**にした（ロゴと足元は動かさない）。

★ **切れているものは読めないのと同じ**（C-65）。今回は読めないだけでなく押せなかった。

### ★ `btrim` の既定は半角スペースだけ

表の保存が `btrim($n)` で空白を落としていたため、**全角スペースだけの値**が
「空ではない」ものとして残り、`*_not_blank` に弾かれて**行ごと保存できなくなっていた。**
落とす空白は**制約と同じ集合**にする（`src/commands/sheet.ts` の `BLANK_CHARS`）。

★ 制約を持たない既存列では、同じ穴が「**見えない値が静かに溜まる**」形で潜む。

### ★ 段の位置を番号で仮定しない

2期の先頭に特別選考を足したら、`tests/23〜33` の「位置で段を指す」ヘルパが全部ずれた。
`tests/23` の20周シミュレーションは落とす段を `1..last` から選んでおり、
段が 2..5 へずれて**どの段にも当たらず「落としたはずが通った」。**

### ★★ 孤立を見つける検査が、孤立を隠していた

`tests/49_reachable_layers.test.ts` を**2度間違えた。**

```
1度目  リンクの書き方で拾い漏らし、繋がっている /funnel を「孤立」と数えた
2度目  自分から自分へのリンクを入り口と数え、/pilot を見逃した
```

★ **数えた結果を疑う前に、数え方を疑う**（実行⑫で2回書いた教訓）。

### ★ 訂正は「無かったことにする」ではなく「こう直す」

推薦枠のテストを1つ間違えて書いた。打ち消し行は**そのとき正しい値を持ち**、
それが新しい現在値になる（0016 と同じ）。**実装ではなく理解のほうが誤っていた。**

### ★ 測る前に、測れる状態かを見る

画面を測って「はみ出し14件」と読んだが、`vw = 0` ―― ペインが休んでいただけだった。

---

## 画面の形は依頼者が決める

`design.md` は無い（実行⑨で削除）。**先回りして仕様を書かない。**
決まったことだけを `CLAUDE.md` と `basic/DESIGN.md` に残す。

実行⑬で決まったこと ――

- 縦タブと横タブは**1枚の帯**（天端・厚み・接続をそろえる）
- 評価軸は**動かさず全件**を出す（折り返す。流さない）
- ホームの各セクションから詳細へ飛べる。**行き先は `canOpen` で守る**
- 入力者を追加は**表（スプシ形式）**

### フォームと表の使い分け（実行⑬でこちらが判断した。C-120）

★ **「フォームをすべて表にする」ことが目的ではない。**
まとめて打つものが表で、1件を開いて直すものがフォームである。

```
表のまま     /people/new ・ /approach（団体を直す・接触）・ /staff/new
フォームのまま  /approach/new（団体の新規作成。写真と初回接触を伴う）
             /people/{id}/edit（1人の詳細。一括は /people/new の表が持つ）
             /applications/{id}（採点と判定。依頼者が外した面接シートと同じ仕事）
```

---

## 残る課題

- ★ **説明のつかない本番書き込み**（最上段。まず特定する）
- ★ **2期を3期へ適用する**（依頼者の指示。未着手。呼び名の違いを先に確かめる）
- 認証がない。**誰が入ったかを記録できない**（C-84）。
  表の「入力者」もメモの「書いた人」も**自己申告である**
- 本番はデモモードのまま。実データを出すのは依頼者の判断（C-94）
- **書類選考・グループ面接の採点軸が無い**（呼び名が応募管理表に無い）
- **シート52 の39人の結果を取り込んでいない**（二次の満点が読めない）
- 関わり方（0030 / 0031）は自由入力なので**集計できない**。語を受け取ったらマスタへ
- 推薦枠ステイタス（0035）は**画面から訂正できない**（打ち消しは SQL でしか書けない）
- AWARD参加・関連学生を**接点として入力する道が画面に無い**
- 点の訂正（E4）は未実装
- 顔写真はDB内の data URL。大量運用前にオブジェクトストレージへ
- 予定とやることを画面から作る・終える入口がまだ無い
- 2期の集客開始日が推測値のまま（接触記録240件のうち187件がどの期にも紐づかない）
- 表は全件を出す（ページ送りが無い）。**数百行の重さを測っていない**
- 団体の名前・既存行の接点は表で直せない
- `/pilot` は繋いだが、**Pilot が走れば役目を終える。** 観測が取れたら消す
- Pilot判定は **HOLD**

---

## 守ること

- `basic/` は変更しない。**適用済みマイグレーションを編集しない**（新番号で足す）
- `'use client'` は**表と追従光の2つだけ**（C-95 / C-104）。増やさない
- 層の判定は `src/auth/tiers.ts` の `canOpen` **1箇所**。画面ごとに書かない
- **操作可能な母集団と画面に出す母集団を一致させる**（押すと弾かれる窓を残さない）
- 実在個人情報をデモ・テスト・スクリーンショット・報告書・コミット文へ使わない
- 実データは `db/private/`（gitignore 済み）。`*.xlsx` も追跡外。
  **両リモートは公開である**（`origin` = NEO-AX/YDB ・ `mirror` = ymatsushita-creator/YDB）
- `.env.local` の接続文字列を出力しない・コミットしない
- ★ **`git add -A` を確かめずに打たない**（C-80）。運営から受け取った
  `.xlsx` / `.pptx` はリポジトリ直下に置かれる
- 開発サーバ起動中に `pnpm db:reset` を打たない
- 記録にない値を創作しない。**読めない値を、意味のある列にしない**
- 単位と母集団は**画面に書かない**（C-62）。定義はクエリのコメントと `db/DECISIONS.md`

---

## 完了確認

```bash
pnpm test
pnpm exec tsc --noEmit
pnpm build
```

現在 **582件 全通過**。`tsc` / `build` クリーン。

見張りとして置いてあるもの ――

```
tests/45_home.test.ts              ホーム・タブ・帯の幾何・'use client' が2つのままか
tests/49_reachable_layers.test.ts  **入る道の無い階層が0件のまま**か
tests/48_criteria_weighting.test.ts 重み付け（必須/加点）と、軸を勝手に足していないか
tests/50_partner_recommendation.test.ts 推薦枠が期ごとに分かれているか
```

変更理由は `db/DECISIONS.md` に **C-121 から**追記し、
次の報告書は `REPORT-14.0.md` とする。
