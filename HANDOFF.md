# 引き継ぎプロンプト

新しいセッションの最初に、この節（`---` から `---` まで）をそのまま貼る。

この文書は報告書と違って**常に最新に保つ**。版は切らない。
引き継ぎの内容が変わったら、ここを書き換える。

---

YouthDB の開発を引き継いでほしい。
起業家アカデミーの集客〜選考を一元管理するデータベースと、その上のダッシュボード。

**あなたの作業は実行⑩になる。**

## 0. 最初に読む — Pilot は **HOLD** のまま

- **Validated Operator Problem は 0 件**（実行⑥終了時から1件も増えていない）
- **実データでは1周していない。誰も運営として使っていない**
- **Pilot Go を、このブロッカーが残っている間に主張しない。基準も緩めない**
- 判定の全文と Go の条件は `docs/pilot/DECISION-LOG.md`（追記専用）

**★ 実行⑦が出した「シミュレーション Go」を Pilot Go と混同しない。**
架空データで6週回して安定を確認しただけで、別の指標である。

**★ 実行⑨の UI 大改修も Validated ではない。** 依頼者から届いた仕様であり、
運営が触って見つけた問題ではない。**「新しい UI のほうが良い」は観測ではない。**

## 1. 実行⑨で何が変わったか ★最重要

**依頼者から画面仕様（画像）と設計思想の変更が届き、上位文書が改定された。**
実行⑧までの前提の多くが、ここで書き換わっている。

### 1-1. 情報構造が3つのタブに集約された

```
年度（2025 / 2026）          ← 最上位。タブを移っても保たれる
   ├── ヘッドハンティング  /headhunting  誰に声を掛けるか（経営者の視点）
   ├── ボーダーライン      /borderline   誰を通すか（現場の視点）
   └── アプローチ          /approach     どこから来ているか（流入の視点）
```

- **タブは3つで固定。4つ目を増やさない**
- 旧「運転席」は**ボーダーライン**に改名（`/cockpit` → `/borderline`）。
  **「運転席」という語は使わない**（依頼者の指示）
- 旧「森と流入元」は**アプローチ**（`/sources` → `/approach`）
- 旧ファネル（`/`）は `/funnel` へ移り、アプローチの中に畳んだ
- `/operations` `/people` `/applications/[id]` `/forests/[id]` `/pilot` は
  **タブから外し、3つのどれかの中に畳んだ**
- **どの画面がどのタブに属するかは、そのルートの `layout.tsx` が宣言する**
  （`<Shell active="borderline">`）。これが畳んだ先の唯一の記録である
- `/` は `/headhunting` へ送る

### 1-2. 一番上の帯は「いま開いている階層」（パンくず）

**年度を切り替える場所ではない。** 最初の版は帯に年度のボタンを並べたが、
依頼者の指摘で直した ―― **一番よく見える場所から年度を変えられる必要はない。**
年度の切り替えは帯の右端に小さく置いてある（`YearSwitch`）。

**頻度の低い操作を一番目立つ場所に置かない。**

### 1-3. 意匠の正典が `basic/DESIGN.md`（依頼者の画像から）に移った

**実行⑧まで未決だった「衝突1」が決着した**（C-39）。

- **「色は線、面は黒」は廃した。** 面に色を塗ってよい
- トークン名は1つも変えていない（既存画面が参照しているため）
- **`app/tokens.css` は生成物。手で編集しない**（直すのは `basic/DESIGN.md`）
- **ボタンは物理ボタン**（`btn-physical`）。厚みと沈み込みで押せることを形で伝える

### 1-4. 記録層が2つ増えた

| | 何を記録するか |
|---|---|
| **0016** | アプローチ状態（未アプローチ / 検討中 / アプローチ中 / 面談調整中 / 見送り） |
| **0017** | 確度スコア（**実装段階[3] スコアリングへの着手**） |

- **どちらも追記専用。** 訂正は打ち消し行の追記（`status_histories` と同じ型）
- **0017 は規則そのものを作っていない。** 点数と閾値は依頼者から未受領で、
  **本番シードには規則が1件も無い。** デモにだけ仮の規則がある
- 規則が0件なら確度は算出されず、画面は「規則未登録」と出る。**0% ではない**

### 1-5. 上位文書が改定された（依頼者の指示）

**`director.md` と `design.md` を書き換えた。** 撤回した条文は次のとおり。

| 文書 | 撤回した条文 |
|---|---|
| `director.md` | 「最上位オブジェクトは Forest」「人が最上位に来る UI は禁止」「一覧を増やさない」「テーブルを作らない」「一覧→詳細は禁止」 |
| `design.md` | `Never: List → Detail`、Core Components 8つ、「Soft green」「Spacious」 |

**改定前の条文は各文書に引用として残してある。** 消すと、なぜ実装がこうなって
いるのかを次に読む人が追えなくなる。

**改定していないもの** —— `director.md` 末尾の「憲法が上書きしないもの」。
思想は画面を支配するが、**事実を作り替えることはできない。**

`CLAUDE.md` は3節に「画面の最上位は年度と3つのタブ」を新設し、**以降の節番号が
1つずつ繰り下がった。** 他文書からの節参照は実行⑨で直してある。

### 1-6. デモの年度が 4 → 2 になった

**募集は 2025 と 2026 しか行われていない**（依頼者）。
架空の年度が画面の切替に並ぶと、運営に「その年度の記録がどこかにある」と思わせる。

**この変更でデモが踏んでいた経路が2つ壊れた**（テストが落ちて気づいた）。

1. 「年度に紐づかない接点」が**1件も無くなった。** 乱数の裾で偶然できていた。
   **偶然に頼っていた経路は、条件が変わると黙って消える。** 明示的に1件置いた
2. 「再応募して合格」と「判断待ちが十分ある」が両立しなくなった。
   2026 の応募期間を締切間際まで延ばして両立させた

## 2. まず状態を確認する

リポジトリは GitHub にある。

- `https://github.com/NEO-AX/YDB.git`（origin）
- `https://github.com/ymatsushita-creator/YDB.git`（mirror。同じ内容を push している）
- **どちらも public。** 実データは `db/private/` にあり追跡下に無いが、
  漏れれば取り消せない。コミットのたびに確認する

```bash
pnpm install && pnpm db:reset && pnpm test
```

**テスト333件**が通れば正常。`pnpm exec tsc --noEmit` と `pnpm build` も通ること、
`pnpm tokens` を流して `app/tokens.css` に差分が出ないことを確かめる。

画面は2通りある。**どちらを使うかで見えるデータが違う。**

```bash
pnpm dev        # ポート3111。.env.local に DATABASE_URL があれば Supabase を見る
pnpm dev:demo   # ポート3112。使い捨てのインメモリ DB（デモ）
```

**★ `.env.local` に `DATABASE_URL`（Supabase）がある場合、`pnpm dev` は
本番 DB を見る。本番にはマイグレーション 0016 / 0017 が入っていないので、
ヘッドハンティング画面は 500 になる。** 適用するかは依頼者の判断（7節）。

### この環境で実際に壊した3つ

1. **開発サーバを起動したまま `pnpm db:reset` を実行しない。**
   PGlite の dataDir にプロセス間ロックが無く、**エラーも警告もなく壊れる**
   ―― 実行⑧でガードを入れた（ポート3111が使用中なら拒否）。
   **ただしポートを変えて起動していれば素通りする**（`pnpm dev:demo` は3112）。過信しない
2. **`.pgdata` を触っているプロセスを `kill` / `pkill` しない。** 同上
3. **計測は使い捨てのインメモリ DB で行う**（`freshDb()` + `seedDemo()`）

外付けドライブに置いてあるため、**作業中に抜けると作業ディレクトリごと消える。**
**区切りごとにコミットして push する。**

## 3. 読む順番

```
vision.md     理念（Why）      ← 内容は未受領。枠のみ
     ↓
director.md   憲法（Principles）  ← ★実行⑨で改定された
     ↓
domain.md     世界設定（Model）
     ↓
design.md     UI（UX/UI）        ← ★実行⑨で改定された
     ↓
CLAUDE.md     実装の規律          ← ★3節を新設。以降の節番号が繰り下がった
     ↓
process.md    開発の回し方（Loop）
```

1. **`director.md`** — 憲法。**「実装前の自己チェック」4項目は着手条件。**
   **提出時に4つへの答えを添える**
2. **`design.md`** — UI。**改定後の Core Components は「実装のある部品」だけ**
3. **`domain.md`** — 世界設定。**10節に未解決がある**
4. **`CLAUDE.md`** — 実装の規律と4つの問い
5. **`process.md`** — MVP モードの回し方と **Pilot Rule**
6. `db/DECISIONS.md` — **記録の本体。** 特に **C-39 〜 C-43**（実行⑨）、
   C-16 〜 C-38、**A-16**（絞ると9000倍遅い）、
   **D-8** / D-10 / **D-11** / D-13 / D-14（未決）
7. **`REPORT-9.0.md`（最新）** → `8.0` → `7.5` → … → `1.0`
8. `docs/pilot/` — Pilot の道具10点
9. `basic/*.sql` — 原典（SQL）。**変更しない**

## 4. いま何ができているか

```
今日やることを見る → 候補者を開く → 担当を決める / 替える / 保留にする・解く →
評価する（軸ごとに点と根拠）→ 確定する → 判定する → 次のステップへ
```

- 画面 **10枚**（3タブ＋その中に畳んだ7枚）
- マイグレーション **17**、テスト **333**（29ファイル）、
  **`'use client'` 0件**、`TODO(MVP)` 20箇所前後
- 書き込みの型（C-20）

```
判定と更新  src/commands/     ← テストから直接呼べる。ここに事実の確認を置く
受け渡し    app/*/actions.ts  'use server'。結果はコードで返す（氏名を URL に入れない）
表示        app/*/page.tsx    素の <form action={...}>。'use client' は増やさない
```

### 部品

```
app/_components/shell.tsx       外枠（Shell / Breadcrumb / YearSwitch）
app/_components/ui.tsx          Card / Kpi / Empty / LevelBadge / 整形関数
app/_components/charts.tsx      可視化
app/_components/headhunting.tsx ApproachChip / RankMark / RankDelta / Stars / Confidence
app/base.css                    手書きのスタイル
app/tokens.css                  ★生成物。手で編集しない（pnpm tokens）
```

### 画面にまだ無いもの

**ヘッドハンティング画面の下に一覧として出してある。** 黙って省くと、
次に触る人が実装漏れと読んで作り直しにかかる。

顔写真 / 学年・卒業年度 / 職種 / 成績ランキングの順位変動 / 手で作るタスク /
期限の時刻 / ログアウト（認証が無い）。
加えて **アプローチ状態を進める操作が画面に無い**（記録層はあるが書き込み経路が未実装）。
点の訂正・辞退・申し送り・新規応募の登録も無い。

## 5. 依頼者の判断を待っているもの

**あなたには決められない。勝手に決めて進めない。**

| # | 待っているもの | これが無いと |
|---|---|---|
| 1 | **改定した憲法の確認** | 撤回した条文が意図どおりかを見てもらう必要がある |
| 2 | **確度の規則（どの事実に何点か）** | 本番で確度が1件も出ない |
| 3 | **2025年度の実データ** | 本番シードには 2026 しか無い（D-8） |
| 4 | **ボーダーライン／アプローチの仕様** | いまは旧画面の中身がそのまま入っている |
| 5 | **候補者の生年月日**（他4件も） | 実データで1周できない。**1人も入らない** |
| 6 | **運営4名の日程** | 観測が0件のまま |
| 7 | 「見送り」の語と、見送りに理由が要るか | 終端状態は構造上の必要として仮に置いた |
| 8 | 卒業年度・職種を記録層に持たせるか | いまは年度で代用している |
| 9 | 本番へ 0016 / 0017 を適用するか | **勝手に適用していない** |

### 補足

- **実データ（旧 NEO-Youth の Supabase 全10表）は受領済み**
  （`db/private/legacy-youthdb-2026-08-07/`）。
  **それでも実データで1人も応募者を作れない。**
  **候補者の生年月日は旧 DB に列そのものが無い。**
  `persons.birth_date` は NOT NULL なので、①改めて集める ②記録層を変えて
  NULL 可にする、のどちらかを判断する必要がある（②は勝手に決めない）
- **チャネルが0件だと接点が1行も入らない**（`channel_id` は NOT NULL）。
  **実データを入れても森と流入元は全部0である**（C-31）。
  **アプローチのタブに直接効く**
- **`step_history` が 0 行だった。** 滞留と SLA は実データから再現できない

## 6. 公開先について — 混同しない

| 場所 | 何か | 使ってよい範囲 |
|---|---|---|
| `https://ydb-ten.vercel.app` | **デモモード**（C-30） | 見せるためだけ。**認証なし、保存されない、架空データのみ** |
| **Supabase** | **Pilot 本番の DB。** マイグレーション**15件まで**適用済み | **実データはまだ入っていない。** 認証もバックアップも未整備 |
| （未着手） | **Pilot 本番の URL** | `DEPLOY-READINESS.md` の残り6項目が必要 |

デモモードは `VERCEL` を検出すると自動で入る（`YOUTHDB_DEMO=1` で明示も可）。
**この経路に実データを載せてはならない。**

**`.env.local` の接続文字列を絶対にコミットしない。**

## 7. 破ってはいけない規律

原典 `basic/` の設計者は、カラムと制約の1つ1つに「なぜそうしたか」を
日本語で書いている。**コメントは装飾ではなく設計の本体。** それに合わせる。

### UI に効くもの

- **`app/tokens.css` は生成物。手で編集しない**（直すのは `basic/DESIGN.md`）
- **色・字送り・角丸を直値で書かない。** トークンに寄せる
- **可視化コンポーネントに SQL を書かせない。** データ取得は `src/queries/` に集約
- **集計定義をコンポーネント側に書かない**
- **単位の違うものを同じ軸に置かない。並べても割らない**
- **絵は必ず数字と併置する**（星は比で塗り、素点を隣に出す）
- **個体を描くときは個人を漏らさない**
- **画面に内部の識別子（関数名・ビュー名・原則番号）を出さない**（C-32・C-37）
- **画面に出した数字の単位と母集団を、注記として画面上に置く**
- **表示の丸めで、注記が画面上の嘘にならないようにする**
- **`'use client'` を増やす判断は単独で示す。いまは0件**
- **押しても何も起きないものを、押せるように見せない**
- **頻度の低い操作を一番目立つ場所に置かない**

### 全体に効くもの

- **`basic/*.sql` は変更しない。** 実装は `db/migrations/`。
  **`basic/DESIGN.md` は例外**（トークンの生成元）
- **適用済みマイグレーションは編集しない。** チェックサムが変わってランナーが止まる
- **理由なき差分を作らない。** `db/DECISIONS.md` に
  「発見の経緯・なぜそう直したか・どのテストで固定したか」を書く。
  テスト参照が実在するかは `tests/10_decisions_references.test.ts` が機械的に検査する
- **書き込みの判定を `app/` に書かない。** 事実の確認は `src/commands/` に置く（C-20）
- **操作できる母集団を、画面に出す述語と揃える**
- **氏名や id を URL に入れない**（判定の結果。利用者自身の検索語は別）
- **種別が排他なら、枝を並べずに優先順位で決める**（A-18 / C-23）
- **種別ごとに条件を足したら、全体の性質でテストする**
- **止まる前に、その判断が本当に必要かを確かめる**（C-21）
- **日付は必ず `jst_date()` を通す。** 今日は `jst_today()`
- **追記専用の表は追記のみ。** 訂正は打ち消し行の追記で表現する
- **集計マスタは追加と非活性化で運用する**
- **実データ・実在の個人情報をデモ・スクリーンショット・報告書・
  コミットメッセージに使わない。接続文字列もコミットしない**
- **報告書は凍結、記録は更新。** 版は `REPORT-<実行番号>.<連番>.md`
- **シミュレーションで得た「Go」を Pilot Go と呼ばない**

優先順位は常にこう。**意匠の改修であっても変わらない。**

```
記録の構造（何を事実として残すか）
  > 集計の定義（その事実をどう数えるか）
    > 画面（数えた結果をどう見せるか）
```

## 8. 壊しやすいもの

**踏んではいけない地雷が1つある。** `v_forest_season_activity` と
`v_open_tasks` は、**絞り込みを高い供給元まで降ろすと計算量が爆発する。**
`MATERIALIZED` と `ss.season_id` はそのための指定で、外すと **9000倍**
遅くなる（A-16）。`tests/15` が定義そのものを見張り、`tests/24` が
絞り込みの有無を両方測る。

- **`v_person_season_state` は `persons × seasons` の直積**
- **`v_scoring_facts` も `persons × seasons` の直積**（実行⑨で追加）。
  **性能を測っていない。** 人が増えれば効く
- **集合を返す関数を結合の内側に直接置かない**（実行③。一覧が444秒かかった）
- **性能の比較で `LIMIT` だけを付けない**（実行④。438.9秒が3ミリ秒に見えた）

## 9. 進め方と、これまでに踏んだ失敗

```
1. 動く状態にする
2. 実データで動かして計測する
3. 見つけた欠陥を先にテストへ書き、落ちることを確認する
4. 直す
5. 理由を DECISIONS.md に記録する
6. コミットする
```

**サイクルの回し方**（`process.md`）—— 1機能を終えたら次をすぐ実装しない。
PM Report → Validation Report → 残課題を4分類 → 次を推奨する。
**優先順位の最大値ではなく、実装量あたりの学習量が最大のものを選ぶ。**

**サイクル開始時に必ず答える** ――
「この改善は実際の運営で観測された問題か、開発中に想像した問題か」。

**繰り返さないこと。**

- **修正が別の場所を壊す。** 実行⑨で3回踏んだ（ビューの登録簿・原典の制約テスト・
  デモの年度を減らしたら経路が2つ消えた）
- **偶然にできていた経路は、条件が変わると黙って消える**（実行⑨で発見）。
  **踏ませたい形は明示的に置く**
- **記録が実装より厳密に見える。記録が無いより悪い**
- **デモデータが検証したい経路を踏んでいない。同じ見落としを5回繰り返した**
- **目視は検証ではない。** DOM か SQL の値で確かめる。
  **「見た目が良くなった」は検証ではない**
- **作り物のデータから出た数字をドキュメントに残さない**
- **単位の違うものを割らない**
- **表示の丸めで、注記が画面上の嘘になる**
- **丸め込みが、欠陥そのものを隠す**
- **`LEFT JOIN` の `ON` に絞り込みを書いても、行は落ちない**（A-17）
- **同じ言葉を2つの定義で使わない**
- **「数えるか」と「動いているか」を同じ述語で扱わない**
- **内部の識別子を画面に出さない**（C-32 → C-37）
- **架空データのシミュレーションは、実運用の観測の代わりにならない**

## 10. 技術構成

- PostgreSQL 15+ 想定。開発とテストは **PGlite**（WASM 版 PostgreSQL 18）。
  **本番は Supabase**（PostgreSQL 17.6、Session pooler 経由）
- `src/db/client.ts` の `Db` は `query`/`exec`/`close` の3つだけ。
  **`DATABASE_URL` があれば `src/db/postgres.ts`**、無ければ PGlite（`.pgdata`）
- **`isDemoMode()`**（`src/db/server.ts`）。`YOUTHDB_DEMO` が最優先、
  無ければ `VERCEL` を見る
- Next.js App Router（**サーバコンポーネントのみ、`'use client'` が0件**）
- テストは `node:test`。フレームワーク無し
- Tailwind も shadcn も認証も lint 設定も持たない。**不足ではなく設計判断**
  （ただし**認証は、公開する時点で前提が変わる**）
- デザイントークンは `basic/DESIGN.md` の frontmatter から
  `app/tokens.css` を生成（`pnpm tokens`）

## 11. 未決の一覧

### `domain.md` 10節

| # | 内容 | 扱い |
|---|---|---|
| 10-1 | `partner_relations` と Relationship の**語が衝突する** | **未決** |
| 10-2 | **Task の定義が無い** | 記録層を作らず**導出**にした（C-17） |
| 10-3 | **Interested（芽）の判定事実** | **触っていない** |
| 10-4 | **Forest / Community の実体** | `partners` に親を1本足した（C-16） |
| 10-5 | **Membership の粒度** | **触っていない** |
| 10-6 | 実装段階を憲法で上書きするか | Health は作らず事実の旗で代えた（C-18） |
| 10-7 | `director.md` の IA に重複 | **実行⑨の改定で解消**（IA が3タブになった） |

### 以前からの未確定

- **D-8 `seasons` の実数値。** 2026年度は確定。**2025年度は未受領**
- **D-10 個人情報削除を受けた人の応募を、木に数え続けるか。** **未決のまま**
- **D-11 段の語（森・林・木・幹）が旧定義のまま。** **未決のまま**
- **D-14 書類選考の軸は凍結。流入チャネルは廃止ではなく未実施**
- ~~トークンの生成元~~ **実行⑨で決着（C-39）**

**推測で埋めた事実は1つも無い。**

## 12. リポジトリの外にあるもの

- `<repo>/../academy-ui-assets/` … **UI 素材ライブラリ**（実行②で作成、約10MB）
- `<repo>/.pgdata/` … PGlite のデータ。`pnpm db:reset` で作り直せる
- `<repo>/.env.local` … **`DATABASE_URL`（Supabase）。絶対にコミットしない**
- `<repo>/db/private/legacy-youthdb-2026-08-07/` … **実データの受け入れ口**
- ルート直下の **`.pptx` 2件** は**追跡下に無い。** 扱いは未決

## 13. 版の規約と完了の定義

- 報告書は `REPORT-<実行番号>.<連番>.md`。連番は0から。
  **実行⑩の第1報は `REPORT-10.0.md`**
- **報告書は書いたら凍結する。** 追記が要るときは新しい連番を作る
- **記録（`db/DECISIONS.md`）は更新する**
- **この `HANDOFF.md` も更新する。** 版は切らない
- **コミットは origin と mirror の両方へ push する**

完了の定義。

- [ ] `pnpm test` が全通過する（新しい判断にはテストが1つ以上ある）
- [ ] `pnpm exec tsc --noEmit` がクリーン
- [ ] `pnpm build` が通り、`pnpm tokens` で `app/tokens.css` に差分が出ない
- [ ] 実データで動かし、**DOM か SQL の値で**結果を確認した
- [ ] 画面に出した数字の**単位と母集団**が、注記として画面上にある
- [ ] **画面に内部の識別子が出ていない**（`grep` で検査した）
- [ ] `db/DECISIONS.md` に理由とテスト参照を書いた
- [ ] `TODO(MVP)` の一覧を出した
- [ ] **PM Report と Validation Report を出した**
- [ ] **`director.md` の自己チェック4項目への答えを添えた**
- [ ] **未決を勝手に寄せていない**（判断が要るものは判断を仰いだ）
- [ ] **Pilot Go を主張していない**
- [ ] **シミュレーションの「Go」と Pilot Go を混同していない**
- [ ] **成果を「Validated」と書いていない**（運営が触っていない）

---
