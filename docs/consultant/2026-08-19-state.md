# 現況と残件 —— 2026-08-19 時点の締め

この回はここで止めた。**続きを引くための記録**であって、報告書ではない。
実行⑯・⑰の報告書が無いこと（欠番45件の原因）と同じ穴を、この回で開けないために置く。

## 実測

```
テスト        887 / 887 合格（205 スイート・176 秒）・失敗 0
構成基準      11 / 12 ✔   ―― S9 のみ ✘
索引          最新（220 件）※ C-46 / C-116 の番号重複を索引の先頭に掲示
コミット      17 件（push 未実施）
ブランチ      fix/quality-gate-false-pass
```

構成基準の内訳 ―― ✔ S4 危険操作の deny（29件）／S5 調査とレビューの分離／
S6 実データがリポジトリ外／S7 検査を緩める記述なし／S8 テストが実際に走る（84ファイル）／
S10 docs/ の分類／S11 基準文書と実装の一致（13件）／S12 凍結文書の但し書き／
C0x コンサル診断 100/100・所見なし。

## 残件 —— 4件

### 1. S9 ✘ 履歴の API キー（**人間しか閉じられない**）

`.env.localecho` が git 履歴に在る。作業ツリーには無い。
指紋照合で、履歴の鍵は**現在使用中の鍵と同一**（`b952cd9dfda8603b`）。

閉じ方は**鍵の失効・再発行**である。履歴の書き換えは要らない。
Anthropic のコンソールで現行キーを失効し、新しいキーを画面から登録し直す
（`app/ai` の APIキー登録。暗号化保存 = C-195）。

★ 検査の側を緩めて通してはならない（`.consultant/STRUCTURE.md`）。
詳細は `docs/audit/2026-08-19-secret-in-history.md`。

### 2. push 未実施 —— 17 件が手元だけに在る

```
git push -u origin fix/quality-gate-false-pass
```

`.claude/settings.json` の deny により AI 側からは実行しない（S4 の必須項目）。
**人間が叩く。** 叩かない限り、この 17 件はこの1台の中にしか無い。

### 3. 欠番 45 件 —— 未記入（機能への影響なし）

`C-165`〜`C-212` の 44 件と `D-30`。コードが番号で参照しているのに
`db/DECISIONS.md` に見出しが無い。原因は実行⑯・⑰が判断記録を残さず終わったこと。

下ごしらえは機械で出せる ―― `pnpm decisions:missing`。
参照元のコード行が番号ごとに並ぶだけで、**中身は作らない**
（`CLAUDE.md`「記録にない値の創作」の禁止）。

★ 手がかりはコード注釈に残っている。例 ―― `C-176` は
`app/_styles/01-foundation.css:42` が「題名は画面から隠している」と書いている。
起こすなら出典（ファイル:行）を各項目に付け、人間が確認してから本文へ入れる。

### 4. 必須チェックのサーバ側強制 —— **設定不能**（是正案ではない）

```
gh api repos/NEO-AX/YDB/branches/main/protection → 403 Upgrade to GitHub Pro
gh api repos/NEO-AX/YDB/rulesets                 → 403 同上
```

private + 現プランでは、ブランチ保護も ruleset も使えない。
`quality.yml` を required status check にする道はこのプランに存在しない。

★★ **これを「未着手」として持ち回らない。** ★★
12 回にわたり「設定せよ」と指摘され続けたが、**誰にも実行できない是正だった。**
取れる道は3つで、どれも人間の判断である ――

1. プランを上げる（GitHub Team 等）―― 保護と ruleset が使えるようになる
2. 限定事項として引き受ける ―― 人間が push 前に CI を見る
3. 公開にする ―― **採らない**（現役の鍵が履歴に在る。残件1が先）

選んで記録すれば、この所見は閉じる。記録するまでは13回目の指摘が来る。

## 検査範囲の外にあるもの

機械検査が見るのは、危険操作の遮断・調査とレビューの分離・実データの外置き・
テストが実際に走ること・文書と実装の一致など、**確かめられる範囲だけ**である。
11/12 が通ったことは「AI開発に適した構成である」ことを意味しない。

範囲外で分かっているもの ―― 上の残件3・4に加えて:

- `db/DECISIONS.md` が 8,500 行 1 本。索引（`DECISIONS-INDEX.md`）は作ったが、
  ファイルそのものは分割していない。
- `docs/reports/` が実行⑮で止まっている。⑯・⑰の報告書は無い（欠番の原因そのもの）。

## この回の作業順の誤り

欠番45件の参照元142箇所を読み終えた**後で**、優先順位を確認した。
残件1（稼働中の鍵）が開いたままの状態で、機能に影響しない記録の欠落へ先に手を付けた。
順番は 1 → 2 → 4 → 3 である。

## 追記 —— PII 走査が自分の報告書で落ちた（未処理・要判断）

この記録をコミットしようとして、`.audit/reports/*.json`（5件・未追跡）が
PII 走査に Critical 20 件で止められた。

**中身を見た結果、実データではない。** 当たっているのは走査自身の所見文である ――

```
"fact": "`docs/audit-2026-08-19-remediation.md` に BIRTHDATE が 2 件（…）"
                                                   ^^^^^^^^^ ここに当たっている
```

生年月日そのものは無く、**「BIRTHDATE」という語**が引っかかっている。
走査が自分の報告書を読んで、自分の検出ラベルを個人情報として数えている。

★ **allowlist に登録していない。** 走査の側が
「人間の承認のもとで allowlist に限定して登録すること」と言っているため、
AI 側で通さない。`--no-verify` も使わない。

★ このコミットは**記録の本文だけ**にした（報告書 5 件は未追跡のまま残してある）。
先行する報告書は追跡されているので、この5件も本来は入るべきものである。

判断が要ること ―― ① この5件を allowlist へ入れて追跡に戻すか、
② 走査が自分の報告書を対象から外すようにするか（`.audit/reports/` を重点パスから除く）。
②のほうが根が浅い ―― 同じ誤検知は報告書を出すたびに再発する。
