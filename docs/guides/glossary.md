# 用語（`Hitler.md` §3「用語は1箇所に集約し、表記揺れを作らない」）

**正典は `domain.md`**（封印対象。記録モデルと現行語彙）。本書は**引き当てるための1枚**である。

```
pnpm decisions:show C-45
```

## 現行語と、コードに残る旧語

旧生態系比喩は現行UIの語ではない。コードで出会ったら下の対応で読む。
出典と経緯は [`docs/product/legacy-terms.md`](../product/legacy-terms.md)（D-3 / `db/migrations/0002_views.sql` / `src/queries/`）。

| 旧語 | 現行 |
|---|---|
| 森 | アプローチ可能圏／構成コミュニティ |
| 林 | 接点継続中 |
| 木 | 応募 |
| 幹 | 合格 |

「種」は比喩ではない（「種類」等の誤検出だった）。

## 数える単位

- 数えるのは**期**である。**学年という概念を持たない**（`Hitler.md` §0 の非目的）。
- 単位と母集団は画面に書かない（C-62）。定義は `src/queries/` のコメントと `db/DECISIONS.md`。
