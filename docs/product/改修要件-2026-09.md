# 改修要件 — 2026-09 MTG 由来（正本）

出典: 2026-09-09 の改修MTG文字起こし。工数は Codex による read-only 実測（推測ではない）。
**この表がループの入力。** プランJSON（`templates/改修プラン.json` 型）はここからしか起こさない。

| # | 要件 | 現状 | 工数(分) | 根拠 |
|---|---|---|---|---|
| R1 | 候補者CSV一括登録（**ブラウザ**アップロードまで） | 一部（CLI が既存バグで失敗） | 240 | `scripts/ingest-sheet.ts:47,98`（存在しない `staffs.name` 参照）／再利用元 `app/headhunting/actions.ts:13` |
| R2 | 対象外(F)＋候補者数の算出＋S/A/B/C内訳 | 無 | 55 (30+25) | `db/migrations/0039_confidence_grade.sql:29`／`src/queries/dashboard.ts:187` |
| R3 | 個人ページの参加イベント履歴 | 一部（接点はあるがイベント名を引いていない） | 35 | `src/queries/drilldown.ts:314` |
| R4 | イベント一括登録＋対応者リスト | 一部（**対応者が1人しか持てない** `owner_staff_id`） | 80 | `db/migrations/0019_appointments.sql:61` |
| R5 | 1〜4評価シート＋チェックリスト | 一部（1〜4点評価は在る／チェックリストが無い） | 95 | `db/migrations/0001_schema.sql:218` ／**チェック項目本文と閾値の回答待ちで保留** |
| R6 | 面接シート | **実装済** | 0 | `tests/35_interview_sheet.test.ts:75` 合格17件・不合格0件で実証 |
| R7 | KPI目標・実績連動＋**アワード参加人数連携**＋マイルストーン | 一部 | 165 (90+75) | `db/migrations/0047_kpis.sql:2`／`app/kpis/page.tsx:31`。`72_kpi.test.ts` 合格2件・`72_kpi_metric.test.ts` 合格5件 |
| R8 | AI自然言語クエリ（**自由記述**まで） | 一部（固定SELECT8種は動く／自由記述は返せない） | 120 | `src/ai/ask.ts:5`／`app/ai/page.tsx:112`。`73_ask_tiers` 合格4件・`70_ai_pre_assessment` 合格19件 |
| R9 | 団体プロフィール（紹介文・HP・ロゴ） | 一部（ロゴ転用可の列のみ） | 60 | `db/migrations/0026_partner_photos.sql:1` |
| | **合計** | | **850（約14.2時間）** | 実装のみの実時間。統制・レビュー・ゲート込みは別 |

## 議事録との認識のずれ（実測で判明した4件）

1. **R7 で無いのはマイルストーンだけ、ではない。** 目標と実績の連動は動くが、KPI が自動カウントできるのは9種（`candidates` `applicants` `accepted` `touchpoints` `partners` `special` `confidence_s/a/b`）で、**アワード来場者数は数えられない**。`touchpoints` は全接点を数え、イベント参加者を人数として区別・重複排除しない。利用者の「連携できていなかった」は事実。
2. **R2 は列を1つ足す話ではない。** 候補者数・KPIの候補者数・確度順一覧・AI集計の**4か所が別々に数えている**。定義を1本に寄せるのが中身。
3. **R4 の本体は一括登録ではなく「対応者が1人しか持てない」構造。** 多対多テーブルの新設が要る＝R4が最も重い理由。
4. **R6 は実装済み**（工数0・テストで実証）。R8 は「Sランクは何人」は動くが「特徴的な人はいるか」は返せない。

## Fの置き場所（設計の確定事項）

**F は確度（`confidence_grades`）ではなく、アプローチ状態（`approach_states`）側。**
確度側に置くと「確度」と「対象可否」の二重管理になる。
「候補者数 = 全体数 − 対象外」は既存の `v_person_approach_state.is_terminal` で算出する。

## 3期の実データ（2026-09-09・本番DB実測）

3期に紐づく人 **498**／内訳: 「そもそもこの期の人ではない」印 **247**・「追わない」**12**・追える **73**。
応募0・評価0・面接シート0・イベント参加記録0。
**247人は全員が2期にも記録があり、2期で応募した人は0人。印は2026年8月に247件すべて一括で付いた。**
「追える73人」にも2期の人が19人。3期だけの人は **220**、うち追えるのは **54**。
**追わない扱いなのに確度(S/A/B/C)が付いている人が33人**（not_in_season 32・declined 1）。

## フェーズ（850分の割り方）

- **第1（180分）**: R2(55) → R7アワード連携(90) → R3(35) — ダッシュボードの数字と「連携できていない」箇所を先に潰す
- **第2（320分）**: R1 ブラウザCSV(240) → R4のうち対応者リスト(80)
- **第3（350分）**: R8自由記述(120) → R5(95・回答待ち) → R9(60) → R4残り／R7マイルストーン(75)

**接続**: 本番DBは `.env.local` の `DATABASE_URL` を Claude 直轄で読む（Codex は DNS 遮断で DB を見られない）。
**テスト前提**: `npm test` は 901件中894成功・7失敗（既存の失敗）。**AC は全体テストではなく該当テストファイル単位で書く。**
