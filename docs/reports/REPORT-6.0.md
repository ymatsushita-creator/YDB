# REPORT-6.0 — 実行⑥ 運転席を作り、森を実体にする

2026-08-07。MVP モード。
**この報告書は凍結する。** 追記が要るときは `REPORT-6.1.md` を作る。
記録（`db/DECISIONS.md`）だけは更新し続ける。

---

## 0. 一行で

**採用担当者が今日使える画面を1枚作った。**
そのために、憲法が要求する「森」を記録層の実体にし、
「いまやること」を既存の事実からの導出として定義した。

コードを書く前と後で、こう変わった。

| | 実行⑤まで | 実行⑥ |
|---|---|---|
| 森の実体 | 無い（`partners` はフラットな表） | ある（`partners` に親を1本。親なし=森、親あり=林） |
| 「次に何をすべきか」 | **仕組みが無い**（CLAUDE.md 8節が④の穴と明記） | `v_open_tasks` が4種のやることを出す |
| 運転者の入口 | ファネル（集計の画面） | `/cockpit`（やることの画面） |
| テスト | 149 | 187 |

---

## 1. 着手条件（`director.md` の4項目）

憲法はこの4つを**着手条件**と定めている。実装前に答えた内容を、
実装後の結果と併せて残す。

**1. 世界観は壊れていないか？**

壊していない。`domain.md` 11節が定義し直した森・林・木の語は、実装では
まだ旧定義（D-2: 林は人、木と幹は応募）のまま動いている。ここで新しい
意味を既存のビュー名に混ぜると、実行③で踏んだ「同じ言葉が2つの定義を持つ」
の再演になる。そこで 0012 は**既存の 林/木/幹 のビューを1つも触らず、
Forest / Community という英語の語で新しい実体を作った。**
字が違うので、同じ語が2つの意味を持つ形にはなっていない（C-16、D-11）。

**2. 森が主役になっているか？**

なっている。ただし**画面を先に作っていない。** 憲法自身がこう書いている ――
「森が主役に見える画面を、森の実体が無いまま作ってはならない。その場合に
直すのは画面ではなく、記録層である」。順序どおり、記録層（0012）→
集計（`src/queries/cockpit.ts`）→ 画面の順で作った。

**3. ユーザーは次に何をすべきか一目で分かるか？**

分かる。`/cockpit` の第1ブロックが「いま何をすべきか」で、種別・対象者・
担当・待ち日数・期限が1行に並ぶ。期限を超えたものが先頭に来る。

**4. このUIは「管理画面」ではなく「コックピット」になっているか？**

なっている、と言い切るには留保が要る。**表を使っている。**
`director.md` は「テーブルを作らない」と書いている。この点は3節で説明する。

---

## 2. 作ったもの

### 記録層 — `db/migrations/0012_forest_and_tasks.sql`

| 追加したもの | 何か |
|---|---|
| `partners.parent_partner_id` | 親の団体。NULL なら森、値があれば林 |
| `partners_two_levels_only`（トリガ） | 3段目を拒否する。`CHECK` は他の行を見られない |
| `v_partner_forest` | 団体 → 属する森。**森の集計は必ずここを通す** |
| `v_forests` / `v_communities` | 森（親なし）と林（親あり） |
| `v_open_tasks` | いまやること4種。**既存の事実からの導出** |
| `v_forest_activity` | 森の活動（年度を問わない） |
| `v_forest_season_activity` | 森×年度（応募・合格・やること） |

`partners` を育てた理由は、既存のぶら下がりを動かさずに済むからである。
`partner_reaches` / `partner_relations` / `touchpoints` はいずれも
`partner_id` を指しており、どちらの段に付いていても `v_partner_forest` が
森へ畳む。新テーブルへ移すと3つとも張り替えになる（C-16）。

### 集計 — `src/queries/cockpit.ts`

`getOpenTasks` / `getTaskTotals` / `getWaitingPersons` / `getForests` /
`getForest` / `getCommunities` / `getForestPersons`。
集計の定義はビュー側に置き、ここは呼んで並べ替えるだけに留めた。

### 画面 — 2枚

- **`/cockpit`** — 4つの問いを4枚のタイルにそのまま置き、
  やること・要注意の森・待っている人を出す
- **`/forests/[id]`** — 森を1つ開く。森 → 林 → 人 のズームの真ん中の段

既存の6枚（`/`・`/people`・`/people/[id]`・`/applications/[id]`・
`/operations`・`/sources`）は**1行も変えていない。** 入口の順序だけ、
`app/layout.tsx` のナビで運転席を先頭にした。

---

## 3. 憲法との食い違い（勝手に寄せていない）

`director.md` は「一覧を増やさない」「テーブルを作らない」「一覧→詳細の
遷移は禁止」と書いている。`/cockpit` は表を3つ持っている。

**寄せずに、判断の根拠を残す。**

- ここに出ている表は**在庫の一覧ではなく、やることの列である。**
  行が0件になれば表そのものが消える。母集団は
  `v_active_applications`（いま誰かが判断すべき応募）なので、
  動いていないものは並ばない。「Applicant List を主役にしない」
  （`design.md`）には反していないと判断した
- 遷移は一覧→詳細ではなく、**森 → 林 → 人 → 応募**のズームになっている。
  コックピットの森の行から `/forests/[id]` に入り、そこから人へ降りる
- ただし**カード表現ではなく表である**ことは事実で、`design.md` が挙げる
  Forest Map / Health Ring / Relationship Map は作っていない。
  Health は記録層に事実が無く（実装段階[3]、原典は着手判断を1年後と明記）、
  Relationship も実体が無い（`domain.md` 10-1 が未決）

**この食い違いは残っている。** 指示を仰ぐ項目として8節に挙げる。

---

## 4. 動かして見つけた欠陥

MVP モードでも、この往復だけは変えていない。

### 4-1. 年度を1つ指定すると 9000 倍遅くなった（A-16）★影響大

画面から呼んだら**開発サーバが応答しなくなった。** CPU を 64% 使い続けて
おり、止まっているのではなく回り続けていた。

| 測り方 | 実測 |
|---|---|
| 全件（`WHERE` なし） | 19 ミリ秒 |
| 年度を1つ指定 | 177,694 ミリ秒 |

**書いた直後に測ってはいた。** 全件が 19 ミリ秒だったので速いと判断した。
その測り方が間違っていた。

原因は述語のプッシュダウンである。`season_id = X` が CTE の内側へ降りると、
プランナが入れ子ループを選び、その内側に居る再帰 CTE
（`v_effective_status_histories`）が外側の行ごとに回り直す。

**A-11 と向きが逆である。** あちらは「氏名で絞ると速いので、手元で軽く
触るだけでは気づけない」だった。だから実行③以降「一覧は必ず素の状態
（絞り込みなし）で開いて確かめる」を手順にしていた。
今回は**その手順どおりに測って見逃した。** 片側だけを測る限り、
どちらの向きも取り逃す。**両方で測る。**

対応は2つ。供給元の CTE を `MATERIALIZED` にしてプッシュダウンを禁じ
（177 秒 → 0.15 秒）、`v_open_tasks` の年度は応募ではなく選考ステップから
取るようにした（0.413 秒 → 0.076 秒）。
`v_open_tasks` を丸ごと `MATERIALIZED` で押さえる案も測ったが、
絞り込みの有無によらず 1.05 秒になったので捨てた。
**採らなかった案も測ってから捨てる。**

固定の仕方について。**実行時間はテストに書いていない。** データ量と
マシンに依存する数字は追試できないし、閾値は環境が変われば嘘になる。
残したのは**プランが崩れない形のほう**で、ビュー定義そのものを見張っている。

### 4-2. 削除済みの除外が、1文字も効いていなかった（A-17）

`LEFT JOIN persons p ON p.id = t.person_id AND p.deleted_at IS NULL` と
書いた。**`LEFT JOIN` は行を落とさない。** 削除を受けた人の接点は `p` を
NULL にしたまま残り、`count(DISTINCT t.person_id)` がそれを数えていた。

同じ絞り込みを `v_forest_activity` では内部結合で書いており、そちらは
正しかった。**同じ意図が2箇所で違う書き方になり、片方だけ壊れていた。**
書いてすぐテストで落ちたので画面には出ていないが、A 節に記録した。
削除済みの除外は運用の画面すべてにかかる規律で（A-14）、
書き方を1つ間違えるだけで静かに破れるからである。

### 4-3. 自分で `.pgdata` を2回壊した

原因はどちらも自分の操作である。1度目は応答しなくなった開発サーバを
`kill` したとき、2度目は `.pgdata` を触っているプロセスを `pkill` したとき。
PGlite の dataDir にはプロセス間ロックが無い（CLAUDE.md 9節）。

**計測は使い捨てのインメモリ DB で行うべきだった。** `.pgdata` を触ると、
開発サーバと衝突するか、途中で止めたときに壊れるかのどちらかになる。
以降は `freshDb()` + `seedDemo()` で測った。壊れても捨てるだけで済む。

---

## 5. デモデータが踏んでいなかった経路

「デモデータが検証したい経路を踏んでいない」を4回繰り返している。
**5回目にしないため**、実装したものが実際に踏まれているかを機械に確かめさせた。
結果、3つが踏まれていなかった。

| 踏まれていなかった形 | どう置いたか |
|---|---|
| 判断前の利益相反（`reassign`） | 乱数で生まれた利益相反3件は**すべて `submitted`** だった。紹介者がそのまま面接官の登場人物を置いた |
| 1人が複数のやることを持つ | 選考は順番に進むので、常に1人1件だった。**件と人が常に一致するデータでは、画面が混同していても気づけない。** 二次面接に面接官を2人置いた |
| 林に付いた接点 | 団体は8つともフラットだった。森9・林4の階層に組み直し、接点が両方に付くようにした |

`reassign` が踏まれていないということは、**`evaluate` 側で利益相反を除いて
いる条件も一度も実行されていなかった**ということである。片方の分岐が
死んでいると、対になっている条件も一緒に死ぬ。

要注意の3つの形（接点なし・休眠・滞留）も、確率ではなく明示的に置いた。
これらの存在は `tests/13` が検査する。

---

## 6. 確かめたこと

**目視ではなく DOM と SQL の値で確かめた。**

`/cockpit`（2027年度）の画面上の数と、同じ条件で SQL に問い合わせた数。

| 画面に出ている数 | DOM | SQL |
|---|---|---|
| いま何をすべきか（件） | 27 | 27 |
| 誰が待っているか（人） | 26 | 26 |
| 何が止まっているか（件） | 3 | 3 |
| 森の総数 | 9 | 9 |
| 旗が立った森 | 3 | 3 |
| 柏木 朔 のやること（件） | 2 | 2 |
| 大学連携コンソーシアムの滞留 | 2 | 2 |

**件（27）と人（26）が一致していない。** 1人が2件持っているためで、
これが5節で置いた形が効いていることの確認でもある。

森 → 林 → 人 の畳み込みも確認した。大学連携コンソーシアムは
接点のある人 67 人で、その内訳は高校生探究部会 23・起業サークル連合 24・
森に直付け 20。林に付いた接点が森に上がっている。

応答時間（`curl`、開発サーバ）——
`/` 0.58 秒 / `/people` 1.39 秒 / `/operations` 0.47 秒 /
`/sources` 0.18 秒 / `/cockpit` 0.73 秒。
**この数字はデモデータの量に依存するので、追試の基準にはならない。**
残す意味があるのは 4-1 の計算量のほうである。

- `pnpm test` 187 件 全通過（実行⑤時点は 149 件）
- `pnpm exec tsc --noEmit` クリーン
- `pnpm tokens` 差分なし

---

## 7. `TODO(MVP)` の一覧

実装に残した印は6つ。全文と根拠は `DECISIONS.md` D-12 にある。

1. 森と林を別テーブルに分けるか（`0012` 冒頭）
2. 手で作るタスク（連絡・催促・資料送付）は表せない（`0012`、`app/cockpit/page.tsx`）
3. やることの種別に優先順位を付けていない（`src/queries/cockpit.ts`）
4. 森と人の結び付きは「接触があった」であって「所属している」ではない（`getForestPersons`）
5. Health / Relationship Strength / Owner は未実装（`getForests`、森の画面）
6. 森の画面の「接点がある人」は 60 人で打ち切り（画面にも注記済み）

**推測で埋めた事実は1つも無い。** 埋めなかったものは上に全部挙げてある。

---

## 8. 指示を仰ぎたいこと

### 8-1. 表を使っていること（3節）

`director.md` は「テーブルを作らない」と書いているが、`/cockpit` は表を
3つ持っている。やることの列であって在庫の一覧ではない、という判断で
作ったが、**憲法の字面には反している。**
カード表現へ寄せるか、この形でよいかを決めてほしい。

### 8-2. 休眠の日数（60 日）

仮の値である。森との関係が「切れた」と見なすまでの日数は運用で決まる。
引数にしてあるので、決まれば1箇所で変わる。

### 8-3. 以前からの未確定（動かしていない）

- **`seasons` の実数値**（D-8）。本番シードに年度が1行も無い。
  計画を立てる土台そのものが未確定で、優先度が高い
- **個人情報削除を受けた人の応募を、木に数え続けるか**（D-10）。
  過去年度の応募数が動く変更なので触っていない

### 8-4. 段の語の付け替え（D-11）

174 箇所は実行⑥でも触っていない（指示どおり）。
いま旧語（林・木・幹）と新語（Forest / Community）が同じコードベースに
生きている。**字が違うので同じ語の二重定義にはなっていないが、
過渡的な状態であり、放置してよいものではない。**

---

## 9. 次にやるなら

MVP として動いたので、次は**運用して壊れるところを見つける**段になる。

- **手で作るタスク**（TODO(MVP) 2）。「いまやること」に載らない仕事
  ―― 連絡する、催促する、資料を送る ―― は運用の大半を占めるはずである。
  ただし `tasks` テーブルと、それを書く画面（＝クライアント境界の新設）が要る。
  **クライアント境界の判断は単独で示す**（HANDOFF の技術構成）
- **Owner（担当）**。森に担当者が付かないと「誰のボールか」が森の段で答えられない
- `/people` が 1.39 秒かかっている。実行③の 444 秒からは直っているが、
  他の画面の2〜3倍ある
