# REPORT-15.1 — 帯の札を消し、これまでの修正点を点検した

> **【凍結】この文書は書かれた時点の記録で、後から書き換えない。**
> 現行の仕様と食い違う記述があれば、`CLAUDE.md` と `db/DECISIONS.md` が正である。
> 設計判断は番号で引ける —— `pnpm decisions:show C-121`（`db/DECISIONS-INDEX.md` に一覧）。

依頼者の指示 ――「**かてんとかどうでもいいんだよ。消せよ**」、
そして「**これまでの修正点を満たせているかテストを行い、レポートまで書いて**」。

決定は `db/DECISIONS.md` の **C-134**。

---

## 0. 先に言うべきこと ―― **送られた画面は、直す前のコードだった**

送られた画面には、**通し番号（1〜15）と折り返し**があった。
これは C-128（実行⑮の最初）で直したはずのものである。調べた ――

| 見たもの | 記録・コード |
|---|---|
| 「11. リスペクト」の **11.** | 記録の軸名は「リスペクト」。**番号は入っていない**（0002 / 0005） |
| 3行に折り返して天端が切れる | いまのコードは **1行・横送り・縦は中央**（C-128） |
| 字が小さい | いまのコードは **13px**（C-128 で 10px → 13px） |
| 番号が段をまたいで 1〜15 | いまのコードは**番号を作らない**（C-128 で削除） |

`b643378^`（C-128 の1つ前）を開くと、まさにこう書いてある ――

```jsx
{i + 1}. {c.name}          // 通し番号を画面が作っていた
flex-wrap: wrap            // 折り返していた
overflow-y: auto           // 縦へ溢れて天端が切れていた
font-size: 11px            // 小さかった
```

**送られた画面の4点すべてが、この版と一致する。**

★ **直した画面が、依頼者の手元へ届いていなかった。**
C-128 は本日 18:48 のコミットで、**本番へは出していない**（実行⑮は
「本番へ出すのは依頼者の判断」として一度も触っていない）。

```
開発サーバで見ている場合   再起動すれば入る（コードは直っている）
本番で見ている場合         pnpm deploy:production が要る（この環境の vercel は未認証）
```

★ **「直した」と「見えている」は別である。** テストが通っていることは、
依頼者の画面が直っていることの証明にならない。以後、見た目の指示に答えるときは
**どこに出せば見えるのか**まで一緒に言う。

---

## 1. 札を消した（C-134）

```
消したもの   帯の札（必須／加点）と、その CSS 規則（.hh-criteria-tag*）
残したもの   記録層の重み付け（evaluation_criteria.kind。0033）と、
             それを読むクエリ ―― **消したのは出し方であって、事実ではない**
```

★ **「必須」も一緒に消した。** 加点だけ消すと9軸のうち3本にだけ札が残り、
「これは何の印か」を新たに問わせる。指示は「どうでもいい」であって、
片側を強調してほしいという話ではないと読んだ。**戻すのはいつでもできる**
（重み付けは記録層にそのまま残っている）。

★ 使わない CSS 規則も消した ―― 残すと、次に触る人が「まだあるもの」として扱う。

---

## 2. 点検 ―― 帯が指示どおりか（同じ CSS・同じ15軸で測った）

画面は認証の内側にあるので、**同じ CSS と実際の15軸で帯だけを再現した頁**を
作って測った（C-128 と同じやり方）。**実データの画面は見ていない。**

| 依頼者の指示 | 測った結果 |
|---|---|
| 真ん中配置 | 縦の溢れ **0**（帯 67px に対し軸 16px、中央） |
| 文字大きめ | **13px** |
| 横スクロール | 送れる幅 **2012px** ／ 見えている幅 **742px**、1行 |
| 全部入る | 軸 **15本**、右端まで送ると**最後の軸が右端に届く** |
| 番号を作らない | 番号 **0**（名前だけ） |
| 札を消す | 札 **0** ―― 札があった頃より帯は **359px 短くなった** |
| 段の境目 | 区切り線 **1px**（特別選考9本 ／ 最終面接6本） |

---

## 3. 点検 ―― 実行⑮で直した5件が、いまも満たされているか

`pnpm test` を通した（**638件 全通過**）。各修正を見張っているテストは以下。

| 修正 | 見張り | 結果 |
|---|---|---|
| C-128 帯（1行・横送り・中央・13px・番号なし） | `tests/45_home.test.ts` | ✅ |
| C-129 打ち消しの道が画面まで通っているか | `tests/58_corrections.test.ts` | ✅ |
| C-130 別の人・別の団体・別の期を打ち消せない | `tests/58_corrections.test.ts` | ✅ |
| C-131 訂正すると正しい値が現れる（元の行は残る） | `tests/58_corrections.test.ts` | ✅ |
| C-132 置いた順が現在に出る（同着で負けない） | `tests/58_corrections.test.ts` | ✅ |
| C-133 点の訂正は確定前だけ・版は追記専用 | `tests/20_score.test.ts` | ✅ |
| C-134 帯に札を出さない／重み付けは記録層に残る | `tests/45_home.test.ts` | ✅ |
| 重み付け（必須3・加点6・段分け無し6） | `tests/48_criteria_weighting.test.ts` | ✅ |

★ ただし上の表が言えるのは「**このコードでは満たされている**」までである。
**依頼者の画面がどの版で動いているかは、こちらからは見えない**（0節）。

## 完了確認

```
pnpm test               638件 全通過
pnpm exec tsc --noEmit  クリーン
pnpm build              クリーン
本番                    **触っていない。** 0036 / 0037 も未適用のまま
```

## 次にやること（依頼者の判断が要る）

1. **画面を新しくする** ―― 開発サーバなら再起動、本番なら `pnpm deploy:production`
   （この環境の `vercel` は未認証なので、**依頼者の手元で打つ**形になる）
2. 0036 / 0037 を本番へ流すか（`pnpm db:migrate:production`）
3. GitHub へ push するか（`origin` は公開リモート）
