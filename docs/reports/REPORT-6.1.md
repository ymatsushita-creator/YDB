# REPORT-6.1 — 森のマップと、開発の回し方の受領

2026-08-07。`REPORT-6.0.md` は凍結済みのため、続きをここに書く。
**この報告書も書いた時点で凍結する。**

---

## 0. この版でやったこと

1. `process.md` を新設（MVP モードの回し方。受領した2文書の全文）
2. `design.md` を受領版に差し替え
3. **機能を1つ**: `/cockpit` の森を、表から**マップ**に置き換えた

`process.md` の Development Cycle は「Choose exactly ONE feature」と定めている。
文書の受け入れを済ませたあと、実装は1つに絞った。

---

## 1. 文書

### `process.md`（新設）

序列は `CLAUDE.md` の下に付けた。**何を作るか**ではなく**どう回すか**を
定める文書なので、Engineering の下という位置になる。

```
vision.md > director.md > domain.md > design.md > CLAUDE.md > process.md
```

**上位文書と衝突する3点は、消さずに末尾の付記に残した。**
`director.md` が「食い違いを勝手にどちらかへ寄せてはならない」と定めているため。

| 衝突 | 扱い |
|---|---|
| 「Documentation follows implementation」と、記録の規律 | 両立する読み方を採った。**文書が実装を先回りして縛らない**が、**実装した結果は必ず記録に残す。** `DECISIONS.md` は仕様書ではなく実装の記録なので順序は逆転しない |
| Forest Health が `process.md` では provisional、`design.md` では画面要素 | **作らない**を継続。3者のうち2者（`process.md`・原典の実装段階）が「まだ」と言っている |
| Relationship も provisional | 同上。接触があった事実で代用（TODO(MVP) 4） |

MVP モードでも緩めないものも明記した ―― 単位の違うものを割らない、
無い記録から数字を作らない、同じ言葉を2つの定義で使わない、
適用済みマイグレーションを編集しない、`basic/` を変更しない。

**「動くものを早く出す」は、記録を雑にする許可ではない。**
早く出す理由は、運用しなければ分からない欠陥を早く踏むためである。

### `design.md`（差し替え）

受領版で置き換えた。**本文は既存のものと同一だった**（`diff` で確認。
差は区切り線1本のみ）。序列とトークン生成の事実は失うと次の人が困るので、
本文の下に「付記（リポジトリ側の事実）」として移した。

---

## 2. 機能 — 森のマップ

### 何を選んだか、なぜか

`REPORT-6.0.md` 8-1 に、未決として1つ挙げていた。

> `/cockpit` は表を3つ持っている。`director.md` は「テーブルを作らない」と
> 書いており、**憲法の字面には反している。** カード表現へ寄せるか、
> この形でよいかを決めてほしい。

**モックアップの提示が、この問いへの回答である。** `process.md` の
Development Cycle でいう Human Review にあたるので、そのフィードバックだけを
実装した（「Implement only the feedback from the review」）。

### 作ったもの

`app/base.css` に `.forest-map` / `.forest-node` を足し、
`app/cockpit/page.tsx` の森の表を区画の並びに置き換えた。

区画1つが出すもの ——

- 森の名前（クリックで `/forests/[id]` へズーム）
- **接点のある人 / 応募 / 合格** の3つ
- 要注意の旗（滞留 N 件 / 接点なし / 休眠 N 日）
- 林の数、最終接触、推定リーチ

並び順は変えていない（期限超過 → 未処理 → 休眠日数）。

### 出さなかったもの

**健康度（Health）を出していない。** モックアップには 76% / 85% / 70% …
と描かれているが、作らなかった。理由は3つある。

1. 同時に受け取った `process.md` の Freeze Core Concepts が
   Forest Health を**暫定扱い**に置いている
2. 原典の実装段階は [3]（スコアリング）で、**原典自身が着手判断を1年後**と書いている
3. **記録層にその事実が無い。** いま % を出せば、根拠のない数字が
   一目で読める場所に座る

画面（モックアップ）と文書が食い違うときは文書が勝つ。加えて
「無い記録から数字を作らない」は憲法でも上書きできない（`director.md` 末尾）。

代わりに、**要注意の理由を旗で名指しする**。「健康度 65%」より
「休眠 934 日」のほうが、次の一手が決まる。

Owner（担当）も同じ理由で出していない。記録層に概念が無い。

### 自己レビューで直したこと

- **同じ日数を2度書いていた。** 「休眠 934 日」の隣に「最終接触 934 日前」が
  並び、2つの事実があるように読めた。旗が最終接触の話をしているときは
  足元に書かない
- **枠と地の色が同じ軸を指していた。** 休眠の森を地の色（灰）だけで示したため、
  並び順では前に居るのに一番弱く見えるという逆転が起きた。
  **枠＝旗が立っているか、地の色＝眠っているか**に分けた

### 単位

推定リーチは3つの数（接点のある人 / 応募 / 合格）と**同じ枠に入れていない。**
接触機会の推定値で、実人数とは単位が違う（`domain.md` 8節）。
足元に単位つきで別に置き、画面下の注記でも**割らない**ことを明示した。

---

## 3. 確かめたこと

DOM から9区画を読み出して照合した（`REPORT-6.0.md` 6節の SQL と同じ値）。

| 森 | 接点のある人 / 応募 / 合格 | 旗 | 枠 |
|---|---|---|---|
| 大学連携コンソーシアム | 18 / 2 / 0 | 滞留 2 件 | あり |
| 地域創生ファンド | 6 / 2 / 0 | — | なし |
| 西部工業高等専門学校 | 0 / 0 / 0 | 接点なし | あり |
| 高校生新聞社 | 0 / 0 / 0 | 休眠 934 日 | あり |
| NPO法人みらい教育 | 12 / 1 / 0 | — | なし |
| ユースセンターまち | 5 / 1 / 0 | — | なし |
| 起業支援センターK | 12 / 1 / 0 | — | なし |
| 市立図書館連携事業 | 6 / 0 / 0 | — | なし |
| 県教育委員会 | 6 / 0 / 0 | — | なし |

旗が立った森は3つで、KPI タイルの「どの森が要注意か 3」と一致する。
**枠が付いているのはその3つだけ**で、並び順の先頭3つと一致する。

- `pnpm test` 187 件 全通過
- `pnpm exec tsc --noEmit` クリーン
- `pnpm tokens` 差分なし（色は `--color-brand-green` と
  `--color-card-tint-mint`。**直値は1つも書いていない**）

---

## 4. Product Review（`process.md` の3問）

**1. 運用者がいまできて、前はできなかったことは何か。**

森を**一目で見比べられる。** 表のときは行を上から読む必要があったが、
区画になったので「枠が付いている3つ」が視線の最初に入る。
森からのズーム（森 → 林 → 人）の入口も、区画そのものになった。

**2. 残っている一番の摩擦は何か。**

**やることを、画面から片付けられない。** 一覧で「評価する」と分かっても、
そこから先は画面の外（別のツール）でやることになる。
書き込みの経路が1本も無いのが、いま一番大きい。

**3. 一番小さくて一番効く次の変更は何か。**

**「担当を決める」だけを画面から実行できるようにすること。**
やること27件のうち10件が `assign` で、しかも運営が1人で完結できる。
評価そのものより判断が軽く、記録層は `evaluations.interviewer_staff_id` を
更新するだけで済む。

ただしこれは**書き込みの新設**であり、`HANDOFF.md` が
「クライアント境界の新設は単独で示す」と定めている判断に触れる。
サーバアクションだけで（`'use client'` を増やさずに）できるかを、
着手前に確かめる必要がある。**これを次の1機能として提案する。**

---

## 5. 残っている `TODO(MVP)`

`REPORT-6.0.md` 7節の6つから増減なし。健康度と Owner は 5 に含まれる。

1. 森と林を別テーブルに分けるか
2. 手で作るタスクは表せない
3. やることの種別に優先順位を付けていない
4. 森と人の結び付きは「接触があった」であって「所属している」ではない
5. Health / Relationship Strength / Owner は未実装
6. 森の画面の「接点がある人」は 60 人で打ち切り（画面に注記済み）
