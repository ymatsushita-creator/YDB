# REPORT-7.3 — 同じシミュレーションをもう一週。直した3件の確認

実行⑦の第4報。2026-08-08。**この文書は凍結する。**
`REPORT-7.2.md`（週1）の続き。

---

## Goal

`REPORT-7.2` で見つけた3件の言葉の食い違いを直したあと、
**同じシミュレーションをもう一週回す。** 依頼どおりの手順である。

```
1. 週1を回す（小規模・大規模）        → REPORT-7.2
2. 見つけた食い違いを直す              → REPORT-7.2 で実施済み
3. 同じシミュレーションを週2として回す → 本書
```

週2は**別の乱数シード**を使う（`WEEK=2` を渡すと、両規模の種が
`+2000` ずれる）。同じ乱数で回しても直した文言の効果は測れない ――
文言は表示だけで結果の数字を変えないので、**新しい乱数で新しい分岐を
踏ませて**、直した箇所が今度は出てこないか、別の食い違いが無いかを見る。

## What Changed（週2の結果）

### 小規模（4人 x 30人・週2）

```
ファネル: 応募受付30 → 書類選考25 → グループ面接18 → 最終面接14
合格 10 / 辞退 5
不変条件: すべて成立
討論で割れた回数: 50 / 84
気づき（想定外の異常）: 0 件
```

### 大規模（10人 x 40人・週2）

```
ファネル: 応募受付40 → 書類選考40 → グループ面接30 → 最終面接18
合格 14 / 辞退 0
不変条件: すべて成立
討論で割れた回数: 65 / 108
気づき（想定外の異常）: 0 件
```

### 週1と週2の比較

| | 週1・小規模 | 週2・小規模 | 週1・大規模 | 週2・大規模 |
|---|---:|---:|---:|---:|
| 応募受付 → 最終面接 | 30 → 13 | 30 → 14 | 40 → 21 | 40 → 18 |
| 合格 | 10 | 10 | 19 | 14 |
| 辞退 | 0 | 5 | 4 | 0 |
| 不変条件 | すべて成立 | すべて成立 | すべて成立 | すべて成立 |
| 気づき（異常） | 0件 | 0件 | 0件 | 0件 |

**乱数を変えても不変条件は崩れず、異常は0件のまま。**
辞退が0件から5件に変わるなど、周ごとの分岐の踏み方は変わっている ――
同じ道だけを繰り返していない（`tests/23` と同じ考え方）。

**大規模・週2で書類選考の通過率が40/40（100%）になった。**
設定した通過率は0.70である。`REPORT-7.2` の Technical Debt に書いた
「固定シードの線形合同法は分布に偏りが出ることがある」がここでも再現した。
**これはアプリの欠陥ではなく、シミュレーション側の乱数の質の問題である。**
不変条件（二重計上・根拠の空・満点超過・合格数の一致）はここでも全部成立しており、
アプリが数え間違えたわけではない。

## 直した3件は、今回どうだったか

| 直した箇所 | 週2での確認 |
|---|---|
| `bad_decision`（不正 → 正しくない） | この経路は `<select>` が `advance`/`reject` しか出さないため、
  通常操作では踏めない。**週2でも到達しなかった**（想定どおり） |
| `already_decided`（判断が下りている → 判定が済んでいる） | 週2でも担当変更（reassign）を6%の確率で挟んでいるが、
  確定済みへの reassign 自体が起きなかった。**文言の実地確認は次回以降に残る** |
| `advanced`（担当を決める → 「今日やること」に出る） | 週2で42件の通過（advance）が発生。文言は一貫して新しいほうで出た |

**2件は今回のシミュレーションでは経路に到達しなかった。**
到達しなかったことを「直った」とは言わない ―― 確認できたのは
「まだ壊れていない」（`tsc` 通過・テスト308件通過）だけである。

## 新しく見つけた気づき

**無し。** 週2で新たに見つかった言葉の食い違いは0件だった。

もう一段深く見るなら、`status_histories.note` に残る委員会の一言
（例:「面接官・長谷: 通過でよい（地頭力を重視する。減点方式で厳しめ）」）は
**ペルソナの `voice` 説明文をそのまま埋め込んでいる**ため、実際の運営が書く
メモよりも長く説明的である。実データでは運営はもっと短く書くはずで、
**これはシミュレーションの都合であって、直すべき画面の言葉ではない。**
記録として残すが、修正対象には入れない。

## Validation Report

### 誰が検証したか

**開発者だけ。** 週1・週2とも同じ。Validated Operator Problem は
実行⑦を通じて**0件のまま**である。

### 確認した内容

- 直した3件の文言が、`tsc` とテスト308件を壊していないこと
- 別の乱数シードでも不変条件が崩れないこと（合計2週 x 2規模 = 4回分）
- 週2で新しい食い違いが出ないこと

### 未検証のケース

`REPORT-7.2` から変わらない。**実在の採点官が触っていない**ことが、
このシミュレーションでは埋まらない部分である。

## Technical Debt

`REPORT-7.2` の1件を確認のうえ持ち越す。

- 固定シードの線形合同法は分布に偏りが出る（週2の大規模で再現）。
  **これで十分と判断する** ―― このスクリプトの目的は「壊れずに動くか」の確認で、
  分布の厳密さは要らない。より良い乱数への差し替えは行わない

## Recommendation

**シミュレーションをこれ以上重ねることを推奨しない。**

2週 x 2規模、4回のシミュレーションを通じて、気づき（異常）は0件、
言葉の食い違いは3件見つけて直し、週2ではそのうち到達できたものだけを
確認した。**ここから先は、架空データを何周させても新しい情報が増えにくい。**

理由は `REPORT-7.1` から変わらない ―― **Developer Hypothesis は
Validated には自分ではなれない。** 運営が触るまで、この種の検証は
「壊れていないことの確認」以上のものにならない。

**次にやるべきことは、運営4名の日程を取ることである。** これは私には決められない。

## Confidence

| 対象 | 確度 | 根拠 |
|---|---|---|
| 選考が架空の量と分岐で壊れないこと | **高** | 4回のシミュレーション＋ tests/23〜26 が同じ不変条件を見ている |
| 直した3件の文言が正しいこと | **中** | 2件は経路に到達しなかったため実地確認は済んでいない |
| シミュレーションで見つかる言葉の問題が尽きたこと | **低** | 私が書いたペルソナが気づける範囲でしかない |
| 実運用に効いたこと | **無し** | Validated Operator Problem は0件のまま |

---

## Pilot の判定

**HOLD のまま。基準は緩めない。**

| # | Go の条件 | 現状 |
|---|---|---|
| 1 | 実データで1周 | **未達**（生年月日が無い。`REPORT-7.0`） |
| 2 | 開発者以外の運営が使った | **未達** |
| 3 | 観測報告のレビュー | **未達** |
| 4 | Validated の分類と優先順位 | 部分的（Validated 0件） |

実行⑦は `REPORT-7.0` 〜 `7.3` の4本になった。増えたのはすべて
**こちら側の確度**（テスト・シミュレーション・言葉の整備）であり、
**Pilot の判定を動かす条件は1つも増えていない。**
