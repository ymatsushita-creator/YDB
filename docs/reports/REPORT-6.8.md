# REPORT-6.8 — E3 確定 と D1 判定：採用の1周が閉じた

2026-08-07。**PM Report 形式。** `REPORT-6.0` 〜 `6.7` は凍結済み。
**この報告書も書いた時点で凍結する。**

---

## Goal — 今回解決しようとした課題

**採用の1周が、システムの中で終わらなかった。**

前の版（E2）で点と根拠は入るようになったが、そこで止まっていた。
確定できず、判定できず、次のステップへも進めない。
運用者から見ると「入力は終わったのに、まだ判断待ちに出ている」状態である。

今日の目標は機能の数ではなく、**1周を通すこと**だった。

---

## What Changed — オペレーターから見て何が変わったか

**Definition of Done の9項目が、すべて画面から実行できる。**

| # | できること | どこで | 状態 |
|---|---|---|---|
| 1 | 今日やることを見る | `/cockpit` | 済 |
| 2 | 候補者を開く | `/people/[id]`・`/applications/[id]` | 済 |
| 3 | 担当を決める | `/cockpit` | 済 |
| 4 | 担当を替える | `/cockpit` | 済 |
| 5 | 保留を解く | `/cockpit` | 済 |
| 6 | 評価する | `/applications/[id]` | 済（軸ごと＋確定） |
| 7 | **選考を判定する** | `/applications/[id]` | **今回** |
| 8 | **次のステージへ進む** | 判定の結果として自動 | **今回** |
| 9 | 今日の作業を終える | 運転席が減る | 済 |

**7 と 8 が今回である。** そして重要なのは、**8 が自動だ**ということ ――
通過にすると次のステップの評価行ができ、それが運転席に
「担当を決める」として並ぶ。**判定の結果が、次の一手になって戻ってくる。**

---

## Operator Value — 運営担当者が新しくできるようになったこと

1. **評価を確定できる。** 全軸そろうとボタンが出る。確定すると判断待ちから外れる
2. **通過・不合格を記録できる。** 判定した人と補足も残る
3. **次のステップが自動で立ち上がる。** 通過したら次の面接の「担当を決める」が
   運転席に現れる。**運用者が次にやることを探さなくていい**
4. **最終面接を通過すると合格になり、その応募のやることが空になる。**
   1件が完了したことが画面で分かる
5. **面接官が2人いるステップは、2人そろってから判定できる。**
   片方だけで先に進めない

### 実際に1周まわした（デモデータ、DOM で確認）

```
柏木 朔 / 二次面接（面接官2人）
  → 軸に点と根拠を入れる（3軸 × 2人 = 6回）
  → 1人目を確定  … 判定の欄はまだ出ない
  → 2人目を確定  … 「二次面接 の判定（評価は 2 件すべて確定済み）」が現れる
  → 通過にする   … 履歴に「通過 ・ 3. 二次面接 ・ 佐藤 咲良 ・ 二次面接を通過。最終面接へ」
  → 最終面接の軸が「先に担当を決める」と表示される
  → 運転席に「担当を決める / 4. 最終面接 / 柏木 朔」が並ぶ
```

運転席の数も正直に動いた ――
やること **27 → 26**（確定2件が消え、次の担当1件が増えた）、
止まっているもの **3 → 1**（23日超過の2件が確定した）、
要注意の森 **3 → 2**（その森の滞留が解けた）。

---

## Remaining Friction — まだ使いにくい点

1. **訂正の経路が無い。** 点は直せず（`already_scored`）、判定も画面から
   取り消せない。`status_histories` は追記専用なので、訂正は打ち消し行の
   追記でしか表せない。**画面にその入口が無い**
2. **辞退を記録できない。** 合格後の辞退（`withdraw`）は画面に無い。
   理由が必須（0008）なので、理由の選択欄が要る
3. **1軸ごとに画面が再読み込みされる。** 3軸なら3回。面接中の入力としては重い。
   直すにはクライアント部品が要るので、この回では触っていない
4. **申し送り（`handover_note`）を書けない**
5. **新しい応募を登録できない。** 応募は外部フォームから来る想定のままで、
   画面に入口が無い（今日の運用が既存の応募を進めることなら足りている）

---

## Technical Debt — 今回増えた負債と TODO(MVP)

**新しく増えたのは 12 と 13。**

| # | 内容 |
|---|---|
| 1〜6 | 森・林の分割 / 手で作るタスク / 種別の優先順位の重み / 所属と接触の違い / Health・Owner / 森の60人打ち切り |
| 7 | 担当を決めた・解いた・替えた記録が残らない |
| 8 | 利益相反の組み合わせを選ぶ前に警告していない |
| 9 | 軸の適用規則が、トリガと問い合わせの2箇所にある |
| 10 | 記録層の拒否を、制約名とトリガの文面で写している |
| 11 | 点の訂正ができない（E4） |
| **12** | **判定の訂正が画面に無い。**打ち消し行の追記でしか直せない |
| **13** | **全軸そろう前の確定を認めていない。**必要だと分かったら緩める |

**返した負債。** `reject` にステップが結び付くようになった（C-26）。
`CLAUDE.md` 8節が構造の穴として挙げていた項目である。
**ただしデモと過去の記録には入っていない** ―― 集計を作るときは母数が
ここから先の判定だけであることを注記する必要がある。

---

## Risks — 今回見つかったリスク

### 1. 判定は押した瞬間に確定し、画面から戻せない ★

`status_histories` は追記専用なので、間違って「不合格」を押しても
画面では取り消せない。**確認ダイアログを入れていない**（クライアント部品に
なるため）。代わりに判定の欄に「判定は打ち消しの追記でしか訂正できない。
押す前に確かめる」と出している。

**これは今日いちばん大きいリスクである。** 訂正の入口（TODO 12）を
早めに入れるべきだと考える。

### 2. 「どのステップで落ちたか」の母数が混ざる

画面からの判定にはステップが入るが、デモと過去の記録には入っていない。
**この列で集計を作ると、母数が「実行⑥以降の判定だけ」になる。**
気づかずに率を出すと、実際より小さい数を見ることになる。
注記を `CLAUDE.md` に残した。

### 3. 実際の面接では試していない

1軸ごとの再読み込みが実務で許容できるか、判定の欄が出るタイミングが
自然かは、**本物の面接で使わなければ分からない。**

---

## Recommendation — 次に実装すべき機能（1つだけ）

**判定の訂正（打ち消し行の追記）を画面から行えるようにする。**

理由。

- **押し間違いが取り返せない状態は、今日の運用でいちばん危ない**（リスク1）
- 記録層は最初からこの形を想定している ―― `corrects_history_id` と
  `is_correction` があり、`v_effective_status_histories` が打ち消しを
  解釈する。**新しい概念を足さずに、既存の仕組みへ入口を付けるだけ**
- 訂正が入れば、不合格を通過に直す・通過を取り消すの両方が同じ経路で済む

そのうえで、残りの優先順位はこう考える。

1. 判定の訂正（上記）
2. 辞退の記録（合格後の辞退は必ず起きる。理由が必須）
3. 点の訂正（E4）
4. 申し送り

**新しい応募の登録は後回しでよい。** 今日の運用が「すでに来ている応募を
進める」ことなら、入口は要らない。

---

## Confidence — 85%

**高い理由。**

- **1周を実データで通した。** 点の入力 → 確定 → 判定 → 次のステップの
  立ち上がり → 運転席への反映まで、すべて DOM の値で確かめた
- 途中で止まる形も実データで踏んだ（1人目の確定だけでは判定の欄が出ない）
- 弾くべき経路をテストで固定した（軸が残っている・担当未割当・二度の確定・
  二度の判定・動いていない応募・非活性化された職員）
- テスト **264 件** 全通過、`tsc` クリーン、`'use client'` 0件

**85% に留める理由。**

- **訂正の経路が無い。** 押し間違いを画面で戻せないので、
  「1周できる」とは言えても「安心して使える」とは言い切れない
- 辞退が記録できない。合格まで進めても、辞退の運用が画面の外に残る
- 本物の面接で使っていない

---

## 残作業の見積り（今日の目標に対して）

**Definition of Done は満たした。** 9項目すべてが画面から実行できる。

残っているものは、**1周を回すのに必須ではない**ものである。

| 残作業 | 1周に必須か | 見積り |
|---|---|---|
| 判定の訂正 | いいえ（ただし安全のため強く推奨） | 1サイクル |
| 辞退の記録 | いいえ（合格後の運用） | 1サイクル |
| 点の訂正（E4） | いいえ | 1サイクル |
| 申し送り | いいえ | 1サイクル未満 |
| 新しい応募の登録 | いいえ（外部フォーム想定） | 1〜2サイクル |
| 1軸ごとの再読み込みの解消 | いいえ | クライアント境界の判断が別に必要 |

**ブロックしているものは無い。**

---

## 付録 — 確かめたこと

- `pnpm test` **264 件** 全通過（E2 時点は 250）
- `pnpm exec tsc --noEmit` クリーン ／ `pnpm tokens` 差分なし ／ `'use client'` 0件
- 1周の実測は「Operator Value」の節にある

**次のレトロスペクティブ**は E1 から数えて5回目（E1・E2・E3+D1 …）。
**Pruning Review** は10回目。どちらも足す前に削る方針で行う。
