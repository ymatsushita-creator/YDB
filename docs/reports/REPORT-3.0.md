# YouthDB 作業報告 3.0 — 実行③（個人・応募のドリルダウン）

2026-08-07

実行③の第1報。扱うのは `HANDOFF.md`「次にやりうること」の最有力だった
**個人・応募のドリルダウン**。`CLAUDE.md` 1節の問い①（誰がどんな状態か）と
②（誰が何を評価したか）に直接効く。

実行②までの報告書（`REPORT-1.0` 〜 `REPORT-2.2`）は凍結済みで、内容は変更していない。

---

## 1. なぜここに手を付けたか

`CLAUDE.md` 8節の表で、①と②の「画面」列だけが空いていた。

| 問い | 記録層 | 集計 | 画面（実行②終了時点） |
|---|---|---|---|
| ① 誰がどんな状態か | あり | `v_person_season_state` | **個人を引く画面が無い** |
| ② 誰が何を評価したか | あり | 面接官別の負荷まで | **応募1件の評価履歴を見る画面が無い** |

記録層は足りていて、足りないのは集計と画面だけ ―― という見立てだった。
**これは半分だけ当たっていた。** 記録層は確かに足りていたが、集計の側には
名前と定義の食い違いが1つ、計算量の問題が1つ埋まっていた。
どちらも、年度単位の集計だけを見ている限り表に出ない性質のものだった。

## 2. 作ったもの

| 場所 | 中身 |
|---|---|
| `db/migrations/0010_person_season_detail.sql` | `f_person_season_state(active_window_days)`。個人×年度の段と接点の鮮度 |
| `src/queries/drilldown.ts` | 個人の検索・個人1人・年度別の現在地・応募一覧・接点の時系列・応募1件・遷移の全履歴・評価とスコア |
| `app/people/page.tsx` | 人を探す（検索と年度別の内訳） |
| `app/people/[id]/page.tsx` | 個人1人 |
| `app/applications/[id]/page.tsx` | 応募1件（遷移の履歴と評価） |
| `src/seed/demo.ts` | 経路を確実に踏ませる登場人物を追加 |
| `tests/12_drilldown.test.ts` | ドリルダウン（18件） |
| `tests/13_demo_coverage.test.ts` | デモデータが経路を踏んでいるか（9件） |

## 3. 動かして見つけた欠陥

進め方は `CLAUDE.md` 6節のとおり。**先にテストへ書き、落ちることを確認してから直した。**
詳細と理由は `db/DECISIONS.md` に書いた。ここは経緯だけ。

### 3.1 個人の「林」と、年度サマリの「林」が桁違いだった（A-10）★

同じ言葉が2つの違う定義を指していた。

- `v_person_season_state.current_level = 'identified_person'` … その年度に応募していない識別済みの人。**窓を持たない**
- `f_funnel_daily.identified_person_cum`（林の KPI） … 基準日から遡って N 日以内に接点がある人

デモデータの2027年度で、前者 2,576 人に対して後者 661 人。約4倍違う。
個人の画面がそのまま「林」と出せば、①の答えが**見る画面で変わる**。

`CLAUDE.md` 3節は「単位の違うものを同じ軸に置かない」と言っているが、
これは軸ではなく**名前の側**で同じことが起きていた。実行①で日次の 林 → 木
転換率を消し、実行②で「推定リーチに対する識別率」を作りかけて消したのと
同じ種類の間違いで、今回は割り算ではなくラベルの形で出た。

直し方は3つ考えられた。

1. `v_person_season_state` に窓を持ち込む → 資料2-3「林に期は存在しない」の
   実装であり、位置づけが崩れる。既存の集計にも波及する
2. 画面側で数え直す → 集計定義がアプリとデータベースの二箇所に散る（C-11 で却下した形）
3. 段と鮮度を、直交する2つの事実として並べる

**3 を採った。** 段は「その年度に到達した最高地点」、鮮度は「基準日時点で
生きている接点があるか」で、そもそも別の軸である。木や幹になった人も接点を
持てば林の窓に入る。

段ごとに数えて足しても年度サマリの林にはならないが、**段を問わず窓の内側を
数えれば一致する。** この一致がドリルダウンと年度サマリをつなぐ唯一の約束なので、
テストで固定した。`/people` の画面にも両方を並べ、一致していなければ
その場で分かるようにしてある。

### 3.2 年度を指定した検索が 444 秒かかった（A-11）★

`/people` を開いたら 2.6 分返ってこなかった。A-8 と同じ構造の問題が、
今度は問い合わせの書き方の側で出た。

原因は `LEFT JOIN f_person_season_state($4) ps ON ps.person_id = p.id`。
集合を返す関数を結合の内側に置くと、プランナが入れ子ループを選んだときに
**Person 1行ごとに関数全体を評価し直す**。関数そのものは速く（単体で 0.2 秒弱）、
遅いのは呼ばれ方のほうだった。

| 問い合わせ | 時間 |
|---|---|
| 年度を指定しない検索（関数を使わない） | 0.6 秒 |
| 年度を指定した検索 | **444 秒** |
| 氏名で絞り込んだ検索（外側が2行） | 0.5 秒 |

**氏名で絞ると速くなる**のが診断を決めた。外側の行数に比例している。
逆に言えば、手元で何か検索して触るだけでは気づけない。一覧を素で開いて
初めて出る。

対応は `WITH ... AS MATERIALIZED` への切り出し（関数を年度で絞った状態で1回だけ評価）。
**ただし直したあとの実測はまだ取っていない。3節の残った穴を参照。**

### 3.3 デモデータが「何度流しても同じ」ではなかった（A-12）

`src/seed/demo.ts` の冒頭には「乱数は固定シードで、何度流しても同じデータに
なる」と書いてある。乱数はそのとおりだったが、`asOf`（すでに起きたことの締め）の
既定値が `Date.now()` だった。同じ日に `pnpm db:reset` を数分あけて2回流したら、
進行中の年度の判断待ちが 23 件と 26 件で食い違った。

記録が実装より厳密に見える形で、`DECISIONS.md` が一貫して警戒してきた
種類の齟齬そのものである。既定値を JST の暦日に丸めた。

### 3.4 一覧の絞り込みに空文字を渡すと該当0件になった（A-13）

`/people` を開いた最初の状態が「該当する人がいない」だった。
`<select>` の「すべての段」は空文字を送る。それをそのまま
`WHERE current_level = ''` に渡していた。「指定なし」と「空文字という指定」を
区別していなかった。

## 4. 決めたこと

### 4.1 個別の画面は「起きた事実」を落とさない（C-12）

集計と個別で、守る規律を1つだけ変えた。

```
集計は「数えてよいものだけ」を数える（v_countable_applications）
個別は「起きた事実」を落とさず出し、数えるかどうかを列で示す
```

集計と同じ絞り込みを個別にも適用すると、名寄せ誤りで無効化された応募が消える。
そこに評価と遷移がぶら下がっていると、記録層にはあるのに画面のどこにも出ない。
集計の都合で事実を隠すことになる。

同じ理由で、状態遷移の履歴は有効な行だけに絞らず全行を出す。
「不合格の連絡をしたのに、なぜ合格しているのか」に答えられるのは経緯のほうで、
結論ではない。ただし訂正をさらに訂正すると元が有効に戻る（会計の逆仕訳）ので、
**「訂正された＝無効」ではない。** 打ち消し行があることと、いま有効かどうかは
別の列で示し、有効性の判定は `v_effective_status_histories` に任せている。

逆に個人情報削除（`deleted_at`）は個別からも消す。集計から外すだけでは
削除の依頼（資料9-2）に応えたことにならない。知らない id・壊れた id・削除済みを
すべて同じ「見つからない」で返すのは、削除済みだけ別の応答にすると、
その差が「その人は存在した」を漏らすため。

これに伴い、E-4 で「どの画面からも読んでいない」としていた
`v_application_state.is_voided` が読まれるようになった。

### 4.2 デモに「経路を確実に踏ませる登場人物」を置いた（C-13）

「デモデータが検証したい経路を踏んでいない」は、この開発で3回繰り返した失敗である。
今回もそうだった。次の2つの形は一度も生成されていなかった。

- 集計から外れる無効化応募（名寄せ誤り）に、評価と遷移がぶら下がる形
- 同一年度に集計対象の応募が2件ある形（取り下げて出し直し）

後者は 0007 で `v_person_season_state` を「応募をまたいだ最高到達点」に直した
理由そのものなのに、その分岐がデモで一度も踏まれていなかった。

乱数で作る大勢とは別に、名前と経緯を決め打ちした登場人物を置いた。
確率ではなく明示的に置く。何を確かめるために置いたかは `persons.note` に
書いてあり、個人の画面でそのまま読める。実在の人物ではない。

**踏んでいること自体を機械に確かめさせる**のが要点で、`tests/13` がそれをやる。
数は固定していない（「1件以上あるか」だけを見る）。乱数の分布に依存する値を
テストに書くと、デモの規模を変えただけで落ちるため。

### 4.3 timestamptz の表示を運用タイムゾーンで書き出す（C-14）

既存の `ymd()` は `date` 列専用で、`timestamptz` に使うと UTC の壁時計になる。
JST 09:00 より前の出来事が前日に見える ―― A-1 とまったく同じ間違いを表示側で
やることになる。`Intl.DateTimeFormat` に `timeZone: 'Asia/Tokyo'` を明示した
`jstDateTime()` / `jstDay()` を足した。

## 5. 残した穴（実行④の先頭に置くもの）

**この報告書で最も重要な節。** 実行③は完了していない。

1. **A-11 の対策後の実測を取っていない。** 原因の特定（444 秒、外側の行数に
   比例する再評価）までは計測で確定しているが、`AS MATERIALIZED` を入れた
   あとの数字は取っていない。意味論が変わっていないことはテストで固定してあるが、
   **速くなったことは確かめていない。** 実行④の最初にここを計測する
2. **画面の DOM 検証が途中まで。** `/people` は DOM を読んで
   「窓の内側の合計 661 = ファネルの林 661」の一致を確認したが、
   `/people/[id]` と `/applications/[id]` は**一度も開いていない**。
   `CLAUDE.md` 7節の「DOM か SQL の値で結果を確認した」を満たしていない
3. **`v_person_season_state` 自体の計算量は見直していない。** 全件で 0.2 秒程度
   なので当面は問題にならないが、Person が増えれば効いてくる形
   （`persons × seasons` の直積）である

## 6. 数字

| | 実行②終了時 | 実行③（本報告時点） |
|---|---|---|
| テスト | 111 | **138**（全通過） |
| マイグレーション | 9 | **10** |
| 画面 | 3 | **6** |
| 問い合わせモジュール | 1（`dashboard.ts`） | **2**（`drilldown.ts` 追加） |

`CLAUDE.md` 7節の完了条件のうち、`pnpm test` 全通過と `tsc --noEmit` クリーンは
満たしている。**満たしていないのは「実データで動かし、DOM か SQL の値で結果を
確認した」で、5節の2番のとおり画面2枚が未確認である。**

デモデータの件数や所要ミリ秒はここに残さない（追試できないため）。
例外は 3.1 の 2,576 対 661 と 3.2 の 23 対 26、3.2/3.3 の秒数で、
いずれも**比の大きさそのものが欠陥の説明**になっており、数字を外すと
何が問題だったのか読めなくなるため残した。デモの規模を変えれば値は動く。

## 7. 4つの問いへの効き

| 問い | 実行③で変わったこと |
|---|---|
| ① 誰がどんな状態か | 個人を1人引く画面ができた。段と接点の鮮度を分けたことで、年度サマリの林と個人の表示が同じ定義でつながった |
| ② 誰が何を評価したか | 応募1件の評価・スコア・根拠エピソード・遷移の全履歴（訂正含む）が1画面で読める |
| ③ いま何が起きているか | 変わっていない（`/operations` のまま） |
| ④ 次に何をすべきか | 変わっていない |

---

以上。凍結する。追記が要るときは `REPORT-3.1.md` を作る。
