# 【凍結】監査意見 —— 2026-08-19 / 対象コミット `02aa6bd`

**この文書は凍結されている。現役の監査意見ではない。**

いまの意見は `HANDOFF.md`「監査意見」と `.audit/reports/` の最新版を見ること
（`bcf18f3` 時点で High 2）。本書の時点では High 5 件だったが、
うち3件（公開ミラー関連）は公開リモート `mirror=ymatsushita-creator/YDB` を
外したことで解消している。**本書の所見を現行の課題として扱わない。**

NEO 構成基準 R1（ルート直下に読み物を置かない）により、
`/Volumes/KIOXIA_2TB/NEO/YouthDB-監査報告.md` から移設した。

---

==============================================================================
監査意見: 【限定付適正】
  理由: High 5 件。限定事項として列挙する。
  対象: /Volumes/KIOXIA_2TB/NEO/YouthDB @ 02aa6bd
  実施: 17/18 手続（実施不能 1 件 = 6%）
  所見: Critical 0 / High 5 / Medium 34 / Low 1
==============================================================================

── 所見（重大度順）
× [High] D2-01  実データ候補 4 件が作業ツリーにあり、追跡外である（＝`.gitignore` の1行が唯一の防壁。ignore を1行崩せば公開される）  ★過去1回同じ指摘
      根拠: YouthDB_DB構造説明資料_2026-08-08.pptx
      根拠: YouthDB_社長向け説明資料_2026-08-08.pptx
      根拠: db/private/legacy-youthdb-2026-08-07/tables/candidates.json
      根拠: db/private/legacy-youthdb-2026-08-07/tables/youth_candidates.json
      要求する是正: 実データはリポジトリの外（別ディレクトリ・別ボリューム）へ移し、リポジトリ内には受け入れ口を置かない。ignore に依存した保護をやめる
× [High] D3-05  `ymatsushita-creator/YDB` の `main` に必須ステータスチェックが無い（CIが落ちてもマージできる）  ★過去1回同じ指摘
      根拠: GitHub ブランチ保護設定
      要求する是正: 監査ジョブを required status check にし、人間のレビューを必須にする
× [High] D3-05  `ymatsushita-creator/YDB` の `main` に人間のレビュー必須設定が無い  ★過去1回同じ指摘
      根拠: GitHub ブランチ保護設定
      要求する是正: 監査ジョブを required status check にし、人間のレビューを必須にする
× [High] D3-06  公開リモート `mirror=ymatsushita-creator/YDB` がある。ここへ push されたものは即時に公開される（履歴に入れば削除しても公開済みの事実は消えない）  ★過去1回同じ指摘
      根拠: git remote -v
      要求する是正: 公開先を持つ必要があるなら、公開先へ push する前段に監査の合格を必須にする。不要なら公開リモートを外す（リポジトリ設定の変更は人間が行う）
× [High] D5-03  本番へ出した記録が1件も無い。いつ・どのコミット・どの未コミット差分を出したのかを事後に確認できない  ★過去1回同じ指摘
      根拠: .audit/deploys
      要求する是正: deploy-gate を通して記録を残す。過去分は復元できないので、以後の記録から始める
△ [Medium] D2-02  本番投入用データ（人物データは検出されず） が追跡下にある（8 件）  ★過去1回同じ指摘
      根拠: db/seeds/0002_season_2026.production.sql
      根拠: db/seeds/0003_season_2027.production.sql
      根拠: db/seeds/0005_season2_special_selection.production.sql
      根拠: db/seeds/0006_season3_align_to_season2.production.sql
      根拠: db/seeds/0007_season3_target.production.sql
      根拠: db/seeds/0008_group_interview_criteria.production.sql
      要求する是正: 追跡から外し、必要なら履歴からの除去を人間の承認のもとで行う（.audit/IRREVERSIBLE_OPS.md）
△ [Medium] D3-06  リモートが 2 件ある（公開と非公開が混在しうる）。push 先を1つ間違えるだけで公開範囲が変わる  ★過去1回同じ指摘
      根拠: remotes=['mirror', 'origin']
      要求する是正: 既定の push 先を1つに固定し、他方への push は明示操作のみに限る
△ [Medium] D5-03  作業ツリーに 1 件の差分がある。いま出せばリポジトリに存在しないファイルが本番へ入る
      根拠: git status --porcelain: 1 件
      要求する是正: コミットしてから出す。または deploy-gate の記録に残したうえで人間が承認する
△ [Medium] D6-01  `app/borderline/page.tsx` に JP_PERSON_NAME_WEAK が 2 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: app/borderline/page.tsx: L65, L67
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `db/migrations/0023_person_optional_facts.sql` に JP_PERSON_NAME_WEAK が 1 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: db/migrations/0023_person_optional_facts.sql: L59
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `db/seeds/0001_reference.example.sql` に JP_PERSON_NAME_WEAK が 4 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: db/seeds/0001_reference.example.sql: L64, L70, L73, L75
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `db/seeds/0004_channels.sql` に JP_PERSON_NAME_WEAK が 6 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: db/seeds/0004_channels.sql: L31, L32, L33, L35, L36, L37
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `db/seeds/0005_season2_special_selection.production.sql` に JP_PERSON_NAME_WEAK が 1 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: db/seeds/0005_season2_special_selection.production.sql: L55
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `scripts/import-approach-2026.ts` に JP_PERSON_NAME_WEAK が 2 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: scripts/import-approach-2026.ts: L110, L147
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `scripts/import-legacy-2026.ts` に JP_PERSON_NAME_WEAK が 1 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: scripts/import-legacy-2026.ts: L304
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `src/commands/ai_pre_assessment.ts` に JP_PERSON_NAME_WEAK が 2 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: src/commands/ai_pre_assessment.ts: L193, L210
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `src/import/legacy_2026.ts` に JP_PERSON_NAME_WEAK が 5 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: src/import/legacy_2026.ts: L313, L314, L315, L316, L317
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `src/queries/ai_pre_assessment.ts` に JP_PERSON_NAME_WEAK が 1 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: src/queries/ai_pre_assessment.ts: L24
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `src/queries/document_screening.ts` に JP_PERSON_NAME_WEAK が 1 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: src/queries/document_screening.ts: L103
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `src/queries/tasks.ts` に JP_PERSON_NAME_WEAK が 1 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: src/queries/tasks.ts: L256
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `tests/21_decide.test.ts` に JP_PERSON_NAME_WEAK が 1 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: tests/21_decide.test.ts: L345
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `tests/22_reference_season.test.ts` に JP_PERSON_NAME_WEAK が 7 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: tests/22_reference_season.test.ts: L161, L162, L163, L165, L232, L255 …ほか 1 箇所
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `tests/29_profile_editing.test.ts` に BIRTHDATE が 1 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: tests/29_profile_editing.test.ts: L32
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `tests/33_borderline_scoring.test.ts` に JP_PERSON_NAME_WEAK が 2 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: tests/33_borderline_scoring.test.ts: L42, L206
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `tests/34_legacy_import.test.ts` に BIRTHDATE が 10 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: tests/34_legacy_import.test.ts: L34, L37, L44, L52, L61, L72 …ほか 4 箇所
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `tests/34_legacy_import.test.ts` に JP_PERSON_NAME_WEAK が 3 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: tests/34_legacy_import.test.ts: L52, L60, L94
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `tests/35_interview_sheet.test.ts` に JP_PERSON_NAME_WEAK が 1 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: tests/35_interview_sheet.test.ts: L44
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `tests/36_intake.test.ts` に JP_PERSON_NAME_WEAK が 8 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: tests/36_intake.test.ts: L304, L317, L321, L328, L341, L345 …ほか 2 箇所
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `tests/41_sheet_bulk.test.ts` に JP_PERSON_NAME_WEAK が 2 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: tests/41_sheet_bulk.test.ts: L316, L326
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `tests/48_criteria_weighting.test.ts` に JP_PERSON_NAME_WEAK が 2 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: tests/48_criteria_weighting.test.ts: L48, L89
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `tests/52_season3_alignment.test.ts` に JP_PERSON_NAME_WEAK が 5 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: tests/52_season3_alignment.test.ts: L28, L30, L33, L33, L90
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `tests/53_deploy_target.test.ts` に JP_PERSON_NAME_WEAK が 3 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: tests/53_deploy_target.test.ts: L29, L37, L55
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `tests/54_list_weight.test.ts` に JP_PERSON_NAME_WEAK が 1 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: tests/54_list_weight.test.ts: L41
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `tests/70_ai_pre_assessment.test.ts` に JP_PERSON_NAME_WEAK が 1 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: tests/70_ai_pre_assessment.test.ts: L204
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `tests/75_end_to_end_selection.test.ts` に JP_PERSON_NAME_WEAK が 1 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: tests/75_end_to_end_selection.test.ts: L270
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `tests/76_document_gate.test.ts` に JP_PERSON_NAME_WEAK が 3 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: tests/76_document_gate.test.ts: L47, L63, L71
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  `tests/78_pilot_hirashain.test.ts` に JP_PERSON_NAME_WEAK が 3 件（追跡ファイル（作業ツリー））  ★過去1回同じ指摘
      根拠: tests/78_pilot_hirashain.test.ts: L36, L38, L216
      要求する是正: 実データを取り除き、ダミーは Faker(ja_JP) で生成する。既に push 済みなら履歴からの除去を不可逆操作として人間の承認のもとで行う
△ [Medium] D6-01  中身を検査できなかったファイルが 7 件ある（安全だと言っていない）  ★過去1回同じ指摘
      根拠: public/brand/icon_64.png
      根拠: public/brand/logo_black.png
      根拠: public/brand/logo_gradient.png
      根拠: public/brand/logo_gradient_720.png
      根拠: public/brand/logo_white.png
      根拠: public/brand/logo_ydb.png
      要求する是正: 検査できる形式へ変換するか、リポジトリの外へ出す
△ [Medium] D7-01  `.audit/allowlist.yml` は改変されていないが、ツール側の雛形と内容が違う（雛形が更新された）  ★過去1回同じ指摘
      根拠: .audit/allowlist.yml
      要求する是正: kurosaki install で最新の雛形へ更新する
・ [Low] D6-02  確定パターン 11 種では秘密を検出しなかった（これは秘密が無いことの証明ではない。独自形式のトークンは対象外）  ★過去1回同じ指摘
      根拠: D6-02 の走査範囲
      要求する是正: 必要なら gitleaks / detect-secrets を併用する

── 実施した手続 17 件（各手続が何を見たか。これが意見の根拠）
  D1-01 [体制文書] AI開発体制の規律文書が実在するか  → 所見 0 件
      検査した内容: 規律文書候補 7 件・上位文書候補 6 件の実在を確認（発見: 規律 ['CLAUDE.md', 'AGENTS.md', '.cursor/rules'] / 上位 ['SUPERVISOR.md', '.audit/AUDIT_CHARTER.md']）
  D1-02 [体制文書] 上位文書・規律文書が実装AIから書き換え可能でないか  → 所見 0 件
      検査した内容: 5 件の文書について、CODEOWNERS・監査マニフェスト・フック本文のいずれかで保護されているかを照合（CODEOWNERS=無 / MANIFEST=有 / フック 2 本）
  D2-01 [フォルダ構成] 実データ候補が git の外に置かれているか（.gitignore 単独防壁でないか）  → 所見 1 件
      検査した内容: 作業ツリー 505 ファイルを走査し、人物を示す語・機械検査できない形式を持つ未追跡ファイルを 4 件検出
  D2-02 [フォルダ構成] 追跡してはならないものが追跡下に入っていないか  → 所見 1 件
      検査した内容: 追跡ファイル 378 件を5分類の禁止パターンへ突合
  D3-01 [機械ゲート] git が実行するフックが実在し、実行可能か  → 所見 0 件
      検査した内容: `core.hooksPath`=.githooks を読み、.githooks 配下の pre-commit, pre-push の実在と実行権限を確認（実在 2 / 欠落 0 / 実行不可 0）
  D3-02 [機械ゲート] フックが本ツールの検査を実際に呼んでいるか  → 所見 0 件
      検査した内容: フック 2 本の本文を読み、4 種の呼び出し形を照合（呼んでいる: ['.githooks/pre-commit', '.githooks/pre-push'] / 呼んでいない: なし）
  D3-03 [機械ゲート] CI 定義が実在し、その中で監査の検査が走るか  → 所見 0 件
      検査した内容: CI 定義の置き場 6 箇所を確認し、見つかった 1 本の定義本文へ監査呼び出しを照合（定義: ['.github/workflows/audit.yml'] / 呼んでいる: ['.github/workflows/audit.yml']）
  D3-04 [機械ゲート] 監査を無効化する記述（continue-on-error 等）が混入していないか  → 所見 0 件
      検査した内容: CI 定義・フック・package.json/Makefile を合わせた 5 本について、8 種の無効化パターンを行単位で照合（対象: ['.audit/allowlist.yml', '.githooks/pre-commit', '.githooks/pre-push', '.github/workflows/audit.yml', 'package.json']）
  D3-06 [機械ゲート] 公開リモートの有無と、そこへ何が届く構造になっているか  → 所見 2 件
      検査した内容: リモート 2 件を列挙し、GitHub の 2 件について公開状態を照会（公開: ['mirror=ymatsushita-creator/YDB'] / 非公開: ['origin=NEO-AX/YDB'] / 不明: なし）
  D5-01 [不可逆操作] 不可逆操作の一覧と承認手順が文書として実在し、必須項目を網羅しているか  → 所見 0 件
      検査した内容: `.audit/IRREVERSIBLE_OPS.md`（1285 文字）を読み、必須 6 項目の記載と承認者の明記を照合（欠落 0 項目 / 承認者記載 有）
  D5-02 [不可逆操作] 本番・不可逆へ到達する経路に人間ゲートがあるか（AIへ渡した権限も含む）  → 所見 0 件
      検査した内容: package.json の scripts 22 件から本番到達候補 5 件を抽出。.claude/settings.local.json の allow 49 件を危険パターン 9 種へ突合
  D5-03 [不可逆操作] 本番へ出した実体が記録され、リポジトリと一致しているか  → 所見 2 件
      検査した内容: package.json から本番へ出す経路 1 件を抽出（['deploy:production']／ゲート済み ['deploy:production']）。`.audit/deploys` の記録 0 件、未pushコミット 0、作業ツリーの差分 1 件を確認
  D6-01 [データ取扱い] 個人情報が追跡下・履歴に混入していないか  → 所見 30 件
      検査した内容: 追跡ファイル（作業ツリー） を走査: 検査 377 件 / 除外 1 件 / 中身未検査 7 件。allowlist 除外 149 件
  D6-02 [データ取扱い] 確定パターンのシークレットが追跡下に入っていないか  → 所見 1 件
      検査した内容: 追跡ファイル 378 件のうち 370 件を 11 種の確定パターンへ突合（8 件は形式・除外対象でスキップ）。汎用のシークレット検出は範囲外（gitleaks=未確認/無し）。allowlist で除外 1 件
  D7-01 [監査の独立性] 配置済みの監査基盤が導入後に改変されていないか  → 所見 1 件
      検査した内容: `.audit/MANIFEST.sha256` の 14 件について、現物のsha256を再計算し、さらにツール側 templates/ の雛形と突合（一致 13 / 改変 0 / 欠落 0 / 雛形差 1）
  D7-02 [監査の独立性] allowlist が抜け道になっていないか  → 所見 0 件
      検査した内容: `.audit/allowlist.yml` を読み、有効 36 件／無効 0 件を判定（広すぎるパス 0 / 全ルール除外 0 / 指紋指定なし 0 / 期限なし 0）。直近の変更者: Yuji Matsushita, Yuji Matsushita
  D7-03 [監査の独立性] 過去の監査調書が改変されていないか（ハッシュ連鎖）  → 所見 0 件
      検査した内容: `.audit/reports` の調書 1 件について、自己ハッシュと前後の連鎖を検証（不整合 0 件）

── 実施できなかった手続 1 件（**この範囲は検査していない。安全だと言っていない**）
  D3-05 [機械ゲート] 既定ブランチが保護され、監査の合格が必須になっているか
      理由: ブランチ保護を確認できないリモートがある（プラン制限・権限不足・未認証）: origin=NEO-AX/YDB: プラン制限または権限不足で確認できない（gh: Upgrade to GitHub Pro or make this repository public to ）

→ 限定事項（High）を承知の上で進めるか、解消してから再監査すること。
