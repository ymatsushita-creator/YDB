# REPORT-6.7 — E2: 点と根拠を1軸ずつ保存する

2026-08-07。**PM Report 形式**（この版から適用）。
`REPORT-6.0` 〜 `6.6` は凍結済み。**この報告書も書いた時点で凍結する。**

---

## Goal — 今回解決しようとした課題

**評価の結果を、画面から記録できなかった。**

前の版（E1）で「何を評価するのか」は見えるようになった。しかし入力欄が
無いため、運用者は軸を見てから**別の場所に書き、あとで転記する**ことになる。

やること27件のうち10件が「評価する」で、**残りの摩擦のすべてがここだった。**

---

## What Changed — オペレーターから見て何が変わったか

| | 前 | 後 |
|---|---|---|
| 応募の画面 | 評価すべき軸が並ぶ（読むだけ） | **軸ごとに点と根拠を入れて保存できる** |
| 保存の単位 | — | **1軸ずつ。**途中で止めても入れた分は残る |
| 運転席 | `0/3 軸 ・ 未着手` | `1/3 軸` のように**埋まっていく** |
| 点を付けられないとき | — | **なぜ付けられないかを名指しする**（「先に担当を決める」など） |

面接官が2人いる同じステップは、**それぞれ独立に数える**（同じ応募でも
`1/3 軸` と `0/3 軸 ・ 未着手` が並ぶ）。

---

## Operator Value — 運営担当者が新しくできるようになったこと

1. **面接の途中で、1軸だけ書き留められる。** 全部そろうまで待つ必要がない。
   `evaluation_scores` の一意制約が1軸単位なので、記録層に逆らっていない
2. **どこまで入力したかが運転席で分かる。** 「評価する」10件のうち、
   着手済みと未着手が一覧で区別できる
3. **点を付けられないときに、次の一手が分かる。** 保留・担当未割当・利益相反の
   評価では入力欄を出さず、代わりに「先に保留を解く」などと出す。
   **止まらずに、やるべき順序へ戻れる**
4. **0 点を記録できる。** 「未入力」と「0 点だった」が区別される

---

## Remaining Friction — まだ使いにくい点

1. **確定できない。** 全軸そろっても `pending` のままで、選考は次へ進まない。
   運用者から見ると「入力は終わったのに、まだ判断待ちに出ている」状態になる。
   **これが次の一手**（E3）
2. **点を直せない。** 一度保存した軸は上書きできず、`already_scored` が返る。
   打ち間違いに気づいても画面では戻せない（E4）
3. **1軸ごとに画面が再読み込みされる。** 素の HTML フォームなので、
   3軸なら3回。面接中の入力としてはやや重い。ただし
   **クライアント部品を入れる判断**にあたるので、この回では触っていない
4. **申し送り（`handover_note`）は書けない。** 評価とは別の列で、まだ入口が無い

---

## Technical Debt — 今回増えた負債と TODO(MVP)

**新しく増えたのは 10 と 11。** 9（軸の適用規則が2箇所）は E2 でも畳んでいない。

| # | 内容 |
|---|---|
| 1 | 森と林を別テーブルに分けるか |
| 2 | 手で作るタスクは表せない |
| 3 | やることの種別に優先順位を付けていない（並べ替えの重み） |
| 4 | 森と人の結び付きは「接触があった」であって「所属している」ではない |
| 5 | Health / Relationship Strength / Owner は未実装 |
| 6 | 森の画面の「接点がある人」は 60 人で打ち切り |
| 7 | 担当を決めた・保留を解いた・担当を替えた記録が残らない |
| 8 | 利益相反になる組み合わせを、選ぶ前に警告していない |
| 9 | 軸の適用規則が、トリガと問い合わせの2箇所にある |
| **10** | **記録層の拒否を、制約名とトリガの文面で写している。**文面が変われば写しが外れる（テストで落ちるようにはしてある） |
| **11** | **点の訂正ができない。**E4 で決める |

**返した負債が1つある** ―― 0015 で「根拠は必須」の穴を塞いだ（次節）。

---

## Risks — 今回見つかったリスク

### 1.（解消済み）「根拠は必須」が破れていた ★

**改行だけ・タブだけ・全角スペースだけの根拠が保存できていた。**
`btrim` の既定が半角スペースしか落とさないためである。

`DECISIONS.md` の B 節には「`rationale` が空白のみは不可」と書いてあった。
**記録が実装より厳密に見える形**で、読んだ人は守られていると思って
確認をやめる。このプロジェクトが最も警戒している壊れ方である。

**表に出なかった理由が重要である。** 画面から入力できなかったので、
根拠はデモとテストからしか入らず、どちらも実文だった。
**入力欄を作った瞬間に踏める穴になった。**
今回塞いだ（0015、A-19）。実際に `required` を外して全角スペースを送り、
記録層が弾くことを確かめている。

### 2. 記録層の文面に依存している（残存）

拒否の写しが制約名とトリガのメッセージに依存する。文面が変われば
写しが外れ、**運用者に意味の通らないエラーが出る**か、例外が素通りする。
テストで1つずつ踏ませてあるので落ちるが、依存そのものは残る（負債 10）。

### 3. 点を消せないことが、運用で問題になりうる（未検証）

打ち間違いを直せない。E4 まで、運用者は間違いに気づいても画面では戻せない。
**どのくらい起きるかは分からない。** 使ってみて頻度を見たい。

---

## Recommendation — 次に実装すべき機能（1つだけ）

**E3: 評価を確定する（`submitted` にする）。**

理由。

- **いま「入力は終わったのに判断待ちのまま」という状態が作れてしまう。**
  E2 を入れたことで新しく生まれた不整合で、いちばん近い穴である
- 全軸そろってから通す規則を1つ足すだけで、更新は `evaluations` の2列
  （`state` と `submitted_at`）。`assign` と同じ型がそのまま使える
- これが入ると、**やること4種すべてが画面の中で完結する**

決める必要があること（実装を止めずに仮定で進める予定）——
**全軸そろう前の確定を認めるか。** 認めないのが単純なので、そちらを採る。
認める運用が必要だと分かったら、そのとき緩める。

E4（訂正）はその次。頻度が分かってからのほうが、上書きか打ち消しの追記かを
決めやすい。

---

## Confidence — 80%

**高い理由。**

- 実際にフォームから保存し、DOM で結果を確かめた（`4 / 5` と根拠が表に出た）
- 失敗の経路も実データで踏んだ（全角スペースの根拠が `rationale_blank` で戻った）
- 弾くべき5つの状態（担当未割当・保留・利益相反・取り下げ・削除済み）を
  テストで固定した
- テスト 250 件全通過、`tsc` クリーン、`'use client'` 0件

**80% に留める理由。**

- **本物の面接で使っていない。** 1軸ごとに画面が再読み込みされる重さが
  実務で許容できるかは、使わなければ分からない
- 拒否の写しが記録層の文面に依存している（負債 10）。テストで守っているが、
  構造としては弱い
- 点を消せないことの影響を測れていない

---

## 付録 — 確かめたこと（DOM の値）

**保存の成功**

```
URL      ?score=saved
通知     点と根拠を保存した。（callout ok）
入力欄   6 → 5
評価の表 課題設定力  4 / 5  勉強会を自分で立ち上げ、続かなかった理由も…
```

**保存の失敗**（`required` を外して全角スペースを送信）

```
URL      ?score=rationale_blank
通知     根拠が空である。何を見てその点にしたかを書く。（警告色）
入力欄   5（増えていない）
```

**運転席**

```
評価する  柏木 朔  3. 二次面接  1/3 軸           原 健一
評価する  柏木 朔  3. 二次面接  0/3 軸 ・ 未着手  藤本 みなみ
```

- `pnpm test` **250 件** 全通過（E1 時点は 230）
- `pnpm exec tsc --noEmit` クリーン ／ `pnpm tokens` 差分なし ／ `'use client'` 0件

---

## 運用ルールの記録

この版から次を適用している。

- **PM Report を毎サイクル出す**（本書の形式）
- **10サイクルごとに Pruning Review**（剪定レビュー）。足す前に削る。
  実行⑥のサイクル番号では **E5 相当が10回目**にあたる
- 5サイクルごとのレトロスペクティブは継続（1回目は `REPORT-6.5.md`）
- 成功基準は**「運営担当者が今日の業務をより早く、迷わず進められるか」**の1つだけ
