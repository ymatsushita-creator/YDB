# src/commands/ —— 書き込み判定

```
pnpm verify:fast
```

- **ここが唯一の書き込み口である。** 画面もクエリも判定を持たない。
- 引数は再検証する。呼び出し側（画面）の検証を根拠にしない。
- 追記専用の記録は上書きしない。訂正は打ち消し行を足す。
- 削除済み候補者は編集できない。
- 判定を変えたら `db/DECISIONS.md` に理由を書き、`tests/` に番号付きのテストを足す。
