# src/queries/ —— 読み取り

```
pnpm verify:fast
```

- **判定を書かない。** 読み取りだけ。書き込みは `src/commands/`。
- 単位と母集団を**クエリのコメントに書く**（画面には出さない。C-62）。
- 単位の違う値を割らない。
- 母集団は「操作できる集合」と一致させる。画面に出すだけの集合を作らない。
- 旧生態系比喩（森・林・木・幹）を現行の語として使わない。対応表は `docs/product/legacy-terms.md`。
