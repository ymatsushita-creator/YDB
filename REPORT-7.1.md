# REPORT-7.1 — 20周と自作テスト3種、そこから出した1つの判断

実行⑦の第2報。2026-08-07。**この文書は凍結する。**
`REPORT-7.0.md`（実データ受領〜公開まで）の続き。

---

## Goal

`REPORT-7.0` の Technical Debt に、こう書いた。

> **10周と10人×3回は使い捨ての脚本で、リポジトリに残していない。**
> 残すなら「軸が0本のステップを確定できてしまう」を先にテストへ書く順序になる。

**それをやった。** 検証を回数で重ねるだけでなく、**リポジトリに残る形**にする。
残らない検証は、次に触る人にとって存在しないのと同じである。

## What Changed

### テストが 289 → 301 件になった

| ファイル | 何を見るか | 件数 |
|---|---|---:|
| `tests/23_selection_rounds.test.ts` | 選考を**20周**。周ごとに道を変える ＋ 軸0本の段 | 4 |
| `tests/24_scaling.test.ts` | 量に対する**伸び方**。絞り込みの有無を**両方** | 3 |
| `tests/25_correction_roundtrip.test.ts` | 判定の訂正を**10回往復** | 5 |

**すべて実在する2026年度（本番シード）で回す。** デモの年度で回すと、
デモが埋めてしまっている欠落が見えない ――
実行④〜⑥で5回踏んだ「デモデータが検証したい経路を踏んでいない」を避けるため。

### その1 —— 20周（`tests/23`）

周ごとに**どこで落ちるか・保留を挟むか・誰が見るか**を変える。
**同じ道を20回通っても、通らなかった道の穴は1つも見つからない。**

乱数は固定シードの線形合同法を自前で持っている。
`Math.random()` を使うと、落ちたときに同じ状態を作り直せず原因を追えない。

毎周、次の6つを検査する。**周ごとの中身が違っても成り立つべき性質**である。

```
やることの二重計上が0            （A-18 の再発を全体の性質で見る）
根拠が空の点が0                  （A-19 で一度破れていた）
満点を超えた点が0
合格の数 ＝ 最終段を通過した応募の数
動いている応募 ⊂ 数える応募
片付いた応募にやることが残らない
```

**20周すべて成立した。**

### その2 —— 計算量（`tests/24`）

**秒数は書き残さない。** 機械が違えば違う数字が出るし、
作り物のデータから出た秒数は追試できない（`CLAUDE.md` 9節）。
固定するのは**伸び方**だけである。

- 人が **100 → 300** になったとき、桁で跳ねないこと
- **絞り込みなし**と**年度で絞る**の**両方**を測ること
- **絞ったほうが遅くならないこと**

最後の1つが本題である。**A-11 は絞ると速くなって見逃し、
A-16 は絞ると 9000 倍遅くなった。** 片側だけ測る限り、
どちらかの向きを必ず取り逃す。

実行計画も直接見ている。`Function Scan` が現れないこと ――
実行③で一覧が 444 秒かかった原因は、時間ではなく計画に出る。

### その3 —— 訂正の往復（`tests/25`）

判定を**10回**訂正し、偶数回と奇数回で結末が入れ替わることを毎回検査する。
**1回だけではどちらか片方の経路しか通らない。**

ここで**自分の期待が間違っていた。** 「有効な判定は常に1件」と書いたが、
`tests/03` が「深さ2は復活する」と明示している。2回訂正すると
**元の行と最新の行の両方が有効になる**のが設計である。

期待のほうを直した。そのうえで
**「件数で数えると二重になる」ことをテストとして残した。**
`v_application_outcome` も `dashboard` も `EXISTS` で判定しているので
いまは影響が無いが、「どのステップで落ちたか」を `count(*)` で数えると二重になる。
**罠が実在することを、落とし穴の形のまま置いておく。**

## 出した判断と、その実行（C-33）

`REPORT-7.0` で挙げた穴に手を入れた。

> **4段のうち3段に評価軸が1本も無いので、点を付けずに確定できる。**

### 止めない、を選んだ

素直な直し方は「軸が0本の段は確定できない」だが、**採らなかった。**

3段が該当するので、**選考そのものが1件も進まなくなる。**
書類選考の軸は呼び名が未確定で凍結中（D-14）であり、
止めれば**実データを入れた瞬間に業務が止まる。**
優先順位1（実運用で業務が止まる問題）を、優先順位2を直すために作ることになる。

### 代わりに、事実を画面に出した

運転席の「評価の進捗」は、軸0本のときに `0 / 0 軸` ＋
「評価待ちの軸はありません」と出していた。
**「全部付け終わった」と区別が付かない。** 分けた。

```
軸がある    3 / 6 軸           次に評価できる: 最終面接
軸が0本     評価の観点が未登録   この段は点を付けずに確定できます
```

隠して辻褄を合わせない ―― **丸め込みは欠陥そのものを隠す**（実行⑥の教訓）。

### テストで固定した

**軸が0本の段の顔ぶれ**（応募受付・書類選考・グループ面接）を固定してある。
軸を足すとこのテストが落ちる。**落ちたら画面の分岐も一緒に見直す** ――
「記録が実装より厳密に見える」を作らないため。

## Operator Value

**運営にとって増えた価値は1つだけである** ――
「点を入れ忘れた」のか「そもそも点を付ける項目が無い」のかが、画面で区別できる。

残りは全部こちら側の話（テストが残った）であって、
**できる仕事は1つも増えていない。**

## Validation Report

### 誰が検証したか

**開発者だけ。運営は誰も触っていない。** Validated Operator Problem は**0件のまま。**

### 確認した内容

| 何を | どうやって | 結果 |
|---|---|---|
| 選考が20通りの道で壊れないか | `tests/23`。毎周6つの不変条件 | 20周とも成立 |
| 量に対する伸び方 | `tests/24`。100人と300人、絞り込みの有無を両方 | 桁で跳ねない。絞っても遅くならない |
| 訂正の往復 | `tests/25`。10回。結末が毎回1つに決まるか | 成立。**期待のほうを1つ訂正した** |
| 軸0本の段の扱い | `tests/23`。顔ぶれを固定 | 3段で確定が通る（**いまの事実として固定**） |
| 画面が壊れていないか | `tsc` / `pnpm build` / 全301件 | 通過 |

### 未検証のケース

- **実データでの1周**（生年月日が無い。`REPORT-7.0` の A 段）
- **日をまたぐ挙動。** 滞留と SLA は同日の検証では現れない
- **画面の文言が運営に通じるか。** 軸0本の表示も**開発者が読んだだけ**
- **公開環境での書き込み**（デモモードは保存されない）

### 今後確認すべきケース

- 「評価の観点が未登録」を見た運営が、**誰に言えばいいか分かるか**
- 20周のうち、実運用で本当に起きる道はどれか（いまは機械が選んでいる）

## Remaining Friction

`REPORT-7.0` の5件から、1件が形を変えた。

| # | 何が | 実行⑦での状態 |
|---|---|---|
| 1 | 生年月日が無い | **変わらず。1人も入らない** |
| 2 | 保留にする経路が無い | **塞いだ**（C-35。運転席と候補者の画面） |
| 3 | 軸0本の段で点なし確定 | **止めないと決めた。画面に出した**（C-33） |
| 4 | チャネル0件だと接点が入らない | **変わらず** |
| 5 | 担当の履歴が無い | **変わらず** |

## Technical Debt

`REPORT-7.0` の4件のうち1件を返した。

- ~~10周と10人×3回が使い捨て~~ → **`tests/23` 〜 `25` として残した**
- 色に対する自動テストが無い（CSS の静的解析になり `node:test` の枠外）
- `getCommunityMap` にテストが無い
- デモモードのコールドスタートが約12秒

**新しく1件増えた** ―― `tests/24` の伸び方のしきい値（10倍・5倍）は
手元の1台で決めた値である。遅い機械では偽陽性になりうる。

## Risks

- **`tests/23` は乱数で道を選ぶ。** シードは固定してあるが、
  **シードを変えると通らない道が残る。** 20周は全数ではない
- **`tests/24` は時間を測る。** 機械の負荷で揺れる。落ちたときに
  「本当に遅くなった」のか「機械が忙しかった」のかは、そのままでは分からない
- **軸0本の表示は、問題を可視化しただけで解いていない。**
  運営が見て「これでいい」と流す可能性がある

## Recommendation

**`REPORT-7.0` の推奨（Option B: 観測を先にやる）を変えない。**

実行⑦で足したのは全部**こちら側の確度**であって、
**観測0件というブロッカーには1ミリも効いていない。**
テストを301件に増やしても、運営が使っていないことは変わらない。

次にやるべきことは、**運営4名の日程を取ること**である。
これは私には決められない。

日程が決まるまでの間にできることとして、優先順位が高い順に3つ。

1. **`hold` コマンド**（優先順位1: 実運用で業務が止まる）。
   保留にできないのは、`unhold` があるのに片道しか無いという不整合でもある
2. **`getCommunityMap` のテスト**（Technical Debt）
3. **担当の履歴**（C-22。上書きで消える値がある）

**1を推す。** 実装量が小さく、`unhold` という手本が既にある。
「解けるのに保留にできない」は、運営が最初に踏む段差になりやすい。

### 実行した（C-35）

**1をその場で実装した。** `src/commands/hold.ts` と、運転席・候補者の画面。
テストは 301 → **308 件**。

途中で1つ分かったことがある。**最初は運転席にだけ置いたが、届かなかった。**
運転席のコンテキスト欄は**先頭のやることしか出さない**（`tasks[0]`）ので、
先頭が「評価する」でない限りボタンに到達できない。
デモの4年度すべてで先頭が別の種別だったため、**実際に到達不能だった。**
候補者の画面にも置いて解決した。

**「画面に出した」と「操作できる」は別である。** DOM に文字列があることを
確認しただけでは足りず、実際に押して `?hold=held` に戻り、
運転席のやることが「評価する」から「保留を解く」に変わることまで見た。

## Confidence

| 対象 | 確度 | 根拠 |
|---|---|---|
| 選考の1周が壊れていないこと | **高** | 301件。20周＋訂正10往復＋不変条件6つ |
| 量が増えても持つこと | **中** | 300人までしか測っていない。しきい値は手元の1台で決めた |
| 軸0本の表示が伝わること | **低** | **開発者が読んだだけ** |
| 実データが入ること | **低** | 変わらず。生年月日が無い |

---

## 残課題の4分類

### Validated Operator Problem

**0件。** 変わらず。

### Developer Hypothesis

`REPORT-7.0` の7件から1件減り（軸0本は画面に出した）、1件増えた。

1. 「評価の観点が未登録」を見ても、運営は誰に言えばいいか分からない **（新規）**
2. 運転席のコンテキスト欄は先頭のやることしか出さない **（新規）**
3. 「連携先」という語が通じない可能性がある
4. 担当を替えたとき、前の担当が消えることに気づけない
5. 森と流入元が全部0だと、壊れていると誤解される
6. デモの初回12秒を「固まった」と読む
7. 実行⑥から持ち越しの7件

### Technical Debt

3件（1件返却、1件新規）＋ 実行⑥から持ち越しの12件。

### Product Backlog

実行⑥から持ち越しの4件。実行⑦では増やしていない。

---

## Pilot の判定

**HOLD のまま。基準は緩めない。**

テストを 289 → 301 に増やしたことは、**Go の条件を1つも満たさない。**

| # | Go の条件 | 現状 |
|---|---|---|
| 1 | 実データで1周 | **未達**（生年月日が無い） |
| 2 | 開発者以外の運営が使った | **未達** |
| 3 | 観測報告のレビュー | **未達** |
| 4 | Validated の分類と優先順位 | 部分的（Validated 0件） |
