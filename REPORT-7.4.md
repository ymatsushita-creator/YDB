# REPORT-7.4 — 自己改善サイクル（週3・週4）。シミュレーション Go に到達

実行⑦の第5報。2026-08-08。**この文書は凍結する。**
`REPORT-7.3.md`（週2）の続き。

---

## ★ これは Pilot Go ではない

この文書で「Go」と呼ぶのは、**このシミュレーション自己改善サイクル専用に
私が設定した、技術的な確度の指標**である。`docs/pilot/EXIT_CRITERIA.md` の
Pilot Go とは**別物**であり、これが出ても Pilot の判定は動かない。

> Pilot の Go は、運営が実際に使って初めて満たせる（Pilot Rule）。
> シミュレーションのペルソナは架空で、何回回しても Validated Operator
> Problem を1件も作れない。

混同を避けるため、以降「**シミュレーション Go**」と書く。

## シミュレーション Go の条件（`REPORT-7.3` の依頼で設定）

直近の週で、次の4つが**すべて**成立したら「シミュレーション Go」とする。

1. 小規模・大規模それぞれで「気づき（想定外の異常）」が0件
2. 採点官ペルソナで画面の言葉を読み直し、新たな食い違いが0件
3. 不変条件（二重計上0・根拠が空0・満点超過0・合格数の一致）が両規模で成立
4. `pnpm test` 全通過・`tsc --noEmit` クリーン

1つでも欠けたら、見つけた問題を直し、次の週を回して再判定する。

## 週3 —— 条件2で止まった

```
小規模: 応募受付30 → 書類選考29 → グループ面接24 → 最終面接15 / 合格9 / 辞退1
大規模: 応募受付40 → 書類選考39 → グループ面接24 → 最終面接19 / 合格14 / 辞退1
不変条件: すべて成立 / 気づき（異常）: 0件
```

条件1・3は満たしたが、**条件2で止まった。**
週3ではシミュレーションの実行と並行して、**画面全体を採点官の目で読み直す
範囲を広げた**（それまでは触れた命令の結果コードだけを読んでいた）。
その結果、**8件**の同種の問題が見つかった。

### 内部の識別子が、そのまま画面に出ていた（8件）

`REPORT-7.2` の3件、`db/DECISIONS.md` C-32 の9件に続く、**同じ種類の
第4のまとまり**である。関数名・ビュー名・設計原則の番号が、
説明の中に生のまま埋め込まれていた。

| 画面 | 直す前 | 直した後 |
|---|---|---|
| `/applications/[id]` | 打ち消し行として履歴に残る**（原則5）**。 | 訂正しても打ち消しの記録として残る。 |
| `/applications/[id]` | この解決は `v_effective_status_histories` が行っており… | この判定はシステムが自動で行っており… |
| `/people` | 定義は集計側（`f_person_season_state()`）にあり… | 定義は集計の仕組みが決めており… |
| `/people/[id]` | 日付境界は `jst_date()` を通しており… | 日付境界も同じタイムゾーンで揃えており… |
| `/people/[id]` | `f_person_season_state({ACTIVE_WINDOW_DAYS})` の値をそのまま出しており… | 直近 {ACTIVE_WINDOW_DAYS} 日以内に接点があるかで集計側が決めており… |
| `/forests/[id]` | この森の数に含めている（`v_partner_forest` が畳む）。 | この森の数に含めている（同じ森に属する林をまとめて数える）。 |
| `/sources` | `estimated_reach` は推定値であり… 日付境界は `jst_date()` を通している。 | 推定リーチは推定値であり… 日付境界は同じ運用タイムゾーンで揃えている。 |
| `/`（ファネル） | `jst_date()` を通している。訂正された遷移は `v_effective_status_histories` で解決済み。 | 同じ運用タイムゾーンで揃えている。訂正された遷移はシステムが自動で解決済みにしている。 |

**なぜ今まで見つからなかったか。** C-32（実行⑦前半）は
「3人の採点官のつもりで文言を全部読んだ」としていたが、実際には
**コマンドの結果コード（`ASSIGN_CODE_MESSAGE` などの短文）を中心に読んでいた。**
各画面の `footnote`（脚注として長めに説明している段落）は
今回の自己改善サイクルで初めて同じ密度で読んだ。**読む範囲が
広がって初めて見つかった**、というのが実情である。

同じ理由（原則の直接参照）は開発者向けの docstring コメントには残した。
**コードコメントと画面表示は別**で、後者だけを直す。

## 週4 —— 4条件すべて成立。シミュレーション Go

```
小規模: 応募受付30 → 書類選考29 → グループ面接22 → 最終面接16 / 合格13 / 辞退1
大規模: 応募受付40 → 書類選考40 → グループ面接30 → 最終面接18 / 合格15 / 辞退0
不変条件: すべて成立 / 気づき（異常）: 0件
```

週3で直した8件を含めて全画面を再度スイープし、**新しい漏れは0件だった。**

- 条件1: 小規模・大規模とも異常0件 ✓
- 条件2: 新たな食い違い0件（全画面スイープで確認） ✓
- 条件3: 不変条件、両規模で成立 ✓
- 条件4: `tsc --noEmit` クリーン、テスト308件全通過 ✓

**4条件すべて成立。シミュレーション Go。**

## Validation Report

### 誰が検証したか

**開発者だけ。** 週3・週4とも同じ。Validated Operator Problem は
実行⑦を通じて**0件のまま**である。**シミュレーション Go はこの数字を
1件も動かさない。**

### 確認した内容

- 週3で見つけた8件の画面文言を、実際に `tsc` とテスト308件を壊さずに直したこと
- 週4で同じ範囲を再度読み、新しい食い違いが出ないことを確認したこと
- 4回目・5回目・6回目・7回目（週1〜4の小規模＋大規模、実質8回）の
  シミュレーションで不変条件と `ok` 判定が一度も破れていないこと

### 未検証のケース

`REPORT-7.2`・`7.3` から変わらない。**実在の採点官がこの文言で
迷わないかは、シミュレーションでは検証できない。**

## Technical Debt

`REPORT-7.3` から持ち越し（乱数の質）に加えて増減なし。

## Recommendation

依頼により、**もう一度サイクルを回す**（週5・週6、`REPORT-7.5.md`）。
シミュレーション Go に達したあとの1サイクルは、**安定していることの
再確認**という位置づけになる ―― 新しい修正が無い前提で回し、
本当に何も出ないかを見る。

`REPORT-7.1` の推奨は変わらない。**次にやるべきことは運営4名の日程を
取ることである。** これは私には決められない。

## Confidence

| 対象 | 確度 | 根拠 |
|---|---|---|
| 選考が架空の量と分岐で壊れないこと | **高** | 週1〜4、8回のシミュレーションで不変条件が一度も破れていない |
| 画面の内部識別子の漏れを見つけ切ったこと | **中** | 全 `page.tsx` を機械的にスイープしたが、動的に組み立てる文字列までは辿っていない |
| 実運用に効いたこと | **無し** | Validated Operator Problem は0件のまま |

---

## Pilot の判定

**HOLD のまま。基準は緩めない。**

| # | Go の条件 | 現状 |
|---|---|---|
| 1 | 実データで1周 | **未達**（生年月日が無い。`REPORT-7.0`） |
| 2 | 開発者以外の運営が使った | **未達** |
| 3 | 観測報告のレビュー | **未達** |
| 4 | Validated の分類と優先順位 | 部分的（Validated 0件） |

**シミュレーション Go は、この4条件のどれも動かしていない。**
