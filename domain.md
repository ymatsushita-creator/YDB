# domain.md — 用語と記録モデル

本書は現行ドメイン語彙の唯一の正典である。

## 用語

| 用語 | 定義 | 単位 |
|---|---|---|
| 期 | 1回の募集。1期・2期…と運営が数える。**年から導かない**（募集を休んだ年があると番号がずれる） | 期 |
| アプローチ可能圏 | 継続的に候補者へ接触できる大学、団体、企業、イベント、紹介ネットワーク等 | 圏 |
| 構成コミュニティ | アプローチ可能圏の内側にある具体的な団体・グループ | 組織 |
| 候補者 | 個人として識別された人 | 人 |
| 接点 | 候補者との接触事実 | 件 |
| 応募 | 候補者が期の選考へ応募した事実 | 応募件数 |
| 合格 | 応募が最終選考を通過した事実 | 応募件数 |
| アプローチ状態 | 未アプローチ、検討中、アプローチ中、面談調整中、見送り | 人×期 |
| 予定 | これから会う日時。**接点（起きたこと）とは別** | 件 |
| やること | 人が作るものと、選考の記録から導くものの2種 | 件 |

**学年という概念は無い。** 候補者は期で数える。

旧生態系比喩は現行UI・現行仕様では使用しない。
適用済みSQLの `forest` / `community` 識別子はDB互換性のため残る
**内部の旧識別子**であり、ユーザー向け用語ではない。

### 旧語との対応（読むためだけの表）

比喩は廃止したが、**コードのコメント・SQL・テスト名には残っている**
（`森` 179 / `林` 152 / `木` 56 箇所。2026-08-19 実測）。
新しく書くときは使わない。**読むときにここへ戻る。**

| 旧語 | 現行語 | 実体 | 典拠 |
|---|---|---|---|
| 森 | アプローチ可能圏 / 構成コミュニティ | `schools`（旧識別子 `forest`） | C-45、`src/queries/cockpit.ts` |
| 林 | 接点継続中 | 直近 `active_window_days` 日以内に接点がある候補者（`identified_person_cum`） | D-3、`db/migrations/0002_views.sql` |
| 木 | 応募 | その年度に応募した候補者 | D-3、`src/queries/dashboard.ts` |
| 幹 | 合格 | 最終選考ステップへの有効な advance（`accepted`） | `db/migrations/0002_views.sql` |

- **林・木・幹の3段は人を数える。森は単位が違う**ため同じファネルに含めない
  （`db/migrations/0002_views.sql`）。
- 「種」は比喩ではない。コード中の出現はすべて「種類」「一種」等である。
- 同じ `森` が組織階層（`森 → 林 → 人` のズーム）とファネルの両方で使われていた。
  **この二重性が比喩を廃止した理由である**（C-45）。

## 関係

```text
アプローチ可能圏
└── 構成コミュニティ
    └── 接点 ── 候補者 ── 応募 ── 評価・判定
```

候補者が圏やコミュニティに「所属する」とは限らない。
現在記録できるのは、その組織を介した接点があった事実である。

## 候補者プロフィール

現在値は `persons`、変更後スナップショットは
`person_profile_revisions` に保存する。編集可能項目は次のとおり。

- 氏名・かな
- 生年月日
- 学校・学部学科
- メール・電話・LINE ID
- 紹介者
- 担当者メモ
- 顔写真
- アプローチ状態（別の追記履歴）

確度、順位、成績、最終接点は導出値であり直接編集しない。

## 数え方

- 推定リーチは接触機会の推定値であり、人数ではない
- 接点継続中は指定日数内に接点がある実人数
- 応募と合格は応募件数
- 単位が違う値を割らない

## 内部互換性

適用済みマイグレーション、SQLビュー、凍結報告書は改名しない。
新しいコードと画面では現行語彙を使い、境界で旧識別子を読み替える。
