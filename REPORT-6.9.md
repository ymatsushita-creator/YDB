# REPORT-6.9 — 判定の訂正（実運用フェーズ 1件目）

2026-08-07。**PM Report + Validation Report**（この版から後者を追加）。
`REPORT-6.0` 〜 `6.8` は凍結済み。**この報告書も書いた時点で凍結する。**

---

## Validation First — この改善は観測された問題か

**開発者が1周を通したときに観測した「経路の欠落」である。**

`REPORT-6.8` の1周検証で「通過にする」を押したあと、**画面から戻る手段が
無い**ことに気づいた。使っていて分かったことなので想像ではない。

**ただし、実際の押し間違いは観測していない。** 「間違って押す運用が
どのくらい起きるか」は分からない。運営担当者による検証も済んでいない。

**優先順位での位置** ―― #2（データの整合性を壊す問題）。
誤った判定が記録に残り続け、直す手段が無い状態だった。
#1（業務が止まる）にも接する ―― 誤って不合格にすると、その応募は
やることから消えて**運用上そこで終わってしまう。**

---

## Goal

**押し間違いを画面から戻せなかった。**

判定は `status_histories` への追記で、追記専用（0003）である。訂正は
打ち消し行の追記でしか表せない。**記録層はその仕組みを最初から持っていたが、
画面に入口が無かった。**

---

## What Changed

| | 前 | 後 |
|---|---|---|
| 判定を間違えたとき | **戻せない** | 応募の画面から差し替えられる（通過↔不合格） |
| 不合格を通過に直したとき | — | **次のステップの担当を決める段が立ち上がる** |
| 通過を不合格に直したとき | — | やることが消える（結末が `rejected` になる） |
| 履歴の見え方 | （既にできていた） | 元の判定に取り消し線、訂正行に「訂正」 |

**新しい概念は1つも足していない。** 既存の `corrects_history_id` /
`is_correction` / `v_effective_status_histories` に入口を付けただけである。
履歴の表示も最初から訂正を扱えていた（C-12）ので、そこは触っていない。

---

## Operator Value

1. **押し間違いを直せる。** 通過↔不合格を差し替えられる
2. **直した先で手が止まらない。** 不合格→通過に直すと、次のステップの
   「担当を決める」が運転席に現れる。**直しただけで放置されない**
3. **何を直したかが残る。** 元の判定は消えず、取り消し線つきで履歴に並ぶ。
   訂正の理由も書ける
4. **往復しても壊れない。** 通過→不合格→通過と直しても、次のステップの
   評価行は1つのままである

### 実データで確認した往復

```
柏木朔 / 二次面接（通過済み）
  → 「不合格」に訂正   … 履歴に取り消し線が付き、訂正行が並ぶ
  → 「通過」に訂正      … 最終面接の軸が「先に担当を決める」に戻る
  → 運転席            … 「担当を決める / 4. 最終面接」が並ぶ。やること 26 のまま
```

---

## Remaining Friction

1. **判定を「無かったこと」にはできない。** 表せるのは差し替えだけである。
   訂正行そのものが有効な遷移になるため、「判断待ちに戻す」が書けない。
   誤って判定してしまい**まだ決めたくない**場合の逃げ道が無い
2. **点は直せない。** `already_scored` で弾かれる（次の候補）
3. **辞退を記録できない。** 合格後の辞退は必ず起きる
4. **確認ダイアログが無い。** 訂正で戻せるようになったので危険度は下がったが、
   ボタンは1クリックで効く
5. 申し送りが書けない／新規応募の登録口が無い

---

## Technical Debt

**新しく増えたのは 14 と 15。**

| # | 内容 |
|---|---|
| 1〜6 | 森・林の分割 / 手で作るタスク / 種別の重み / 所属と接触 / Health・Owner / 60人打ち切り |
| 7 | 担当を決めた・解いた・替えた記録が残らない |
| 8 | 利益相反の組み合わせを選ぶ前に警告していない |
| 9 | 軸の適用規則が2箇所にある |
| 10 | 記録層の拒否を制約名とトリガの文面で写している |
| 11 | 点の訂正ができない |
| 12 | ~~判定の訂正が画面に無い~~ **→ 返済（C-27）** |
| 13 | 全軸そろう前の確定を認めていない |
| **14** | **判定を「無かったこと」にできない**（差し替えしか表せない） |
| **15** | **通過→不合格に直すと、次のステップの評価行が宙に浮く。**結末で絞っているので数字は狂わないが、行は残る |

---

## Risks

### 1. 訂正が「差し替え」でしかないことが運用に合わない可能性（未検証）

「まだ決めたくない」場合に戻す先が無い。誤って通過にしたとき、
不合格へ差し替えるしかない ―― **本当は判断待ちに戻したい**はずである。
運用で起きるかどうかは分からない。**起きたら記録層の設計判断になる**
（`transition_type` に何を足すか）。

### 2. 宙に浮いた評価行（負債 15）

通過→不合格に直すと、次のステップの評価行が残る。集計はすべて結末で
絞っているので数字は狂わないが、**将来「評価行を数える」集計を書いたときに
効く。** 書くときに気づけるよう記録した。

### 3. 訂正は1クリックで効く

確認ダイアログが無い。訂正の訂正ができるので致命的ではないが、
**訂正が連鎖すると履歴が読みにくくなる**（深さの偶奇で有効性が決まるため、
人が目で追うのは難しい）。

---

## Recommendation

### Option A — 実運用で最も大きなリスクを減らす

**辞退（`withdraw`）を記録できるようにする。**

- **Operator Value**: 合格者の辞退は**必ず起きる。** いま記録する手段が無く、
  辞退した人が「合格のまま」残る。定員の充足率が実際より高く見える ――
  **数字が嘘になる。** 席が空いたことも分からない
- **Implementation Cost**: 小。`status_histories` に `withdraw` を1行追記する。
  理由が必須（0008）なので理由の選択欄が要る。`withdraw_reasons` は既存。
  判定と同じ型がそのまま使える
- **Risk**: 低。追記のみで、既存の集計（`v_application_outcome` は
  `withdraw` を最優先で解釈する）が最初から対応している
- **Recommendation**: **これを推す。** 「数字が嘘になる」のは優先順位 #2 で、
  かつ実運用で確実に起きる。想像ではない

### Option B — 最も大きな運営価値を追加する

**点の訂正（E4）を入れる。**

- **Operator Value**: 打ち間違いを直せる。面接中の入力なので、
  **打ち間違いは辞退より頻繁に起きるはず**である
- **Implementation Cost**: 中。上書きにするか打ち消しの追記にするかを
  決める必要がある。スコアは「再現不能なものだけ凍結する」（原則2）の対象で、
  `evaluation_scores` は追記専用ではないため上書きは可能。
  **どちらを選ぶかが設計判断になる**
- **Risk**: 中。上書きにすると「元は何点だったか」が消える。
  評価の履歴は②（誰が何を評価したか）の中身なので、消すと答えが弱くなる
- **Recommendation**: Option A のあと。**設計判断を1つ含むので、
  観測を待つ価値がある** ―― 実際にどのくらい打ち間違えるかが分かってから
  上書きか追記かを決めたい

**私の推奨は Option A（辞退）である。** 最終的な優先順位はご判断に委ねる。

---

## Confidence — 85%

**高い理由。**

- 往復（通過→不合格→通過）を実データで通し、DOM で確かめた
- 次のステップが二重に作られないことをテストと実データの両方で確認した
- 古い画面から押した形（`history_id` が食い違う）を弾くことをテストで固定した
- テスト **272 件** 全通過、`tsc` クリーン、`'use client'` 0件

**85% に留める理由。**

- **実際の押し間違いを観測していない。** 直す形が運用に合っているかは未検証
- 「判断待ちに戻したい」場合に応えられない（負債 14）
- 運営担当者が触っていない

---

# Validation Report

## この改善は誰が検証したか

- [x] **開発者のみ**
- [ ] 運営担当者
- [ ] 実際の採用で検証済み

**運営担当者による検証は行われていない。** 以降のサイクルも、
運営が触るまでこの欄は「開発者のみ」のままになる。

## 実データで確認した内容

デモデータ（2027年度・柏木朔・二次面接、面接官2人）で以下を DOM の値で確認。

| 操作 | 確認したこと |
|---|---|
| 「不合格」に訂正 | 通知が出る／履歴に取り消し線と訂正行が並ぶ／やることが消える |
| 「通過」に訂正（戻す） | 最終面接の軸が「先に担当を決める」に戻る |
| 運転席 | 「担当を決める / 4. 最終面接」が並ぶ。やること **26 のまま**（二重に増えない） |
| 訂正の欄の見出し | 「いまの判定は『通過』（二次面接・佐藤 咲良）」と、直す対象が明示される |

テストで固定した経路 ―― 不合格→通過で次ステップが立つ／通過→不合格で
やることが消える／元の記録が残る／往復しても評価行は1つ／古い
`history_id` は弾く／未判定は訂正できない／削除された人は訂正できない／
訂正した人が選ばれていなければ記録しない。

## 未検証のケース

- **運営担当者が実際に押し間違え、それを直す**という一連の流れ
- **訂正が3回以上連鎖したとき**の履歴の読みやすさ（深さの偶奇で有効性が
  決まるので、人が目で追えるかは分からない）
- **複数人が同時に同じ応募を触ったとき。** `history_id` の食い違いは弾くが、
  実際に2人が同時に開いている状況は再現していない
- 最終ステップの判定を訂正したとき（合格→不合格→合格）。
  テストは第1ステップで書いた
- 個人情報削除と訂正が同じ日に起きる順序

## 今後確認すべきケース

1. **辞退が発生したときに何が起きるか。** いま記録できないので、
   運営はどこかに別途書くことになる。**その回避策を観測したい**
   （どこに書いているかが、次に作るものを決める）
2. **点の打ち間違いの頻度。** Option B の設計判断（上書きか追記か）は
   これが分かってから決めたい
3. **判定を「判断待ちに戻したい」場面が実際にあるか。** あれば記録層の
   設計判断になる（負債 14）
4. **1軸ごとの再読み込みが面接中に許容できるか。** 許容できなければ
   クライアント境界の判断が必要になる
