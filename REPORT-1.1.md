# YouthDB 作業報告 1.1（補足）

2026-08-06

`REPORT-1.0.md` の補足。1.0 は送付済みのため内容を凍結し、
その後の作業をこちらに分ける。1.0 だけでも、1.0 → 1.1 と続けても読める。

---

## 0. なぜ補足を分けたか

1.0 を送付したあと、私はその同じファイルに追記した。送ったものと
リポジトリにあるものが食い違う状態を自分で作っていた。

このプロジェクトが一貫して警戒してきたのは「事実と記録の乖離」である。
1.0 の第4節では、私自身がその種の失敗を4件報告している。報告書の扱いで
5件目をやっていた。

報告書は特定時点の文書であって、更新するものではない。
1.0 は送付時点の内容に戻し、以降は版を分ける。

---

## 1. 1.0 送付後に受けた指示と対応

「実装指示書 — 2026-08-06 レビュー反映分」への対応。

なお指示により、1.0 の 3-1（タイムゾーン依存）・3-3（訂正チェーンの循環）・
4-4（コメントの嘘）は**基本設計側の欠陥として認められ、`basic/` 側に反映**される。
実装側での追加対応は不要となった。

| 指示 | 対応 |
|---|---|
| 1. 未記録差分の追記（最優先） | `DECISIONS.md` に E 節を新設 |
| 1. マイグレーション記録のトランザクション外（最優先） | `BEGIN`/`COMMIT` で包み、`ROLLBACK` も追加 |
| 2. 参照データの本番/サンプル分離 | `*.example.sql` を本番シードから除外 |
| 3. ファネル起点・単位混在・アクティブ判定 | 仕様として確定。実装変更なし |
| 4-1. 単位混在の注記 | 段のカードに常設 |
| 4-2. 日次の林→木転換率 | 削除 |
| 4-3. 年度サマリ指標 | 新設。暫定/確定を切り替え |
| 5. 辞退理由の必須化 | `0008` で制約追加 |

### 1-1. 未記録差分（`DECISIONS.md` E 節）

1.0 の 4-2 で報告した「記録が実装より厳密であるかのように書かれていた」への対応。

記録していなかった差分は次のとおり。式インデックス7本、CHECK 制約2本、
自己訂正を禁じる CHECK、`v_application_state.is_voided` 列、
`f_partner_reach_summary` の削除済み Person 除外、原典コメントの写し漏れ、
原典が述べているのに担保していなかった意図2件。

このうち `f_partner_reach_summary` の変更だけは**集計値が変わる**ものだった。
森の `identified_count` から削除済み Person を外している。ファネルの林も
生涯サマリもチャネル別もすべて外しており、森だけ残すと個人情報削除の依頼が
そこから漏れるため足したのだが、C-2 には「窓を引数にした」としか書いていなかった。

**あわせて、宣言が守られているかを機械が検査するようにした。**
`DECISIONS.md` は冒頭で「各項目の末尾に、その判断を固定しているテストを示す」と
宣言している。B 節では11個の制約に対して末尾に1行だけテストを挙げており、
実際にあったのは5件だった。参照が嘘だと、読んだ人は検証済みと思って確認をやめる。
記録が無いより悪い。

不足していた6件のテストを書き、B 節の表に列を足して制約1行ごとに示した。
そのうえで `tests/10_decisions_references.test.ts` が `DECISIONS.md` を読み、
挙げられているテストが実在するかを検査する。存在しない参照を入れると落ちることも
確認してある。人が見直す運用に戻すと同じことが起きるので、機械に見せる。

### 1-2. マイグレーションの原子性

適用（`db.exec`）と記録（`INSERT INTO schema_migrations`）が別トランザクション
だった。間でプロセスが落ちると、スキーマは変わっているのに記録が無い状態になり、
次回以降 `migrate()` が同じ位置で `already exists` を出し続ける。
手で `schema_migrations` を埋めるまで復旧しない。

`BEGIN; …; COMMIT;` で包んだ。あわせて2つの検出を足した。

- 適用済みなのにファイルが無い（誤って消した、別ブランチを見ている）
- 適用済みより前に並ぶ新しいファイル（番号の採り違え）

どちらも従来は素通りしており、手元と本番でスキーマの作られ方が静かに分岐しえた。

### 1-3. 参照データの分離

集計に関わるマスタは追加と非活性化でしか運用できない（原則3）。
つまり**最初に入れた値が事実上の初期値として固定化される**。
サンプルと本番が同じ入口から入る構造だと、誰かが一度流した時点で
私の創作物が正式なマスタになる。

- `db/seeds/0001_reference.example.sql` … 動作確認用。本番では読まない
- `db/seeds/0001_reference.sql` … 本番用

本番の中身は `channels` 0件、`void_reasons` 0件、`withdraw_reasons` 1件
（`unconfirmed` = 未確認）のみ。この行数はテストで固定してある。

### 1-4. 辞退理由の必須化

`transition_type = 'withdraw'` のとき `withdraw_reason_id` を NOT NULL にした。

NULL を許すと「まだ聞けていない」「聞いたが答えなかった」「記録し忘れた」が
すべて同じ NULL になり、分布の合計が辞退件数に届かない理由を説明できなくなる。
理由不明は「理由が無い」ではなく「未確認という事実がある」と捉える。
「未確認」が分布のどれだけを占めるかは、そのまま運用の質を表す。

### 1-5. 表示の修正

- **単位混在の注記**を段のカードに常設した
- **日次の 林 → 木 転換率を削除**した。日次の林はローリングウィンドウ、
  日次の木は年度累積で、母集団の定義が日ごとに違う
- **年度単位の 林 → 木 転換率を新設**した。分母は募集期間中に一度でも接点が
  あった Person の実人数、分子はその年度に応募した Person の実人数。
  選考完了日を過ぎるまでは「暫定」、過ぎたら「確定」と表示する
- **`active_window_days` の 90 日が仮の値**であることを、林の推移カードと
  フッタに明記した

---

## 2. 対応中に見つかった欠陥

### 2-1. トランザクションを閉じ忘れると、失敗理由が別のものに化ける

1-2 を実装した直後、書いたテストが別の欠陥を検出した。

明示 `BEGIN` の中で失敗すると、トランザクションは**中断状態のまま開いている**。
`COMMIT` にも `ROLLBACK` にも到達していないため、同じコネクションでの
以降のクエリはすべて `current transaction is aborted` で弾かれる。
呼び出し側は本当の失敗理由を見失う。

`ROLLBACK` を足した。あわせてテストを「`migrate()` の組み立てを文字列で
再現する」形から「壊れたマイグレーションを実際に `migrate()` に食わせる」形に
変えた。前者だと実装が変わってもテストは通り続ける。

### 2-2. チャートの折れ線が枠外に切れていた（1.0 の 3-6）

指示外だが、1.0 で報告済みのものとして直した。目盛り生成が軸の上端を
データ最大値以下に丸めており、上の点が `y < 0` に落ちて SVG の
`overflow: hidden` で切れていた。24パターンで上端 ≧ 最大値を検証し、
全4年度 × 2枚で枠外がゼロになったことを DOM から確認した。

---

## 3. 現在の状態

- テスト **97件** 全通過（1.0 時点は 69件）
- 型チェック クリーン
- マイグレーション 8本（1.0 時点は 7本）
- 未コミットの変更なし

### 1.0 の「未対応」表がどうなったか

| 1.0 の項目 | 現在 |
|---|---|
| `DECISIONS.md` の未記録差分10件の追記 | 完了（E 節） |
| B節の欠けているテスト6件 | 完了（`tests/09`）＋参照の機械検査 |
| `README.md` の「何度流しても同じ」の訂正 | 完了 |
| マイグレーションの記録がトランザクション外 | 完了 |
| `ymd()` が本番ドライバで1日ずれる | 見送り（指示6）→ D-9 |
| `getInterviewerLoad` が表示名で集約 | 見送り（指示6）→ D-9 |
| PGlite の dataDir にプロセス間ロックがない | 見送り（指示6）→ D-9 |

---

## 4. 残っている判断待ち

`DECISIONS.md` の D 節にある。1.0 の 6-2 で挙げた5点のうち3点は
指示で確定したので、残りは次のとおり。

### 4-1. `seasons` の実数値（D-8）★要確認

指示で「年度・定員(36)・目標応募数(100)は実数値」と伝えられたが、
**それがどの年度で、4つの日付がいつなのかを確認できていない**。
そのため本番のシードに `seasons` の行を入れていない。

デモの4年度（定員 24/30/36/40、目標 150/220/300/340）はすべて創作である。
年度と `outreach_start_date` / `application_open_date` /
`application_close_date` / `selection_end_date` が分かれば、
定員36・目標100の年度を本番シードに追加する。

### 4-2. 「募集期間」の解釈（D-7）

年度サマリ指標の分母を「募集期間中に一度でも接点があった人」と指示されたが、
募集期間の範囲が明文化されていない。集客期に林が積み上がることと、
応募締切より後に初めて接点を持った人はその年度の応募母集団ではないことから、
`outreach_start_date` 〜 `application_close_date` と解釈した。
違えば `getReachConversion` の1箇所を直せばよい。

### 4-3. マスタの分類（D-5 / D-6）

`channels` と `void_reasons` は本番シードで空にしてある。
実運用の分類が決まってから入れる。`void_reasons` については、
「代替の応募が生まれるか」という規則だけでは決められない場合がある
（テスト送信は代替が生まれないが数えてはならない）。

### 4-4. 未着手（D-9）

- 森（`partner_reaches`）が画面のどこにも出ていない。(4)（流入元）を作るときに使う
- 名寄せ候補の提示と `is_reapplication` の再計算は実装段階[4]

---

## 5. 版の扱い

報告書は更新しない。追記が必要になったら版を上げる。

- `REPORT-1.0.md` … 2026-08-06 送付。凍結
- `REPORT-1.1.md` … 本書

プロジェクトの記録は `db/DECISIONS.md` が担う。こちらは更新してよい。
報告書と記録の役割を混ぜない。
