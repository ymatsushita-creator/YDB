# REPORT-7.5 — 自己改善サイクルをもう一度（週5・週6）。安定を確認

実行⑦の第6報。2026-08-08。**この文書は凍結する。**
`REPORT-7.4.md`（週3・週4。シミュレーション Go に到達）の続き。

依頼により、シミュレーション Go に到達したサイクルを**もう一度**回した。

---

## ★ これは Pilot Go ではない

`REPORT-7.4` と同じ注記を繰り返す。**この文書で言う「Go」は
シミュレーション自己改善サイクル専用の指標**であり、`docs/pilot/EXIT_CRITERIA.md`
の Pilot Go とは別物である。何度回しても Validated Operator Problem は
1件も作れない ―― ペルソナは架空だからである。

## シミュレーション Go の条件（`REPORT-7.3`〜`7.4` から変更なし）

1. 小規模・大規模それぞれで「気づき（想定外の異常）」が0件
2. 採点官ペルソナ＋画面全体のスイープで、新たな食い違いが0件
3. 不変条件（二重計上0・根拠が空0・満点超過0・合格数の一致）が両規模で成立
4. `pnpm test` 全通過・`tsc --noEmit` クリーン

## 週5

```
小規模: 応募受付30 → 書類選考30 → グループ面接20 → 最終面接14 / 合格12 / 辞退0
大規模: 応募受付40 → 書類選考38 → グループ面接27 → 最終面接19 / 合格15 / 辞退2
不変条件: すべて成立 / 気づき（異常）: 0件
```

条件2の確認として、`REPORT-7.4` で使った grep スイープ
（`<code>v_` / `<code>f_` / `（原則` / `（設計原則` / 不正・違反・禁止）を
全 `page.tsx` に対して再実行した。**新しい漏れは0件。**

**4条件すべて成立。**

## 週6

```
小規模: 応募受付30 → 書類選考29 → グループ面接21 → 最終面接14 / 合格12 / 辞退1
大規模: 応募受付40 → 書類選考37 → グループ面接30 → 最終面接21 / 合格18 / 辞退3
不変条件: すべて成立 / 気づき（異常）: 0件
```

**4条件すべて成立。修正は0件だった** ―― 週3で見つけた8件のあと、
週4・週5・週6と3週連続で新しい言葉の食い違いが出ていない。

## このサイクルで分かったこと

**「もう一度回す」ことの意味は、直すことではなく確かめることだった。**
週5・週6は週3のような発見が無く、`tsc` とテスト308件、
不変条件のチェックだけが繰り返された。

これは2つに読める。

1. **見つけるべきものは、週3までにだいたい見つかった。** 画面全体を
   スイープする網は週4で一度張り終えており、そこから漏れる新しい種類の
   問題は今回の6週では出なかった
2. **ただし、これは「もう存在しない」という証明ではない。** シミュレーションの
   ペルソナが読む範囲・書く文章の範囲でしか気づけない。実在の採点官が
   同じ画面を読んだときに何を感じるかは、依然として分からない

## Validation Report

### 誰が検証したか

**開発者だけ。** 週1〜6を通じて変わらない。Validated Operator Problem は
**0件のまま。**

### 確認した内容

- 週5・週6とも、`REPORT-7.4` で確立した4条件が成立すること
- 週3〜4で直した8件の画面文言のスイープを再実行し、新しい漏れが無いこと
- 6週・12回のシミュレーション（小規模6回＋大規模6回）を通じて、
  不変条件と `ok` 判定が一度も破れていないこと

### 未検証のケース

`REPORT-7.0`〜`7.4` から変わらない。**実在の採点官が触っていない**ことは、
6週分のシミュレーションを重ねても埋まらない。

## Technical Debt

変更なし（`REPORT-7.3` の乱数の質のみ、既知のまま）。

## Recommendation

**シミュレーションの自己改善サイクルは、今回で一区切りとする。**

理由は3つ。

1. **シミュレーション Go に到達し、2週にわたって安定を確認した**
   （週4で到達、週5・週6で再現）
2. **これ以上回しても、見つかる問題の種類が尽きている。** 6週・12回を通じて
   条件2（新たな食い違い）に該当したのは週3の1回だけで、それ以降は0件が続いた
3. **`REPORT-7.1`・`7.2`・`7.3`・`7.4` で繰り返した結論と同じ**
   ―― Developer Hypothesis は自分では Validated になれない。
   ここから先の学習量は、運営4名の日程を取ることのほうが大きい

**次にやるべきことは、運営4名の日程を取ることである。** これは私には決められない。

## Confidence

| 対象 | 確度 | 根拠 |
|---|---|---|
| 選考が架空の量と分岐で壊れないこと | **高** | 6週・12回のシミュレーションで不変条件が一度も破れていない |
| 画面の内部識別子の漏れが尽きたこと | **中** | 静的な `page.tsx` は網羅した。動的に組み立てる文字列までは辿っていない |
| 実運用に効いたこと | **無し** | Validated Operator Problem は6週を通じて0件のまま |

---

## Pilot の判定

**HOLD のまま。基準は緩めない。**

| # | Go の条件 | 現状 |
|---|---|---|
| 1 | 実データで1周 | **未達**（生年月日が無い。`REPORT-7.0`） |
| 2 | 開発者以外の運営が使った | **未達** |
| 3 | 観測報告のレビュー | **未達** |
| 4 | Validated の分類と優先順位 | 部分的（Validated 0件） |

**シミュレーション Go は6週にわたって安定したが、Pilot の4条件は
実行⑦の開始時から1つも増えていない。**
