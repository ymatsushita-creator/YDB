# YouthDB — このプロジェクトで何を達成するのか

起業家アカデミーの**集客から選考までを一元管理する**データベースと、その上のダッシュボード。

## 1. 達成すべきこと（判断に迷ったらここへ戻る）

**担当者が誰であっても、候補者が誰であっても、次の4つが一発で、直感的に分かること。**

1. **誰がどんな状態か** — その人はいま林か、木か、幹か。どの年度の話か
2. **誰が何を評価したか** — どのステップで、誰が、何を根拠に判断したか
3. **いま何が起きているか** — 誰の判断待ちか、どこで滞留しているか、何が保留か
4. **次に何をすべきか** — 割り当て、催促、連絡、判断のうち、次の一手はどれか

この4つに答えられない機能は、どれだけ作り込んでも価値がない。
逆に、この4つに効くなら地味な表でよい。

## 2. この開発は選考プロセスドリブンである

**UI の充実やデザインの良し悪しが主題ではない。**

主題は、**システムとデータベースの構造そのものが、集客から選考までの一連の流れを
「評価し、計画を考え、実行する」に足りうるものになっているか**である。

だから優先順位は常にこうなる。

```
記録の構造（何を事実として残すか）
  > 集計の定義（その事実をどう数えるか）
    > 画面（数えた結果をどう見せるか）
```

画面で困ったとき、まず疑うのは画面ではなく**その手前の定義**である。
「表示のために数え直す」「画面側で辻褄を合わせる」は、この順序を壊す。
実際、実行①〜②で見つかった重い欠陥は、すべて記録層と集計定義の側にあった。

## 3. 森・林・木・幹という比喩の位置づけ

| 段 | 実体 | 単位 |
|---|---|---|
| 森 `partner_reaches` | 個人を識別しないリーチ | 接触機会（**推定値**） |
| 林 `identified_person` | 識別済みで直近に接点がある人 | **人** |
| 木 `applicant` | その年度に応募した | **応募** |
| 幹 `accepted` | 最終選考を通過した | **応募** |

比喩とアイコンは、**1節の4つの問いに一目で答えさせるための手段**である。
状態を可視化・構造化するために使う。装飾ではないし、数字の代わりでもない。

守ること。

- **単位の違うものを同じ軸に置かない。並べても割らない。**
  森は推定値、林は人、木と幹は応募。実行①で日次の 林 → 木 転換率を消し、
  実行②で「推定リーチに対する識別率」を作りかけて消したのは、どちらもこの規律
- **絵は必ず数字と併置する。** 森ビューは表とカードの代替ではなく補助
- **可視化コンポーネントに SQL を書かせない。** データ取得は `src/queries/` に集約する。
  UI が独自に数え直すと、同じ画面の別の数字と食い違う
- **個体を描くときは個人を漏らさない。** 表示用 seed は鍵付きハッシュから作り、
  氏名・メール・`person_id` をそのまま使わない

## 4. 触る前に読むもの

1. `README.md` — 起動手順と、触る前に知るべきこと
2. `db/DECISIONS.md` — **記録の本体。** 原典から変えた点と理由。ここが最新
3. `HANDOFF.md` — 実行の区切り、版の規約、いま確認が要ること、次の候補
4. `basic/*.sql` — **原典。変更しない。** 冒頭に設計原則7つと実装段階がある

原典の設計原則（要約。本文は `basic/academy_schema.sql` の冒頭）。

1. 記録層は上書きカラムを持たない。導出可能な値は物理保存しない
2. 再現不能なものだけ凍結する（スコアのみ）
3. 集計定義に関わるマスタは更新せず、追加と非活性化で運用する
4. 定義が変わる可能性のあるものは変更履歴を残す
5. 訂正は打ち消しの追記で表現し、元の記録は残す
6. 変わるものはデータ、変わらないものはコード
7. 事実の有無で判定し、理由で分岐しない

実装段階は [1] 記録層＋マスタ＋ダッシュボード(1)(2) / [2] Touchpoint 分析＋(3)(4)＋Partner 系 /
[3] スコアリング / [4] 名寄せ候補提示。**先の段階の機能を勝手に始めない。**

## 5. 破ってはいけない規律

- **`basic/` は変更しない。** 正典として残す。実装は `db/migrations/`
- **理由なき差分を作らない。** 原典から変えたら `db/DECISIONS.md` に
  「発見の経緯・なぜそう直したか・どのテストで固定したか」を書く。
  テスト参照が実在するかは `tests/10_decisions_references.test.ts` が機械的に検査する
- **適用済みマイグレーションは編集しない。** 新しい番号で足す
- **日付は必ず `jst_date()` を通す。** `::date` と `CURRENT_DATE` は接続の
  タイムゾーン依存。今日は `jst_today()`
- **`status_histories` は追記のみ。** 訂正は打ち消し行の追記で表現する
- **集計マスタは追加と非活性化で運用する。** 最初に入れた値が事実上の初期値として
  固定化されるため、本番シードは分類が決まるまで空にしてある
- **デザインは `basic/DESIGN.md` のトークンに寄せる。** 色・字送り・角丸を直値で書かない。
  `app/tokens.css` は生成物（`pnpm tokens`）。手で編集しない
- **報告書は凍結、記録は更新。** 版は `REPORT-<実行番号>.<連番>.md`（規約は `HANDOFF.md`）

## 6. 進め方

```
1. 動く状態にする
2. 実データで動かして計測する
3. 見つけた欠陥を先にテストへ書き、落ちることを確認する
4. 直す
5. 理由を DECISIONS.md に記録する
6. コミットする
```

3 を先にするのは、直したつもりで直っていないのを防ぐため。

繰り返した失敗は `HANDOFF.md` の「これまでに踏んだ失敗」にある。着手前に読む。
要点だけ挙げると ―― 修正が別の場所を壊す／記録が実装より厳密に見える／
デモデータが検証したい経路を踏んでいない／目視は検証ではない／
作り物のデータから出た数字を残さない／単位の違うものを割らない／
表示の丸めで注記が嘘になる／丸め込みが欠陥そのものを隠す／
バグが偶然に作っていた経路は、バグを直すと一緒に消える。

## 7. 完了の定義

次のすべてを満たして初めて「できた」と言う。

- [ ] `pnpm test` が全通過する（新しい判断にはテストが1つ以上ある）
- [ ] `pnpm exec tsc --noEmit` がクリーン
- [ ] 実データで動かし、**DOM か SQL の値で**結果を確認した（目視だけで判断しない）
- [ ] 画面に出した数字の**単位と母集団**が、注記として画面上にある
- [ ] `db/DECISIONS.md` に理由とテスト参照を書いた
- [ ] **1節の4つの問いのどれに効いたかを一言で言える**

## 8. いまの構造で、4つの問いに答えられているか

2026-08-07（実行③時点）の実測。**主題は構造なので、この表を最新に保つ。**

| 問い | 記録層 | 集計 | 画面 |
|---|---|---|---|
| ① 誰がどんな状態か | あり | `f_person_season_state`（段と接点の鮮度） | `/people`・`/people/[id]` |
| ② 誰が何を評価したか | あり（`evaluations` / `evaluation_scores` / `rationale` 必須） | 面接官別の負荷、応募1件の評価と全履歴 | `/applications/[id]` |
| ③ いま何が起きているか | あり | 判断待ち・SLA 超過・担当未割当・保留・利益相反。母集団は `v_active_applications` に統一（A-14） | `/operations` にある |
| ④ 次に何をすべきか | 部分的 | 滞留の一覧までは出る | **「次の一手」を提示する仕組みが無い** |

集計には軸が2本ある。混ぜると、催促しない相手を催促する。

| 軸 | 述語 | 問い |
|---|---|---|
| 数えるか | `v_countable_applications` | 木に数えるか（①の量） |
| 動いているか | `v_active_applications` | いま誰かが判断すべきか（③④） |

選考開始前に取り下げられた応募は「数えるが、動いていない」。

構造側で分かっている穴。

- **`reject` にステップが結び付いていない。** 列（`status_histories.selection_step_id`）は
  あるが、運用でもデモでも埋めていないため「どのステップで落ちたか」を集計できない。
  推測で埋めると根拠のない数字が一人歩きするので、**必要になったら reject に
  ステップを持たせる**（`getStepFlow` のコメント参照）
- **`is_reapplication` の再計算処理が無い。** 原典は「名寄せ確定処理から必ず再計算する」と
  述べているが、その処理は実装段階[4]。年輪（再応募回数）の表示はそれまで保留（E-7）
- **`seasons` の実数値が未確定。** 本番シードに年度の行が無い（D-8）。
  計画を立てるための土台そのものが未確定なので、確認の優先度は高い
- **個人情報削除を受けた人の応募を、木に数え続けるか未確定（D-10）。**
  森・林・生涯サマリは削除済みを外すが、木と幹は数え続けている。
  同じ年度で 林 < 木 が起こりうる。過去年度の応募数が動く変更なので、
  確認を取るまで触らない
- **`v_person_season_state` は `persons × seasons` の直積。** 全件 0.2 秒程度で
  当面は問題にならないが、人が増えれば効く（実行③から持ち越し）

## 9. やってはいけない操作

- **開発サーバを起動したまま `pnpm db:reset` を実行しない。** PGlite の dataDir には
  プロセス間ロックが無く、エラーも警告もなく後勝ちで壊れる
- 適用済みマイグレーションの編集（チェックサムが変わってランナーが止まる）
- `app/tokens.css` の手編集（生成物）
- 実データ・実在の個人情報をデモやスクリーンショットに使うこと
- 未確定の値を「たぶんこうだろう」で埋めること。分からないものは
  `DECISIONS.md` の D 節に保留として残す（例: `seasons` の実数値は D-8 で未確定）
