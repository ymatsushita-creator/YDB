# Hitler.md — プロジェクト最高権限規律ファイル

> 本ファイルはプロジェクトルート直下に置く**最上位の規律文書**である。
> 各プロジェクトにコピーし、`## 0. プロジェクト固有` 以下だけを書き換えて使う。
> 名称は運用上の慣習であり、公開リポジトリに含める場合はリネームを検討する。

---

## 1. 権限階層（Precedence）

上が強い。下位は上位に矛盾できない。矛盾したら**作業を止めて人間に確認**する。

1. **人間の直接指示**（チャット・レビューコメント）
2. **本ファイル `Hitler.md`**（プロジェクト全体の不可侵ルール／禁止事項／ゲート）
3. **編集対象に最も近い `AGENTS.md`**（ディレクトリ固有の規約）
4. **ルート `AGENTS.md`**（ビルド・テスト・コードスタイル等の共通規約）
5. **`CLAUDE.md` / ツール固有設定**（`AGENTS.md` へのリンクまたは差分のみ）
6. `docs/adr/`（確定した設計判断）・`docs/specs/`（要求仕様）

2026年時点のデファクトは AGENTS.md（Linux Foundation / AAIF 管理、主要ツール20+が読む）。
`CLAUDE.md` は `AGENTS.md` へのシンボリックリンク、または Claude 固有の差分のみを置く。
本ファイルはその上に立つ「憲法」であり、日常のコマンド類は書かない（`AGENTS.md` の役割）。

---

## 2. ディレクトリ規約

```
<project>/
├── Hitler.md              # 本ファイル（最高権限・不可侵ルール）
├── AGENTS.md              # 実体。ビルド/テスト/コードスタイル。150〜200行以内
├── CLAUDE.md              # → AGENTS.md へのリンク or 差分のみ
├── README.md              # 人間向け
├── .claude/
│   ├── settings.json      # 権限・フック・plansDirectory
│   ├── skills/            # 反復手順（skill 化したもの）
│   └── plans/             # /plan の保存先（バージョン管理する）
├── docs/
│   ├── adr/               # 設計判断。追記のみ・**改変禁止**（0001-*.md）
│   ├── specs/             # 要求仕様。更新可
│   ├── api/               # OpenAPI 等の機械可読仕様
│   └── guides/            # 開発手順
├── src/                   # 実装。サブパッケージには各々 AGENTS.md
├── tests/
└── data/                  # 個人情報を含むものは .gitignore 必須
```

- **モノレポ**: パッケージごとに `AGENTS.md` を置く。近い方が優先。
- 各 `AGENTS.md` は散文より**実行可能なコマンドを先頭**に。
- 1ファイル200行を超えたら分割する（長文は遵守率を下げる）。

---

## 3. 記法規約

- Markdown のみ。YAML front matter は `.claude/skills/` を除き使わない。
- 見出しは `##` から。命令は**箇条書きの断定形**（「〜する」「〜禁止」）。
- 「なるべく」「可能なら」を使わない。守れないなら書かない。
- コマンドは必ずフェンス付きコードブロックに、1ブロック1コマンド。
- ファイル参照は `path/to/file.ts:42` 形式。
- ADR は `docs/adr/NNNN-短い題.md`、`Status: Proposed | Accepted | Superseded by NNNN`。
  一度 Accepted になった ADR は**書き換えず、新しい ADR で置き換える**。
- 用語は `docs/guides/glossary.md` に集約し、表記揺れを作らない。

---

## 4. 不可侵ルール（エージェント向け）

### 禁止（人間の明示指示があっても実行しない）
- `git push --force` / 履歴改変
- 生成物の「テストが通ったことにする」等、結果の虚偽報告
- 無意味に課題を作り出すこと

### 事前承認が必要
- 依存パッケージの追加・メジャー更新
- スキーマ変更、公開APIの破壊的変更
- `.github/workflows/`、`.claude/settings.json`、本ファイルの変更
- 対外送信（メール・投稿・PR作成・Issue コメント）

### 常に必須
- 変更前に関連する `docs/adr/` と近傍 `AGENTS.md` を読む
- 設計判断をしたら ADR を追記する（コードだけ変えて終わらせない）
- 未確定事項は推測で埋めず、前提を明記するか質問する

---

## 5. 完了ゲート（Definition of Done）

以下すべてが**実行され、出力が確認されている**こと。「通るはず」は不可。

1. Lint / Format
2. 型チェック
3. テスト（変更範囲のテストを追加済み）
4. ビルド
5. ADR / specs の更新（設計判断を伴う場合）
6. 秘匿情報・個人情報が差分に含まれていないことの確認

失敗したものは失敗として報告する。スキップしたものは明示する。

---

## 6. 破棄・回収（Recovery）

作業が破綻した／方針が誤っていたと判明した場合の手順。

1. 追加のコミットを積まず、現状の差分を `git stash` または作業ブランチに退避
2. 何が壊れたかを事実のみで報告（推測と事実を分けて書く）
3. 原因が設計判断なら、当該 ADR に `Superseded by` を付けて新規 ADR を起票
4. 原因が規律の不備なら、本ファイルまたは `AGENTS.md` に1行追加してから再開
5. 同じ失敗を2回した時点で、手作業をやめて `.claude/skills/` に手順化する

---

## 0. プロジェクト固有（各プロジェクトで記述）

> 本節のみ YouthDB 用に記述してある（本ファイル冒頭の指示どおり）。
> §1〜§6 は全プロジェクト共通の原本（`../Hitler/Hitler.md`）のまま**書き換えていない。**
> 記入は依頼者の承認による（§4「事前承認が必要 —— 本ファイルの変更」。2026-08-19）。

- **プロジェクト名**: YouthDB

- **目的**: 起業家アカデミーの集客・候補者選定・選考を前進させる運営コックピット。
  画面は「ヘッドハンティング／ボーダーライン／アプローチ」の3つの問いに属する（`director.md`）。

- **非目的**: 候補者台帳にしない。報告画面にしない。採用管理システムにしない。
  4つ目の主タブを増やさない。学年という概念を持たない（数えるのは期）。

- **本番影響のある領域**:
  - Vercel へのデプロイ（`vercel.json` / `.vercelignore`）
  - `db/migrations/` の適用済みマイグレーション
  - `origin` への push。公開ミラーは 2026-08-19 に外した。**再追加は依頼者の判断のみ**

- **個人情報を含むパス**: **リポジトリ内にも、このファイルにも書かない。**
  実データ（応募管理 xlsx・お顔データ・legacy DB dump）は NEO の外にある。
  場所は **`YOUTHDB_INTAKE_DIR` が指す**（2026-08-19 に既定値を廃止。C-229 ――
  既定があると、設定を忘れた実行が見当違いの場所を掴む）。
  受け入れ口は `scripts/intake-dir.ts` のみで、リポジトリの中を指す指定は拒否される。
  `.claude/settings.json` の deny が読み取りも塞いでいる。
  `.gitignore` の該当行は**残骸への保険であって防壁ではない。**
  ★ **所在を追跡下の文書に書かない。** 書けばリポジトリが個人情報の索引になる。
  ★ NEO 構成基準 R2 は「別ボリューム、または暗号化ディスクへ」としている。
    2026-08-19 に NEO の外へ移したが、**同じ物理ディスク上にある。**
    ディスクごと持ち出されれば一緒に出るので、**条文は未充足である。**

---

### この規律を緩めている点とその理由

**§2・§3 のディレクトリ規約と ADR 記法を、YouthDB は採らない。**
原本は全プロジェクト共通のテンプレートであり、このリポジトリの実体と食い違う。
食い違いを下位文書の但し書きで逃がすと**最高権限が下位に無効化される**ので、ここに書く。

| §2/§3 の要求 | YouthDB の実体 | 理由 |
|---|---|---|
| ~~`AGENTS.md` が実体、`CLAUDE.md` は差分~~ | **§2 どおりにした**（2026-08-20。C-232）。`AGENTS.md` が実装規律、`CLAUDE.md` は一枚のポインタ | 逸脱を解消した。Codex / Cursor など `AGENTS.md` を読むツールへ写しを置かずに規律を届けるには、実体が `AGENTS.md` に無いと届かない（写しがずれた事故は C-230） |
| ADR は `docs/adr/NNNN-短い題.md` | `db/DECISIONS.md` の `C-nnn` / `D-n`（追記のみ） | 番号がコード内コメントと**コミットメッセージ**から参照されている。git 履歴は後から直せない |
| `Status: Proposed \| Accepted \| Superseded by` | 節（A〜G）と本文で状態を表す | 同上。既存220件超の書式を変えると全参照が切れる |
| `docs/specs/` `docs/api/` ~~`docs/guides/`~~ | `docs/guides/` は**置いた**（2026-08-20。C-233。§3「用語は1箇所に集約」の充足）。仕様は `domain.md`、手順は `process.md` | 残る2つは規模に対して階層が過剰。使われない枠を作らない |
| `data/` | 無い。**リポジトリの外**（監査 D2-01 の是正） | 実データを中に置かない。`data/` を作ると受け入れ口に見える |
| `.claude/skills/` `.claude/plans/` | 無い。`.claude/commands/` と `.claude/agents/` を使う | 反復手順はコマンド、役割はサブエージェントで足りている |

~~**1ファイル200行の上限を、`db/DECISIONS.md` は満たしていない。**~~
**解消した**（2026-08-20。C-234）。原稿を `db/decisions-src/`（節と番号ごとに284本・最大95行）に置き、
正典 `db/DECISIONS.md` は**その連結＝生成物**にした。**パスを動かしていない**ので、
git 履歴・コード内コメント・索引・`decisions:show` の参照は1つも切れていない。
連結はバイト単位で往復し、`pnpm decisions:canon -- --check` が `verify` と CI で毎回確かめる。
1件ずつ開く導線（`db/decisions/<番号>.md`。C-220）はこれまでどおり在る。

~~**`.consultant/STRUCTURE.md` も超過している。**~~ **解消した**（2026-08-20。C-235）。
原稿を `.consultant/structure-src/`（節ごとに16本・最大45行）に置き、本文はその連結＝生成物にした。
`pnpm structure:canon -- --check` が `verify` と CI で往復を確かめる。
★ **これで §2 の行数に関する逸脱は残っていない。**

**S9（秘密が履歴に無い）を意図して赤いまま運用している。**
現役の `ANTHROPIC_API_KEY` が git 履歴 `0c9cd6e` に在り、依頼者の判断でその鍵を使い続けている。
事実が変わるまで緑にしない（`docs/audit/2026-08-19-secret-in-history.md`）。

**`quality.yml` が required status check に未設定。** CI が落ちてもマージできる。
`NEO-AX/YDB` は private で、現在のプランではブランチ保護も ruleset も使えない（`HANDOFF.md`）。

**Lint は 2026-08-20 に足した（C-231）。Format は持たない。**
`pnpm lint`（biome）が `pnpm verify` の第1段と `.github/workflows/quality.yml` で走る。
書式（formatter）は無効のままである —— 既存コードの整形をこの差分で動かすと、
意匠の判断と実装の差分が混ざって読めなくなるため。有効化は別の判断で行う。
したがって §5-1 は Lint のみ充足、§5 の 2〜6 は充足である。
