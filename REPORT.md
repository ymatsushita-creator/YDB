# YouthDB 作業報告

2026-08-06

---

## 1. 何をしたか

`basic/` にあった設計（`academy_schema.sql` / `academy_views_revised.sql` /
`DESIGN.md`）を、**実際に動いて検証できる状態**にした。

原典は正典として変更していない。実装は `db/migrations/` に置き、
原典から変えた点は `db/DECISIONS.md` に理由付きで記録した。

成果物は3層。

| 層 | 中身 |
|---|---|
| データベース | マイグレーション8本、参照データ（本番用/サンプル）、デモデータ生成器 |
| 検証 | テスト97件（PGlite = WASM 版 PostgreSQL 18、デーモン不要） |
| 画面 | Next.js のダッシュボード2枚（ファネル / 選考オペレーション） |

```bash
pnpm db:reset && pnpm dev
```

---

## 2. 進め方

「動かして計測し、そこで初めて分かる欠陥を潰す」往復を回した。

各ループの手順を固定した。

1. 動く状態にする
2. 実データで動かす
3. 見つけた欠陥を**先にテストへ書き、落ちることを確認する**
4. 直す
5. 理由を記録する
6. コミットする

3 を先にしたのは、直したつもりで直っていないのを防ぐため。
実際、性能の書き直しで踏んだ `GREATEST()` の罠はこの手順で即座に捕まった。

---

## 3. 見つかった欠陥

読むだけでは分からず、動かして初めて出たものだけを挙げる。

### 3-1. 日付境界がセッションのタイムゾーンに依存していた

原典は日付への丸めを一貫して `x::date` で書いていた。
`timestamptz::date` はセッションの `TimeZone` 設定で結果が変わる。

| セッション TZ | `'2025-04-01T08:00:00+09:00'::date` |
|---|---|
| Asia/Tokyo | 2025-04-01 |
| UTC | **2025-03-31** |

日本時間の朝9時より前に起きたことが、すべて前日に寄る。
応募開始日の朝に届いた応募は「応募開始前」に計上され、日次ファネルの
初日が丸ごとずれる。接続文字列を書き換えただけで過去の集計値が動く状態だった。

`jst_date()` を定義し、集計の日付境界はすべてこれを通した。
タイムゾーンをリテラルで与えるので `IMMUTABLE` にでき、式インデックスも張れる。

### 3-2. マスタのカラムが実装のどこからも読まれていなかった

`void_reasons.counts_as_application` は「この無効化に対応する代替の
Application が生まれるか」を区別するために用意されていた。
ところが実装は無効化済みの応募を無条件に集計から外していたため、
このカラムは一度も参照されない。取り下げ（代替が生まれない無効化）が
応募数から消えていた。

`v_countable_applications` を新設して集計対象の定義を1箇所に置いた。

### 3-3. 訂正チェーンの循環で記録が静かに消える

`h1` が `h2` を訂正し `h2` が `h1` を訂正する状態を作ると、どちらも
「誰にも訂正されていない行」にならないため再帰の基底に現れず、
チェーンごと集計から脱落する。エラーは出ない。

循環検出トリガを書く前に、より根本的な性質に気づいた。
`corrects_history_id` は既存の行しか指せないので、INSERT だけで作られる
グラフの辺は必ず過去向きになり、循環はそもそも作れない。
生まれるのは UPDATE で辺を張り替えたときだけである。

そこで `status_histories` を追記専用にした（UPDATE / DELETE を拒否）。
これは原則5「訂正は打ち消しの追記で表現し、元の記録は残す」そのもので、
循環検出は副産物として不要になった。

### 3-4. 集計の計算量が実用に耐えない

`f_funnel_daily` が返す行は（年度 × 選考期間の日数）。そのそれぞれが
6本の相関副問い合わせを走らせ、うち4本が訂正チェーンを解く再帰CTEを
先頭から評価し直す。訂正の解決が「返す行数 × 4」回起きていた。

さらに林（識別済みで直近に接点がある人）を日ごとに
`count(DISTINCT person_id)` で数え直していた。

前者は節目を1行に畳んで増分の累積に、後者は Person ごとのアクティブ区間を
まとめてから累積する形に置き換えた。

同じ構造の問題が `v_application_state` にもあり、こちらは EXISTS を
`bool_or` 付き集約に置き換えた。

### 3-5. `GREATEST()` が NULL を伝播しない

PostgreSQL の `GREATEST` / `LEAST` は、多くの関数と違い NULL 引数を
**無視して**非 NULL の最大値を返す。

```sql
SELECT greatest(NULL::date, '2025-11-01'::date);  --> 2025-11-01（NULL ではない）
```

「まだ合格していない」を表す NULL を応募開始日に丸めるつもりで書いたところ、
NULL が消えて全応募が年度初日に合格したことになった。
テストが即座に落としたので実害はない。

### 3-6. チャートの折れ線が枠外に切れていた

目盛りを生成する関数が、軸の上端をデータ最大値**以下**に丸めていた。
上の方の点が `y < 0` に落ち、SVG は既定で `overflow: hidden` なので
エラーも警告もなく折れ線の頭が切れる。全8枚で発生していた。

スクリーンショットを目視して「描画された」と判断したのが甘かった。
DOM の `getBBox()` を測って初めて分かった。

---

## 4. 自己レビューで見つかった、私自身の欠陥

3種のレビューを並行で走らせた（SQL の意味論 / TypeScript 層 / 主張と実装の一致）。
以下は**私が入れた修正や記録の側**の問題である。

### 4-1. 修正が新しい破綻を生んでいた

3-2 で `counts_as_application` を尊重するようにした結果、同一年度に
同一 Person の集計対象応募が2件存在しうるようになった。
ところが `v_person_season_state` は 1:1 結合のままだったので、
**同じ人が同じ年度に「木」と「幹」の2行で現れる**状態になっていた。

原典は「Person × Season 単位」と定義している。1人1行が定義なので、
応募をまたいで最高到達点を取る形に直した（`0007`）。

### 4-2. 記録が実装より厳密であるかのように書かれていた

`DECISIONS.md` の冒頭に「ここに書かれていない差分は存在しない」
「各項目の末尾に、その判断を固定しているテストを示す」と書いた。
どちらも成立していなかった。

- 未記録の差分が10件以上（式インデックス7本、CHECK 2本、列の追加、
  集計値が変わる意味論の変更1件）
- B節の11個の制約のうち、テストがあるのは5件だけだった

### 4-3. 「何度流しても同じデータ」が嘘だった

デモデータ生成器の既定値が `Date.now()` で、これが生成の地平線になる。
行数が変わると乱数の消費回数が変わり、以降の系列全体がずれる。
日をまたぐと別データになっていた。

これが二次被害を生んでいて、記録に残した実測値（応募件数・ミリ秒）は
どれも再現しなかった。**追試しようとした人が同じ土俵に立てない。**

### 4-4. コメントの嘘 5件

このプロジェクトは設計意図をコメントに託しているので、これが最も重い。

| 場所 | 嘘の内容 |
|---|---|
| `0001` `sort_order` | 「負値は許す」と書いた直後の CHECK が負値を禁じている |
| `0002` 訂正チェーン | 「0003 のトリガで循環を禁止する」→ 0003 は正反対のことをしている |
| `pglite.ts` | 存在しないテストファイルを指している |
| `testing.ts` | 「テストだけ `withSeeds` を立てる」→ 立てているテストは1つもない |
| `0002` / `DECISIONS` | 「5箇所に複製されていた」→ 実数は4。同一ファイル内で5と4が併記 |

### 4-5. デモデータが仕組みを一度も踏んでいなかった

無効化された応募 0件、削除済み Person 0件。つまり 3-2 で直した
`counts_as_application` の分岐は、デモ画面上では一度も効いていなかった。
保留・利益相反でも同じ失敗をしており、**同じ種類の見落としを3回繰り返した**。

画面に出すだけでは検証にならない。出したものが、検証したい経路を
通っているかまで確かめる必要がある。

---

## 5. 現在の状態

- テスト **97件** 全通過
- 型チェック クリーン
- 原典からの構造的な欠落 **なし**（テーブル・カラム・制約は原典を完全に含む）
- 設計原則1（導出値を物理保存しない）違反 **なし**

第4節で挙げた自分自身の欠陥は、下記 5-b のとおりすべて対応した。
残しているのは指示6で「今回は見送り」とされた3件のみ。

---

## 5-b. 2026-08-06 指示書への対応（追記）

| 指示 | 対応 |
|---|---|
| 1. `DECISIONS.md` の未記録差分 | E 節を新設。式インデックス7本・CHECK 2本・自己訂正 CHECK・`is_voided` 列・森の削除済み除外・コメント欠落・未担保の意図を記録 |
| 1. マイグレーション記録のトランザクション外 | `BEGIN`/`COMMIT` で包んだ。失敗時の `ROLLBACK` 漏れをテストが検出したので併せて修正。ファイル消失と番号の採り違えも検出するようにした |
| 2. 参照データの本番/サンプル分離 | `*.example.sql` を本番シードから除外。本番は channels 0件 / void_reasons 0件 / withdraw_reasons 1件 |
| 3. ファネル起点・単位混在・アクティブ判定 | 仕様として確定。D-1〜D-3 に決定として記録。実装は変更なし |
| 3. `90` が仮の値であること | 林の推移カードとフッタに明記 |
| 4-1. 単位混在の注記 | 段のカードに常設 |
| 4-2. 日次の林→木転換率 | 削除（`showRatio: false`） |
| 4-3. 年度サマリ指標 | 新設。暫定/確定を選考完了日で切り替え |
| 5. 辞退理由の必須化 | `0008` で制約追加。`unconfirmed`（未確認）を本番シードに |

**完了確認**

- テスト **97件** 全通過（69 → 97）
- 型チェック クリーン
- 本番シード適用直後: `channels` 0 / `void_reasons` 0 / `withdraw_reasons` 1（未確認のみ）
- `DECISIONS.md` の各項目にテスト参照を付し、
  **参照が実在するかを機械が検査する**（`tests/10_decisions_references.test.ts`）。
  壊れた参照を入れると落ちることも確認済み

指示外で1件直した。チャートの折れ線が viewBox からはみ出して切れていた件で、
これは報告書 3-6 で挙げたもの。

**未対応のまま残したもの**（指示6のとおり）: `ymd()` の本番ドライバでのずれ、
`getInterviewerLoad` の表示名集約、PGlite の dataDir ロック。

---

## 6. まだ決まっていないこと

### 6-1. 本番のマスタは空のまま

参照データは本番用とサンプルに分けた。本番用
（`db/seeds/0001_reference.sql`）は意図的に空にしてある。

| マスタ | 本番の行数 | 決まってから入れる |
|---|---|---|
| `channels` | 0 | 流入チャネルの分類 |
| `void_reasons` | 0 | 無効化理由と `counts_as_application` の対応 |
| `withdraw_reasons` | 1（未確認のみ） | 実運用の辞退理由 |

集計に関わるマスタは追加と非活性化でしか運用できない（原則3）ため、
**最初に入れた値が事実上の初期値として固定化される**。
サンプル（`*.example.sql`）にある12件のチャネルなどは私の創作で、
本番のシードでは読まれない。

デモデータ（学校名・団体名・スタッフ名・応募者名、各年度の日程と通過率）も
すべて創作である。年度の定員36・目標応募数100は実数値との指示を受けたが、
どの年度のものか、日程がいつかを確認できていないため、まだ本番用の
シードには入れていない。

### 6-2. 保留のまま残している判断

`DECISIONS.md` の D 節。指示で確定した D-1〜D-3 と、今回の解釈 D-7 を除く分。

- **D-4** `identity_resolutions` を追記専用にしていない（実装段階[4]まで未使用）
- **D-5** `staff_roles.role` などが自由入力のまま（値が決まり次第マスタ化）
- **D-6** `counts_as_application` の規則に隠れた前提がある
  （テスト送信は「代替が生まれない」が数えてはならない）

あわせて、`active_window_days` の 90 日は仮の値であることを画面に明記した。
正式な日数はスコアリングの減衰半減期と合わせて後で決める。

---

## 7. 引き継ぐときに知っておくこと

- **日付は必ず `jst_date()` を通す。** `::date` と `CURRENT_DATE` は
  接続のタイムゾーン依存。今日を取るときは `jst_today()`
- **`status_histories` は追記のみ。** UPDATE も DELETE もトリガで拒否される
- **適用済みマイグレーションは編集しない。** チェックサムが変わると
  ランナーが止まる。新しい番号で足す
- **集計マスタは追加と非活性化で運用する。** 既存行を更新すると
  過去の集計の意味が変わる
- **`basic/` は変更しない。** 差分は `db/DECISIONS.md` に理由付きで残す
