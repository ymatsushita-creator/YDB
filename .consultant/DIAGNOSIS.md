# 構成診断 —— 2026-08-19

対象: `/Volumes/KIOXIA_2TB/NEO/YouthDB` @ `02aa6bd`
道具: `../コンサル/bin/consultant analyze`

## スコア

```
実施前  75/100   C05（権限設定なし）・C06（サブエージェントなし）・C07（.env.example なし）
実施後 100/100   所見なし
```

## 実測値

| 領域 | 実測 |
|---|---|
| コード | app 9,385行 / src 13,930 / db 15,420 / scripts 3,540 |
| テスト | 84ファイル・**887件**・205 suite・約175秒・全pass |
| ルート直下の `.md` | 44本 → **9本**（`REPORT-*.md` 35本を `docs/reports/` へ） |
| 設計判断 | `db/DECISIONS.md` 8,524行・**220件**（索引を生成） |
| CI | `audit.yml` 1本 → **`audit.yml` + `quality.yml`** |

## 診断ツールが拾えなかった穴 —— これが本題だった

**`consultant analyze` の C04「CIがない」は実施前から通過していた。**
`.github/workflows` の存在だけを見る判定のため、監査CI 1本で満たされていた。

実際に測ると:

```
CI・フックで pnpm test / tsc --noEmit / build が走る箇所 → 1件も無い
```

`CLAUDE.md` の「完了条件」3項目を強制する機械が**1つも無かった**。
887件のテストが1度も回らないままマージできる状態だった。

これは非対称の問題である。個人情報と監査基盤の防御は
pre-commit / pre-push / CI の**三重**に固められ、`--no-verify` すら想定した設計なのに、
**「動くかどうか」は誰も見ていない。**

`.audit/AUDIT_CHARTER.md` §1 は「実装者の説明・コミットメッセージを根拠として採用しない」と
定めている。同じ理屈が品質側に適用されていなかった。

**スコア100は「穴が無い」ではなく「既知の7項目に穴が無い」である。**

## 実施した是正

| 所見 | 是正 |
|---|---|
| **品質ゲート不在**（診断外） | `pnpm verify` と `.github/workflows/quality.yml` |
| **ルートの汚染**（診断外） | `REPORT-*.md` 35本を `docs/reports/` へ。README を着手導線に |
| **索引の不在**（診断外） | `db/DECISIONS-INDEX.md` を生成（220件）。CI が `--check` |
| C05 権限設定 | `.claude/settings.json`（deny 29 / ask 7） |
| C06 役割分離 | `.claude/agents/` に `investigator` と `reviewer` |
| C07 環境変数の契約 | `.env.example`（値は空） |

あわせて監査 2026-08-19 の **D2-01（High）** を解消した ——
`.gitignore` 1行が唯一の防壁だった実データ49MBをリポジトリ外へ移し、
`scripts/intake-dir.ts` に「受け入れ口をリポジトリ内へ向けさせない」検査を追加した。

## 第2次 —— コンサルを機械に組み込んだ（同日）

診断と文書だけでは、黒崎（フック・CI・deploy-gate で強制）に対して
**コンサル層は誰も回さなければ守られない**状態だった。同じ非対称を作らないため、
基準そのものを機械にした。

| 資産 | 役割 |
|---|---|
| `.consultant/TOOL_PATH` | 道具の位置を固定（`.audit/TOOL_PATH` と同じ形） |
| `scripts/check-structure.ts` | `STRUCTURE.md` の S1〜S7 を機械で照合。破れば exit 1 |
| `pnpm structure` | 単体で回す |
| `pnpm verify` | 完了条件の一部として回る（typecheck → test → 索引 → **構成** → build） |
| `quality.yml` | CI でも走る |
| `.claude/commands/plan.md` | 標準工程の第1段（観察→診断→計画。実装しない） |
| `.claude/commands/structure.md` | 崩れたときの直し方 |

### 検査が本当に効くことを確かめた（負のテスト）

「通るだけの検査」を資産と呼ばないため、意図的に4つ壊して落ちることを確認した。

| 壊し方 | 結果 |
|---|---|
| ルートに `MEMO.md` を置く | ✘ S2 で exit 1 |
| `deny` から `git push` を外す | ✘ S4 で exit 1 |
| `db/private/` を復活させる | ✘ S6 で exit 1 |
| `verify` から `test` を抜く | ✘ S1 で exit 1 |

いずれも復旧後に全項目パスへ戻ることも確認した。

### この仕組みの限界

- CI からは外部ツールを解決できないため、**C01〜C07 は CI では実行されない。**
  スクリプトはそれを「未実行」と明記して通す。**「問題なし」とは言わない。**
  CI が必ず強制するのは S1〜S7 である。
- `scripts/check-structure.ts` 自体は封印されていない。条件を消せば検査は黙る。
  検知は人間が差分を読むことに依存する（`CHARTER.md` §5 と同じ限界）。

## 第3次 —— 独立レビュー4班を投入し、指摘を修正した（同日）

`.consultant/CHARTER.md` §4 の標準工程は「計画 → 狭い実装 → 検証 → **独立レビュー**」であり、
「実装した本人の申告を完了の根拠にしない」と定めている。
第1次・第2次の作業はこのレビューを受けていなかったため、4班を並行で投入した。

| 班 | 任務 | 主な成果 |
|---|---|---|
| 実装審査 | 新規スクリプトの正しさ | 偽リポジトリで S1・S1b・S2・S4・S6・S7 の全素通りを実証 |
| 文書整合 | 主張と実測の突合 | 定量的主張に虚偽なし。`verify` 5段化の取り残し4本を発見 |
| ゲート攻撃 | 回避経路の証明 | **15の回避経路**を実証 |
| 是正検証 | D2-01 是正の実体確認 | 移設は完了。防壁の3回避を実証 |

### 実証された重大欠陥と、その修正

| 欠陥 | 何が起きるはずだったか | 修正 |
|---|---|---|
| `pnpm test` のグロブが `sh` に食われる | `tests/` にサブディレクトリを1つ作った瞬間、**既存84本が沈黙して消える。CIは緑のまま** | グロブを引用符で囲い、`scripts/test-preflight.ts` を前段に置いた |
| `node --test` は対象0件で exit 0 | テストを全部消してもCIが通る | 同上（前段で落とす） |
| `intake-dir.ts` の防壁が3経路で抜ける | cwd依存・シンボリックリンク・大小文字。**実データがリポジトリ内へ戻る** | `realpathSync.native` で実体解決。リポジトリ基準の解決へ |
| `check-structure.ts` が全項目文字列一致 | コメント1行で合格。**ハンコ検査だった** | 挙動で見る形へ全面書き直し。S8 を追加 |
| `settings.local.json` の任意コード実行 allow | `node -e ' *` 等の3件が**deny 全項目を無効化**。実APIキーがBash経由で読めた | 3件を削除（allow 49→46） |

修正後、レビューが通した7つの回避を再実行し、**すべて exit 1 で落ちることを確認**した。

### 索引が見つけた、より根本的な問題

参照の照合を厳格化したところ（`SHA-256` を `A-256` と読む誤検知と、
`docs/pilot/` の別番号体系 `B-1`〜`B-7` を除外）、次が確定した。

**`C-165`〜`C-212` のうち 44 個が、本番コードから参照されているのに
`db/DECISIONS.md` に見出しが無い。**

参照元は `app/base.css`（28箇所）・`app/_components/`・`src/commands/`・`src/queries/`・
`db/migrations/0045` ・`tests/` と、いずれも生きているコードである。
つまり「番号で設計判断を指す」というこのリポジトリの設計は、**約2割が空を指している。**

AIはこれを埋められない（`CLAUDE.md`「記録にない値の創作」の禁止にあたる）。
何を決めたのかを知っているのは、その判断を下した人間だけである。

## 第4次 —— 引き継ぎ報告を実測で検証し、緑の根拠を作り直した（同日）

第3次の報告を受けた別セッションが、**報告を根拠として採用せず**実測で検証した
（`.audit/AUDIT_CHARTER.md` §1 と同じ立場を、コンサル層の自己申告にも適用する）。
結果、**第3次の報告には、実装されていない修正が「修正済み」として含まれていた。**

### 実測で覆った主張

| 第3次の報告 | 実測 |
|---|---|
| 「`pnpm verify` は exit 0」 | **RED。** `decisions:index --check` で停止していた |
| 「照合を厳格化し 44件に確定」 | **コードに入っていなかった。** `build-decisions-index.ts` の
コメントが「境界は取得後に自前で見る」と書きながら、その判定を実装していない。
実行すると 54件、コミット済み索引は 53件だった |
| 「負のテストで検査が効くことを確かめた」 | 検査対象を壊す方向しか試していない。
**探査を壊す方向を試していなかった** |

### S6 は「壊れていても合格」していた

`check-structure.ts` の S6 は防壁を実際に動かして確かめる作りだが、
`try { … } catch { /* exit 1 = 正しく止まった */ }` で終了コードしか見ていなかった。
`import()` の失敗も exit 1 である。実測:

```
存在しないモジュールを import した探査  → exit=1
防壁が正しく拒否した探査                → exit=1
```

**`scripts/intake-dir.ts` を退避しても S6 は ✔「防壁は3通りの回避を実際に拒否した」を出した。**
検査対象が存在しないのに合格を出す検査は、資産ではなく負債である。

修正: 拒否を `assertOutsideRepo` 固有のメッセージで同定し、それ以外の失敗は
**「検査できなかった」として ✘ にする**（合格へ倒さない）。あわせて**正の対照**を足し、
正当な外部ディレクトリが受理されることも毎回確かめる。

### 索引生成器の4つの欠陥

| 欠陥 | 実測 | 修正 |
|---|---|---|
| 境界判定が未実装 | `SHA-256` が `A-256` として11件 | 取得後に `(?<![0-9A-Za-z])([A-F])-([0-9]+)(?![0-9])` で判定 |
| バイナリが混入 | `Binary file public/brand/logo_gradient.png matches` が**行ごとIDとして**索引へ | `git grep -I` |
| `NaN` でソートが壊れる | ↑に `Number(id.slice(2))` → `NaN` → 比較子が未定義順序 | 上と同時に解消 |
| 接頭辞を無視したソート | `D-30` が `B-7` と `C-165` の間。同番異接頭辞が同順位 | 接頭辞 → 番号の順で比較 |

### 索引が自分自身を数えていた

`db/DECISIONS-INDEX.md` は追跡下にあり、欠番リストを本文に持ち、
それを `git grep -- .` が読み返していた。**欠番は自己維持する。**

実証: 本診断書に欠番の話を書き足しただけで、欠番が **53 → 54 に増えた。**
境界判定を入れると、残る `A-256` は「`SHA-256` を `A-256` と読む誤検知」と書いた
本書の一文だけになる。**バグを記述した文章が、バグの最後の実例になっていた。**

修正: 生成物（`db/DECISIONS-INDEX.md`）・本書（`.consultant/`）・生成器自身を
照合対象から外す。`B-1`〜`B-7` は `docs/pilot/DEPLOY-READINESS.md` 自身の連番
（`db/DECISIONS.md` に `B-` 見出しは1件も無く、本文が「`DEPLOY-READINESS.md` B-3」と
出典付きで引いている）ため、別番号体系として除外し、除外した事実を索引に明記する。

### S1b も「同じものが CI で走る」を保証していなかった

S1 は `verify` に `decisions:index` の段を要求するのに、**S1b は CI 側で要求していなかった。**
`quality.yml` からその段だけ消しても S1b は ✔ のままで、`CLAUDE.md` の
「同じものが CI で走る」は機械に支えられていなかった。要求を5段へ揃えた。

### ルートに、実データをリポジトリ内に要求するコードが残っていた

`.pp.tmp.ts` が追跡下にあった。使い捨ての集計スクリプトだが、中身が問題である。

```ts
const book = new Workbook(join(process.cwd(), '2期応募管理.xlsx'))
```

**実データの応募管理表がリポジトリのルートに在る前提**で、`scripts/intake-dir.ts` を
通していない。監査 D2-01 の是正（実データはリポジトリの外、解決は受け入れ口が行う）と
正面から矛盾する。参照は0件で、後継の `scripts/import-partners-2026.ts` は
正しく `intakePath('2期応募管理.xlsx')` を使っている。

**どの検査も拾えなかった。** S2 は読み物の拡張子（`.md` 等）しか数えず、
S6 は実データの**存在**は見るが、実データをリポジトリ内に**要求するコード**は見ない。
S2 を追跡下のルート全ファイルへ広げ、当該ファイルを除去した。

### 確定した数

```
欠番 45 件 = C-番号 44 件 + D-30
```

誤検知（`A-256`・バイナリ行）と別体系（`B-1`〜`B-7`）を除いた実数である。
索引には**参照元のファイル名を全件併記**した。行番号は載せない ——
無関係な編集で行がずれるたびに索引が古くなり、CI の `--check` が本題と関係なく落ちて
「検査を緩めろ」という圧力を生むからである（`CLAUDE.md` の禁止事項に直結する）。

### 修正した検査が効くことの確認（負のテスト）

今回は**探査を壊す方向**も試した。第3次がこれを怠ったことが問題の核だったためである。

| 壊し方 | 結果 |
|---|---|
| `scripts/intake-dir.ts` を退避 | ✘ S6「防壁を検査できなかった」（**第2版は ✔ だった**） |
| `quality.yml` から索引の段だけ消す | ✘ S1b「run: に無い: pnpm decisions:index」（**初版は ✔ だった**） |
| ルートに使い捨ての `.ts` を追跡下で置く | ✘ S2「ルートに追跡下の余計なファイルがある」（**初版は ✔ だった**） |
| `assertOutsideRepo` の `process.exit(1)` を `return` へ | ✘ S6「防壁が抜ける」（3経路とも） |
| `db/DECISIONS.md` に `## C-206` を仮置き | 欠番 45 → 44（照合が生きている） |
| 本書に `C-999` と書く | 欠番 45 のまま（自己参照の遮断が効いている） |
| 索引を2回生成 | 一致（決定的） |

いずれも復旧後に全項目パスへ戻ることを確認した。

## 残っている穴

1. **`quality.yml` が required status check ではない。** 設定するまで、CIが落ちてもマージできる。
   人間がGitHubで設定する。
2. ~~**規律文書4本がマニフェストと不一致**（監査 Critical 4件）。~~
   **解消済み。** コミット `6346864` で再封印された。第4次の実測で
   `.audit/MANIFEST.sha256` の14件すべてが一致することを確認した（Critical 0 件）。
3. **`C-45` `C-46` `C-116` が各2件の判断に重複している。** 参照先が定まらない。
   `app/layout.tsx` とコミットメッセージが既に参照しており、AIの判断では振り直していない。
3b. **欠番 45 件（C-番号 44 + `D-30`）は人間にしか埋められない。**
   索引の「参照先が本文に無い番号」に参照元ファイル付きで掲げてある。
   `D-30` は `db/DECISIONS.md` L1092 が「`D-30` のような読み方」と本文で引いているだけで、
   見出しが無い。記録漏れか書き間違いかは、書いた人間にしか分からない。
4. **`.consultant/` は封印されていない**（`CHARTER.md` §5）。改竄は機械では検知されない。
5. **監査意見は【限定付適正】—— High 5 件が残っている**（第4次で実測、`@6346864`）。
   `CLAUDE.md`「Critical / High が出ている状態で commit / push へ進まない」に該当するため、
   第4次の修正は**作業ツリーに置いたまま止めてある。** 5件はいずれも人間の操作が要る。

   | 所見 | 誰が直すか |
   |---|---|
   | D3-05 `main` に required status check が無い（×2） | GitHub の設定。人間 |
   | D3-06 公開リモート `mirror=ymatsushita-creator/YDB` がある | リポジトリ設定。人間 |
   | D5-03 本番へ出した記録が1件も無い | deploy-gate の運用開始。人間 |
   | D5-03 未pushのコミットが2件ある | push。人間（AIは deny されている） |

   D3-05 は残っている穴の1と同じものである。**10回連続で同じ指摘が出ている。**

## 第5次 —— 監査が何を守るためにあるかへ戻った（同日）

第4次までは検査の不具合を直していた。依頼者から「なんのための監査か」という指摘を受け、
**守る対象の側から**見直した。結果、機械が一度も見ていない場所に、現役の秘密があった。

### 現役の API キーが履歴に在る

`ANTHROPIC_API_KEY` が `0c9cd6e` で入り `242ce59` で消されている。
作業ツリーには無い。**履歴にだけ在る。** 指紋照合の結果、いま使っている鍵と同一だった。

```
履歴の鍵の指紋 : b952cd9dfda8603b
現行の鍵の指紋 : b952cd9dfda8603b
```

origin の全ブランチとローカルの `main` から到達できる。
公開リモート `mirror=ymatsushita-creator/YDB` は78コミット遅れており到達しなかったが、
**`git push mirror main` 一発で公開される状態だった。** 監査 D3-06 が10回指摘し続けたのは
この距離である。remote を外した。詳細と手順は `docs/audit/2026-08-19-secret-in-history.md`。

### なぜ10回の監査が止められなかったか

止めていた。**読む側が数字として流していた。**
機械の側にも穴があり、PII走査も D6-02 も作業ツリーしか見ないため、
鍵が履歴に生きている状態で「秘密を検出しなかった」を出し続けていた。
S9 を足して履歴とリモートを見る。

### 実データは無事だった

履歴の全171リビジョンを追加ファイルで洗った。`.xlsx`・顔写真・旧DB原本・`db/private` は
**一度も履歴に入っていない**。入っているのはブランドの PNG 7件のみ。D2-01 の是正は実効していた。
作業ツリーの PII は Critical 0 / High 0、残りは弱シグナルと承認済み allowlist 149件。

### 現在 `pnpm verify` は S9 で RED である

鍵が履歴に在る以上、緑を出すのは嘘になる。**この検査を緩めて通さない。**
解消は人間の操作（失効・再発行 → 公開先の決定 → 履歴除去または allowlist 登録）。

## 第6次 —— `docs/` を分類し、機械で保つようにした（同日）

構成の検査はルート直下（S2）を見ていたが、**`docs/` を見る検査は1つも無かった。**
そのため `docs/` 直下に3種類のものが原理なく同居していた。

```
docs/audit-2026-08-19-remediation.md        監査の是正記録
docs/audit-2026-08-19-allowlist-additions.yml  同上（YAML）
docs/personas.md                            製品の参照資料
docs/secret-in-history-2026-08-19.md        監査の是正記録（第5次で追加）
```

第1次でルート直下44本を9本へ絞り `REPORT-*.md` 35本を `docs/reports/` へ移したが、
**移した先の秩序は誰も決めていなかった。** 追い出した物の行き先が無秩序なら、
追い出した意味が薄れる。

4分類へ整理し、参照（`HANDOFF.md` ほか3本）を追随させた。

```
docs/audit/    3 件    docs/pilot/    10 件
docs/product/  1 件    docs/reports/  36 件
```

S10 を足し、`docs/` 直下のファイルと未定義の分類を落とす。
負のテストで `docs/stray.md` を置き、✘ になることを確認した。

`app/` `src/` `db/` `tests/` `scripts/` は `CLAUDE.md` の実装規律どおりで、乱れは無い
（`src/commands` 19 / `src/queries` 15 / `app/_components` 9 など、役割の分離が保たれている）。

## 第7次 —— 基準の文書が実装より少ない状態を機械にした（同日）

`docs/` を整えたあと、**私自身が S9・S10 を足したことで文書が古くなった**はずだと考えて
突き合わせたところ、**古くなっていたのは今日より前からだった。**

```
.consultant/STRUCTURE.md:15   「S1〜S8」        （実装は S8 まで。ここは合っていた）
.consultant/STRUCTURE.md:27   「CI が強制するのは S1〜S7」
README.md:85 / 97             「S1〜S7」
.claude/commands/structure.md:9  「S1〜S7」＋ 表が S7 まで
```

S8 が足された時点で4箇所が古くなり、**誰も検知していなかった。**
索引生成器が「境界は取得後に自前で見る」と書きながら判定を実装していなかったのと
同じ形である —— **文書が実装と食い違い、機械が何も言わない。**

S11 を足した。`check-structure.ts` が実装している検査IDと、`STRUCTURE.md` の
節見出し `〔S…〕` を突き合わせる。実装にあって文書に無ければ落ち、
文書にあって実装が無くても落ちる。**書いただけの基準も、書かずに足した検査も残らない。**

S11 は追加した直後に自分自身を落とした（`実装にあるが STRUCTURE.md に節が無い: S11`）。
§11 を書いて通した。負のテストで `〔S10〕` を消し、✘ になることも確認した。

現在の検査は 12 件（S1 / S1b / S2〜S11）＋ 外部ツール C01〜C07。
