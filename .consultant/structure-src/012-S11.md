## 11. 基準の文書と実装が一致している〔S11〕

- `scripts/check-structure.ts` が実装している検査IDと、**本書の節見出し `〔S…〕` を突き合わせる。**
- 実装にあって本書に無ければ落ちる。**基準を先に書く。**
  本書に節があって実装が無くても落ちる。**書いただけの基準を残さない。**
- なぜ要るか: 本書も `README.md` も `.claude/commands/structure.md` も
  「S1〜S7」と書いたまま S8 が足され、さらに S9・S10 が足された。
  **基準の文書が実装より少ない状態を、誰も検知していなかった**（2026-08-19）。
  文書が実装と食い違う形は、このリポジトリで繰り返し起きている ——
  索引生成器が「境界は自前で見る」と書きながら判定を実装していなかったのも同じである。

★ **本書自身が生成物である**（2026-08-20。C-235）。原稿は `.consultant/structure-src/`
  （節ごとに16本・最大45行）で、**書くのは原稿の側**。`pnpm structure:canon` で組み立て、
  `-- --check` が `pnpm verify` と CI で往復を確かめる。
  `Hitler.md` §2 の「1ファイル200行」を、**パスを動かさずに**満たすための形である
  （正典 `db/DECISIONS.md` と同じ。C-234）。S11 は組み立て後の本書を読むので、
  節を足す位置は原稿のファイル名の連番で決まる。
