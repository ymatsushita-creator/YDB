# design.md — UI仕様

色・角丸・余白の実数値は `basic/DESIGN.md`、本書は構造だけを定める。

## 階層

```text
年度 → タブ → 対象
```

年度がタブより上にある。タブを移っても年度は保たれる。

## 共通シェル

- 上: 現在地をエクスプローラ形式で示す（`2026 年度 › タブ › 対象`）。
  先頭の年度は押せない。末尾は現在地なので押せない
- 左: ヘッドハンティング / ボーダーライン / アプローチ
- 左下: 名前検索、その下に年度切替
- 年度切替は同じ画面に留まる。切り替えても属するタブは変わらない

表示する場所と切り替える場所を分ける。
現在地は常時の関心なので上に置き、年度切替は稀な操作なので隅に置く。

## 主要画面

### ヘッドハンティング

- 最新のやること
- 応募者成績ランキング
- 候補者確度ランキング
- 候補者パネル
- ヘッドハンティング対象者一覧
- 候補者プロフィール・顔写真・アプローチ状態の編集

### ボーダーライン

- 判断待ち、期限超過、担当未割当、保留、利益相反
- 担当割当、評価、保留、判定

### アプローチ

- アプローチ可能圏
- 構成コミュニティ
- チャネル・接点・推定リーチ
- ファネル

## 記入できる場所と固定の場所

画面には2種類ある。**書き込める場所**と、**記録を映しているだけの場所**。
見分けが付かないと、書けない値をいじろうとするか、書ける値に気づかない。

- 記入できる領域は面と縁の色を変え、`記入できます` の文字を必ず添える
- 行の中に収まる小さなフォームは面を塗らず、入力の縁だけ色を変える
  （行に面を敷くと、行そのものが状態を持っているように読める）
- 書き込む場所が1つも無い画面は、現在地の帯に `固定` と明示する
- **色だけで意味を伝えない。** 印は必ず文字を伴う

この印は人が付けるので、`tests/30_editable_marking.test.ts` が
「固定と書いた画面に記入できる領域が無いか」「書き込むフォームに印が
付いているか」を機械的に見張る。**嘘の注記は、注記が無いより悪い。**

## 編集の規則

- 記録された項目には編集入口を置く
- **判定は画面から取り消せない。** 訂正の入口は置かない（記録層では
  打ち消しの追記で直せるが、画面には出さない）
- 導出値には「根拠を編集する画面」への導線を置く
- 保存成功・失敗を同じ画面で明示する
- 顔写真は JPEG / PNG / WebP、2MB以下
- JavaScriptが無効でも保存できるサーバフォームを優先する

## 表現

- 高密度だが視覚的雑音は少なくする
- カードは機能の容器であり装飾ではない
- ボタンは厚みと押下状態を持つ
- 空欄は架空値で埋めず、未設定または記録なしと示す
- 旧生態系比喩を使用しない
