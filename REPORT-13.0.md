# REPORT-13.0 — 実行⑬。応募管理表から「未受領」を掘り出し、帯を1枚にし、詰めていない階層を潰した

実行⑬の第1報。2026-08-13。**この文書は凍結する。**
`REPORT-12.0.md`（実行⑫）の続き。決定は `db/DECISIONS.md` の **C-116 … C-120** にある。

---

## ★ これは Pilot Go ではない

`REPORT-7.4` 以降と同じ注記を繰り返す。**Pilot は HOLD のまま。基準は緩めない。**

運営はまだ触っていない。本番（https://youthdb.vercel.app）は**デモモードのまま**で、
実データは出ていない（C-94）。今回作ったものは依頼者の指示に対する実装であって、
運用で観測された問題への対処ではない。

## Summary

- 依頼者の指摘 ――「エクセルファイルあるでしょ？ そこから探せる」。
  **「未受領」としていた3つのうち2つは、最初から手元の表にあった。**
  1年ぶんの応募管理表（52シート）を1枚ずつ開いて突き合わせた
- ② 特別選考の重み付けは**数値の配点ではなく「必須A〜C／加点D〜I」の2段**だった。
  記録層に `kind` を足し（0033）、2期に特別選考の段と9軸を入れた（0005）
- ★ **書類選考・グループ面接の軸は足さなかった。** 段の呼び名と工程は表にあるが、
  **採点の軸の呼び名が無い。** 無い語をこちらで名付けない
- 依頼者の追加指示を3つ実装 ―― ホームから詳細へ飛ぶ導線 / 縦タブと横タブを1枚の帯へ /
  評価軸を動かさず全件表示
- 「面接シート以外のフォーム・不要な列・エクセルとの差分・詰めていない階層」を
  **3周繰り返して**点検した
- ★ **出る手段が画面から消えていた**（縦バーの足元が 60px はみ出し、「出る」が押せない）
- ★ **入る道の無い階層が2つあった。** `/reach-zones/{id}` は
  「連携団体 › 団体名」と名乗りながら、どこからもリンクされていなかった
- ★ **孤立を見つけるための検査が、孤立を隠していた**（2度間違えた）
- テスト **546 → 582**（+36）。`tsc` / `build` クリーン

---

## 1. 応募管理表を1枚ずつ開いた（52シート）

依頼者の指摘は正しかった。実行⑫まで「未受領」として残していたものは、
**受け取っていなかったのではなく、こちらが開いていなかった。**

### ② 特別選考の重み付け ―― 配点ではなく2段だった

「特別選考」シートは9軸を**2つの段**に分けて並べていた。

```
基準設計（満たすべき前提）      A. 使い倒す時間と覚悟 / B. Be Playful への適合 /
                               C. 他者と事業を進める前提
強く評価する条件（乗せる理由）  D. 小さく踏み出している / E. 自分ごと / F. FB耐性 /
                               G. 場を生んだ / H. 伸び代が異常 / I. 賭けたい直感
```

これが運営の言う「重み付け」である。**数値の配点は表に無い**ので作らない
（満点は 4 のまま。C-105 と同じ）。`evaluation_criteria.kind` として持つ（0033）。

★ この重みは**表示のための区分**であって、集計には使わない。確度・順位・合否は
従来どおり点と通過で決まる。**重みを点へ勝手に変換しない。**

### ★ 書類選考・グループ面接は「呼び名だけ」あった

`013_選考フロー` に2本のトラックが日付付きで並んでいた ――

```
特別選考フロー  初期接点 → 事前面談（浩江さん・面談シート）→ エントリー →
                グループ面接 → 最終面接 → 入学手続き
通常選考フロー  初期接点 → 説明会 → エントリー → 書類選考 →
                グループ面接 → 最終面接 → 入学手続き
```

段の呼び名は確定した（`書類選考` / `グループ面接` / `最終面接`）。
`シート52` には2期39人の結果もあった（一次 7〜16 ・ 二次 39〜67 ・
最終は 合格30 / 補欠5 / 辞退3 / 不合格1）。

★ **それでも軸は足していない。** 表にあるのは結果の数値だけで、
**採点の軸の呼び名が無い。** 0002 が「書類選考の軸は満点16と分かっているが
呼び名が無いので入れない」と保留したのと同じ理由である ――
こちらで名付けると、運営が使っていない語がマスタとして固定化する（原則3）。

### 特別選考を2期の先頭に置いた（0005）

事前面談は**応募より前の関門**なので `sort_order` は先頭。最終面接は最後のまま
（合格の定義を動かさない）。2期は 特別選考 → 応募受付 → 書類選考 → グループ面接 → 最終面接。

#### ★ 段を1つ足したら、位置を番号で指すテストが静かに壊れた

`tests/23〜33` の作業用ヘルパは2期の段を並べて**位置で指していた**（`steps[1]` ＝ 書類選考）。
先頭へ足すと全部が1つずれる。さらに `tests/23` の20周シミュレーションは
「落とす段」を `1..last` の連番から選んでおり、段が 2..5 へずれると
**どの段にも当たらない値を引いて「落としたはずが通ってしまう」。**

★ **番号で位置を仮定すると、段が増えたときに黙って壊れる。**
実在する段の `sort_order` から選ぶよう直した。

---

## 2. 縦タブと横タブを1枚の帯にした

依頼者の指摘 ――「横タブと縦タブは結合して同じレイヤーにしろ。**幅が違う。**」

### ★ 「幅が違う」の正体は、指定したはずの高さが潰れていたこと

ロゴ枠は `--logo-h`（67px）を指定している。にもかかわらず実測は **24px** だった ――
縦バーが縦の flex なので、既定（`flex-shrink: 1`）で潰れていた。

★ **同じ形を踏むのは3度目である。**

```
C-66   `--logo-h` を指定したのに、送る画面だけ 81 → 58 に潰れた
C-113  操作柱の評価基準が 0 まで潰れ、見出しだけが残った
実行⑬  ロゴ枠が 67 → 24 に潰れ、隣の横バーと厚みが違った
```

★ **高さを指定した枠には `flex: 0 0 auto` を必ず添える。**

### ★ 天端も 16px ずれていた

縦バーは `position: sticky; top: var(--space-md)`。`top` は**枠の余白の内側**から
測られるので、余白ぶんさらに下がっていた。`top: 0` にした。

実測 ―― 天端 16 ・ 厚み 67 ・ 右端 220 で接続 ・ 下端 83。**4つとも一致。**
厚みが同じでも天端がずれていれば2枚に見える。

### 評価軸は動かさず、全件を出す

依頼者の指示 ――「評価軸は動かさなくていいので全部入るように」。

流れているものは狙って読めない。折り返して全件を同時に出した
（実測: 11軸すべて可視・見切れ 0・送り 0）。
流していた頃は同じ並びを2組出してつないでいたが、止めた以上そのぶんは重複なので1組にした。
`prefers-reduced-motion` の分岐も落とした ―― **減らす動きが無い。**

### ★★ 「出る」ボタンが押せなくなっていた

帯を測っている途中で見つけた。縦バーの足元（期の切り替え・デモ札・**出る**）が
**60px はみ出して、枠の外にあった。** 高さは固定なのに中身が縮まず、
あふれたぶんが切り取られていた ―― **画面から出る手段が消えていた。**

縦バーごと送るとロゴまで流れて帯がそこで切れるので、**送るのはタブの並びだけ**にした
（ロゴと足元は動かさない）。C-113 で操作柱に対して行ったのと同じ手当てである。

★ **切れているものは読めないのと同じ**（C-65）。今回は読めないだけでなく、押せなかった。

---

## 3. ホームから詳細へ飛べるようにした

依頼者の指示 ――「ホームから、気になるセクション押したらそこまで飛べるように」。

```
候補者 → /people        連携団体 → /approach      通常選考 A以上 → /borderline
特別選考 → /headhunting  推移 → /funnel            ピックアップ → /headhunting
```

★ 行き先は `canOpen` で守る。**ホーム専用の層判定を書かない**
（2箇所に分かれると必ず食い違う）。開けない層にはリンクを渡さず素の表示に倒す。

実測 ―― `personal` 層では特別選考のタイルとピックアップの見出しが**リンクにならない。**
押すと弾かれる窓を残さない（実行⑪と同じ規律）。

---

## 4. 3周まわして点検した（依頼者の指示）

依頼者の指示 ――「面接シート以外のフォームがスプシ形式になっているか／
表に必要のない項目がないか／エクセルにある内容でDBにないものはないか／
それが終わったら、詰めていない階層がないようにテストして。**こちらのループを確実に3回。**」

### ① フォーム ―― `/staff/new` だけが取り残されていた

1件ずつの素のフォームで、**まとめて足せず、打ち間違いも直せなかった。**
表にして、既存の入力者も同じ表に並べた（読むための表を別に持たない）。

名前を直す `renameStaff` を足したが、判定は `addStaff` と同じ規則である ――
**表のために判定を書き直さない。** 同姓同名は止めないまま（C-96）。

★ 名前は職員の同一性そのものなので、直すと過去の「記録した人」もその名前で読まれる。
それでも直せるようにしたのは、**直す道が無いほうが害が大きい**ためである ――
打ち間違えた名前は選択肢に残り、選ぶたびに間違いが増える。

### ② 表の列 ―― 減らす対象は無かった

`/people/new` と `/approach` の列を1つずつ見たが、**どれも記録層の値に対応していた。**
出ていて記録に行かない列・使われない列は無い。

### ③ エクセルとの差分 ―― **実データの数で決めた**

`011_提携団体` を列ごとに数え、**入っている件数**で足すかを決めた。

```
足した      担当部署 168/179 ・ 社内担当 41/179
足さない    所在地・SNS・関連URL   **0件**（列はあるが1件も入っていない）
            推薦可能人数            2件
          ★ 最適連絡時期          138件あるが **126件が「1.0」** ――
                                    数字が入っており何を指すのか読めない
```

★ **読めない値を、意味のある列にしない。** 列の存在ではなく中身で判断する。

### ④ ★ 入る道の無い階層が2つあった

`tests/49_reachable_layers.test.ts` を足して、**画面はあるのに入り口が無いもの**を数えた。

```
/reach-zones/{id}   パンくずに「連携団体 › 団体名」を出しているのに、
                    **どこからもリンクされていなかった**（手打ちのURLでしか開けない）
/pilot              試運転の手順。入り口が無い
```

URL を打てば開くので、**動かして触っている限り永久に気づけない。**

#### ★ 孤立を見つける検査が、孤立を隠していた

この検査を**2度間違えた。**

```
1度目  リンクの書き方で拾い漏らし、繋がっている /funnel を「孤立」と数えた
       （`href={to('/funnel')}` の形を正規表現が拾えていなかった）
2度目  自分から自分へのリンクを入り口と数え、/pilot を見逃した
       ―― 孤立した画面が自分で自分を指しているだけで「繋がっている」ことになる
```

★ **数えた結果を疑う前に、数え方を疑う**（実行⑫で2回書いた教訓が、また効いた）。

---

## 5. 保留した3つを、こちらで決めた

依頼者 ――「君が判断して良い。信じて続けろ」。

### ★ 2つは足さなかった ―― 既存の記録層で表せる

```
AWARDへの応募有無     イベントへの参加は**接点（touchpoints）**である。
                      0028 で同じ問いに答えが出ている ――「参加という事実を人に貼る
                      場所はすでにある。新しい属性の列を作ると、同じ事実が2箇所に載る」
関連するアカデミア生   `touchpoints` は `person_id` と `partner_id` の両方を持つので、
                      **その団体経由で接点のある人**として既に導出できる
```

★ **列を足すことは、答えを出すことではない。**
表に列があるからDBにも列が要るとは限らない ――
同じ事実が2箇所に載れば、必ずどちらかが古くなる。

### 推薦枠ステイタスだけ足した（0035）

**これだけが置き場所を持たなかった。**「**2期生**推薦枠ステイタス」は期の言葉で、
団体の現在値にすると 3期を入れた瞬間に 2期が消える。

形は 0016（アプローチ状態）と同じ ―― マスタ＋追記専用の出来事＋最新から導く現在値。
**同じ性質のものに別の形を与えない。** 値は運営の語のまま
（未連絡／メール送信済み／アポ実施済み／対象外）。

実測（デモ）―― 2期で「アポ実施済み」を保存して「1件を直した」。
**3期へ切り替えると推薦枠は空**で、担当部署・社内担当（団体の属性）はそのまま残る。
**期ごとと団体ごとが、画面の上で分かれている。**

### `/pilot` は消さずに繋いだ

画面自身が「手順が終わったら消してよい」と書いているが、**Pilot は HOLD のままで
手順はまだ走っていない** ―― 消すのは観測を取るための足場を先に捨てることになる。
画面が「通常選考 › 試運転」と名乗っていたので、**名乗った親から**入れるようにした。

### 残るフォームはそのままにした

```
/approach/new       団体の新規作成。写真と最初の接触を伴う。1件の作成であって羅列ではない
/people/{id}/edit   1人の詳細。まとめて直す道は `/people/new` の表が持つ
/applications/{id}  採点と判定。依頼者が外した面接シートと同じ仕事
```

★ **「フォームをすべて表にする」ことが目的ではない。**
まとめて打つものが表で、1件を開いて直すものがフォームである。
表にする理由が無いものを表にすると、1件を直すのに表を探すことになる。

---

## 6. 記録層に足したもの

```
0033_criteria_kind.sql            評価軸の重み付け（standard / required / strong）
0034_partner_department.sql       団体の担当部署・社内担当
0035_partner_recommendation.sql   推薦枠ステイタス（マスタ＋追記専用＋現在値のビュー）
0005_season2_special_selection.production.sql   2期の特別選考の段と9軸
```

### ★ 足した列を、テストが1つ捕まえた

表の保存は `btrim($n)` で空白を落としていたが、**`btrim` の既定は半角スペースだけ**である。
全角スペースだけの値が「空ではない」ものとして残り、`*_not_blank` に弾かれて
**行ごと保存できなくなっていた。**

落とす空白を制約と同じ集合にそろえた。0015 で一度学んだ教訓が、
**制約を持たない既存列では「見えない値が静かに溜まる」形で潜んでいた** ――
弾かれて初めて見えた。

---

## 7. 確かめ方

```
pnpm test              582 件 全通過（546 → +36）
pnpm exec tsc --noEmit クリーン
pnpm build             クリーン
```

画面（デモ経路）で通した ――

| 見たこと | 結果 |
|---|---|
| 帯の幾何を実測 | 天端16・厚み67・右端220で接続・下端83。**4つとも一致** |
| 評価軸 | 11軸すべて可視。見切れ 0・送り 0・動かない |
| 縦バーの足元 | 「出る」が押せる（60px はみ出しを解消） |
| ホームの候補者タイルを押す | `/people` へ遷移。期は保持 |
| `personal` 層でホームを開く | 特別選考とピックアップが**リンクにならない** |
| 入力者を追加 | 表で既存も編集できる |
| 団体の表で担当部署・社内担当・推薦枠を保存 | 「1件を直した」 |
| そのまま3期へ切り替え | **推薦枠は空。担当部署・社内担当は残る** |
| 通常選考から試運転へ | `/pilot` に入れる（孤立 0 件） |

★ 途中で1度、`vw = 0` の状態で画面を測って「はみ出し 14 件」と読んだ。
**ペインが休んでいただけで、欠陥ではなかった。** 測る前に、測れる状態かを見る。

---

## 8. 残る課題

実行⑫から引き継いだもの ――

- 認証がない。**誰が入ったかを記録できない**（合言葉は層で共有。C-84）。
  だから表の「入力者」も、メモの「書いた人」も**自己申告である**
- 本番はデモモードのまま。実データを出すのは依頼者の判断（C-94）
- ⑧⑨の関わり方は自由入力なので**集計できない**。語を受け取ったらマスタへ
- 点の訂正（E4）は未実装
- 顔写真はDB内の data URL。大量運用前にオブジェクトストレージへ
- 予定とやることを画面から作る・終える入口がまだ無い
- 2期の集客開始日が推測値のまま
- 表は全件を出す（ページ送りは無い）。**数百行になったときの重さは測っていない**
- 団体の名前・既存行の接点は表で直せない
- Pilot判定は **HOLD**

実行⑬で増えたもの ――

- **書類選考・グループ面接の採点軸が無い**（呼び名が応募管理表に無い）。
  段と結果の数値だけが分かっている状態
- **`シート52` の39人の選考結果を取り込んでいない** ―― 二次選考結果の満点が
  表から読めず、書類選考の軸の呼び名も無い。**分母の無い点を入れない**
- 応募管理表で置き場所を保留したまま残っているものは無いが、
  **足さないと決めた2つ（AWARD応募有無・関連するアカデミア生）は、
  接点として入力する道がまだ画面に無い**（記録層にはある）
- 推薦枠ステイタスは**画面から訂正できない**（打ち消し行は SQL でしか書けない）
- `/pilot` は繋いだが、**Pilot が走れば役目を終える。** 観測が取れたら消す
- 3期は特別選考の9軸を持つが、**2期で完成させたものを3期へ適用する作業は未着手**
  （依頼者の指示は「まず2期で完成させて、それを3期に適用していく流れ」）
