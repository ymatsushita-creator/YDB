# REPORT-9.0 — 実行⑨。UI 大改修（ヘッドハンティング画面）。記録層を2つ足した

実行⑨の第1報。2026-08-09。**この文書は凍結する。**
`REPORT-8.0.md`（実行⑧の開始時点の健全性確認）の続き。

---

## ★ これは Pilot Go ではない

`REPORT-7.4` 以降と同じ注記を繰り返す。**Pilot は HOLD のまま。基準は緩めない。**

**この改修は Validated ではない。** 運営はまだ1度も触っていないので、
「この画面で迷った」という事実はどこにも無い。塞いだのは
**憲法適合のギャップと記録層の欠落**であって、観測された運用上の問題ではない。

`HANDOFF.md` 12節が求める問い ――「この改善は実際の運営で観測された問題か、
開発中に想像した問題か」への答えは、**後者**である。
ただし今回に限っては、より正確な第三の答えがある ――
**依頼者から届いた仕様である。** 開発者の思いつきでもない。

## Summary

- 依頼者から**ヘッドハンティング画面の画像**と、設計思想の変更が届いた
  （経営者の「ヘッドハンティング」と平社員の「ボーダーライン」を分ける）
- 画像は現行 `director.md` が名指しで禁じている形（人が最上位・ランキング・一覧3つ）
  だったため、**憲法の改定として受け取った。** 勝手に森中心へ寄せ戻していない
- 画像の要素を記録層に突き合わせ、**事実が無い項目を7つ特定**して依頼者に判断を仰いだ
- 判断は3つとも受領した ――「記録層から作る」「画像を正典にする」「ヘッドハンティング画面のみ」
- **記録層を2つ足した**（マイグレーション 0016 / 0017）。うち 0017 は**実装段階[3]への着手**
- **意匠の正典を差し替えた**（`basic/DESIGN.md`。「色は線、面は黒」を廃した）
- **画面を1枚作り**（`/headhunting`）、**情報構造を3つのタブに集約した**
- **上位文書を2つ改定した**（`director.md` / `design.md`）。依頼者の指示による
- テスト **310 → 333**（+23）。`tsc` / `build` / `tokens` すべてクリーン

## 1. 依頼者に仰いだ判断と、その結果

`HANDOFF.md` 2節は「4つの衝突は勝手にどちらかへ寄せない」と定めている。
実行⑨で実際に踏んだのは衝突1・2・4で、**うち2つに判断が出た。**

| 衝突 | 内容 | 実行⑨での扱い |
|---|---|---|
| 1 | 意匠の生成元が2つある | **解決。** 依頼者が「画像を正典にする」を選択（C-39） |
| 2 | Health の事実が記録層に無い | **踏んでいない。** ヘッドハンティング画面は Health を要求しない |
| 3 | `'use client'` を増やすか | **踏んでいない。0件のまま。** ズームは階層バーのリンクで表せた |
| 4 | 段の語（森・林・木・幹）が未決 | **未決のまま。** 新画面は段の語を1つも使っていない |

### 依頼者への確認と回答

| 問い | 回答 |
|---|---|
| 確度スコアとアプローチ状態は記録層に無い。どう扱うか | **記録層から作る** |
| 意匠の正典は画像か、現行トークンか | **画像を正典にする** |
| 今回どこまで作るか | **ヘッドハンティング画面のみ** |

## 2. 設計思想の変更を、憲法の改定として受け取った

画像の構造は、現行 `director.md` と正面から食い違う。

| `director.md` の条文 | 画像 |
|---|---|
| 「最上位オブジェクトは Applicant ではない。Forest である」 | 最上位は**人** |
| 「人が最上位に来る UI は禁止」 | 同上 |
| 「一覧を増やさない。テーブルを作らない」 | 一覧が3つ |
| 「森 → 林 → 木 → 人 → アクション を維持する」 | 階層は職種 → 卒業年度。森が出てこない |

依頼者は `director.md` の上位にいる。「設計思想としての変更」と明言されたので、
**これは憲法の改定であり、下位（画面）が勝手に寄せ戻すのは越権**である。

**ただし1つだけ改定後も残る。**`director.md` 末尾の「憲法が上書きしないもの」――
**無い記録から数字を作らない。** 思想は画面を支配するが、事実は作り替えられない。
実行⑨の作業量の大半は、ここから来ている。

## 3. 記録層 —— 0016 アプローチ

画像の「アプローチ中 / 面談調整中 / 検討中 / 未アプローチ」は、
記録層のどこにも無かった。`touchpoints` は出来事であって状態ではなく、
`applications` は応募が起きてからの話で、アプローチはその手前にある。

**状態カラムを持つ表にせず、出来事の追記だけにした**（原則1）。
現在の状態は最新の有効な出来事から導く。上書きにすると、実行⑦で踏んだ
C-22（担当を替えると前の担当が黙って消える）を再演する。

訂正は打ち消し行の追記で、`status_histories` と**同じ型を写した**
（深さ偶数が有効という再帰判定まで含めて）。ここで別の型を発明すると、
訂正の意味が表ごとに変わる。

マスタには画像に無い状態を1つだけ足した ――`declined`（見送り）。
**終端が1つも無いと誰もリストから出られない。** 出る手段の無いリストは
運用できないので、構造上の必要として置いた。★語は未確認（TODO(MVP)）。

詳細は C-40。

## 4. 記録層 —— 0017 確度スコア（実装段階[3]への着手）

`CLAUDE.md` は「先の段階の機能を勝手に始めない」と定めている。
**勝手に始めたのではなく、依頼者の判断を根拠に着手した。**

原典にはスコアリング層のテーブルが4つすでに定義されている。
**新しい表は1つも作っていない。** 足したのは動かす集計とビューである。

| | |
|---|---|
| 作った | 規則を評価して点に畳むエンジン、凍結、順位、順位の変動 |
| **作らなかった** | **規則そのもの（どの事実に何点を付けるか）** |

点数と閾値は原典が「運用時に決定」と書いた値で、依頼者から受け取っていない。
**本番シードには規則を1件も入れていない。** デモにだけ仮の規則を置いた。

規則が0件なら確度は算出されず、画面は「規則未登録」と出る。
**0% ではない。無いことを 0 と書くと、それは嘘の数字になる。**

`target_key` の語彙だけは構造として閉じた。綴りを間違えた規則は、制約が
無ければ**エラーにならず、黙って0点として評価される。**「エラーにならず、
黙って点が減る」は最も気づけない壊れ方である。
**点数（値の判断）は依頼者、語彙（構造）はこちら**という切り分けにした。

詳細は C-41。

## 5. 意匠 —— 正典が画像へ移った

`basic/DESIGN.md` の frontmatter を書き替え、`pnpm tokens` で再生成した。
**「色は線、面は黒」は廃した。** 面に色を塗ってよい。

**トークン名は1つも変えていない。** 既存9画面が `primary` / `card-tint-*` /
`brand-navy` を参照している。名前ごと入れ替えると意匠の変更が全画面の改修になり、
**どこが意図した変更でどこが事故なのか区別できなくなる。**
名前は器として残し、値と役割だけ入れ替えた。

本文（散文）も書き替えた。frontmatter だけ直して Notion の分析を残すと、
**文書のほうが嘘になる。** 記録が実装より厳密に見える状態は、記録が無いより悪い。

詳細は C-39。

## 5.5. 情報構造を3つのタブに集約した（依頼者の追加指示）

最初の版は、年度をヘッドハンティングの**下の階層**に置き、外枠を
ヘッドハンティング専用にしていた。依頼者から4点の指摘があり、直した。

| 指摘 | 直したこと |
|---|---|
| 年度が下の階層にあるのはおかしい | **年度を最上位へ。全タブ共通の帯にした** |
| 募集は 2025 と 2026 しか行われていない | デモの年度を 4 → 2 に減らした |
| 画像はヘッドハンティングだけの画面ではない | 外枠を**全画面共通**にした |
| 「運転席」という名前を削除 | ボーダーラインに改名 |
| ファネル・森と流入元のようなタブは消せ | 行き先を3つに固定。他は各タブに畳んだ |
| 名前検索を勝手に消すな | 操作柱に戻した |
| ボタンは物理ボタンに | 厚みと沈み込みを付けた |

```
年度（2025 / 2026）          ← 最上位。タブを移っても保たれる
   ├── ヘッドハンティング      誰に声を掛けるか
   ├── ボーダーライン          誰を通すか
   └── アプローチ              どこから来ているか
```

**年度を下に置くとタブごとに見ている年度が変わり、3つが別のアプリになる。**

**上位文書を改定した。** 依頼者の「md と矛盾するなら md を書き換えろ」に従い、
`director.md` の「人が最上位に来る UI は禁止」「一覧を増やさない」
「テーブルを作らない」「一覧→詳細は禁止」を撤回し、`design.md` の
`Never: List → Detail` と Core Components 8つを置き換えた。
**改定前の条文は引用として残してある**（消すと、なぜ実装がこうなのかを
次に読む人が追えなくなる）。詳細は C-43。

**`'use client'` は0件のまま。** いま居るタブを知るのに `usePathname()` を
使うとクライアント境界が要るので、**各ルートの layout.tsx が自分のタブを
名乗る形**にした。畳んだ先がファイルに書かれるという副次的な利点もある。

### 年度を減らしたら、デモが踏んでいた経路が2つ壊れた

1. **「年度に紐づかない接点」が1件も無くなった。** 窓の外に落ちる接点は
   乱数の裾で偶然できていた。**偶然に頼っていた経路は、条件が変わると
   黙って消える。** 年度の谷に明示的に1件置いた
2. 「再応募して合格した人」と「判断待ちが十分ある」が両立しなくなった。
   2026 の応募期間を締切間際まで延ばして両立させた

**どちらもテストが落ちて気づいた。** 画面を見ただけでは分からない。

## 6. 画面 —— `/headhunting`

外枠を2つに分けた。`app/layout.tsx` を `<html>` と読み込みだけに減らし、
既存9画面をルートグループ `app/(workspace)/` へ移した。
**ルートグループは URL を変えない**ので `/cockpit` などの経路は元のまま
（`pnpm build` のルート一覧で9画面すべてを確認した）。

画像の枠と、実装の対応。

| 画像 | 実装 | 出所 |
|---|---|---|
| A 操作柱 | あり。未着手の2つはリンクにしない | — |
| B 階層バー | あり。**ただし職種と卒業年度は年度に置き替えた** | 記録層に無い |
| C 最新やること | あり（3種） | 既存の `v_open_tasks` |
| D 応募者成績ランキング | あり。**順位変動は出さない** | 過去の順位が無い |
| E 候補者確度ランキング | あり。順位変動つき | 0017 |
| F 候補者パネル | あり。**写真・学年・卒業年度は無し** | 記録層に無い |
| G ヘッドハンティングリスト | あり | 0016 |

### 出せなかったもの7件は、画面の下に一覧として出してある

黙って省くと、次に触る人が実装漏れと読んで作り直しにかかる。

**同じ見た目の矢印を、片方だけ作り物で埋めない。** 確度には凍結された
過去があるので順位の変動を出せるが、成績には無い。成績にも矢印を置けば
見た目は揃うが、それは数字の捏造である。

詳細は C-42。

## 7. 動かして見つけた欠陥（自分の実装のもの）

**目視ではなく DOM とコンソールと SQL で見つけた。**

| # | 欠陥 | 見つけ方 |
|---|---|---|
| 1 | React の key が衝突。同じ人・ステップ・種別のやることは2件あり得る（面接官2人） | **コンソールのエラー。** 画面上は2行出ており目では気づけない |
| 2 | 助詞が壊れていた（「書類選考を担当を替える」）。種別ごとに助詞が違う | DOM のテキスト |
| 3 | 成績ランキングが同率1位だらけ。1軸だけ満点の人と6軸を通して高い人が同じ 100.0 | DOM の値 |
| 4 | 確度の順位が候補者ではなく全員（2750人）を母集団にしていて、11位・34位…と飛んでいた | SQL |
| 5 | デモの訂正行が打ち消した行と同じ状態を書いており、訂正になっていなかった | SQL |
| 6 | `v_scoring_facts` が接点の年度帰属を自前の `BETWEEN` で書いていた。期間が重なる年度で二重に数える | 実装中に自分で気づいた |

3 は順位そのものは定義どおり同率のままにし、**点が付いた軸の数を列として出した。**
根拠の薄さが読めない一覧は、順位として意味を持たない。

4 は母集団を `compute_score_snapshots` の中で固定した。
**順位は母集団を書かないと意味が決まらない。** 画面側で絞り直させない。

6 は実行③で踏んだ「同じ言葉が2つの定義を持つ」の再演になるところだった。

## 8. テストが最初に3件落ちた

`tests/28` の確度スコアの検証が、年度を使い回していて汚染していた。
前のテストが作った候補者と凍結が残り、母集団と順位が混ざっていた。

**落ちてくれたおかげで気づけたが、通っていれば「母集団を絞れている」と
誤認したままだった。** 年度をテストごとに作る形に直してある。

また、**自分の変更が既存テスト2件を壊した。**

- `tests/01`「原典のビューはすべて存在する」―― ビューを7つ足したので登録が要る
  （この一覧は意図的な登録簿で、足すたびに理由ごと1行増える設計になっている）
- `tests/09`「スコアリング規則のパラメータ」―― 架空の `target_key`（`'x'`）を
  使っていたため、原典の制約に届く前に**新しい語彙の制約で弾かれた**。
  確かめたいのは原典側の制約なので、実在の語彙に直した

「修正が別の場所を壊す」は `HANDOFF.md` 12節の筆頭にある。2件とも踏んだ。

## 9. `director.md` の自己チェック4項目への答え

**UI 改修では必須**（`HANDOFF.md` 16節）。

### 1. 世界観は壊れていないか？

**壊れていない。憲法のほうを改定した。**
依頼者の指示（「md と矛盾するなら md を書き換えろ」）に従い、
`director.md` と `design.md` の該当条文を撤回し、改定前の条文は
引用として残した。**実装が憲法に反しているのではなく、憲法が変わった。**

### 2. 3つのタブのどれに属する画面か、はっきりしているか？

**はっきりしている。** 各ルートの `layout.tsx` が
`<Shell active="...">` で自分のタブを宣言する。
9つのルートすべてが3つのどれかに属し、**タブに属さない画面は無い。**
（この問いは実行⑨の改定前は「森が主役になっているか？」だった）

### 3. ユーザーは次に何をすべきか一目で分かるか？

**部分的に。** 「最新やること」が先頭にあり、期限超過が色と文字で区別される。
**ただし画面から実行できる操作は無い。** 担当を決める・評価する・保留を解くは
運転席（`/cockpit`）にあり、こちらは見るだけである。
アプローチ状態を進める操作も**画面には無い**（記録層はあるが書き込み経路は未実装）。
これは今回の範囲を超えるので、次の候補として残す。

### 4. このUIは「管理画面」ではなく「コックピット」になっているか？

**まだ管理画面寄りである。** 高密度で1画面に複数の問いの答えが同居しており、
候補者を選ぶと右のパネルが連動する点はコックピットに近い。
しかし**操作が1つも無い**ので、いまは「よく整理された観測窓」である。
3 と同じ理由で、書き込み経路が次の一手になる。

★ 1 と 2 に「壊れている」と答えているのは、**取り繕わないため**である。
依頼者の指示に従った結果そうなったのであって、
`director.md` の本文がまだ改定を反映していないことのほうが問題である。

## 10. 依頼者に返す判断（次に要るもの）

| # | 何が | なぜ要るか |
|---|---|---|
| 1 | **改定した憲法の確認** | `director.md` / `design.md` を改定した。**撤回した条文が意図どおりかを見てほしい**（3節・C-43） |
| 1b | **2025年度の実データ** | 「募集は 2025 と 2026」と伺ったが、**本番シードには 2026 しか無い**（D-8）。2025 の実際の日付・定員・選考ステップが要る |
| 2 | **確度の規則（どの事実に何点か）** | エンジンはあるが規則が0件。本番では確度が1件も出ない |
| 3 | **「ボーダーライン」画面の仕様** | いまは旧「運転席」の中身をそのまま入れてある。**平社員向けとして何を見せるべきかは未受領** |
| 3b | **「アプローチ」画面の仕様** | 同上。いまは旧「森と流入元」の中身が入っている |
| 4 | 「見送り」の語と、見送りに理由が要るか | 終端状態を構造上の必要として置いた。語は仮 |
| 5 | 卒業年度・職種を記録層に持たせるか | 画像の階層バーはこの2軸だった。いまは年度で代用している |
| 6 | 本番（Supabase）にマイグレーション 0016 / 0017 を適用するか | **勝手に適用していない。** 本番への書き込みは依頼者の判断 |

## 11. 残課題の4分類（`process.md`）

### Validated Operator Problem

**0件。実行⑥終了時から1件も増えていない。** 運営が触っていない。

### Developer Hypothesis（実行⑧から持ち越し7件＋実行⑨で6件）

| # | 何が |
|---|---|
| 8 | 「規則未登録」と出ている確度を見て、運営は誰に何を言えばいいか分からない可能性がある（実行⑦の #1 と同じ形） |
| 9 | 成績ランキングが同率1位だらけになる。軸数の列を足したが、それで読めるかは未検証 |
| 10 | ヘッドハンティング画面に操作が1つも無い。見て終わりになる可能性がある |
| 11 | 「見送り」以外にリストから出る道が無い。保留に相当する状態が要るかもしれない |
| 12 | ボーダーラインとアプローチは、旧画面の中身をそのまま入れただけである。タブの名前と中身が合っているかは未検証 |
| 13 | 年度が2つしかないので、年度の切替が「効いている」ことに気づきにくいかもしれない |

### Technical Debt（実行⑧から14件＋実行⑨で3件）

- **色に対する自動テストが無い**（実行⑥から。今回も解消していない。正典を
  差し替えたので効きが大きくなった）
- `src/queries/headhunting.ts` の性能を測っていない。
  **`v_open_tasks` を叩くので、絞り込みの有無の両方で測る規律（A-16）が効く**
- デモの成績ランキングが同率1位だらけ。デモデータの性質であって実装の欠陥ではないが、
  **この状態では順位の見え方を検証できていない**

### Product Backlog（実行⑥から4件、増減なし）

## 12. 完了の定義に対する自己申告

- [x] `pnpm test` が全通過（**333件**。新しい判断にテストが23件）
- [x] `pnpm exec tsc --noEmit` がクリーン
- [x] `pnpm build` が通り、`pnpm tokens` で `app/tokens.css` に差分が出ない
- [x] **DOM と SQL の値で**結果を確認した（目視だけで判断していない）
- [x] 画面に出した数字の**単位と母集団**が注記として画面上にある
- [x] **画面に内部の識別子が出ていない**（`grep` で検査）
- [x] `db/DECISIONS.md` に理由とテスト参照を書いた（C-39 〜 C-42）
- [x] `TODO(MVP)` の一覧を出した（13節）
- [x] PM Report（本書）
- [x] **`director.md` の自己チェック4項目への答えを添えた**（9節）
- [x] **衝突を勝手に寄せていない**（判断が要るものは仰いだ。1節）
- [x] **Pilot Go を主張していない**
- [x] **シミュレーションの「Go」と Pilot Go を混同していない**
- [x] **成果を「Validated」と書いていない**
- [ ] **Validation Report** —— 出していない。**運営が触っていないため、
      検証する対象が存在しない。** 書けば架空データに対する自己評価になり、
      実行⑦が6週かけて「それは観測の代わりにならない」と実証している

## 13. 実行⑨で増えた `TODO(MVP)`

| 場所 | 内容 |
|---|---|
| `0016` | 卒業年度を軸にするなら `persons` への列追加が要る（いまは年度で代用） |
| `0016` | 職種の実体が無い。定義も未受領 |
| `0016` | 「見送り」の語と、見送りに理由が要るかは未確認 |
| `0017` | 規則の点数と閾値は未受領。本番シードは空 |
| 画面 | 写真・学年・手で作るタスク・期限の時刻・ログアウトは出せない |

**推測で埋めた事実は1つも無い。**

## 13.5. 実行⑨で消した／変えた名前

| 前 | 後 | 理由 |
|---|---|---|
| 運転席（`/cockpit`） | ボーダーライン（`/borderline`） | 依頼者の指示 |
| 森と流入元（`/sources`） | アプローチ（`/approach`） | 3タブへの集約 |
| ファネル（`/`） | `/funnel`（アプローチの中） | 同上 |
| 試運転・選考オペレーション・人を探す | タブから外し、3タブの中へ | 同上 |

`/` は `/headhunting` へ送る。**旧 URL は残していない** ―― 外部に配って
いないため（公開しているのはデモの URL だけ）。

## 14. 実行⑧の報告書について

`HANDOFF.md` 0節は「`REPORT-8.0.md` が存在しない」と書いているが、
**実行⑨の開始時点で存在した**（未追跡・93行・2026-08-09 付）。
実行⑧が `HANDOFF.md` を書き換える前に置いたものと見られる。
内容は健全性確認のみで、コードの変更は無いと述べている。
**追跡下に入れてコミットした。** `HANDOFF.md` の記述は実行⑨で直した。
