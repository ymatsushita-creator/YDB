# REPORT-6.10 — 残課題の分類と、次の優先順位（実装なし）

2026-08-07。**このサイクルはコードを1行も変えていない。**
新ルール（1機能ごとに立ち止まり、分類と優先順位を提示する）の1回目。

`REPORT-6.0` 〜 `6.9` は凍結済み。**この報告書も書いた時点で凍結する。**

---

## 0. まず、いちばん重要なこと

**「Observed Operator Problem」に該当する項目は、1件も無い。**

このプロダクトは**まだ誰も運営として使っていない。** 検証はすべて
開発者が1周を通したときのもので、実際の採用業務での観測は0件である。

これは分類の結果というより、**いまの制約そのもの**である。
後述の優先順位で Confidence が最大 4、多くが 1〜2 に留まるのは、
機能の作りの問題ではなく**観測が無いこと**が原因である。

---

## 1. 残課題の分類（全 19 件）

### A. Observed Operator Problem（実際の運営で観測された問題）

| # | 内容 |
|---|---|
| — | **該当なし。** 運営担当者がまだ使っていない |

### B. Developer Observation（開発者が使って気づいたこと）

| # | 内容 | どう気づいたか |
|---|---|---|
| B-1 | **辞退（`withdraw`）を記録できない。** 合格者が辞退しても記録手段がなく、辞退者が「合格のまま」残る。定員の充足率が実際より高く見え、席が空いたことも分からない | 記録層とデモには `withdraw` の記録があるのに、画面に入口が無いことに気づいた。**頻度の「必ず起きる」はドメインからの推論であって観測ではない** |
| B-2 | **1軸ごとに画面が再読み込みされる。** 3軸なら3回、面接官2人なら6回 | E2 の検証で実際に6回クリックした |
| B-3 | **点を直せない**（`already_scored`） | E2 のテストで再保存を試して弾かれた |
| B-4 | **判定を「判断待ちに戻す」ことができない。** 表せるのは差し替えだけ | D2 の実装中に記録層の限界として分かった |
| B-5 | **判定・訂正に確認ダイアログが無い。** 1クリックで効く | D2 の検証で実際に1クリックで訂正が通った |
| B-6 | **利益相反になる担当を、選ぶ前に止めていない。** 選べてしまい、直後に「担当を替える」が出る | C-20 で意図的にそうした。1手戻ることを実データで確認した |
| B-7 | **やることの種別に優先順位（重み）が無い。** 並べ替えは期限と待ち日数だけ | 運転席を見て、どの種別から手を付けるべきかが画面から読めないと感じた |

### C. Technical Debt（運営には見えない内部の負債）

| # | 内容 | 影響 |
|---|---|---|
| C-1 | 軸の適用規則が、トリガと問い合わせの2箇所にある | 食い違えば「画面が出したのに保存できない軸」。テストで固定済み |
| C-2 | 記録層の拒否を、制約名とトリガの文面で写している | 文面が変われば写しが外れる。テストで落ちる |
| C-3 | 通過→不合格に訂正すると、次のステップの評価行が宙に浮く | 集計は結末で絞るので数字は狂わない。行は残る |
| C-4 | 担当を決めた・解いた・替えた記録が残らない | 「前は誰だったか」が追えない |
| C-5 | 森と林を別テーブルに分けるか未決 | いまの階層で足りている |
| C-6 | 森と人の結び付きが「接触があった」でしかない | 所属・役割を表せない |
| C-7 | 森の画面の「接点がある人」は 60 人で打ち切り | 画面に注記済み |
| C-8 | 全軸そろう前の確定を認めていない | 運用が要求すれば緩める |
| C-9 | `seasons` の実数値が未確定（D-8） | **本番シードに年度が1行も無い。本番で使う前に必要** |
| C-10 | 個人情報削除を受けた人の応募を木に数え続けるか未確定（D-10） | 過去年度の数字が動く変更 |
| C-11 | 段の語（森・林・木・幹）が旧定義のまま（D-11） | 旧語と新語が同居している |
| C-12 | `reject` のステップは実行⑥以降の判定だけに入っている | 集計時に母数の注記が必要 |

### D. Product Backlog（まだ必要と確認できていない機能）

| # | 内容 | なぜ後回しか |
|---|---|---|
| D-1 | **手で作るタスク**（連絡する・催促する・資料を送る） | 記録層に Task の実体が無く（`domain.md` 10-2 未決）、運用で何が必要かも未観測。**潜在的には最大だが、確度が最も低い** |
| D-2 | 申し送り（`handover_note`） | 入口は小さいが、要る場面を観測していない |
| D-3 | 新規応募の登録 | 応募は外部フォームから来る想定。今日の運用は既存の応募を進めること |
| D-4 | Forest Health / Owner / Relationship | `process.md` が暫定扱いに置いている。記録層に事実が無い |

---

## 2. 優先順位 = Impact × Frequency × Confidence

各項目を 1〜5 で見積もった。**Confidence は「これが本当に解くべき問題だと
言い切れる度合い」**で、観測が無いものは低くなる。

| 候補 | Impact | Frequency | Confidence | 積 |
|---|---|---|---|---|
| **B-1 辞退の記録** | **5** | **4** | **4** | **80** |
| B-2 1軸ごとの再読み込みの解消 | 3 | 5 | 2 | 30 |
| B-3 点の訂正 | 3 | 4 | 2 | 24 |
| D-1 手で作るタスク | 4 | 5 | 1 | 20 |
| C-9 `seasons` の実数値 | 5 | 1 | 3 | 15 |
| D-2 申し送り | 2 | 2 | 2 | 8 |
| B-5 確認ダイアログ | 2 | 2 | 2 | 8 |
| B-7 種別の重み | 2 | 3 | 1 | 6 |
| B-6 利益相反の事前警告 | 2 | 1 | 3 | 6 |
| B-4 判断待ちに戻す | 2 | 1 | 1 | 2 |
| D-3 新規応募の登録 | 2 | 1 | 1 | 2 |

見積りの根拠（主なもの）。

- **B-1 の Impact 5** ―― 辞退が記録できないと**画面の数字が嘘になる。**
  充足率が実際より高く出て、席が空いたことも分からない。
  これは「機能が無い」ではなく「数字が間違う」であり、優先順位 #2 に当たる
- **B-1 の Confidence 4** ―― 5 にしないのは、**観測ではなくドメインからの
  推論**だからである。ただし記録層とデモには辞退の記録が実在し、
  `v_application_outcome` は辞退を最優先で解釈する。**設計者は辞退が
  起きる前提で作っている**ので、確度は高い
- **B-2 の Frequency 5、Confidence 2** ―― 毎回の評価で必ず起きるが、
  **面接中に許容できるかは分からない。** 直すにはクライアント境界の判断が
  別に要る（`HANDOFF.md` の規律）。観測なしで境界を動かすのは早い
- **D-1 の Impact 4、Confidence 1** ―― 連絡・催促は運用の大半を占めるはずだが、
  **何をどう記録したいかを1つも観測していない。** ここで作ると
  「想像した運用」を記録層に固定してしまう
- **C-9 の Frequency 1** ―― 一度決めれば済む。ただし**本番で使う前には必須**

---

## 3. Recommendation

### Option A — 実運用で最も大きなリスクを減らす

**辞退（`withdraw`）を記録できるようにする。**

- **Operator Value**: 辞退した合格者が「合格のまま」残らなくなる。
  充足率が正しくなり、席が空いたことが分かる。
  **いま画面に出ている数字が正しくなる**という値である
- **Implementation Cost**: 小（1サイクル）。`status_histories` に `withdraw` を1行追記するだけ。理由が必須（0008）
  なので `withdraw_reasons` の選択欄が要る。判定（D1）と同じ型がそのまま使える
- **Risk**: 低。追記のみ。`v_application_outcome` は辞退を最優先で解釈し、
  ファネルの `net_accepted_cum` も辞退を控除済み ――
  **集計側は最初から辞退を待っている**
- **Recommendation**: **これを推す。** 積が 80 で2位（30）の2.7倍。
  Impact・Frequency・Confidence のどれも欠けていない唯一の候補である

### Option B — 最も大きな運営価値を追加する

**手で作るタスク（連絡する・催促する）を記録できるようにする。**

- **Operator Value**: 潜在的には最大。運用の大半は「誰かに連絡する」であり、
  いま運転席に出るのは選考の内部作業だけである。
  **運営の実際の1日は、この外側にある可能性が高い**
- **Implementation Cost**: 大。`tasks` テーブルの新設が要る。
  そして `domain.md` 10-2（Task の定義：誰の・何に対する・いつまでの・
  どの状態か）が**未決のまま**である
- **Risk**: **高。** 何を記録したいかを1つも観測していない。
  ここで決めると「想像した運用」を記録層に固定する。
  記録層は後から変えるのがいちばん高い（原則3・原則4）
- **Recommendation**: **いまは推さない。** 観測を待つ価値がこの項目に
  いちばん大きい。運営が「いまどこに連絡の記録を書いているか」を見てから作る

### そして、機能より先に推したいこと

**優先順位表のすべての Confidence が 4 以下である。** 最大の制約は
実装ではなく**観測が無いこと**なので、次にいちばん価値があるのは
**運営担当者に1周使ってもらうこと**だと考える。

30 分で終わる形の案（これも実装ではなく手順である）——

```
1. /cockpit を開き、今日やることを上から3件片付ける
2. うち1件は「評価する」を選び、点と根拠を最後まで入れて確定する
3. 判定まで進める（通過でも不合格でもよい）
4. わざと1件、押し間違えて訂正する
5. 手が止まった場所・迷った場所・別の場所に書きたくなった内容を教えてもらう
```

5 で出てきたものが **Observed Operator Problem** になり、
この優先順位表は書き換わる。**Option A を実装するかどうかも、
その観測の後で決めたほうが確度が上がる。**

**最終的な優先順位はご判断に委ねる。**

---

## 4. いまの状態（引き継ぎ用）

- 採用の1周が画面の中で閉じている（`REPORT-6.8`）。判定の訂正も入った（`REPORT-6.9`）
- `pnpm test` **272 件** 全通過、`pnpm exec tsc --noEmit` クリーン、
  `pnpm tokens` 差分なし、`'use client'` **0件**
- コード中の `TODO(MVP)` は **31 箇所**（8ファイル）
- **本番で使う前に必須なものが1つある** ―― `seasons` の実数値（C-9）。
  本番シードに年度の行が1つも無いため、いまのままでは本番データベースで
  画面が空になる
