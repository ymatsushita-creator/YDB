# director.md — プロダクト原則

本書に序列は無い。権限を持つのは `Hitler.md` だけである。
本書が扱うのは**何を作るか**であって、規律ではない。
旧原則はGit履歴と凍結報告書に残るため、現行要件と同じ文書に併記しない。

2026-08-19、`vision.md`（内容未受領のまま枠だけ在った）と
`SUPERVISOR.md`（多層序列の廃止で権限の条文が空になった）を本書と `process.md` へ畳んだ。

## 目的

YouthDB は、起業家アカデミーの集客・候補者選定・選考を前進させる
運営コックピットである。単なる候補者台帳や報告画面にしない。

> このプロダクトは採用管理システムではない。
> 起業家アカデミー全体の「採用エコシステム」を運営する OS である。
> ユーザーが管理するのは応募者ではない。
> 人との関係性であり、コミュニティであり、採用可能圏(Reachable Network)である。
> 応募はその途中に過ぎない。

★ **理念そのものは依頼者から受け取っていない。** 旧 `vision.md` は
その到着を待つ枠として置かれていたが、序列が無くなり枠の意味も消えたので畳んだ。
上の引用が、いま効いている唯一の理念である。届いたら本節を差し替える。

## 最上位の3つの問い

1. **ヘッドハンティング** — 誰に声を掛けるか
2. **ボーダーライン** — 誰を通すか
3. **アプローチ** — どのアプローチ可能圏から関係を作るか

画面はこの3つに属する。4つ目の主タブを増やさない。
期（1期・2期…）はこの3つより上位の軸で、タブを移っても保たれる。
学年という概念は無い。

## 必ず守ること

- 次に行う作業、担当、停止理由を隠さない
- 記録にない数字や状態を画面で作らない
- 候補者の編集は可能にするが、履歴を消さない
- 順位・確度・集計値は直接編集させず、根拠となる記録を編集する
- 個人情報削除済みの候補者を運用画面へ戻さない

## UX判断（旧 `SUPERVISOR.md` から移設）

- ページやDB構造ではなく、**ユーザーが終えたい仕事**を設計単位にする。
- ダッシュボードは報告画面ではなく、次の仕事を提示し、その場で完了できる運転席にする。
- 操作数、画面遷移、探索時間、判断時間、認知負荷、結果の明確さを優先する。
- 既存UIを前提にせず、必要なら情報構造・ナビゲーション・操作方法を変更する。

## 単位と母集団は画面に書かない（C-62）

- 単位・母集団・注記を**画面に出さない。** 実行⑩で依頼者が
  `unit-note` / `footnote` / `Card` の `note` / `readonly-note` をまとめて外した。
  「意味の分からない印は、意味を推測させるぶんだけ邪魔になる」。
- ただし**説明できないまま出してよいわけではない。**
  定義はクエリのコメントと `db/DECISIONS.md` に置く。
  ★ 旧 `director.md` は「一覧の数字には単位と母集団を添える」と書いており、
    C-62 と正面から矛盾していた。C-62 が依頼者の明示指示なのでそちらを残した。

## 実装前の自己チェック

1. 3つの問いのどれに答える画面か明確か。期がその上にあると分かるか
2. 次に何をすべきか一目で分かるか
3. 表示値の根拠・単位・母集団を、**画面の外で**説明できるか
4. 編集によって過去の事実が消えないか

1つでも答えられなければ実装前に設計を直す。

## 原則が上書きできないもの

思想は事実を作れない。常に次の順序を守る。

```text
記録の構造 > 集計の定義 > 画面
```
